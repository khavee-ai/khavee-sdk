---
context: default
phase: none (multi-repo quick-task work, not GSD-phase-tracked in this repo)
task: floating widget mobile redesign — sheet sizing follow-up
total_tasks: n/a
status: in_progress
last_updated: 2026-07-14T19:20:42.466Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: **This session's actual work spans THREE separate checkouts, not this repo's GSD phases.** Before doing anything, read the "Where the work actually lives" section below — do not assume `.planning/phases/` in this repo (`/Users/whitemalt/Documents/khavee-sdk`, branch `gsd/phase-10-shared-animation-architecture-crossfade-engine`) has anything to do with this work. It doesn't.
- [ ] CONSTRAINT: **The main `khavee-sdk` checkout's `wordpress-plugin/` directory currently has test-only deployment copies of files from a different branch (`feat/floating-widget-mobile-redesign` in a worktree).** These are `git status --short` dirty on THIS branch (`gsd/phase-10-...`) but must NEVER be committed here — they're only there so `wp-env` (which serves from this checkout's path) can render the feature under test. Revert them (`git checkout -- wordpress-plugin/...`) once done testing, the same way it was done multiple times earlier this session.
- [ ] CONSTRAINT: **`core-api` (port 3001) is running locally and WordPress is currently pointed at it, not production** — `KnowledgeClient::BASE_URL` in the worktree's `wordpress-plugin/includes/Platform/KnowledgeClient.php` (and its copy in the main checkout) is temporarily set to `http://host.docker.internal:3001/...`. Must be reverted to `https://api.platform.khavee.ai/...` before any final push, if not already done.

**Do not proceed until you've read the sections below.**

<current_state>
Two unrelated features were built this session, both in a dedicated git worktree at `/Users/whitemalt/Documents/khavee-sdk-wp-knowledge` (NOT the main `khavee-sdk` checkout):

1. **WordPress knowledge-base RAG feature** (branch `feat/wp-knowledge-base`) — DONE, fully committed and pushed to both repos. Not touched this final stretch.
2. **Floating widget mobile redesign** (branch `feat/floating-widget-mobile-redesign`) — IN PROGRESS, the thing being paused mid-way. Just finished a third design iteration (chat sheet as a 60%-height overlay nested inside the avatar area) — built successfully, deployed to the local wp-env test site, but **NOT YET re-verified live in the browser** and **NOT YET committed**.

The user's last three rounds of feedback on the floating widget were, in order:
- "chat panel must be IN the floating widget, buttons too big, increase widget size not the toggle button" → fixed (shrunk buttons 72px→48px, enlarged panel, confined sheet width to widget footprint instead of full page width)
- "chat panel still overlaps the avatar entirely, and there's a white gap below the avatar" → fixed by making the sheet a normal in-flow stacked card below the panel (zero overlap) and removing the panel's stray fixed height
- "I meant ~60% overlap is fine, just make the widget TALLER" → this is the CURRENT unverified state: sheet moved back to being an absolute overlay, but now nested INSIDE `.khaveeai-floating-avatar-area` (not the whole widget), sized to ~250px / max-height:60%, with avatar-area height increased 276px→420px so the top ~40% (avatar's face) stays visible above the overlay when chat is open.
</current_state>

<where_the_work_lives>
**Repo 1: `/Users/whitemalt/Documents/khavee-app`** (branch `main`)
- Already fully committed and PUSHED. Two commits landed on `main` this session:
  - `a5cb701` — reuse `DATABASE_URL` instead of a separate `VECTOR_DATABASE_URL` for the knowledge routes
  - `246c2ce` — stamp `userId` on inserted documents (fixes documents added via WordPress not showing on the platform dashboard)
- Pushing to `main` triggers a real AWS App Runner Docker build via GitHub Actions, but **that Action only builds+pushes the image to ECR — it does NOT trigger an actual App Runner deployment** unless App Runner's "Auto deployments" setting is on. Confirmed via `gh run view` that after the user's "I deployed prod" claim, the new `sdk/knowledge` routes still 404'd on `https://api.platform.khavee.ai` — someone needs to manually trigger an App Runner deployment (AWS Console → App Runner → `pj-khavee-platform-api` → Deploy) for the knowledge-base backend fix to actually go live.
- `.planning/STATE.md` has unrelated pre-existing drift in this repo — deliberately left out of every commit here, not ours to touch.

**Repo 2: `/Users/whitemalt/Documents/khavee-sdk-wp-knowledge`** (a dedicated git worktree of khavee-sdk)
- Branch `feat/wp-knowledge-base`: fully committed and pushed (`1518b2f..cf829bc` and further, all pushed). Nothing pending here.
- Branch `feat/floating-widget-mobile-redesign`: **currently checked out, has uncommitted changes** (see below). Two commits already exist here from earlier (`c2376f5`, `d0f6d60`) — NOT pushed yet.
- **Uncommitted right now** (`git status --short` in this worktree):
  - `packages/wp-bundle/src/floating/FloatingWidget.tsx` — sheet JSX moved inside the avatar-area div
  - `packages/wp-bundle/styles.css` — avatar-area height 276→420px, sheet CSS rewritten as an absolute overlay (60% height) nested in avatar-area instead of a sibling
  - `wordpress-plugin/build/khaveeai-bundle.{js,css}`, `khaveeai-preview.css` — rebuilt output from the above (build ran clean, STUDIO-02 safety assertion passed)

**Repo 3: `/Users/whitemalt/Documents/khavee-sdk`** (main checkout, THIS repo, branch `gsd/phase-10-shared-animation-architecture-crossfade-engine`)
- Used ONLY as the live test target for `wp-env` (which mounts `wordpress-plugin/` from this exact path, not from the worktree). Every time a fix was made in the worktree, the changed file(s) were copied here, `wp cache flush`'d, then checked live in the browser.
- Currently dirty with test-deployment copies (see blocking constraint above) — build artifacts + `KnowledgeClient.php`/`KnowledgeAdminController.php`/`KnowledgeSearchController.php`/`SettingsPage.php`/`Plugin.php`/`AvatarRenderer.php`/`WpOptionsConfigSource.php` are copies from the worktree, NOT meant to be committed on this branch.
- `KnowledgeClient::BASE_URL` here is pointed at `http://host.docker.internal:3001/...` (local core-api), not production.
</where_the_work_lives>

<local_test_environment>
- `core-api` running locally on port 3001 (background process, started via `pnpm dev:api` in khavee-app) — logs at `/private/tmp/khavee-api.log`.
- `apps/web` (the platform dashboard) running locally on port 3000.
- Local Postgres (`khavee-dev-postgres` Docker container) has test data:
  - Test user `qwe@qwe.qwe` (password reset to `local-dev-test-123` for this session, local-only) owns project `9045055d-dd6a-49ab-a7de-5053d2fc3eb9` ("1"), which has ~5-9 test knowledge documents in it (some may have been cleaned up, some not — not critical, all local-only data).
  - A local API key row was inserted mirroring the WordPress site's actual configured key `khavee_01181b8b-e170-4b1b-b17b-b9e8347bee83_...` (the "asd" key on project `9045055d`) so the WP site's real config validates against local core-api.
- `wp-env` running (`cd /Users/whitemalt/Documents/khavee-sdk/wordpress-plugin && npx @wordpress/env start` if it needs restarting) — admin login `admin` / `khavee-local-dev` at `http://localhost:8888/wp-admin`.
- Chrome browser automation (claude-in-chrome) was used throughout for live verification — tools were loaded via ToolSearch; reload if starting fresh (`select:mcp__claude-in-chrome__tabs_context_mcp,mcp__claude-in-chrome__navigate,mcp__claude-in-chrome__computer,mcp__claude-in-chrome__read_page,mcp__claude-in-chrome__tabs_create_mcp,mcp__claude-in-chrome__read_network_requests,mcp__claude-in-chrome__resize_window`).
</local_test_environment>

<completed_work>
This session (chronological):
1. Diagnosed and fixed why WP-added knowledge documents weren't showing on the platform dashboard (missing `metadata.userId` stamp) — committed + pushed to khavee-app `main`.
2. Diagnosed and fixed a real security issue: the public `/wp-json/khaveeai/v1/knowledge-search` route was leaking `metadata.userId`/`metadata.projectId` (internal platform UUIDs) to anonymous visitors — fixed in both `KnowledgeSearchController.php` and `KnowledgeAdminController.php`, committed + pushed.
3. Reduced the Knowledge Base admin panel's list-column width (was too wide) — committed + pushed.
4. Built the floating widget mobile redesign from scratch (centered mic+chat buttons, default-closed chat) via a full plan→execute quick-task cycle — committed (not pushed).
5. Three follow-up rounds of live-tested CSS/layout fixes to the floating widget per direct user feedback (see current_state above) — the third round is what's uncommitted right now.
</completed_work>

<remaining_work>
1. **Live-verify the current (uncommitted) floating widget state in the browser.** Steps: `cd /Users/whitemalt/Documents/khavee-sdk/wordpress-plugin && npx @wordpress/env run cli wp cache flush`, navigate to `http://localhost:8888`, open the floating widget, click "Click to talk" to connect, click the chat toggle button, confirm: (a) chat sheet overlays roughly the bottom 60% of the avatar area, (b) top ~40% of avatar (face) stays visible above it, (c) sheet stays within the widget's own width (does not spill across the page), (d) drag handle / re-tapping toggle closes it cleanly. Screenshot and check against these four points explicitly — don't just eyeball it.
2. If it looks right: commit in the worktree (`/Users/whitemalt/Documents/khavee-sdk-wp-knowledge`, branch `feat/floating-widget-mobile-redesign`) and push. If the user has further feedback, iterate the same way (edit `packages/wp-bundle/styles.css` and/or `FloatingWidget.tsx` in the worktree → `pnpm --filter @khaveeai/wp-bundle build` → copy the 4 build output files to the main checkout → `wp cache flush` → check live).
3. Revert the main `khavee-sdk` checkout's test-deployment copies once the user confirms they're done testing (`git checkout -- wordpress-plugin/build/khaveeai-bundle.css wordpress-plugin/build/khaveeai-bundle.js wordpress-plugin/build/khaveeai-preview.css wordpress-plugin/build/khaveeai-preview.js wordpress-plugin/includes/Admin/SettingsPage.php wordpress-plugin/includes/ConfigSource/WpOptionsConfigSource.php wordpress-plugin/includes/Plugin.php wordpress-plugin/includes/Render/AvatarRenderer.php` and `rm` the untracked `KnowledgeClient.php`/`KnowledgeAdminController.php`/`KnowledgeSearchController.php`).
4. Revert `KnowledgeClient::BASE_URL` back to production (`https://api.platform.khavee.ai/api/v1/projects/sdk/knowledge`) in the worktree once local testing is fully done, rebuild, and that becomes part of a final commit.
5. Stop local `core-api`/`apps/web` dev servers if the user is done testing (`lsof -ti:3001 | xargs kill`, etc.) — optional, low priority, not urgent.
6. Ask the user whether they want the App Runner production deployment triggered manually (AWS Console) for the knowledge-base backend fix to actually go live — the GitHub Action alone does not do this (see Repo 1 notes above).
7. Eventually: open PRs / merge `feat/wp-knowledge-base` and `feat/floating-widget-mobile-redesign` (khavee-sdk) and `main`'s recent commits (khavee-app) — not asked for yet, don't do proactively.
</remaining_work>

<decisions>
- Floating widget redesign target reference was `khavee-app/apps/web/src/components/settings/preview/PreviewControls.tsx` (the platform's own shared/preview page mobile chat UX) — translated behaviorally, not copied verbatim, into the WordPress plugin's plain-CSS/no-framework codebase.
- Chose to mirror the WP-configured platform API key into local Postgres (rather than changing WP settings) for local testing throughout, so production config never needed touching.
- `.planning/STATE.md` in khavee-app is deliberately excluded from every commit — unrelated pre-existing drift, not part of this work.
</decisions>

<human_actions_pending>
- Manually trigger an AWS App Runner deployment for `pj-khavee-platform-api` if the user wants the knowledge-base backend fix live in production (the automated GitHub Action does not do this on its own).
- Confirm floating widget's current 60%-overlay design is what they want once shown, or give another round of feedback.
</human_actions_pending>
</content>
