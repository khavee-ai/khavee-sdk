---
status: resolved
trigger: "The Gutenberg block editor's live avatar preview shows a completely different camera framing than the published front-end page for the exact same block/config — the editor shows a tight close-up headshot (head+shoulders filling the frame), while the published page shows the full body from further away."
created: 2026-07-10T00:00:00Z
updated: 2026-07-10T03:35:00Z
---

## Current Focus

reasoning_checkpoint:
  hypothesis: "The reported bug is REAL and only manifests when devicePixelRatio != 1, which is why every prior self-verification round (all run at default headless dpr=1) found nothing. Root cause: @react-three/fiber's Canvas resize subscription (node_modules/@react-three/fiber/dist/events-d0449b76.cjs.dev.js:1120-1121) computes `const updateStyle = typeof HTMLCanvasElement !== 'undefined' && gl.domElement instanceof HTMLCanvasElement; gl.setSize(size.width, size.height, updateStyle);`. khaveeai-preview.js (packages/wp-bundle/src/preview.ts) is, BY DESIGN and per its own file-header comment, loaded and EXECUTED in the TOP wp-admin window's JS realm (WordPress's block.json `editorScript` field is never injected into the Gutenberg block-canvas iframe — only `script` is), while the mount-point `<div data-khaveeai-preview-config>` it targets via `createRoot(el)` physically lives INSIDE `iframe[name=\"editor-canvas\"]` — a SEPARATE JS realm/document. React creates the `<canvas>` DOM node via that div's own `ownerDocument` (the iframe's document), so `gl.domElement` is an iframe-realm HTMLCanvasElement instance. The bundled R3F code's bare `HTMLCanvasElement` identifier, however, resolves in the TOP window's realm (since that's where the module executes). Cross-realm `instanceof` checks against a different window's constructor always return `false` — so `updateStyle` evaluates to `false`, and three.js's `WebGLRenderer.setSize()` NEVER sets `canvas.style.width`/`style.height`. With no CSS size set, the browser falls back to the canvas element's intrinsic size, which equals its `width`/`height` HTML ATTRIBUTES (correctly computed as `containerCSSSize * devicePixelRatio` — i.e. the intended DRAW-BUFFER resolution) interpreted directly as CSS pixels. At dpr=1 this attribute value numerically equals the intended CSS size, so the bug is invisible. At dpr=2 (the human's real Retina Mac, and this session's `--force-device-scale-factor=2` headless reproduction) the canvas renders at exactly 2x its intended CSS footprint in both width and height, overflowing/getting clipped by ancestor containers — producing the reported 'zoomed in / cropped to head+shoulders' framing, ONLY on the editor/iframe path, never the published page (whose mount point lives in the SAME top-document realm as its own script, so the instanceof check succeeds there and style is set correctly)."
  confirming_evidence:
    - "Live CDP measurement (Chrome headless launched with --force-device-scale-factor=2 --high-dpi-support=1, forcing devicePixelRatio=2): PUBLISHED page canvas — attrW=4000,attrH=800, CSS rect (getBoundingClientRect) = 2000x400 — the CORRECT 2:1 ratio (attribute = CSS * dpr, exactly as three.js's WebGLRenderer.setSize is supposed to produce). EDITOR canvas (inside iframe[name=\"editor-canvas\"]) — attrW=3996,attrH=800, CSS rect = 3996x800 — attribute and CSS rect are IDENTICAL (a 1:1 ratio), while its own immediate parent div's rect is 1998x400 (exactly HALF the canvas's reported CSS size) — i.e. the canvas is genuinely laid out at literally 2x the CSS footprint of its own direct parent, a physically impossible outcome for a correctly CSS-sized child unless its style width/height were never actually set."
    - "Direct inspection of the editor canvas's own inline style (`canvas.style.cssText`) via CDP Runtime.evaluate inside the iframe's own execution realm: `\"display: block;\"` — NO width/height present. getComputedStyle(canvas).width/height read '3996px'/'800px', matching the canvas's raw width/height ATTRIBUTES (also 3996/800) exactly — proof the rendered CSS size is coming from the canvas element's intrinsic (attribute-based) sizing fallback, not from any applied style."
    - "Read node_modules/@react-three/fiber/dist/events-d0449b76.cjs.dev.js:1120-1121 directly: `const updateStyle = typeof HTMLCanvasElement !== 'undefined' && gl.domElement instanceof HTMLCanvasElement; gl.setSize(size.width, size.height, updateStyle);` — this is the ONLY place in the installed R3F version that decides whether canvas.style.width/height get set on resize, and it directly gates on an `instanceof` check against a bare (realm-dependent) `HTMLCanvasElement` global."
    - "Read packages/wp-bundle/src/preview.ts lines 20-33 (the file's OWN documented design note, not inference): explicitly states khaveeai-preview.js executes in the TOP admin window's document (via block.json's editorScript field) while its mount-point div lives inside the iframe's DOM — i.e. the codebase's own authors already knew and documented this exact cross-realm relationship (for an unrelated prior bug fix, 'preview never updates', which fixed a DIFFERENT instanceof crossing — `instanceof HTMLElement` in a MutationObserver callback, replaced with a realm-agnostic `nodeType` check, see preview.ts:94-98). The Canvas-resize instanceof crossing (inside R3F's own bundled code, not our source) was never addressed because it lives in a third-party dependency, not our own callback code, and its failure mode is silent (no error, no console warning) — it only ever manifests visually, and only at non-1 devicePixelRatio, which is exactly why it was never caught by the same 'nodeType instead of instanceof' fix already applied elsewhere in this same file."
  falsification_test: "If this hypothesis is correct, a component that unconditionally sets `gl.domElement.style.width/height` from R3F's own resolved `size` state (bypassing R3F's broken instanceof gate entirely) added to PreviewAvatarCanvas.tsx should make the editor canvas's CSS rect match its container at devicePixelRatio=2, eliminating the oversized/cropped render. Re-running the exact same --force-device-scale-factor=2 CDP measurement after the fix should show canvas CSS rect == parent container rect (not 2x), and a full-page screenshot at dpr=2 should show full-body framing in the editor matching the published page. If the canvas CSS rect still mismatches its container after this fix, the hypothesis is incomplete and the actual style-application code path must be re-traced (e.g. a later effect or a CSS rule overriding the explicitly-set inline style)."
  fix_rationale: "The fix does not patch node_modules (unmaintainable, and the checked-in build already vendors these deps) — instead it adds a small sibling component (mirroring the existing CameraController/CameraRefCapture pattern already used in this same file) that reads R3F's own already-correct `size` state via useThree() and explicitly, unconditionally applies it to `gl.domElement.style.width/height` on every size change. This bypasses the broken cross-realm instanceof check entirely rather than trying to fix or avoid the realm-crossing itself (which is required by WordPress's own editorScript-loads-in-top-window constraint, not something this codebase controls). This directly addresses the root cause (canvas style never being set) rather than a downstream symptom."
  blind_spots: "This fix is scoped to PreviewAvatarCanvas.tsx (the only iframe-rendered Canvas). mount.tsx's AvatarScene (published page) and PreviewFloatingWidget's own avatar area (if it renders its own separate Canvas — not yet re-checked in this round) execute in the top-document realm already and should be unaffected, but PreviewFloatingWidget's Canvas has NOT been independently re-verified in this session for the same defect — if it ALSO mounts into an iframe-realm node (e.g. if it's ever rendered from the same preview.ts entry into an iframe context), it would need the same fix. Also: this fix corrects the CSS-visible symptom directly; it does not fix the underlying three.js/R3F library defect, so any OTHER R3F-internal code path that also gates on `instanceof HTMLCanvasElement` (not yet found by a full audit of the installed version) could still be affected — a full grep of the installed @react-three/fiber dist files for `instanceof HTMLCanvasElement` found only this one call site, but a newer/older R3F version could differ."

next_action: "RESOLVED. Human confirmed the CanvasStyleSync fix on their real dpr=2 Retina display: hard-reloaded http://localhost:8888/wp-admin/post.php?post=87&action=edit and full body now renders correctly, matching the published page's framing and offset exactly. Session archived to .planning/debug/resolved/camera-framing-mismatch.md."

reasoning_checkpoint:
  hypothesis: "mount.tsx's published-page AvatarScene Canvas never corrects the camera's LOOK-AT rotation to resolveSceneDefaults().cameraTarget. React-three-fiber's own default-camera setup (node_modules/@react-three/fiber dist/events-d0449b76.cjs.dev.js:1843-1867, the Canvas configure() function) hardcodes `camera.lookAt(0, 0, 0)` once, at first creation, whenever the `camera` prop has no explicit `rotation` field — which is true for BOTH mount.tsx's `camera={{position, fov}}` and PreviewAvatarCanvas.tsx's identical prop. mount.tsx has no code that ever corrects this afterward, so the published page's camera is permanently aimed at the world origin (0,0,0), NOT at CAMERA_PRESETS[preset].target (e.g. [0,0.15,0] for front/left-angle/right-angle, [0,0.1,0] for wide). PreviewAvatarCanvas.tsx (editor path), by contrast, has TWO independent mechanisms that continuously correct this: CameraController's useEffect (camera.lookAt(target) on every position/target/fov change) AND OrbitControls' per-frame update() loop, which unconditionally calls `scope.object.lookAt(scope.target)` every single frame (three-stdlib controls/OrbitControls.js:232) using controls.target = sceneDefaults.cameraTarget (set via the R3F primitive/reconciler prop). So the editor path is aimed at the CORRECT config-driven target every frame; the published path is permanently aimed at the wrong, hardcoded (0,0,0) target — this is a genuine, source-verified rotation divergence, structurally independent of position/fov (which ARE identical, per prior Evidence)."
  confirming_evidence:
    - "Read node_modules/@react-three+fiber@9.3.0.../dist/events-d0449b76.cjs.dev.js:1862 directly: `if (!state.camera && !(cameraOptions != null && cameraOptions.rotation)) camera.lookAt(0, 0, 0);` — runs once per Canvas's default-camera creation, camera prop from both mount.tsx and PreviewAvatarCanvas.tsx never sets `rotation`, so both get this hardcoded lookAt(0,0,0) at creation."
    - "Read node_modules/three-stdlib@2.36.0/controls/OrbitControls.js:181-296 (`this.update`): every call recomputes spherical offset from `scope.object.position` MINUS `scope.target`, then on line 232 unconditionally calls `scope.object.lookAt(scope.target)` — this fires every frame via drei's `useFrame(() => controls.update(), -1)` (node_modules/@react-three/drei OrbitControls.js:29-31), which only exists in PreviewAvatarCanvas.tsx, not mount.tsx."
    - "Read packages/wp-bundle/src/mount.tsx in full: AvatarScene's <Canvas camera={{position, fov}}> has ZERO child components analogous to CameraController — no useThree()/lookAt() call anywhere in the file. Confirmed by direct file read, not inference."
    - "Confirmed via wp-cli (docker exec into wp-env) that post=87's actual resolved data-khaveeai-config (published) and data-khaveeai-preview-config (editor) both correctly compute cameraPreset='front' -> target=[0,0.15,0], avatarScale=1, avatarOffsetX=-1, and IDENTICAL avatarUrl (a live Platform-signed S3 GLB URL, confirmed both via curl on the published page AND via a direct wp eval call reproducing PlatformConfigSource->get_runtime_config(), same signature/timestamp since both hit the same 5-min transient cache) — ruling out config-value and avatar-model divergence definitively; the only remaining structural difference is this rotation/lookAt mechanism."
  falsification_test: "If this hypothesis is correct, adding an equivalent camera.lookAt(cameraTarget) correction to mount.tsx's AvatarScene (matching PreviewAvatarCanvas.tsx's CameraController) should make the published page's camera aim match the editor's, eliminating the framing discrepancy. If the discrepancy persists after this fix (verified via re-screenshotting both contexts), the hypothesis is wrong or incomplete and CameraController must be re-investigated for a further-reaching effect (e.g. OrbitControls' distance/zoom clamping) not yet identified."
  fix_rationale: "The fix addresses the root cause directly: it makes the published page's camera continuously (via a mount-effect, matching the editor's CameraController) aim at resolveSceneDefaults().cameraTarget instead of relying on react-three-fiber's undocumented hardcoded lookAt(0,0,0) fallback. This is the SAME correction PreviewAvatarCanvas.tsx already applies, extracted into a shared component so both rendering paths share one source of truth for camera aim (matches the codebase's existing EMBED-04 'shared renderer, no drift' pattern used for AvatarRenderer/AvatarBlock/AvatarShortcode)."
  blind_spots: "The precise ANGULAR magnitude of this divergence for the 'front' preset test case (position=[0,1.3,3.1], hardcoded target=(0,0,0) vs correct target=[0,0.15,0]) computes to only ~2.5 degrees of tilt by hand-calculation — smaller than what 'headshot vs full body' suggests at first read. I do not have live browser/CDP tool access in this session to directly screenshot-verify the actual rendered magnitude, so I cannot rule out that this 2.5-degree tilt combined with the fov=20 narrow lens and avatarOffsetX=-1 (avatar's own visual center is off-axis from where the camera aims, in BOTH contexts, since CAMERA_PRESETS targets are always centered at x=0 regardless of avatarOffsetX) produces a visually larger apparent difference than the raw angle suggests, or that another, still-undiscovered factor also contributes. This is the most concrete, mechanistically-verified divergence found after exhaustively eliminating config values, avatarUrl, aspect-ratio/vertical-FOV mathematics (proven aspect ratio cannot affect vertical framing), and OrbitControls position-mutation (proven idempotent by tracing three-stdlib's update() math). Recommend human verification of the actual visual result after the fix, per the mode's required checkpoint."

next_action: "Awaiting human re-verification (see CHECKPOINT below): re-check http://localhost:8888/wp-admin/post.php?post=87&action=edit in a PRIVATE/incognito window (or with DevTools 'Disable cache' checked + hard reload) against http://localhost:8888/?page_id=87, because a fresh no-cache headless re-test in THIS session now shows the two contexts rendering identically (full body, matching pose/crop) — contradicting the prior checkpoint report of a persistent tight-crop in the editor. If the human still sees a mismatch in a guaranteed-fresh browser state, resume investigation from the 'if still mismatched' branch noted in Evidence below (re-check for a genuinely different avatarUrl/model reference, or a Gutenberg 'zoomed-out' view scale artifact)."

## Symptoms

expected: The Gutenberg block editor's live preview should show the avatar framed identically to how it appears on the published front-end page, for the same resolved camera config (same cameraPreset/avatarScale/avatarOffsetX/avatarOffsetY/fov).
actual: Editor preview shows a tight close-up headshot (head+shoulders fill the frame); published page shows the full body from further away, for the exact same underlying post/block.
errors: None. No console errors in either context.
timeline: First observed today (2026-07-10), immediately after confirming the avatar-visibility fix (commit 2e43ee6, resolved debug session vrm-parse-hang-in-editor) — that fix made the avatar render at all in the editor; this framing mismatch became visible only once the avatar was actually visible.
reproduction: Compare http://localhost:8888/wp-admin/post.php?post=87&action=edit (editor, avatar cropped to head/shoulders) against http://localhost:8888/?page_id=87 (published, full body visible), same post, same block config, both with a connected Khavee Platform API key.

## Evidence

- timestamp: 2026-07-10T00:00:00Z
  finding: "Read the actual resolved data-khaveeai-preview-config (editor, inside iframe[name=\"editor-canvas\"]) and data-khaveeai-config (published page) JSON for post=87. cameraPreset, cameraRotationY, avatarScale, avatarOffsetX, avatarOffsetY, containerWidth, containerHeight, fullWidth are ALL byte-identical after resolution: cameraPreset resolves to \"front\" in both (editor sends raw \"\", published sends already-resolved \"front\" — both correctly become CAMERA_PRESETS.front via the `||` fallback in resolveSceneDefaults()), avatarScale=1 in both (editor sends raw 0 relying on the `> 0` sentinel fallback to 1.0, published sends already-resolved 1 — both correctly resolve to numeric 1), avatarOffsetX=-1 in both (a real, matching custom value), avatarOffsetY=0 in both, containerWidth=0/containerHeight=0/fullWidth=false in both. Both paths call the exact same shared resolveSceneDefaults() (packages/wp-bundle/src/config.ts) which would therefore compute numerically identical cameraPosition=[0,1.3,3.1], cameraTarget=[0,0.15,0], cameraFov=20 in both contexts."
  method: "Direct JS injection via Chrome DevTools Protocol, reading the mount div's data-* attribute and JSON.parsing it, in both the editor iframe context and the top-level published-page document."
- timestamp: 2026-07-10T00:00:00Z
  finding: "The tight-headshot framing was already present on the VERY FIRST paint after a genuine hard reload (navigate with force:true — a real new document/iframe/React tree, not a soft re-render), with zero canvas click/drag interaction performed before the screenshot. Rules out leftover OrbitControls drag residue from earlier manual testing in the same session as the explanation."
  method: "navigate(force:true) -> wait -> screenshot, no intervening computer actions on the canvas."
- timestamp: 2026-07-10T01:00:00Z
  finding: "CHECKPOINT RESPONSE: human re-tested the rotation-only CameraController fix live in wp-env after a hard reload of both contexts. Published page: full body, offset left — UNCHANGED from before the fix. Editor: STILL a tight head+shoulders crop, centered — UNCHANGED from before the fix. Human corrected the problem framing: the PUBLISHED page's full-body view is the CORRECT/desired one; the bug is the EDITOR cropping tight. Requested direct measurement of the LIVE THREE.Camera's actual runtime position/fov/rotation/aspect in both contexts (not just React props), since a ~2.5-degree rotation fix visibly changing nothing is consistent with the fix being real but insufficient, or with a runtime divergence not yet captured."
  implication: "Rotation-only fix (previous Resolution) is not proven sufficient by direct visual test. Must instrument LIVE runtime camera/canvas state in both contexts per explicit request, not just static config/props."
- timestamp: 2026-07-10T01:15:00Z
  finding: "Added TEMPORARY per-frame debug instrumentation to CameraController.tsx (writes live camera.position/fov/rotation/aspect/canvasSize to window.__khaveeCameraDebugs, keyed by camera.uuid to avoid clobbering when multiple Canvas instances exist on one page) and to PreviewAvatarCanvas.tsx's CameraRefCapture (captures live OrbitControls.target/enableZoom). Rebuilt wp-bundle (node build.mjs). Launched an ISOLATED headless Chrome instance (separate --user-data-dir, --remote-debugging-port=9333, does not touch the user's real running Chrome) and drove it via raw Chrome DevTools Protocol over Node's native fetch+WebSocket (no puppeteer/playwright available/installed — built a ~90-line CDP client script instead). Logged into wp-admin via curl (admin/password) and injected the resulting session cookies into the headless browser via Network.setCookie so the wp-admin editor page loads authenticated."
  method: "Node script using native WebSocket/fetch APIs (Node v23) talking directly to Chrome's /json/new + DevTools Protocol (Page.navigate, Runtime.evaluate, Page.captureScreenshot)."
- timestamp: 2026-07-10T01:20:00Z
  finding: "Published page (page_id=87) actually has TWO Canvas/camera instances: the main block's AvatarScene (696-1380px wide depending on viewport, since containerWidth=0 resolves to an UNCONSTRAINED auto width with no theme max-width — this test site's theme has no entry-content max-width) x 400px tall, AND a separate site-wide FloatingWidget avatar preview (~291-324px, a fixed small area) that renders on every page per FLOAT-01. The debug instrumentation had to be keyed by camera.uuid (not a single global) specifically because of this — a naive single window.__khaveeCameraDebug global was silently clobbered by whichever Canvas rendered/updated last, which is itself worth flagging as a real footgun for any FUTURE debugging of this codebase (not this bug's cause, but noted for the knowledge base)."
  implication: "Any future runtime-camera-debug instrumentation on this codebase's pages MUST account for multiple simultaneous Canvas/camera instances (main block + floating widget) or risk reading the wrong one."
- timestamp: 2026-07-10T01:25:00Z
  finding: "Read the LIVE runtime camera state via Runtime.evaluate in both contexts (main block instance only, disambiguated from the floating widget by its ~696-1380x400 canvas size). Published (page_id=87 main block): configPosition=[0,1.3,3.1], livePosition=[0,1.3,3.1] (exact match), liveFov=20, liveRotation=[-0.3552,0,0] (a pure X-tilt, matching lookAt([0,0.15,0]) from that position — Y/Z rotation are exactly 0), liveAspect=1.735, canvasSize={694,400}. Editor (post=87, inside iframe[name=\"editor-canvas\"], instrumentation read from the TOP window because the preview bundle's React tree — though its DOM mount point lives inside the iframe — executes in the top wp-admin document's JS realm, not the iframe's own window; iframe.contentWindow.__khaveeCameraDebugs was null, top window.__khaveeCameraDebugs had the data): configPosition=[0,1.3,3.1], livePosition=[0,1.3,3.1] (exact match), liveFov=20, liveRotation=[-0.3552,0,0] (matches published to 4 decimal places), liveAspect=1.735, canvasSize={694,400}. OrbitControls live state: target=[0,0.15,0] (matches), enableZoom=true, minDistance=0, maxDistance=null(=Infinity). CONCLUSION: the live runtime camera position/rotation/fov/aspect are effectively IDENTICAL between the two contexts for the main block instance — no runtime divergence exists with the current code."
  implication: "The rotation-fix hypothesis's own falsification test (re-screenshot both contexts) must now actually be run, since runtime camera state alone shows no remaining divergence to explain a headshot-vs-full-body difference."
- timestamp: 2026-07-10T01:30:00Z
  finding: "Captured actual PNG screenshots (Page.captureScreenshot, clipped to the avatar canvas's own getBoundingClientRect, computed from inside the iframe for the editor case) of BOTH contexts, at two different emulated viewport widths (1400px and 1440px), with Network.setCacheDisabled(true) set before every navigation (i.e. a guaranteed no-cache load, stronger than a normal hard-reload). At BOTH viewport widths, in a completely FRESH headless Chrome profile (never touched before this session, so zero pre-existing browser cache/localStorage/serviceWorker state), the published page and the editor both render the AVATAR IDENTICALLY: full body visible head-to-shoes, same pose, same crop, same relative framing within the canvas — NOT a tight head+shoulders crop in the editor. Re-ran again after removing the temporary debug instrumentation and rebuilding (to rule out the instrumentation itself changing behavior) — same result: full body in both contexts."
  method: "CDP Page.captureScreenshot with an explicit clip rect matching the canvas's own bounding box (computed via iframe.contentDocument.querySelector('canvas') for the editor, offset by the iframe's own position in the top document)."
  implication: "With the CURRENT code (the CameraController fix from the prior session's Resolution, still in place, unmodified further), the reported bug DOES NOT REPRODUCE. The most likely explanation for the human's contrary checkpoint report is that their browser session was NOT actually loading the rebuilt bundle at test time (stale cache/service worker/opcache, or the verification happened before the build completed) — this needs a targeted re-check in a guaranteed-fresh browser state (private/incognito window) before the fix can be confirmed as verified end-to-end."

- timestamp: 2026-07-10T02:15:00Z
  finding: "CHECKPOINT RESPONSE (second round): reported the fix still doesn't work AND a new, sharper symptom — avatar renders on the OPPOSITE horizontal side between contexts (published: left of center; editor: right of center) despite identical avatarOffsetX=-1 and camera position/target, requesting camera.up be checked as a possible handedness/mirroring cause. Before re-running any live browser test, did a full code-level re-trace of every candidate mechanism: (1) re-read CameraController.tsx, mount.tsx, PreviewAvatarCanvas.tsx in full; (2) read three-stdlib@2.36.0's actual installed OrbitControls.js update() function directly (controls/OrbitControls.js:173-296) — the up-alignment quaternion (`quat = Quaternion().setFromUnitVectors(object.up, (0,1,0))`) is RECOMPUTED FROM camera.up on EVERY SINGLE update() call (line 183: `quat.setFromUnitVectors(object.up, up)`), not cached at construction — so a stale/mismatched camera.up cannot be the mechanism, contrary to the checkpoint's own leading theory; (3) grepped the entire wp-bundle/wordpress-plugin source tree for any CSS transform/scaleX(-1)/mirror rule — none exists anywhere; (4) confirmed VRMAvatar's default `rotation=[0,Math.PI,0]` (packages/react/src/VRMAvatar.tsx:274) is applied identically in both mount.tsx and PreviewAvatarCanvas.tsx (neither passes an explicit `rotation` prop to override it); (5) confirmed cameraRotationY resolves to 0 in both contexts (already established earlier in this session), so orbitAroundTarget's trig (the only sign-flip-capable code in the whole camera pipeline) is never even invoked. No code-level mechanism for a left/right mirror was found."
  method: "Direct source reading of packages/wp-bundle/src/{CameraController,mount,preview/PreviewAvatarCanvas}.tsx, node_modules/.pnpm/three-stdlib@2.36.0.../controls/OrbitControls.js, node_modules/.pnpm/@react-three+drei.../core/OrbitControls.js, packages/react/src/VRMAvatar.tsx, and an exhaustive grep for scaleX/mirror/transform across packages/wp-bundle and wordpress-plugin."
  implication: "No plausible code mechanism exists for a genuine left/right camera mirror. Proceeded to re-verify empirically with unambiguous (non-Euler) instrumentation rather than trust the checkpoint's narrative at face value, per this session's own security/skepticism posture toward user-supplied evidence."

- timestamp: 2026-07-10T02:45:00Z
  finding: "Added TEMPORARY instrumentation to CameraController.tsx capturing the camera's WORLD-SPACE MATRIX BASIS VECTORS (right/up/forward extracted directly from camera.matrixWorld.elements — mathematically unambiguous, unlike Euler `rotation` which can represent the same orientation via different +/- sign combinations) plus the avatar group's live world position, keyed by camera.uuid on window.__khaveeCamDebug2. Rebuilt wp-bundle. Built a fresh isolated headless Chrome instance (separate profile/port, does not touch the user's real browser) with `--use-gl=swiftshader --enable-unsafe-swiftshader` (required in this sandboxed dev environment — WebGL context creation fails without explicit software-GL flags here; this is an environment quirk of THIS investigation session's sandbox, unrelated to the bug). Read the instrumentation via Runtime.evaluate in both contexts:\n\nPUBLISHED (main block instance): position=[0,1.3,3.1], up=[0,1,0], rightWorld=[1,0,0], upWorld=[0,0.9376,-0.3478], forwardWorld=[0,-0.3478,-0.9376], fov=20, aspect=3.85, avatarGroup.position=[-1,0,0] (matches avatarOffsetX=-1). (A second camera instance was also captured — the site-wide FloatingWidget's own small canvas, aspect=1.8, slightly orbited — expected and irrelevant to the main block comparison.)\n\nEDITOR (post=87, inside iframe[name=\"editor-canvas\"]): position=[0,1.3,3.1], up=[0,1,0], rightWorld=[1,0,0], upWorld=[0,0.9376,-0.3478], forwardWorld=[0,-0.3478,-0.9376], fov=20, aspect=3.845 — IDENTICAL to published to at least 4 decimal places on every basis vector component, including rightWorld=[1,0,0] in BOTH contexts.\n\nCONCLUSION: rightWorld=[1,0,0] identical in both contexts is direct, unambiguous proof there is NO camera-orientation mirroring/flip at the three.js level — the checkpoint's 'opposite side' hypothesis is DISPROVEN by direct measurement, not just code-tracing.\n\nAlso captured actual PNG screenshots (Page.captureScreenshot) of both contexts in this same session, AFTER removing the temporary instrumentation and rebuilding the final clean bundle (md5 cf7c72d8420f8fc86c13f986d33d3cd6 for khaveeai-bundle.js, 5a83bbe28551f868d66abf90e4f79892 for khaveeai-preview.js, both built 2026-07-10T02:27:12Z — confirmed the served `?ver=1783621632` query param on both <script> tags matches this exact build's file mtime epoch, i.e. wp-env was definitely serving the fresh rebuilt bundle, not a cached one). Both screenshots show the avatar full-body, head-to-shoes, SAME side of frame (left-of-center in both), same crop — the only visible difference is the idle animation's hair mid-motion (a harmless animation-timing artifact of capturing two independent screenshots at slightly different animation-loop phases, not a framing/position difference)."
  method: "CDP-driven isolated headless Chrome (Node v23 native fetch+WebSocket, no external deps), Runtime.evaluate reading window.__khaveeCamDebug2 instrumentation in both contexts, plus Page.captureScreenshot for direct visual confirmation, plus explicit build-fingerprint verification (md5 + mtime-vs-served-ver comparison) to rule out stale-cache as a confound in THIS verification pass."
  implication: "Second independent verification pass (different technique than the 01:25-01:30 pass: direct matrix-basis-vector reads instead of Euler rotation, PLUS a build-fingerprint check) reaches the SAME conclusion: the fix works, camera orientation is identical between contexts, and the reported tight-crop/opposite-side symptom does not reproduce against the current code. This is now two-for-two self-verifications finding no bug. The most likely remaining explanation for the checkpoint's contrary report is a stale/cached bundle in the reporting browser session, a different post/config being tested than post=87, or a measurement/screenshot-comparison artifact on their end — NOT a real defect in the current CameraController-based fix."

- timestamp: 2026-07-10T02:50:00Z
  finding: "Removed the temporary window.__khaveeCamDebug2 instrumentation from CameraController.tsx (restored to the exact clean state from the prior session's Resolution — no `scene` destructured from useThree(), no extra useEffect). Rebuilt wp-bundle. `tsc -p packages/wp-bundle/tsconfig.json --noEmit` reproduces exactly one pre-existing error (TS2322 in mount.tsx, OpenAIRealtimeProvider/RealtimeProvider toggleMicrophone Promise<boolean> vs boolean mismatch) — already documented in the prior Resolution as predating this debug session's changes (confirmed via git stash at that time)."
  method: "Direct file edit (Edit tool) + `node build.mjs` + `tsc --noEmit`."
  implication: "Codebase is back to the clean, minimal fix state (CameraController.tsx, mount.tsx, PreviewAvatarCanvas.tsx only) with no debug-only code left behind, ready for a final human-verify checkpoint."

- timestamp: 2026-07-10T03:15:00Z
  finding: "Acted on the checkpoint's own suggested untested variable (devicePixelRatio). Launched an isolated headless Chrome instance with `--force-device-scale-factor=2 --high-dpi-support=1` (forcing devicePixelRatio=2, matching the human's real Retina Mac — every PRIOR self-verification round in this session used the default dpr=1 and is now understood to have been structurally incapable of reproducing this bug). Measured the editor canvas (inside iframe[name=\"editor-canvas\"]) directly: canvas element's own getBoundingClientRect() = 3996x800 CSS px, while its DIRECT PARENT DIV measured 1998x400 — the canvas was laid out at literally 2x its own container's CSS footprint, a physically-impossible outcome for a correctly CSS-sized child. Direct inspection of the canvas's inline style (`canvas.style.cssText`) showed only `\"display: block;\"` — NO width/height ever applied; getComputedStyle confirmed the canvas was falling back to its intrinsic (attribute-based) size. On the PUBLISHED page, at the same forced dpr=2, the canvas measured CORRECTLY: attrW=4000/attrH=800 (drawing buffer) vs CSS rect 2000x400 (exactly half, the expected relationship)."
  method: "CDP-driven isolated headless Chrome (Node v23 native fetch+WebSocket, --force-device-scale-factor=2), Runtime.evaluate reading canvas.getBoundingClientRect()/canvas.style.cssText/getComputedStyle() directly inside the iframe's own execution context (via iframe.contentDocument), plus the same measurements on the top-level published-page document."
  implication: "Root cause found: this is a real, devicePixelRatio-gated bug, structurally invisible to any test run at the default dpr=1 (every prior self-verification round in this session). Traced to @react-three/fiber's Canvas-resize code (node_modules/@react-three/fiber/dist/events-d0449b76.cjs.dev.js:1120-1121): `const updateStyle = typeof HTMLCanvasElement !== 'undefined' && gl.domElement instanceof HTMLCanvasElement; gl.setSize(size.width, size.height, updateStyle);`. khaveeai-preview.js executes in the TOP wp-admin window's JS realm (per preview.ts's own documented design — WordPress's editorScript field is never injected into the block-canvas iframe), while its React root renders into a div living INSIDE iframe[name=\"editor-canvas\"] (a separate realm/document). The canvas DOM node React creates therefore belongs to the iframe's realm, so a cross-realm `instanceof HTMLCanvasElement` check (against the TOP window's constructor) evaluates false, `updateStyle` becomes false, and three.js's WebGLRenderer.setSize() never applies canvas.style.width/height. With no CSS size, the browser renders the canvas at its intrinsic size = its width/height ATTRIBUTES (correctly `containerCSSSize * dpr`) interpreted directly AS CSS pixels — at dpr=1 this coincidentally equals the correct size (bug invisible); at dpr=2 the canvas renders at 2x its intended footprint, producing the reported 'zoomed in / cropped to head+shoulders' framing, ONLY in the editor (iframe) context, never the published page (same-realm, instanceof succeeds there)."
- timestamp: 2026-07-10T03:20:00Z
  finding: "Implemented CanvasStyleSync (packages/wp-bundle/src/preview/PreviewAvatarCanvas.tsx): a small sibling component (mirrors the existing CameraController/CameraRefCapture pattern already used in this file) that reads R3F's own already-correct `size` state via useThree() and unconditionally applies it to `gl.domElement.style.width/height` on every size change — bypassing the broken cross-realm instanceof gate entirely rather than trying to avoid the realm-crossing itself (which is required by WordPress's editorScript-loads-in-top-window constraint). Rebuilt wp-bundle (node build.mjs) and re-ran the exact same --force-device-scale-factor=2 CDP measurement: editor canvas CSS rect is now 1998x400 (correctly matching its parent container), while the drawing-buffer attribute remains 3996x800 (correctly still 2x for sharp Retina rendering) — the fix corrects ONLY the CSS layout size, leaving render resolution/sharpness untouched. Took full-page screenshots of both contexts at dpr=2: editor now shows full-body framing identical to the published page (both stored in scratchpad as fix-dpr2-pub.png / fix-dpr2-ed.png). `tsc --noEmit` reproduces only the same single pre-existing unrelated error (mount.tsx TS2322, documented in the original Resolution as predating this debug session)."
  method: "Direct file edit (Edit tool), node build.mjs, CDP re-measurement (same dpr=2 forced headless Chrome technique), Page.captureScreenshot on both contexts, tsc --noEmit."
  implication: "Fix verified to directly correct the measured CSS-sizing defect at the exact devicePixelRatio (2) that reproduces the human's reported bug — the first successful reproduction AND fix-verification in this entire debug session. PreviewFloatingWidget.tsx was checked and confirmed to reuse this same PreviewAvatarCanvas component (not a separate Canvas), so it is covered by the same fix with no separate change needed."
- timestamp: 2026-07-10T03:35:00Z
  finding: "CHECKPOINT RESPONSE (human, real dpr=2 Retina display): confirmed fixed. Hard-reloaded http://localhost:8888/wp-admin/post.php?post=87&action=edit — full body now renders correctly, matching the published page's framing and offset exactly."
  implication: "End-to-end verification complete on the human's real HiDPI hardware, the exact environment that originally reproduced the bug. Root cause (cross-realm instanceof check on the iframe-realm canvas vs top-window HTMLCanvasElement) and the CanvasStyleSync fix are confirmed correct. Session resolved."

## Eliminated Hypotheses

- hypothesis: "Camera position, rotation/lookAt target, fov, or aspect ratio differ between the editor and published contexts (all rounds of this debug session prior to 03:15)."
  reason: "Exhaustively measured identical (position, Euler rotation, world-space matrix basis vectors, fov, aspect) across two independent self-verification passes using two different techniques, PLUS the final 03:15 dpr=2 CDP pass. The actual defect was never in any three.js camera state — it was a CSS layout-sizing defect on the canvas DOM element itself, invisible to every camera-state measurement taken. This whole line of investigation was a real dead end: the CameraController fix from the FIRST round of this debug session was a genuine, separate, correctly-diagnosed bug (react-three-fiber's default lookAt(0,0,0) on the published path) and remains valid/necessary, but it was never the cause of THIS reported symptom (headshot vs full body)."

- hypothesis: "Config values themselves differ between editor and published (e.g. a stale/wrong cameraPreset, avatarScale, or avatarOffsetX reaching one context but not the other)."
  reason: "Directly read and compared the actual resolved JSON config in both contexts for the same post — all camera/avatar-position-relevant fields are numerically identical after resolution (see Evidence above)."
- hypothesis: "Leftover manual OrbitControls drag/zoom from earlier testing in this browser session is why the editor's camera looks different."
  reason: "Reproduced on a genuine hard full-page reload with zero interaction with the canvas before the first screenshot — a hard navigate creates an entirely new iframe/document/React tree, so no in-memory Three.js camera state could survive from a prior page load."
- hypothesis: "The container's CSS aspect ratio (width/height) differs between the editor and published contexts and this crops the vertical framing."
  reason: "Mathematically ruled out by tracing three.js's PerspectiveCamera semantics: `fov` is the VERTICAL field of view; `aspect` (width/height) only changes the HORIZONTAL extent visible, never the vertical extent, for a fixed camera position/target/fov. A narrower or wider container can only crop/expand the sides of the frame, never turn a full-body view into a head+shoulders-only view. Confirmed by reading three.js's camera model directly, not by empirical aspect measurement."
- hypothesis: "OrbitControls' mount-time internal state capture (position0/target0, spherical coordinate derivation) repositions or re-zooms the camera away from the config-driven position on mount, independent of any user drag."
  reason: "Traced three-stdlib's OrbitControls.js constructor and update() method directly (node_modules/three-stdlib@2.36.0/controls/OrbitControls.js): `update()` recomputes spherical(offset) from the LIVE camera.position minus scope.target every call and converts it straight back to position with no discrepancy when spherical deltas are zero (no user interaction) — mathematically idempotent. `position0`/`target0` (captured at construction) are only consumed by `reset()`, which is never called on mount. The `target` prop reaches `controls.target` via R3F's reconciler (a Vector3.set() call) synchronously during React's commit phase, which always completes before the first requestAnimationFrame-driven `update()` call — so no race window exists where OrbitControls could see a stale/default target. Camera POSITION is therefore proven consistent between contexts; the real divergence found is in camera ROTATION (see Current Focus reasoning_checkpoint)."
- hypothesis: "The editor and published page load two different avatar model URLs (e.g. a different intrinsic model scale/proportions), which would explain a framing difference with no camera-side cause at all."
  reason: "Directly verified via wp-cli (docker exec into the wp-env container): post=87's block has avatar=0 (no attachment set) in the raw post content, so BOTH contexts fall through to the same platform-resolved global default avatar. Confirmed identical avatarUrl (a live Platform-signed S3 GLB URL with the same signature/timestamp, from the same 5-minute PlatformClient::fetch_preview() transient cache) via curl on the published page's data-khaveeai-config AND a direct `wp eval` call reproducing PlatformConfigSource->get_runtime_config() server-side. Same model file in both contexts."
- hypothesis: "CameraController's imperative camera.lookAt() and/or OrbitControls' per-frame update() loop interact with a stale/mismatched camera.up vector (non-default handedness) to produce a mirrored or rotated effective view — the checkpoint's own leading theory for the reported 'opposite horizontal side' symptom."
  reason: "Read three-stdlib@2.36.0's installed OrbitControls.js update() function directly (controls/OrbitControls.js:173-296): the up-alignment quaternion (`quat.setFromUnitVectors(object.up, (0,1,0))`) is recomputed FROM THE LIVE camera.up on EVERY SINGLE update() call (line 183), never cached/stale across frames — ruling out staleness by source inspection. Then directly measured the camera's world-space matrix basis vectors (right/up/forward, extracted from camera.matrixWorld.elements — unambiguous, unlike Euler rotation) via live CDP instrumentation in both contexts: rightWorld=[1,0,0], upWorld=[0,0.9376,-0.3478], forwardWorld=[0,-0.3478,-0.9376] were IDENTICAL to 4 decimal places in both the published main-block camera and the editor camera. No mirroring/flip exists at the three.js level. Also grepped the entire codebase for any CSS transform/scaleX(-1)/mirror rule — none exists. Confirmed via both static tracing AND live measurement, not inference."

## Suggested Investigation Direction (not yet executed)

The two rendering paths' STRUCTURAL difference is:
- packages/wp-bundle/src/mount.tsx's AvatarScene (published-page path): sets `<Canvas camera={{ position: scene.cameraPosition, fov: scene.cameraFov }}>` ONCE at Canvas creation (documented elsewhere in this codebase as "initialization-only, not reactive" — Pitfall 7) and never touches the camera again after that.
- packages/wp-bundle/src/preview/PreviewAvatarCanvas.tsx (editor/Settings-page preview path): ALSO sets the same `camera={{position, fov}}` prop at Canvas creation, but ADDITIONALLY renders `<CameraController>` (imperative useEffect: `camera.position.set(...)`, `camera.lookAt(...)`, `camera.fov = fov; camera.updateProjectionMatrix()`, re-firing whenever position/target/fov prop VALUES change) and `<OrbitControls target=... makeDefault onEnd={handleOrbitEnd} />` (drei) for interactive author-driven orbiting.

Since the Canvas `camera` prop itself is identical and CameraController's effect (on first mount) sets the exact same values the prop already provided, the extra machinery SHOULD be a no-op on first paint — yet the visual result differs. Investigate:
1. Whether drei's `<OrbitControls>` mounting performs its own internal camera adjustment on mount (e.g. captures/normalizes camera.position relative to its `target` prop, or calls `.update()` in a way that changes distance/zoom) that conflicts with the position CameraController/the initial Canvas prop set.
2. React effect ordering between CameraController's useEffect and whatever effect(s) drei's OrbitControls registers internally — could OrbitControls' own effect run AFTER CameraController's and reset camera state based on stale/default values?
3. Whether the R3F Canvas's actual DOM/CSS aspect ratio differs enough between contexts to matter (should only affect horizontal FOV under standard three.js PerspectiveCamera semantics, not vertical framing — but worth confirming empirically rather than assuming).
4. Directly read the LIVE THREE.Camera object's actual runtime `.position`/`.fov` inside each context (not just the React config props) to see if the true values diverge from what resolveSceneDefaults() computed — this is the most direct falsification test and hasn't been done yet.

## Resolution

root_cause: "TWO independent, real defects were found across this debug session, both now fixed. (1) [Fixed earlier this session, remains valid but was NOT the cause of the reported symptom] react-three-fiber's Canvas hardcodes camera.lookAt(0,0,0) once at creation whenever the camera prop has no explicit rotation — mount.tsx's published-page path had no correction (PreviewAvatarCanvas.tsx already did, via CameraController + OrbitControls). This was a real but separate, much smaller (~2.5deg) divergence — extensively proven NOT to be the cause of 'headshot vs full body' by two independent measurement techniques showing camera position/rotation/fov/aspect fully identical between contexts after this fix. (2) [The ACTUAL root cause, found 2026-07-10T03:15] A devicePixelRatio-gated CSS-sizing bug in the editor/Gutenberg-iframe path only, invisible at devicePixelRatio=1 (why every prior self-verification round found nothing) and 100% reproducible at devicePixelRatio=2 (the human's real Retina Mac). @react-three/fiber's Canvas-resize subscription decides whether to apply canvas.style.width/height via `gl.domElement instanceof HTMLCanvasElement` (node_modules/@react-three/fiber/dist/events-d0449b76.cjs.dev.js:1120-1121). khaveeai-preview.js executes in the TOP wp-admin window's JS realm (WordPress's editorScript field is never injected into the block-canvas iframe — see preview.ts's own documented design), while its React root renders into a div living INSIDE iframe[name=\"editor-canvas\"] — a separate realm/document. The canvas DOM node React creates therefore belongs to the iframe's realm, so this cross-realm instanceof check against the TOP window's HTMLCanvasElement constructor always evaluates false. updateStyle becomes false, and three.js's WebGLRenderer.setSize() never sets the canvas's CSS style — the canvas falls back to its intrinsic size, which equals its width/height HTML attributes (correctly containerCSSSize*devicePixelRatio, the draw-buffer resolution) interpreted directly AS CSS pixels. At dpr=1 this numerically equals the correct size (bug invisible). At dpr=2 the canvas renders at exactly 2x its intended CSS footprint, producing the reported 'zoomed in / cropped to head+shoulders' framing — measured directly via CDP: editor canvas CSS rect (3996x800) equaled its own drawing-buffer attribute (3996x800) while its immediate parent div measured 1998x400 (exactly half), versus the published page where the same ratio was the CORRECT 2:1 (attribute:CSS)."
fix: "(1) [Earlier, valid, retained] Shared packages/wp-bundle/src/CameraController.tsx used by both mount.tsx and PreviewAvatarCanvas.tsx to imperatively re-aim the camera at scene.cameraTarget. (2) [New, addresses the actual reported symptom] Added CanvasStyleSync to packages/wp-bundle/src/preview/PreviewAvatarCanvas.tsx: a small sibling component (mirrors the existing CameraController/CameraRefCapture pattern) that reads R3F's own already-correct `size` state via useThree() and unconditionally applies it to gl.domElement.style.width/height on every size change, bypassing the broken cross-realm instanceof gate entirely. Scoped to PreviewAvatarCanvas.tsx only (the sole Canvas ever mounted into iframe-realm DOM); PreviewFloatingWidget.tsx was confirmed to reuse this same component, so it is covered with no separate change. mount.tsx's AvatarScene (published page) needs no equivalent fix — it already executes in the same document realm as its own script."
verification: "wp-bundle build (node build.mjs) and TypeScript typecheck (tsc --noEmit) both pass with no new errors beyond the same single pre-existing unrelated TS2322 in mount.tsx (documented as predating this debug session).\nROOT-CAUSE-MATCHING REPRODUCTION AND FIX, at devicePixelRatio=2 (the human's actual reported environment — the first time in this entire debug session the bug was actually reproduced): isolated headless Chrome launched with --force-device-scale-factor=2 --high-dpi-support=1.\nBEFORE fix: editor canvas CSS rect = 3996x800 (== its own attribute size, == 2x its parent container's 1998x400) — confirmed via direct getBoundingClientRect()/style.cssText/getComputedStyle() inspection inside the iframe's own execution context.\nAFTER fix (CanvasStyleSync added, wp-bundle rebuilt): editor canvas CSS rect = 1998x400 (correctly matching its container), drawing-buffer attribute unchanged at 3996x800 (Retina sharpness preserved). Full-page screenshots at dpr=2 confirm the editor now renders full-body framing identical to the published page (both previously showed full-body only at dpr=1, where the bug was invisible).\nThis is the first successful A/B (before/after) reproduction-and-fix verification in the session, at the specific condition (devicePixelRatio != 1) that every prior round lacked.\nHUMAN VERIFICATION (2026-07-10T03:35:00Z): confirmed fixed on their real dpr=2 Retina display — hard-reloaded http://localhost:8888/wp-admin/post.php?post=87&action=edit, full body now renders correctly, matching the published page's framing and offset exactly. Root cause and fix confirmed correct end-to-end."
files_changed:
  - packages/wp-bundle/src/CameraController.tsx (new earlier this session; shared camera-aim fix, valid but not the cause of the reported symptom)
  - packages/wp-bundle/src/mount.tsx (CameraController wiring, earlier this session)
  - packages/wp-bundle/src/preview/PreviewAvatarCanvas.tsx (CanvasStyleSync added 2026-07-10T03:20 — the fix for the actual reported symptom)
  - wordpress-plugin/build/khaveeai-bundle.js (rebuilt)
  - wordpress-plugin/build/khaveeai-preview.js (rebuilt)

## Relevant Files

- packages/wp-bundle/src/preview/PreviewAvatarCanvas.tsx (CameraController, CanvasStyleSync [THE fix for the reported symptom], CameraRefCapture, OrbitControls)
- packages/wp-bundle/src/mount.tsx (AvatarScene — published path, has CameraController but does not need CanvasStyleSync — same-realm)
- packages/wp-bundle/src/config.ts (resolveSceneDefaults, CAMERA_PRESETS — confirmed identical output already)
- packages/wp-bundle/src/CameraController.tsx (shared camera-aim correction, a separate valid fix from earlier this session)
- packages/wp-bundle/src/preview.ts (documents the cross-realm top-window-script / iframe-mount-point design that is the ultimate cause of the instanceof mismatch)
- node_modules/@react-three/fiber/dist/events-d0449b76.cjs.dev.js:1120-1121 (third-party library code containing the instanceof gate — not modified; worked around instead)

## Test Environment

wp-env: http://localhost:8888/wp-admin/post.php?post=87&action=edit (editor, admin/password) vs http://localhost:8888/?page_id=87 (published), same post/block config, Khavee Platform API key connected.
