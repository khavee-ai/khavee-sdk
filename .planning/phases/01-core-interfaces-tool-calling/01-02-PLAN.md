---
phase: 01-core-interfaces-tool-calling
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - packages/core/src/types/pipeline.ts
  - packages/core/src/types/mock.ts
  - packages/core/src/types/index.ts
  - packages/providers/mock/src/index.ts
  - packages/providers/openai/src/index.ts
autonomous: true
requirements: [CORE-01, CORE-02, CORE-06]
must_haves:
  truths:
    - "VADProvider, STTProvider, LLMProvider, and TTSProvider interfaces exist in @khaveeai/core"
    - "Each provider interface declares at least one capability flag readable synchronously (e.g. supportsStreaming) so a consumer can branch without calling a method"
    - "The LLMProvider completion result carries toolCalls as an array (zero-or-more), with a vendor-neutral ToolCall.id field — no OpenAI-specific tool_call_id naming"
    - "A written sketch demonstrates Anthropic and Gemini multi-tool-call round trips mapping onto the neutral {id, name, args} shape without an interface redesign"
    - "The legacy mock.ts LLMProvider/TTSProvider are renamed so the new pipeline-stage names compile clean through the barrel, and their two consumers still build"
  artifacts:
    - path: "packages/core/src/types/pipeline.ts"
      provides: "VADProvider, STTProvider, LLMProvider, TTSProvider interfaces + STTResult, ToolCall, LLMCompletionResult + capability flags + vendor-mapping sketch"
      contains: "export interface LLMProvider"
    - path: "packages/core/src/types/mock.ts"
      provides: "renamed legacy LegacyLLMProvider/LegacyTTSProvider (collision fix)"
      contains: "LegacyLLMProvider"
  key_links:
    - from: "packages/core/src/types/index.ts"
      to: "packages/core/src/types/pipeline.ts"
      via: "barrel re-export"
      pattern: "export \\* from ['\"]\\./pipeline['\"]"
    - from: "packages/providers/mock/src/index.ts"
      to: "renamed legacy types"
      via: "import of LegacyLLMProvider/LegacyTTSProvider"
      pattern: "Legacy(LLM|TTS)Provider"
    - from: "packages/core/src/types/pipeline.ts (ToolCall)"
      to: "vendor-neutral correlation id"
      via: "ToolCall.id field, never named tool_call_id"
      pattern: "id: string"
---

<objective>
Define the four vendor-neutral pipeline-stage interfaces (`VADProvider`, `STTProvider`, `LLMProvider`, `TTSProvider`) with capability flags and a vendor-neutral `ToolCall`/`LLMCompletionResult` type system in a new `pipeline.ts`, plus a written multi-vendor mapping sketch. Resolve the `LLMProvider` naming collision with the legacy `mock.ts` types by renaming the legacy exports and updating their two consumers.

Purpose: CORE-01 (four interfaces every vendor can implement), CORE-02 (capability flags so consumers branch without redesign), CORE-06 (no OpenAI-specific field names; Anthropic/Gemini-shaped multi-tool-call round trip maps onto the interface). This is the contract Phase 2's orchestrator and Phase 4's adapters build against.
Output: `pipeline.ts` with all four interfaces + flags + sketch, renamed legacy `mock.ts` types, updated barrel and two consumer import sites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-interfaces-tool-calling/01-CONTEXT.md
@.planning/phases/01-core-interfaces-tool-calling/01-RESEARCH.md
@.planning/phases/01-core-interfaces-tool-calling/01-PATTERNS.md

<interfaces>
<!-- Existing concrete stage classes to generalize into interfaces (read for exact method signatures; do NOT modify them this phase). -->
From packages/providers/openai-stt-tts/src/AudioRecorder.ts — VADProvider analog: connect/disconnect/pause/resume + onSpeechStart/onUtteranceReady(wav: Blob)/onError callbacks
From packages/providers/openai-stt-tts/src/STTClient.ts — STTProvider analog: transcribe(wavBlob: Blob, ...) currently returns Promise<string>; D-06 changes return to STTResult { text: string; rejected?: boolean }
From packages/providers/openai-stt-tts/src/ChatClient.ts — LLMProvider analog: complete(args) returns { text, usage }; net-new is toolCalls: ToolCall[] (no tool-calling exists today)
From packages/providers/openai-stt-tts/src/TTSPlayer.ts — TTSProvider analog: speak(text, config, audioContext, onAudioData) — browser-coupled, keep that coupling

<!-- Vendor field-name divergence table (RESEARCH Pattern 4) — the sketch must reduce all three to {id, name, args}. -->
OpenAI: tool_calls[].id / reply tool_call_id
Anthropic: tool_use block id / reply tool_use_id  (multiple tool_use blocks per turn)
Gemini: functionCall.id / functionResponse.id  (parallel calls, matched by id)

<!-- Existing Provider marker interface (Open Question 2 — recommend extending it). -->
From packages/core/src/types/providers.ts:
```typescript
export interface Provider { name: string; version?: string; }
```

<!-- Current legacy mock.ts content that collides on the name LLMProvider/TTSProvider. -->
From packages/core/src/types/mock.ts: exports `LLMProvider` (streamChat) and `TTSProvider` (speak) — these must be renamed to LegacyLLMProvider/LegacyTTSProvider.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve the LLMProvider/TTSProvider naming collision in mock.ts and its consumers</name>
  <files>packages/core/src/types/mock.ts, packages/providers/mock/src/index.ts, packages/providers/openai/src/index.ts</files>
  <read_first>
    - packages/core/src/types/mock.ts (current LLMProvider/TTSProvider/KhaveeConfig definitions)
    - packages/providers/mock/src/index.ts (imports LLMProvider, TTSProvider; MockLLM implements LLMProvider, MockTTS implements TTSProvider)
    - packages/providers/openai/src/index.ts (imports LLMProvider; LLMOpenAI implements LLMProvider)
    - .planning/phases/01-core-interfaces-tool-calling/01-RESEARCH.md (Pitfall 1 — collision resolution, Option 1 rename recommended)
  </read_first>
  <action>
    In packages/core/src/types/mock.ts: rename `export interface LLMProvider` to `export interface LegacyLLMProvider` and `export interface TTSProvider` to `export interface LegacyTTSProvider`. Update the `KhaveeConfig` interface fields to reference the renamed types (`llm?: LegacyLLMProvider; tts?: LegacyTTSProvider;`). This is a rename-only change — do NOT alter the interface bodies (streamChat / speak signatures stay identical) or KhaveeConfig's `realtime`/`tools` fields.
    In packages/providers/mock/src/index.ts: change the import to `import { LegacyLLMProvider, LegacyTTSProvider } from '@khaveeai/core';` and update `class MockLLM implements LegacyLLMProvider` and `class MockTTS implements LegacyTTSProvider`. No behavior change to the class bodies.
    In packages/providers/openai/src/index.ts: change the import to `import { LegacyLLMProvider } from '@khaveeai/core';` and update `class LLMOpenAI implements LegacyLLMProvider`. No behavior change.
    Do NOT add the new pipeline-stage LLMProvider to these consumers — they keep using the legacy (renamed) type.
  </action>
  <verify>
    <automated>grep -q 'LegacyLLMProvider' packages/core/src/types/mock.ts && grep -q 'LegacyTTSProvider' packages/core/src/types/mock.ts && ! grep -qE 'export interface (LLMProvider|TTSProvider)' packages/core/src/types/mock.ts && grep -q 'LegacyLLMProvider' packages/providers/mock/src/index.ts && grep -q 'LegacyLLMProvider' packages/providers/openai/src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/src/types/mock.ts exports `LegacyLLMProvider` and `LegacyTTSProvider`, and no longer exports `LLMProvider` or `TTSProvider`
    - `KhaveeConfig.llm` is typed `LegacyLLMProvider`, `KhaveeConfig.tts` is typed `LegacyTTSProvider`
    - packages/providers/mock/src/index.ts: MockLLM implements LegacyLLMProvider, MockTTS implements LegacyTTSProvider
    - packages/providers/openai/src/index.ts: LLMOpenAI implements LegacyLLMProvider
    - The streamChat/speak signatures and all class bodies are byte-unchanged except the type name (verified by git diff showing only identifier renames)
  </acceptance_criteria>
  <done>The legacy mock.ts types are renamed and both consumers updated; the name `LLMProvider`/`TTSProvider` is now free for the new pipeline interfaces with no duplicate-export collision.</done>
</task>

<task type="auto">
  <name>Task 2: Create pipeline.ts with the four provider interfaces, capability flags, ToolCall/LLMCompletionResult, and the multi-vendor sketch</name>
  <files>packages/core/src/types/pipeline.ts, packages/core/src/types/index.ts</files>
  <read_first>
    - packages/providers/openai-stt-tts/src/AudioRecorder.ts (VADProvider lifecycle + callback shape)
    - packages/providers/openai-stt-tts/src/STTClient.ts (STTProvider.transcribe signature)
    - packages/providers/openai-stt-tts/src/ChatClient.ts (LLMProvider.complete result shape)
    - packages/providers/openai-stt-tts/src/TTSPlayer.ts (TTSProvider.speak signature, browser-coupled)
    - packages/core/src/types/providers.ts (Provider marker interface to extend)
    - packages/core/src/types/tools.ts (ToolResult — created in Plan 01; pipeline.ts imports ToolResult from here, NOT redefining it)
    - .planning/phases/01-core-interfaces-tool-calling/01-PATTERNS.md (Pattern Assignments for pipeline.ts — exact target shapes)
    - .planning/phases/01-core-interfaces-tool-calling/01-RESEARCH.md (Pattern 4 — vendor field-name table for the sketch)
  </read_first>
  <action>
    Create packages/core/src/types/pipeline.ts. Import `Tool` and `ToolResult` from "./tools" (intra-core relative import is fine — these are sibling files). Define:
    1. `ToolCall` interface: `{ id: string; name: string; args: Record<string, any> }` — `id` is the vendor-neutral correlation field per D-05 (NEVER named tool_call_id/call_id/tool_use_id). Add a field-level JSDoc on `id` documenting that it maps from OpenAI tool_calls[].id / Realtime call_id, Anthropic tool_use.id, Gemini functionCall.id (CORE-06).
    2. `LLMCompletionResult` interface: `{ text?: string; toolCalls: ToolCall[] }` — `toolCalls` is an array (zero-or-more) per D-04, with JSDoc noting the length===1 case is today's OpenAI Realtime behavior.
    3. `STTResult` interface: `{ text: string; rejected?: boolean }` per D-06, with JSDoc on `rejected` (set when the vendor flags likely hallucination/silence; omitted by vendors that never reject).
    4. The four provider interfaces, each `extends Provider` (from "./providers" — gives free `name`/`version` for orchestrator diagnostics, per RESEARCH Open Question 2 recommendation):
       - `VADProvider`: `connect(): Promise<void>`, `disconnect(): Promise<void>`, `pause(): Promise<void>`, `resume(): Promise<void>`, `isListening(): boolean`, plus optional callbacks `onSpeechStart?`, `onUtteranceReady?: (wav: Blob) => void`, `onError?: (error: Error) => void` (generalized from AudioRecorder). VAD is inherently event-driven — no supportsStreaming flag here per PATTERNS guidance.
       - `STTProvider`: `readonly supportsStreaming: boolean`, `readonly supportsRejection: boolean`, `transcribe(audio: Blob, opts?: { language?: string }): Promise<STTResult>`.
       - `LLMProvider`: `readonly supportsToolCalling: boolean`, `readonly supportsStreaming: boolean`, `complete(args: { messages: Array<{ role: string; content: string }>; tools?: Tool[] }): Promise<LLMCompletionResult>`. (Clean-slate design — not a retrofit of ChatClient, which has no tool-calling.)
       - `TTSProvider`: `readonly supportsStreaming: boolean`, `speak(text: string, opts: { audioContext: AudioContext; onAudioData?: (analyser: AnalyserNode, audioContext: AudioContext) => void; voice?: string; speed?: number }): Promise<void>` (keep the browser Web Audio coupling per CLAUDE.md browser-only constraint).
       Default capability flags for the two real vendors this milestone (Thonburian STT, JaiTTS) are whole-utterance/non-streaming — document `supportsStreaming` default intent as `false` in JSDoc (the flag is a property each implementation sets, not a default value in the interface).
    5. A written sketch satisfying success criterion 5, delivered as a top-of-file or near-LLMProvider block comment: show how Anthropic (`tool_use` blocks with `id`) and Gemini (`functionCall.id`) multi-tool-call responses each map onto `ToolCall[]` with the neutral `{id, name, args}` shape, with no OpenAI-specific field names in the interface. Reference the RESEARCH Pattern 4 vendor table.
    Apply field-level JSDoc on every capability flag and every field of ToolCall/STTResult/LLMCompletionResult (CLAUDE.md JSDoc convention).
    Add `export * from "./pipeline";` to packages/core/src/types/index.ts (append after the `export * from "./tools";` line added in Plan 01).
  </action>
  <verify>
    <automated>grep -q 'export interface VADProvider' packages/core/src/types/pipeline.ts && grep -q 'export interface STTProvider' packages/core/src/types/pipeline.ts && grep -q 'export interface LLMProvider' packages/core/src/types/pipeline.ts && grep -q 'export interface TTSProvider' packages/core/src/types/pipeline.ts && grep -q 'toolCalls: ToolCall\[\]' packages/core/src/types/pipeline.ts && ! grep -q 'tool_call_id' packages/core/src/types/pipeline.ts && grep -q "export \* from './pipeline'" packages/core/src/types/index.ts && pnpm --filter @khaveeai/core build</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/src/types/pipeline.ts exports `VADProvider`, `STTProvider`, `LLMProvider`, `TTSProvider`, `ToolCall`, `LLMCompletionResult`, `STTResult`
    - Each of the four provider interfaces declares at least one `readonly` capability flag (CORE-02)
    - `LLMCompletionResult.toolCalls` is typed `ToolCall[]` (array, not a single optional) per D-04
    - `ToolCall.id` is named `id` (string); the file contains zero occurrences of `tool_call_id`, `call_id`, or `tool_use_id` as field names (CORE-06)
    - `STTProvider.transcribe` returns `Promise<STTResult>` (not `Promise<string>`) per D-06
    - The file contains a written sketch mapping Anthropic and Gemini multi-tool-call shapes onto `{id, name, args}` (success criterion 5)
    - pipeline.ts imports `ToolResult` from "./tools" (does not redefine it)
    - `pnpm --filter @khaveeai/core build` succeeds with no duplicate-export error (proves the mock.ts rename resolved the collision)
    - packages/core/src/types/index.ts contains `export * from './pipeline';`
  </acceptance_criteria>
  <done>All four vendor-neutral pipeline interfaces, the capability flags, the vendor-neutral ToolCall/LLMCompletionResult, and the multi-vendor mapping sketch exist in @khaveeai/core and build clean through the barrel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds interface/type declarations only — no runtime, no network, no auth, no input handling at this layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | `LLMProvider`/`STTProvider`/`TTSProvider` interface contracts | accept | Pure compile-time type declarations; no runtime surface to attack. Vendor field-name mapping (the actual untrusted-data crossing) is the adapter layer's responsibility in Phase 4, out of scope here |
| T-01-04 | Information Disclosure | `ToolCall.args: Record<string, any>` carrying tool arguments | accept | Type-level shape only; no logging/persistence added this phase. Any sensitive-arg handling is the adapter/tool-author concern downstream |
</threat_model>

<verification>
- `pnpm --filter @khaveeai/core build` succeeds (proves no duplicate `LLMProvider` export after the mock.ts rename)
- `pnpm --filter @khaveeai/providers-mock build` and `pnpm --filter @khaveeai/providers-openai build` succeed (consumers compile against renamed legacy types) — if these package filter names differ, build via `pnpm run build:packages`
- `git diff packages/core/src/types/realtime.ts` shows NO changes (RealtimeTool untouched — RESEARCH Pitfall 3)
- `grep -c 'tool_call_id\|tool_use_id' packages/core/src/types/pipeline.ts` returns 0 (no vendor-specific field names — CORE-06)
</verification>

<success_criteria>
- Four vendor-neutral interfaces with capability flags exist in @khaveeai/core (CORE-01, CORE-02)
- The tool-calling interface uses vendor-neutral `ToolCall.id` and an array result, with a sketch proving Anthropic/Gemini map on without redesign (CORE-06)
- The legacy mock.ts collision is resolved and both consumers build
</success_criteria>

<output>
Create `.planning/phases/01-core-interfaces-tool-calling/01-02-SUMMARY.md` when done
</output>
