---
phase: 01-core-interfaces-tool-calling
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - packages/providers/openai-stt-tts/src/ToolExecutor.ts
  - packages/providers/openai-realtime/src/ToolExecutor.ts
  - packages/providers/openai-stt-tts/src/OpenAISTTTTSProvider.ts
  - packages/providers/openai-realtime/src/OpenAIRealtimeProvider.ts
  - packages/providers/openai-stt-tts/src/index.ts
  - packages/providers/openai-realtime/src/index.ts
autonomous: true
requirements: [CORE-05]
must_haves:
  truths:
    - "No byte-for-byte duplicate ToolExecutor.ts remains in openai-stt-tts or openai-realtime"
    - "Both providers import ToolExecutor from @khaveeai/core (the promoted single copy)"
    - "Both openai-stt-tts and openai-realtime compile and their existing tests pass against the promoted ToolExecutor"
  artifacts:
    - path: "packages/providers/openai-stt-tts/src/OpenAISTTTTSProvider.ts"
      provides: "import of ToolExecutor from @khaveeai/core"
      contains: 'import { ToolExecutor } from "@khaveeai/core"'
    - path: "packages/providers/openai-realtime/src/OpenAIRealtimeProvider.ts"
      provides: "import of ToolExecutor from @khaveeai/core"
      contains: 'from "@khaveeai/core"'
  key_links:
    - from: "packages/providers/openai-stt-tts/src/OpenAISTTTTSProvider.ts"
      to: "@khaveeai/core ToolExecutor"
      via: "import statement (was ./ToolExecutor)"
      pattern: 'ToolExecutor.*@khaveeai/core'
    - from: "packages/providers/openai-realtime/src/OpenAIRealtimeProvider.ts"
      to: "@khaveeai/core ToolExecutor"
      via: "import statement (was ./ToolExecutor)"
      pattern: 'ToolExecutor.*@khaveeai/core'
---

<objective>
Complete CORE-05 deduplication: delete both byte-for-byte-identical local `ToolExecutor.ts` files in `openai-stt-tts` and `openai-realtime`, repoint every importer to the promoted `@khaveeai/core` ToolExecutor, and prove both packages still compile and test green.

Purpose: CORE-05 requires the tool-execution logic to exist exactly once in core with no remaining duplicate. Plan 01 created the promoted copy; this plan removes the duplicates and rewires consumers. Depends on Plan 01 (the promoted ToolExecutor must exist in @khaveeai/core first).
Output: two deleted files, two updated provider imports, two updated package barrels, green builds/tests on both providers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-interfaces-tool-calling/01-CONTEXT.md
@.planning/phases/01-core-interfaces-tool-calling/01-PATTERNS.md

<interfaces>
<!-- The promoted ToolExecutor (from Plan 01) has the SAME public API as the local copies: register(name, fn), execute(name, args), getRegisteredFunctions(). No call-site logic changes — only the import path. -->
From @khaveeai/core: `export class ToolExecutor { register(name, fn); execute(name, args): Promise<{success, message}>; getRegisteredFunctions(): string[] }`

<!-- Exact import lines to change (verified by grep). -->
packages/providers/openai-stt-tts/src/OpenAISTTTTSProvider.ts:19  ->  import { ToolExecutor } from "./ToolExecutor";
packages/providers/openai-realtime/src/OpenAIRealtimeProvider.ts:15  ->  import { ToolExecutor } from "./ToolExecutor";

<!-- Package barrels currently re-export the local ToolExecutor. -->
packages/providers/openai-stt-tts/src/index.ts:3  ->  export { ToolExecutor } from "./ToolExecutor";
packages/providers/openai-realtime/src/index.ts:2  ->  export { ToolExecutor } from './ToolExecutor';
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete duplicate ToolExecutor files and repoint imports to @khaveeai/core</name>
  <files>packages/providers/openai-stt-tts/src/ToolExecutor.ts, packages/providers/openai-realtime/src/ToolExecutor.ts, packages/providers/openai-stt-tts/src/OpenAISTTTTSProvider.ts, packages/providers/openai-realtime/src/OpenAIRealtimeProvider.ts, packages/providers/openai-stt-tts/src/index.ts, packages/providers/openai-realtime/src/index.ts</files>
  <read_first>
    - packages/providers/openai-stt-tts/src/OpenAISTTTTSProvider.ts (line 19 import; `new ToolExecutor()` usage at ~line 129 stays identical)
    - packages/providers/openai-realtime/src/OpenAIRealtimeProvider.ts (line 15 import; `new ToolExecutor()` at ~line 82 and handleToolCall at ~577-604 stay identical)
    - packages/providers/openai-stt-tts/src/index.ts (line 3 re-export to remove)
    - packages/providers/openai-realtime/src/index.ts (line 2 re-export to remove)
    - packages/core/src/types/tools.ts (the promoted ToolExecutor — created in Plan 01; confirm its public API matches before deleting the local copies)
  </read_first>
  <action>
    Delete packages/providers/openai-stt-tts/src/ToolExecutor.ts and packages/providers/openai-realtime/src/ToolExecutor.ts (use `git rm`).
    In packages/providers/openai-stt-tts/src/OpenAISTTTTSProvider.ts: change line 19 from `import { ToolExecutor } from "./ToolExecutor";` to `import { ToolExecutor } from "@khaveeai/core";`. Do NOT change `this.toolExecutor = new ToolExecutor();` or any other line — the promoted class's public API (register/execute/getRegisteredFunctions) is identical.
    In packages/providers/openai-realtime/src/OpenAIRealtimeProvider.ts: change line 15 from `import { ToolExecutor } from "./ToolExecutor";` to `import { ToolExecutor } from "@khaveeai/core";`. Leave handleToolCall (~577-604) and `new ToolExecutor()` (~82) byte-unchanged.
    In packages/providers/openai-stt-tts/src/index.ts: remove the line `export { ToolExecutor } from "./ToolExecutor";` (consumers import from @khaveeai/core going forward, per RESEARCH diagram's clean-removal).
    In packages/providers/openai-realtime/src/index.ts: remove the line `export { ToolExecutor } from './ToolExecutor';`.
    Verify @khaveeai/core resolves as a workspace dependency in both providers' package.json (it already does — both currently import RealtimeTool from it); no dependency addition needed.
  </action>
  <verify>
    <automated>test ! -f packages/providers/openai-stt-tts/src/ToolExecutor.ts && test ! -f packages/providers/openai-realtime/src/ToolExecutor.ts && grep -q 'import { ToolExecutor } from "@khaveeai/core"' packages/providers/openai-stt-tts/src/OpenAISTTTTSProvider.ts && grep -q '@khaveeai/core' packages/providers/openai-realtime/src/OpenAIRealtimeProvider.ts && ! grep -rq 'from "./ToolExecutor"' packages/providers/openai-stt-tts/src && ! grep -rq "from './ToolExecutor'" packages/providers/openai-realtime/src</automated>
  </verify>
  <acceptance_criteria>
    - packages/providers/openai-stt-tts/src/ToolExecutor.ts does NOT exist
    - packages/providers/openai-realtime/src/ToolExecutor.ts does NOT exist
    - OpenAISTTTTSProvider.ts imports ToolExecutor from "@khaveeai/core"
    - OpenAIRealtimeProvider.ts imports ToolExecutor from "@khaveeai/core"
    - Neither provider's src/ contains any `./ToolExecutor` import or re-export (grep returns nothing)
    - The `new ToolExecutor()` construction sites and handleToolCall logic are unchanged (git diff shows only import-line and barrel-line changes)
  </acceptance_criteria>
  <done>Both duplicate ToolExecutor.ts files are deleted; both providers and their barrels reference the promoted @khaveeai/core copy; only import/barrel lines changed.</done>
</task>

<task type="auto">
  <name>Task 2: Verify both providers build and test green against the promoted ToolExecutor</name>
  <files>packages/providers/openai-stt-tts/package.json, packages/providers/openai-realtime/package.json</files>
  <read_first>
    - packages/providers/openai-stt-tts/package.json (build + test scripts)
    - packages/providers/openai-realtime/package.json (build script)
  </read_first>
  <action>
    Build @khaveeai/core first (so its dist is current for the providers' TS path resolution), then build both providers and run openai-stt-tts's existing vitest suite. Run from repo root: `pnpm --filter @khaveeai/core build`, then `pnpm run build:packages` (builds all workspace packages including both providers), then `pnpm --filter @khaveeai/providers-openai-stt-tts test`. If a package filter name does not resolve, fall back to the package's directory `pnpm test`/`pnpm build` script. Confirm no unresolved-import or type errors reference ToolExecutor. Do not modify source to make tests pass beyond what Task 1 already changed — if a real regression surfaces, report it rather than patching around it.
  </action>
  <verify>
    <automated>pnpm --filter @khaveeai/core build && pnpm run build:packages && pnpm --filter @khaveeai/providers-openai-stt-tts test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm run build:packages` completes with no error mentioning ToolExecutor or an unresolved `./ToolExecutor` module
    - openai-stt-tts's existing vitest suite passes (same green state as before the promotion)
    - openai-realtime compiles (tsc build succeeds) with the new @khaveeai/core import
  </acceptance_criteria>
  <done>Both providers build clean and openai-stt-tts's tests pass against the single promoted ToolExecutor — CORE-05 deduplication is complete with no regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan deletes duplicate code and rewires imports; no new runtime, network, or input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Denial of Service | promoted `ToolExecutor.execute()` now shared by both providers | mitigate | The try/catch normalization is preserved verbatim from the original duplicates (Plan 01); Task 2's green test run confirms the shared copy behaves identically to the deleted local copies |
| T-01-06 | Tampering | import-path rewiring could silently pull a wrong/stale ToolExecutor | mitigate | Task 1 verify asserts the local files are gone AND the new import targets @khaveeai/core; Task 2 build proves the import resolves to the promoted class with the expected public API |
</threat_model>

<verification>
- `pnpm run build:packages` succeeds with no ToolExecutor-related errors
- `pnpm --filter @khaveeai/providers-openai-stt-tts test` passes
- `find packages/providers -name ToolExecutor.ts` returns nothing (no duplicate remains anywhere — CORE-05)
- `git diff` on OpenAISTTTTSProvider.ts / OpenAIRealtimeProvider.ts shows only the single import-line change each
</verification>

<success_criteria>
- ToolExecutor exists exactly once (in @khaveeai/core); no byte-for-byte duplicate remains in either provider (CORE-05)
- Both providers compile and openai-stt-tts tests pass green against the promoted version
</success_criteria>

<output>
Create `.planning/phases/01-core-interfaces-tool-calling/01-03-SUMMARY.md` when done
</output>
