---
phase: 02-generic-pipeline-orchestrator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/types/pipeline.ts
  - packages/providers/openai-stt-tts/src/index.ts
  - packages/providers/generic-stt-tts/package.json
  - packages/providers/generic-stt-tts/tsconfig.json
  - packages/providers/generic-stt-tts/vitest.config.ts
  - packages/providers/generic-stt-tts/src/index.ts
autonomous: true
requirements: [ORCH-01, ORCH-03]
must_haves:
  truths:
    - "LLMProvider.complete() and TTSProvider.speak() accept an optional signal?: AbortSignal parameter (D-01), optional and best-effort so no existing or future provider is broken (D-02)"
    - "STTClient, ChatClient, TTSPlayer, AudioRecorder are importable from @khaveeai/providers-openai-stt-tts (additive exports enabling the D-06 adapters)"
    - "The @khaveeai/providers-generic-stt-tts package exists and builds with pnpm workspace linkage"
    - "openai-stt-tts behavior is unchanged — existing tests stay green"
  artifacts:
    - path: "packages/core/src/types/pipeline.ts"
      provides: "signal?: AbortSignal added to LLMProvider.complete args and TTSProvider.speak opts (D-01)"
      contains: "signal?: AbortSignal"
    - path: "packages/providers/openai-stt-tts/src/index.ts"
      provides: "Additive exports of the four helper classes + ChatClient types"
      contains: "export { STTClient }"
    - path: "packages/providers/generic-stt-tts/package.json"
      provides: "New workspace package manifest depending on @khaveeai/core and @khaveeai/providers-openai-stt-tts"
      contains: "@khaveeai/providers-generic-stt-tts"
    - path: "packages/providers/generic-stt-tts/src/index.ts"
      provides: "Barrel file for the new package (placeholder until adapters/orchestrator land)"
  key_links:
    - from: "packages/providers/generic-stt-tts/package.json"
      to: "@khaveeai/providers-openai-stt-tts"
      via: "workspace:* dependency"
      pattern: "providers-openai-stt-tts.*workspace"
---

<objective>
Lay the cross-package foundation the rest of Phase 2 builds on: (1) the D-01 interface extension that makes barge-in cancellation expressible, (2) the additive exports that let the new package import the OpenAI helper classes by package name, and (3) the scaffolding for the new `@khaveeai/providers-generic-stt-tts` workspace package.

Purpose: Without `signal?: AbortSignal` on the interfaces, ORCH-03 cancellation is impossible to express; without the additive exports, the D-06 adapters cannot wrap the helper classes without either a banned relative cross-package import or duplicating ~400 lines; without the package scaffold, no later plan has a place to write code.
Output: Modified `pipeline.ts`, modified `openai-stt-tts/src/index.ts`, and four new scaffold files in `packages/providers/generic-stt-tts/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-generic-pipeline-orchestrator/02-CONTEXT.md
@.planning/phases/02-generic-pipeline-orchestrator/02-RESEARCH.md
@.planning/phases/02-generic-pipeline-orchestrator/02-PATTERNS.md

<interfaces>
<!-- The exact current shapes being modified/scaffolded. Executor uses these directly. -->

From packages/core/src/types/pipeline.ts (LLMProvider — current; ADD signal):
```
complete(args: {
  messages: Array<{ role: string; content: string }>;
  tools?: Tool[];
}): Promise<LLMCompletionResult>;
```

From packages/core/src/types/pipeline.ts (TTSProvider — current; ADD signal):
```
speak(text: string, opts: {
  audioContext: AudioContext;
  onAudioData?: (analyser: AnalyserNode, audioContext: AudioContext) => void;
  voice?: string;
  speed?: number;
}): Promise<void>;
```

From packages/providers/openai-stt-tts/src/index.ts (current, complete contents):
```
export { OpenAISTTTTSProvider } from "./OpenAISTTTTSProvider";
export type { OpenAISTTTTSConfig } from "./OpenAISTTTTSProvider";
export { ToolExecutor } from "@khaveeai/core";
```

From packages/providers/openai-stt-tts/package.json — scaffold template (name, exports, scripts, devDependencies, peerDependencies) to copy for the new package.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add optional signal?: AbortSignal to LLMProvider.complete and TTSProvider.speak (D-01)</name>
  <files>packages/core/src/types/pipeline.ts</files>
  <read_first>
    - packages/core/src/types/pipeline.ts (the file being modified — see current LLMProvider lines 159-176 and TTSProvider lines 185-206)
    - .planning/phases/02-generic-pipeline-orchestrator/02-RESEARCH.md (Pattern 2: AbortSignal Threading, and D-01/D-02 locked decisions)
  </read_first>
  <action>
    In `LLMProvider.complete()`'s args object type, add an optional field `signal?: AbortSignal` after the existing `tools?: Tool[]` field. In `TTSProvider.speak()`'s `opts` object type, add an optional field `signal?: AbortSignal` after the existing `speed?: number` field. Both are optional and best-effort per D-02 — this is additive only, it must NOT change any other field, must NOT remove or rename anything, and must NOT touch `VADProvider`, `STTProvider`, `ToolCall`, `LLMCompletionResult`, or `STTResult`. Add a one-line `/** ... */` doc comment above each new field noting it is best-effort cancellation per D-01/D-02 (providers may ignore it; the orchestrator discards superseded results). Preserve the existing file-header comment block and all CORE-06 mapping documentation verbatim.
  </action>
  <verify>
    <automated>cd /Users/whitemalt/Documents/khavee-sdk && pnpm --filter @khaveeai/core build 2>&1 | tail -5 && grep -c "signal?: AbortSignal" packages/core/src/types/pipeline.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "signal?: AbortSignal" packages/core/src/types/pipeline.ts` returns exactly `2`
    - `pnpm --filter @khaveeai/core build` exits 0 (tsc clean)
    - `git diff packages/core/src/types/pipeline.ts` shows only additions (two new fields + two doc comments), zero deletions of existing lines except whitespace
  </acceptance_criteria>
  <done>Both interface method signatures accept an optional `signal?: AbortSignal`; @khaveeai/core compiles; no existing field changed.</done>
</task>

<task type="auto">
  <name>Task 2: Add additive helper-class exports to openai-stt-tts barrel (resolves Open Question 3 / Pitfall 4)</name>
  <files>packages/providers/openai-stt-tts/src/index.ts</files>
  <read_first>
    - packages/providers/openai-stt-tts/src/index.ts (the file being modified — currently 4 lines, exports only OpenAISTTTTSProvider, its config type, and re-exported ToolExecutor)
    - packages/providers/openai-stt-tts/src/STTClient.ts (exports `STTClient` via `export { STTClient }`)
    - packages/providers/openai-stt-tts/src/ChatClient.ts (exports `ChatClient` class and types `ChatMessage`, `ChatUsage`, `ChatResult`)
    - packages/providers/openai-stt-tts/src/TTSPlayer.ts (exports `TTSPlayer` class)
    - packages/providers/openai-stt-tts/src/AudioRecorder.ts (exports `AudioRecorder` via `export { AudioRecorder }`)
  </read_first>
  <action>
    Append (do NOT remove or reorder the existing three export lines) the following additive exports so the D-06 adapters can import these by package name (`@khaveeai/providers-openai-stt-tts`) per the CLAUDE.md "always via package name, never relative ../../" convention: `export { AudioRecorder } from "./AudioRecorder";`, `export { STTClient } from "./STTClient";`, `export { ChatClient } from "./ChatClient";`, `export type { ChatMessage, ChatUsage, ChatResult } from "./ChatClient";`, `export { TTSPlayer } from "./TTSPlayer";`. This is the explicitly-approved additive exception to the "openai-stt-tts stays untouched" constraint (RESEARCH Open Question 3 default resolution): it adds zero behavior, removes no existing export, and modifies no class body. Add a one-line comment above the new block noting these are additive exports for the generic-stt-tts adapters (D-06), zero behavior change.
  </action>
  <verify>
    <automated>cd /Users/whitemalt/Documents/khavee-sdk && pnpm --filter @khaveeai/providers-openai-stt-tts build 2>&1 | tail -5 && pnpm --filter @khaveeai/providers-openai-stt-tts test --run 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export { STTClient }" packages/providers/openai-stt-tts/src/index.ts` returns `1`
    - `grep -c "export { ChatClient }" packages/providers/openai-stt-tts/src/index.ts` returns `1`
    - `grep -c "export { TTSPlayer }" packages/providers/openai-stt-tts/src/index.ts` returns `1`
    - `grep -c "export { AudioRecorder }" packages/providers/openai-stt-tts/src/index.ts` returns `1`
    - The original `export { OpenAISTTTTSProvider }` line is still present (no deletion)
    - `pnpm --filter @khaveeai/providers-openai-stt-tts test --run` passes with the same test count as before this plan (no behavior regression)
  </acceptance_criteria>
  <done>The four helper classes (and ChatClient types) are exported from the package barrel; existing exports and tests unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold the @khaveeai/providers-generic-stt-tts workspace package</name>
  <files>packages/providers/generic-stt-tts/package.json, packages/providers/generic-stt-tts/tsconfig.json, packages/providers/generic-stt-tts/vitest.config.ts, packages/providers/generic-stt-tts/src/index.ts</files>
  <read_first>
    - packages/providers/openai-stt-tts/package.json (scaffold template — copy structure: author, license, publishConfig, files, exports, main, types, scripts)
    - packages/providers/openai-stt-tts/tsconfig.json (copy verbatim — extends ../../../tsconfig.packages.json)
    - packages/providers/openai-stt-tts/vitest.config.ts (copy verbatim)
    - pnpm-workspace.yaml (confirms packages/providers/* is already a workspace glob — no edit needed there)
  </read_first>
  <action>
    Create `packages/providers/generic-stt-tts/package.json` modeled on `openai-stt-tts/package.json` with: `"name": "@khaveeai/providers-generic-stt-tts"`, `"version": "0.1.0"`, `"description": "Generic composable STT/LLM/TTS pipeline provider for Khavee AI SDK"`, same `author`/`license`/`publishConfig`/`files`/`exports`/`main`/`types`/`scripts` blocks. For `dependencies`, declare `"@khaveeai/core": "workspace:*"` AND `"@khaveeai/providers-openai-stt-tts": "workspace:*"` (the latter is required so the D-06 adapters can import the helper classes by package name). Do NOT add `@ricky0123/vad-web` or `openai` as direct dependencies — those live behind the openai-stt-tts package the adapters wrap. Keep the same `devDependencies` (`typescript ^5.0.0`, `vitest ^2.0.0`, `@vitest/coverage-v8 ^2.0.0`) and `peerDependencies` (`react ^18.0.0 || ^19.0.0`) as openai-stt-tts. Copy `tsconfig.json` and `vitest.config.ts` verbatim from openai-stt-tts. Create `src/index.ts` as a placeholder barrel containing a single comment line documenting it will export `GenericPipelineProvider`, `GenericPipelineConfig`, and the four OpenAI adapters once Plans 02-03 land — leave it otherwise empty (an empty TS module is valid). After creating files, run `pnpm install` at repo root so the workspace links the new package.
  </action>
  <verify>
    <automated>cd /Users/whitemalt/Documents/khavee-sdk && pnpm install 2>&1 | tail -5 && pnpm --filter @khaveeai/providers-generic-stt-tts exec node -e "console.log(require('./package.json').name)" 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - `packages/providers/generic-stt-tts/package.json` parses as JSON and its `.name` is `@khaveeai/providers-generic-stt-tts`
    - `grep -c "providers-openai-stt-tts" packages/providers/generic-stt-tts/package.json` returns `1` (workspace:* dependency present)
    - `grep -c "@khaveeai/core" packages/providers/generic-stt-tts/package.json` returns at least `1`
    - `pnpm install` exits 0 and the new package appears linked (no `EUNRESOLVED` workspace errors)
    - `packages/providers/generic-stt-tts/tsconfig.json` and `vitest.config.ts` exist and match openai-stt-tts's contents
  </acceptance_criteria>
  <done>The new workspace package exists, declares correct workspace dependencies, and is linked by pnpm; placeholder barrel present for later plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser bundle → backend proxy | Adapter HTTP calls cross here in later plans; this plan only adds the interface field and package wiring |
| workspace dependency graph | Adding a new package dependency edge (generic-stt-tts → openai-stt-tts) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | New workspace package dependency on openai-stt-tts | accept | Internal workspace package already in this repo, no external registry fetch; pnpm-lock unchanged for third-party deps |
| T-02-SC | Tampering | npm/pnpm installs | accept | No new external (non-workspace) npm packages introduced — RESEARCH Package Legitimacy Audit confirms "Not applicable this phase"; `pnpm install` only relinks workspace packages |
</threat_model>

<verification>
- `pnpm --filter @khaveeai/core build` clean
- `pnpm --filter @khaveeai/providers-openai-stt-tts build && test --run` clean (no regression)
- `pnpm install` links the new package without errors
- `git diff` on pipeline.ts and openai-stt-tts/index.ts shows additive-only changes
</verification>

<success_criteria>
- `signal?: AbortSignal` present on both `LLMProvider.complete()` args and `TTSProvider.speak()` opts (grep count 2)
- Four helper classes exported from openai-stt-tts barrel; existing tests still green
- New `@khaveeai/providers-generic-stt-tts` package scaffolded, depends on core + openai-stt-tts via `workspace:*`, links via pnpm
</success_criteria>

<output>
Create `.planning/phases/02-generic-pipeline-orchestrator/02-01-SUMMARY.md` when done
</output>
