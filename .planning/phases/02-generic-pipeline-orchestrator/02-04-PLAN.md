---
phase: 02-generic-pipeline-orchestrator
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
gap_closure: true
files_modified:
  - packages/providers/generic-stt-tts/src/GenericPipelineProvider.ts
  - packages/providers/generic-stt-tts/src/__tests__/GenericPipelineProvider.test.ts
autonomous: true
requirements: [ORCH-03]
must_haves:
  truths:
    - "Triggering new user speech mid-turn cancels in-flight LLM/TTS work via an AbortSignal-style hook, observable as the in-progress response stopping rather than completing (ORCH-03 / SC3, full barge-in D-03) — now FULLY true: a superseded turn's stale STT result NEVER lands in conversation/messages, and sendMessage() participates in the single-active-turn invariant instead of racing it"
    - "runTurnFromText() discards a superseded turn before any side effect — its very first statement is an abort-signal check, so no stale user/utterance entry is pushed to this.messages or this.conversation after the turn was aborted (CR-01 closed)"
    - "runTurn() re-checks controller.signal.aborted after stt.transcribe() resolves and before calling runTurnFromText() on the non-empty-text path, so a turn aborted while its STT call was in flight does not proceed (CR-01 closed)"
    - "sendMessage() owns its own AbortController: it aborts any existing activeTurnController, registers a fresh controller, awaits runTurnFromText with that signal, and clears it in a finally block — identical turn-ownership semantics to runTurn() (CR-02 closed)"
  artifacts:
    - path: "packages/providers/generic-stt-tts/src/GenericPipelineProvider.ts"
      provides: "Barge-in/turn-coordination fixes for CR-01 (abort guards) and CR-02 (sendMessage controller ownership)"
      contains: "this.activeTurnController = controller"
    - path: "packages/providers/generic-stt-tts/src/__tests__/GenericPipelineProvider.test.ts"
      provides: "Regression tests proving conversation/message integrity post-barge-in (CR-01) and sendMessage()-vs-concurrent-VAD coordination (CR-02)"
      contains: "CR-01"
  key_links:
    - from: "packages/providers/generic-stt-tts/src/GenericPipelineProvider.ts"
      to: "runTurnFromText entry guard"
      via: "if (signal?.aborted) return; as first statement"
      pattern: "runTurnFromText"
    - from: "packages/providers/generic-stt-tts/src/GenericPipelineProvider.ts"
      to: "sendMessage activeTurnController ownership"
      via: "abort prior + register fresh controller + finally clear"
      pattern: "this\\.activeTurnController = controller"
---

<objective>
Close the single remaining ORCH-03 gap (status: BLOCKED in 02-VERIFICATION.md). The barge-in mechanism delivered in 02-03 works for the VAD-vs-VAD happy path, but the phase's own code review (02-REVIEW.md) found — and the verifier independently reproduced — two concurrency defects that corrupt the single-active-turn invariant ORCH-03/SC3/D-03 depend on:

- CR-01: `runTurnFromText()` mutates shared `this.messages`/`this.conversation` before any abort check, and `runTurn()` does not re-check the signal after STT resolves on the non-empty-text path — so a superseded turn's stale utterance lands in conversation history out of order.
- CR-02: `sendMessage()` never creates/registers its own `AbortController`, so it races uncoordinated against a concurrently-arriving VAD turn (two independent `llm.complete()` calls, duplicated/corrupted conversation entries).

Purpose: Make truth #3 of the phase's must-haves FULLY (not partially) true, lifting ORCH-03 from BLOCKED to SATISFIED. This is a gap-closure plan — it implements the exact documented fixes in 02-REVIEW.md CR-01/CR-02 and adds the regression tests named in 02-VERIFICATION.md's `missing:` list.
Output: Two small, surgical edits to `GenericPipelineProvider.ts` plus two new regression tests appended to the existing suite.

Scope guard: ONLY CR-01 + CR-02 + their regression tests. Do NOT touch WR-01..WR-05 or IN-01..IN-03 (separate Warning/Info findings, explicitly out of this gap's scope). Do NOT modify 02-01/02-02/02-03 plans or any already-passing test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-generic-pipeline-orchestrator/02-REVIEW.md
@.planning/phases/02-generic-pipeline-orchestrator/02-VERIFICATION.md

<interfaces>
<!-- The exact current shape of the two methods being fixed (GenericPipelineProvider.ts as of this planning pass). -->

sendMessage (lines 333-335) — CURRENT, defective:
  async sendMessage(text): Promise<void> { await this.runTurnFromText(text, this.activeTurnController?.signal); }
  Defect CR-02: never creates/registers its own AbortController.

runTurn (lines 348-378) — the CORRECT turn-ownership template sendMessage must mirror:
  creates `const controller = new AbortController(); this.activeTurnController = controller;`,
  aborts prior controller before that (the `if (this.activeTurnController) { this.activeTurnController.abort(); }` block at 349-353),
  and in `finally` clears `if (this.activeTurnController === controller) this.activeTurnController = null;` (374-376).
  Non-empty-text path at line 368: `await this.runTurnFromText(sttResult.text, controller.signal);`
  Defect CR-01 part B: no `if (controller.signal.aborted) return;` between STT resolving (359) and this call (368).
  Note the empty-text branch at 363 ALREADY has `if (controller.signal.aborted) return;` — copy that idiom to the non-empty path.

runTurnFromText (lines 391-404) — CURRENT, defective:
  first statement inside the try (line 394) is `this.messages.push({ role: "user", content: text });` — a side effect with ZERO preceding abort check.
  Defect CR-01 part A: needs `if (signal?.aborted) return;` as the literal first statement of the method body (before the try, or as the first line inside it, before any mutation).

state field (declared in Task 2 of 02-03): `private activeTurnController: AbortController | null = null;`

Conversation type fields used when pushing entries (already imported): { id: crypto.randomUUID(), role, text, timestamp, isFinal: true, status: "final" }.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix CR-01 (abort guards in runTurnFromText + runTurn) and CR-02 (sendMessage controller ownership)</name>
  <files>packages/providers/generic-stt-tts/src/GenericPipelineProvider.ts</files>
  <read_first>
    - packages/providers/generic-stt-tts/src/GenericPipelineProvider.ts (lines 320-460 — sendMessage at 333-335, runTurn at 348-378 including its controller-ownership try/finally and the empty-text guard at 363, runTurnFromText at 391-404 with the unguarded this.messages.push at 394)
    - .planning/phases/02-generic-pipeline-orchestrator/02-REVIEW.md (CR-01 lines 63-79 and CR-02 lines 81-112 — the documented fix snippets; replicate them, adapting the runTurnFromText snippet to this codebase's existing try-block structure)
  </read_first>
  <behavior>
    - CR-01 (runTurnFromText guard): When runTurnFromText is called with an already-aborted signal, it returns immediately and pushes NOTHING to this.messages or this.conversation and does NOT fire onConversationUpdate. A superseded Turn A whose STT resolved after Turn B aborted it leaves conversation containing only Turn B's entries — no "stale" Turn A user/assistant entry, in any position.
    - CR-01 (runTurn guard): After stt.transcribe() resolves on the non-empty-text path, if controller.signal.aborted is true the turn returns without calling runTurnFromText (mirroring the existing empty-text branch guard at line 363).
    - CR-02 (sendMessage ownership): sendMessage() aborts any existing activeTurnController, creates a fresh AbortController, registers it as this.activeTurnController, awaits runTurnFromText with controller.signal, and clears activeTurnController in a finally block only when it still === this controller. A VAD turn arriving while sendMessage() is in flight sees a non-null activeTurnController and supersedes it (one coordinated turn), instead of starting a second independent turn.
    - No regression: build stays clean; all 26 existing tests still pass.
  </behavior>
  <action>
    Apply three surgical edits, all in GenericPipelineProvider.ts, implementing the exact fixes documented in 02-REVIEW.md CR-01 and CR-02.

    EDIT 1 (CR-01 part A — runTurnFromText entry guard): Add `if (signal?.aborted) return;` as the very first statement of the runTurnFromText method body, BEFORE the existing `this.messages.push({ role: "user", content: text });` at line 394 (and before any other side effect). Place it before the `try {` so a superseded turn discards with zero mutation. Add a brief inline comment tying it to CR-01, e.g. note that a turn superseded before its first side effect must be discarded to keep conversation/messages integrity (single-active-turn invariant). Do not introduce a fenced code block in source comments beyond the existing comment style.

    EDIT 2 (CR-01 part B — runTurn non-empty-text guard): In runTurn(), immediately after `const sttResult = await this.stt.transcribe(...)` resolves and before the non-empty-text `await this.runTurnFromText(sttResult.text, controller.signal);` at line 368, add `if (controller.signal.aborted) return;`. This mirrors the guard already present in the empty-text branch at line 363. Place it so it runs on the path that proceeds to runTurnFromText (after the empty/rejected early-return block). Add an inline comment noting it discards a turn aborted while its STT call was in flight (CR-01).

    EDIT 3 (CR-02 — sendMessage controller ownership): Replace the current sendMessage body (lines 333-335) so it gives sendMessage() the same turn-ownership semantics as runTurn(), per the CR-02 documented snippet (02-REVIEW.md lines 97-112): if `this.activeTurnController` exists, call `.abort()` on it; create `const controller = new AbortController();`; assign `this.activeTurnController = controller;`; in a `try`, `await this.runTurnFromText(text, controller.signal);`; in a `finally`, `if (this.activeTurnController === controller) { this.activeTurnController = null; }`. Apply the project error-handling convention used in runTurn: any catch (if you add one) must normalize via `error instanceof Error ? error : new Error(String(error))` before `this.onError?.(...)` — but note runTurnFromText already owns its own try/catch and error normalization, so sendMessage's wrapper does not need its own catch (match runTurn, which relies on runTurnFromText's internal catch for the text path); keep only the try/finally for controller cleanup. Update the JSDoc above sendMessage (currently lines 328-332) to state it now owns the active turn controller and supersedes/aborts any in-flight turn, replacing the stale "Threads the active turn's signal, if any" wording.

    Preserve all existing inline "RESEARCH Pitfall N" / "D-0N" / "SDK-08" traceability comments. Follow the existing 2-space indent, double-quote, section-divider style. No decorative emoji logging.
  </action>
  <verify>
    <automated>cd /Users/whitemalt/Documents/khavee-sdk && pnpm --filter @khaveeai/providers-generic-stt-tts build 2>&1 | tail -6 && grep -c "this.activeTurnController = controller" packages/providers/generic-stt-tts/src/GenericPipelineProvider.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @khaveeai/providers-generic-stt-tts build` exits 0
    - `grep -c "this.activeTurnController = controller" packages/providers/generic-stt-tts/src/GenericPipelineProvider.ts` returns at least `2` (runTurn's existing assignment + sendMessage's new one)
    - The first non-comment statement inside runTurnFromText (before the `this.messages.push` at the old line 394) is `if (signal?.aborted) return;` — confirm by reading the method: the abort check precedes every mutation of this.messages / this.conversation
    - runTurn() contains `if (controller.signal.aborted) return;` on BOTH the empty-text branch AND immediately before the non-empty-text `runTurnFromText` call — `grep -c "controller.signal.aborted) return" packages/providers/generic-stt-tts/src/GenericPipelineProvider.ts` returns at least `2`
    - sendMessage() body no longer contains the bare `await this.runTurnFromText(text, this.activeTurnController?.signal);` one-liner — it now constructs and registers its own controller and clears it in a finally
    - All 26 existing tests still pass (`pnpm --filter @khaveeai/providers-generic-stt-tts test --run` green before new tests are added in Task 2)
  </acceptance_criteria>
  <done>CR-01 and CR-02 are fixed exactly per the 02-REVIEW.md documented snippets: runTurnFromText guards its first side effect, runTurn re-checks the signal post-STT on the non-empty path, and sendMessage owns/aborts/clears its own AbortController. Build is clean and the existing suite is unbroken.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regression tests — CR-01 stale-utterance discard + CR-02 sendMessage-vs-concurrent-VAD coordination</name>
  <files>packages/providers/generic-stt-tts/src/__tests__/GenericPipelineProvider.test.ts</files>
  <read_first>
    - packages/providers/generic-stt-tts/src/__tests__/GenericPipelineProvider.test.ts (the existing ORCH-03 barge-in describe at lines 154-207 — the onUtteranceReady-callback-driving + poll-until-signal-registered idiom to reuse; makeFakeVAD/makeFakeSTT/makeFakeLLM/makeBaseConfig helpers at lines 38-90)
    - packages/providers/generic-stt-tts/src/GenericPipelineProvider.ts (the fixed runTurn/runTurnFromText/sendMessage from Task 1 — the behavior the tests assert against)
    - .planning/phases/02-generic-pipeline-orchestrator/02-VERIFICATION.md (the `missing:` list lines 51-55 and the Behavioral Spot-Checks table lines 107-110 — the two exact reproduction scenarios these tests must encode)
  </read_first>
  <behavior>
    - CR-01 regression: A slow Turn A whose STT resolves AFTER Turn B has already barged in (aborting Turn A's controller) must NOT land any Turn A entry in `provider.conversation`. After both turns settle, conversation contains only Turn B's user (+ assistant) entries — no stale/out-of-order Turn A utterance. Assert by giving Turn A a controllable slow STT and Turn B a fast one, firing both via vad.onUtteranceReady, and inspecting conversation texts.
    - CR-02 regression: Calling `provider.sendMessage("...")` and then firing a VAD utterance shortly after must NOT produce two uncoordinated `llm.complete()` calls or duplicate conversation entries. With sendMessage now owning a controller, the VAD turn supersedes it: assert the single-active-turn invariant holds — e.g. the sendMessage turn's signal becomes aborted when the VAD turn arrives, and conversation does not contain duplicate assistant replies for what should be one coherent active turn.
  </behavior>
  <action>
    Append two new tests to the existing suite — do NOT modify or delete any existing passing test. Reuse the existing fake-deps builders (makeFakeVAD, makeFakeSTT, makeFakeLLM, makeFakeTTS, makeBaseConfig) and the AudioContext polyfill already at the top of the file.

    TEST 1 — `describe("CR-01: a superseded turn's stale STT result does not corrupt conversation", ...)`:
    Build a fake STT whose `transcribe` returns Turn A's text only after a controllable delay (e.g. first call awaits a ~20ms timeout and resolves `{ text: "turn A stale utterance" }`; second call resolves fast with `{ text: "turn B" }`) — use a call counter inside the vi.fn().mockImplementation, mirroring the callCount idiom in the existing ORCH-03 test. Build a fast fake LLM returning `{ text: "reply", toolCalls: [] }`. Construct the provider via makeBaseConfig, connect(). Fire `vad.onUtteranceReady?.(new Blob())` for Turn A; then fire it again for Turn B (the barge-in) — Turn B aborts Turn A's controller. Wait long enough for Turn A's slow STT to resolve (so the OLD/buggy code would push its stale utterance). Assert: `provider.conversation.map(e => e.text)` does NOT include `"turn A stale utterance"`, and includes `"turn B"`. (Under the unfixed code this test FAILS because the stale utterance lands after Turn B's entry — the CR-01 reproduction from 02-VERIFICATION.md line 109.)

    TEST 2 — `describe("CR-02: sendMessage() does not race a concurrent VAD turn", ...)`:
    Build a fake LLM whose `complete` captures each call's `args.signal` into an array and is slow on the first call (await a short timeout) so a VAD utterance can arrive mid-flight; track `complete` call count. Construct provider, connect(). Call (do not await yet) `provider.sendMessage("typed message")`; poll until the sendMessage turn has registered (its signal captured / activeTurnController set). Then fire `vad.onUtteranceReady?.(new Blob())` to simulate a VAD utterance arriving while sendMessage is in flight. Assert the single-active-turn invariant now holds: the sendMessage turn's captured signal became `aborted === true` (it was superseded by the VAD turn — proving sendMessage registered a controller per CR-02 fix), AND conversation does NOT contain duplicate assistant entries for a single coherent turn (e.g. assert no text appears twice / total assistant entries reflect one surviving turn, not two uncoordinated ones). Await all pending work before asserting (settle with a final timeout). (Under the unfixed code, sendMessage passed signal=undefined and never registered a controller, so its signal could never become aborted and the VAD turn ran independently — this test FAILS pre-fix, the CR-02 reproduction from 02-VERIFICATION.md line 110.)

    Use `vi.useFakeTimers()` only if it does not conflict with the existing real-timer poll idiom used by the ORCH-03 test in this file — prefer the same real-timer `setTimeout`/poll approach the existing barge-in test uses, for consistency. Keep both tests deterministic (poll-until-registered, not fixed single-tick waits).
  </action>
  <verify>
    <automated>cd /Users/whitemalt/Documents/khavee-sdk && pnpm --filter @khaveeai/providers-generic-stt-tts test --run 2>&1 | tail -20 && grep -c "CR-01\|CR-02" packages/providers/generic-stt-tts/src/__tests__/GenericPipelineProvider.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @khaveeai/providers-generic-stt-tts test --run` passes with at least 28 tests (the original 26 + the 2 new regression tests), zero failures
    - `grep -c "CR-01" packages/providers/generic-stt-tts/src/__tests__/GenericPipelineProvider.test.ts` returns at least `1` and `grep -c "CR-02" ...` returns at least `1`
    - TEST 1 asserts `provider.conversation` does NOT include the string `"turn A stale utterance"` after the barge-in (CR-01 conversation-integrity check)
    - TEST 2 asserts the sendMessage turn's captured AbortSignal became `aborted === true` after a concurrent VAD utterance (proving sendMessage registered its own controller, CR-02) AND asserts no duplicate assistant conversation entry
    - No existing test was modified or removed (the 26 prior tests remain, only 2 appended)
  </acceptance_criteria>
  <done>Two regression tests encode the exact CR-01 and CR-02 reproductions the verifier ran as throwaways. They pass against the Task 1 fixes and would fail against the unfixed code — locking in the single-active-turn invariant across both the VAD and sendMessage entry points.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VAD utterance → orchestrator | Rapid barge-in races state mutations on shared conversation/messages |
| sendMessage() (React/useRealtime) → orchestrator | Text-entry turn can arrive concurrently with a VAD-driven turn |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | Superseded (aborted) turn mutating shared conversation/messages state (CR-01) | mitigate | `if (signal?.aborted) return;` as runTurnFromText's first statement + a post-STT `controller.signal.aborted` guard in runTurn discard a superseded turn before any side effect; regression TEST 1 asserts no stale entry lands |
| T-02-10 | Tampering | Uncoordinated concurrent turns from sendMessage() racing a VAD turn (CR-02) | mitigate | sendMessage() now aborts the prior controller, registers its own, and clears it in finally — single-active-turn invariant holds across both entry points; regression TEST 2 asserts the supersession |
</threat_model>

<verification>
- `pnpm --filter @khaveeai/providers-generic-stt-tts build` exits 0 (CR-01/CR-02 edits compile clean)
- `pnpm --filter @khaveeai/providers-generic-stt-tts test --run` passes with >= 28 tests (26 prior + 2 regression), zero failures
- The two new regression tests fail against the unfixed code and pass against the Task 1 fixes (proven by encoding the exact 02-VERIFICATION.md repro scenarios)
- `pnpm --filter @khaveeai/providers-openai-stt-tts test --run` still passes 13/13 (compatibility constraint: existing provider untouched)
</verification>

<success_criteria>
- ORCH-03 / SC3 / D-03 full barge-in is now FULLY true: a superseded turn never corrupts conversation/messages, and sendMessage() participates in the single-active-turn invariant
- CR-01 fixed: runTurnFromText guards its first side effect; runTurn re-checks the signal post-STT on the non-empty path
- CR-02 fixed: sendMessage owns/aborts/registers/clears its own AbortController, mirroring runTurn
- Regression tests lock both fixes in; build clean; existing 26 tests + openai-stt-tts 13 tests unaffected
- WR-01..WR-05 and IN-01..IN-03 left untouched (out of this gap's scope)
</success_criteria>

<output>
Create `.planning/phases/02-generic-pipeline-orchestrator/02-04-SUMMARY.md` when done
</output>
