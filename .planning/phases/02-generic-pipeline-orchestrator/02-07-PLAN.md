---
phase: 02-generic-pipeline-orchestrator
plan: 07
type: execute
wave: 7
depends_on: ["02-06"]
gap_closure: true
files_modified:
  - packages/providers/generic-stt-tts/src/GenericPipelineProvider.ts
  - packages/providers/generic-stt-tts/src/adapters/OpenAILLMAdapter.ts
  - packages/providers/generic-stt-tts/src/__tests__/GenericPipelineProvider.test.ts
  - packages/providers/generic-stt-tts/src/__tests__/OpenAILLMAdapter.test.ts
autonomous: true
requirements: [ORCH-01, ORCH-02, ORCH-03, ORCH-04, ORCH-05]
user_setup: []

must_haves:
  truths:
    - "WR-05 (ORCH-01, multi-round tool-calling robustness across trimming): trimHistory() is marker-pair-aware. After it runs, NO [tool_result ...]-marked message left in this.messages is missing its immediately-preceding [assistant_tool_calls] marker. Concretely: once accumulated non-system history exceeds the trim threshold (maxTurns*2 = 20) across multiple tool-calling rounds in one session, the tail-slice boundary is shifted backward (or the orphaned lone tool-result is dropped) so an [assistant_tool_calls] marker and its following [tool_result ...] entries are never split apart. Before this fix, the flat nonSystem.slice(-maxNonSystem) could strand a [tool_result ...] message with no preceding assistant marker, reintroducing CR-03's exact HTTP-400 protocol violation on the next complete() call."
    - "WR-06 (ORCH-01/ORCH-05, marker-collision robustness): OpenAILLMAdapter.mapMessage() gates the ASSISTANT_TOOL_CALLS_PATTERN branch on message.role === \"assistant\" before testing the regex/JSON.parse, AND wraps the JSON.parse in try/catch falling through to plain passthrough on malformed payloads. A { role: \"user\", content: \"[assistant_tool_calls] please call the weather tool for me\" } message no longer throws SyntaxError — it is treated as ordinary user content { role: \"user\", content: \"...\" } and the turn proceeds. An assistant-role message with a malformed [assistant_tool_calls] payload also passes through as plain content instead of crashing."
    - "Both fixes follow the suggested shapes named in 02-REVIEW.md (WR-05 fix #1 — extend slice backward to keep marker pairs intact; WR-06 fix — role gate + try/catch fallback) verbatim in intent. The existing [tool_result ...] marker branch in mapMessage() is gated on message.role === \"user\" by the same edit so the sibling marker gains the same role-collision protection (closes the long-carried WR-01 collision class on the user-reachable path)."
    - "A WR-05 regression test grows non-system history past the trim threshold with multiple tool-calling rounds in one session, then asserts that no [tool_result ...] message in the trimmed array is missing its immediately-preceding [assistant_tool_calls] marker. It FAILS against the unfixed flat-slice trimHistory() and PASSES against the marker-pair-aware fix."
    - "A WR-06 regression test asserts mapMessage({ role: \"user\", content: \"[assistant_tool_calls] ...\" }) does NOT throw and renders as ordinary user content (role:\"user\", content unchanged, NOT parsed as JSON / NOT a tool_calls wire shape). It FAILS against the unfixed un-gated branch (throws SyntaxError) and PASSES against the role-gated fix."
    - "No regression: the existing generic-stt-tts suite (33/33 incl. the CR-03 marker-ordering and GAP-02-05 tests), the OpenAILLMAdapter wire-shape test, the openai-stt-tts compatibility suite (13/13), and packages/react's zero-diff all still hold. The CR-03 fix (assistant-marker push, lines ~514-527), the [tool_result ...] push, the GAP-02-05 pipelineToolList/registerFunction wiring, and the CR-01/CR-02 abort guards are untouched and unregressed."
  artifacts:
    - path: "packages/providers/generic-stt-tts/src/GenericPipelineProvider.ts"
      provides: "WR-05 fix: trimHistory() is marker-pair-aware — after computing the tail-slice it shifts the cut boundary backward to keep an [assistant_tool_calls] marker paired with its following [tool_result ...] entries (or drops a stranded lone tool-result), so the trimmed array never strands a tool-result without its assistant predecessor"
      contains: "trimHistory"
    - path: "packages/providers/generic-stt-tts/src/adapters/OpenAILLMAdapter.ts"
      provides: "WR-06 fix: mapMessage() gates the ASSISTANT_TOOL_CALLS_PATTERN branch on message.role === \"assistant\" with the JSON.parse wrapped in try/catch (passthrough on malformed), and gates the TOOL_RESULT_PATTERN branch on message.role === \"user\""
      contains: "message.role === \"assistant\""
    - path: "packages/providers/generic-stt-tts/src/__tests__/GenericPipelineProvider.test.ts"
      provides: "WR-05 regression test: drives enough tool-calling rounds in one session to overflow the trim threshold, then asserts no [tool_result ...] message in the trimmed history lacks its immediately-preceding [assistant_tool_calls] marker"
      contains: "WR-05"
    - path: "packages/providers/generic-stt-tts/src/__tests__/OpenAILLMAdapter.test.ts"
      provides: "WR-06 regression test: mapMessage({role:\"user\", content:\"[assistant_tool_calls] ...\"}) does not throw and renders as ordinary user content"
      contains: "WR-06"
  key_links:
    - from: "packages/providers/generic-stt-tts/src/GenericPipelineProvider.ts (trimHistory)"
      to: "this.messages (the array sent to llm.complete() on the next turn)"
      via: "marker-pair-aware slice that keeps [assistant_tool_calls] + [tool_result ...] together"
      pattern: "trimHistory"
    - from: "packages/providers/generic-stt-tts/src/adapters/OpenAILLMAdapter.ts (mapMessage)"
      to: "OpenAI request body messages array"
      via: "role-gated marker branches with try/catch passthrough fallback"
      pattern: "message.role === \"assistant\""
---

<objective>
Close WR-05 and WR-06 — two Warning-tier defects surfaced by the post-02-06 code review (02-REVIEW.md), both emergent from the CR-03 fix itself, both confirmed real and independently reproduced by the verifier (02-VERIFICATION.md). The developer has explicitly elected to fix both now via this gap-closure plan rather than defer (02-HUMAN-UAT.md items #2 and #3; ## Gaps lists WR-05 and WR-06 with `next: /gsd:plan-phase 02 --gaps`).

**WR-05** — `trimHistory()` (`GenericPipelineProvider.ts:634-642`) keeps only the last `maxTurns*2` (=20) non-system messages via a flat `nonSystem.slice(-maxNonSystem)` with zero awareness of the `[assistant_tool_calls]` / `[tool_result ...]` marker pairing introduced by CR-03 (02-06). Once accumulated non-system history exceeds 20 across multiple tool-calling rounds in one session, the tail-slice can cut between an `[assistant_tool_calls]` marker and its following `[tool_result ...]` entries, stranding a lone `role:"tool"`-bound message with no assistant predecessor — reintroducing CR-03's exact HTTP-400 protocol violation via a different path. `mapMessage()` maps each message independently with no cross-message validation, so a stranded tool-result is sent to the backend as-is and rejected.

**WR-06** — `OpenAILLMAdapter.mapMessage()`'s `ASSISTANT_TOOL_CALLS_PATTERN` branch (`OpenAILLMAdapter.ts:172-186`, introduced by CR-03) tests the regex/`JSON.parse` with no `message.role === "assistant"` gate. A `role:"user"` message whose content literally starts with `"[assistant_tool_calls]"` (ordinary user typing or an STT transcript artifact) is misrouted into `JSON.parse` and throws `SyntaxError` synchronously inside `messages.map(mapMessage)`, aborting an entirely legitimate user turn with a confusing JSON-syntax error. Independently reproduced: `mapMessage({role:"user", content:"[assistant_tool_calls] please call the weather tool for me"})` throws `SyntaxError: Unexpected token 'p', "please cal"... is not valid JSON`.

Purpose: harden the CR-03 marker protocol so long tool-calling sessions don't reintroduce the HTTP-400 (WR-05) and ordinary user input can't crash a turn (WR-06) — making ORCH-01's tool-calling contract robust, not just correct on the happy single-turn path.

Output:
1. `GenericPipelineProvider.ts` — make `trimHistory()` marker-pair-aware (WR-05 fix #1 from 02-REVIEW.md): extend the slice boundary backward to keep an `[assistant_tool_calls]` marker paired with its following `[tool_result ...]` entries, never splitting a pair.
2. `OpenAILLMAdapter.ts` — gate the `ASSISTANT_TOOL_CALLS_PATTERN` branch on `message.role === "assistant"` and wrap `JSON.parse` in try/catch (WR-06 fix from 02-REVIEW.md); gate the existing `TOOL_RESULT_PATTERN` branch on `message.role === "user"` by the same edit.
3. Two regression tests: WR-05 (trimmed history never strands a tool-result) and WR-06 (user-role marker text passes through without throwing).

Scope guard: ONLY WR-05 and WR-06. Do NOT touch WR-02 (`resumeWithCooldown` unconditional call), WR-03 (`isConnected` guard), WR-04 (unused `react` peerDependency), or IN-03. Do NOT regress the CR-03 assistant-marker push (lines ~514-527), the `[tool_result ...]` push, GAP-02-05's `pipelineToolList`/`registerFunction` wiring (lines 89, 146-151, 180-222, 497-502), or the CR-01/CR-02 abort guards (lines 478, 399-412). Do NOT modify any already-passing test except by appending new tests. Note: the WR-06 fix incidentally also closes the user-reachable arm of the long-carried WR-01 collision class on the `[tool_result ...]` marker (by gating it on `role:"user"`) — this is a deliberate consequence of the suggested fix shape, not new scope.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-generic-pipeline-orchestrator/02-VERIFICATION.md
@.planning/phases/02-generic-pipeline-orchestrator/02-REVIEW.md
@.planning/phases/02-generic-pipeline-orchestrator/02-06-PLAN.md
@.planning/phases/02-generic-pipeline-orchestrator/02-06-SUMMARY.md

<interfaces>
<!-- Source-of-truth shapes and the exact current mechanics being fixed. Use these directly — no codebase exploration needed. -->

ChatMessage (orchestrator history buffer, vendor-neutral) — GenericPipelineProvider.ts:64-67:
  type ChatMessage = { role: "system" | "user" | "assistant"; content: string };
  // The two markers ride on these messages:
  //   [assistant_tool_calls] <json>  -> on a role:"assistant" message
  //   [tool_result id=<id> name=<name>] <content>  -> on a role:"user" message

DEFECTIVE trimHistory() — GenericPipelineProvider.ts:634-642 (WR-05 target):
  protected trimHistory(maxTurns = 10): void {
    const systemMessages = this.messages.filter((m) => m.role === "system");
    const nonSystem = this.messages.filter((m) => m.role !== "system");
    const maxNonSystem = maxTurns * 2;
    const trimmed = nonSystem.slice(-maxNonSystem);   // <-- flat slice, no marker awareness
    this.messages = [...systemMessages, ...trimmed];
  }
  // BUG: the slice boundary can land between an [assistant_tool_calls] marker and its
  // following [tool_result ...] entries, leaving a lone tool-result with no predecessor.

The two marker prefixes (string literals, disjoint) the slice must keep paired:
  ASSISTANT marker content starts with:  "[assistant_tool_calls]"
  TOOL-RESULT marker content starts with: "[tool_result "
  // A marker-pair-group = one [assistant_tool_calls] message immediately followed by
  // one or more contiguous [tool_result ...] messages. trimHistory() must never start the
  // kept slice at a [tool_result ...] message whose [assistant_tool_calls] predecessor was cut.

DEFECTIVE mapMessage() — OpenAILLMAdapter.ts:165-189 (WR-06 target):
  const TOOL_RESULT_PATTERN = /^\[tool_result id=(\S+) name=(\S+)\]\s*([\s\S]*)$/;
  const ASSISTANT_TOOL_CALLS_PATTERN = /^\[assistant_tool_calls\]\s*([\s\S]*)$/;
  function mapMessage(message: InputMessage): OutgoingMessage {
    const toolResultMatch = TOOL_RESULT_PATTERN.exec(message.content);     // <-- no role gate
    if (toolResultMatch) { ... return { role: "tool", tool_call_id: id, content }; }
    const assistantToolCallsMatch = ASSISTANT_TOOL_CALLS_PATTERN.exec(message.content); // <-- no role gate
    if (assistantToolCallsMatch) {
      const parsed = JSON.parse(assistantToolCallsMatch[1]);               // <-- can throw on user text
      return { role: "assistant", content: null, tool_calls: parsed.map(...) };
    }
    return { role: message.role, content: message.content };
  }
  // InputMessage = { role: string; content: string }  — OpenAILLMAdapter.ts:55-56

SUGGESTED WR-06 fix shape (02-REVIEW.md:46-82) — apply verbatim in intent:
  function mapMessage(message: InputMessage): OutgoingMessage {
    if (message.role === "user") {
      const toolResultMatch = TOOL_RESULT_PATTERN.exec(message.content);
      if (toolResultMatch) { const [, id, , content] = toolResultMatch; return { role: "tool", tool_call_id: id, content }; }
    }
    if (message.role === "assistant") {
      const assistantToolCallsMatch = ASSISTANT_TOOL_CALLS_PATTERN.exec(message.content);
      if (assistantToolCallsMatch) {
        try {
          const parsed: Array<{ id: string; name: string; args: Record<string, any> }> = JSON.parse(assistantToolCallsMatch[1]);
          return { role: "assistant", content: null, tool_calls: parsed.map((tc) => ({ id: tc.id, type: "function", function: { name: tc.name, arguments: JSON.stringify(tc.args) } })) };
        } catch { /* malformed marker payload — fall through to passthrough rather than crashing */ }
      }
    }
    return { role: message.role, content: message.content };
  }

Test fake-deps builders (reuse) — GenericPipelineProvider.test.ts:36-90:
  makeFakeVAD(), makeFakeSTT(text?), makeFakeLLM(result?), makeFakeTTS(), makeBaseConfig(overrides?).
  Turn trigger idiom across the suite: `await provider.connect()` then `await provider.sendMessage("...")`.
  FakeAudioContext is stubbed globally at the top of the file. `this.messages`/`trimHistory()` are `protected`
  (NOT public) — the WR-05 test must observe the trimmed array via a captured llm.complete() args.messages
  (shallow-copied per call, as the existing CR-03 test at lines 435-482 does), NOT by reading this.messages directly.

Adapter test idiom (reuse) — OpenAILLMAdapter.test.ts:1-54, 185-222:
  vi.stubGlobal("fetch", vi.fn()); mockFetch.mockResolvedValueOnce(new Response(JSON.stringify({ text: "done" }), { status: 200 }));
  request-body capture: `const [, calledInit] = mockFetch.mock.calls[0]; const body = JSON.parse(calledInit.body)`.
  The existing assistant_tool_calls wire-shape test lives at lines 185-222 — mirror its structure for the new WR-06 test.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix WR-05 (marker-pair-aware trimHistory) and WR-06 (role-gated, try/catch mapMessage)</name>
  <files>packages/providers/generic-stt-tts/src/GenericPipelineProvider.ts, packages/providers/generic-stt-tts/src/adapters/OpenAILLMAdapter.ts</files>
  <read_first>
    - packages/providers/generic-stt-tts/src/GenericPipelineProvider.ts (lines 624-642: the trimHistory() doc comment + flat-slice body being fixed; lines 514-544: the CR-03 [assistant_tool_calls] push and the [tool_result ...] push, to confirm the EXACT marker prefixes the slice must keep paired and to confirm trimHistory() is called at line 564 after the assistant reply is pushed; lines 64-67: the ChatMessage type)
    - packages/providers/generic-stt-tts/src/adapters/OpenAILLMAdapter.ts (lines 37-41: the two PATTERN regex consts; lines 55-66: InputMessage/OutgoingMessage types; lines 159-189: the current mapMessage() with the two un-gated branches being fixed)
    - .planning/phases/02-generic-pipeline-orchestrator/02-REVIEW.md (lines 36-82: the WR-05 fix #1 description and the WR-06 suggested-fix code block — apply both verbatim in intent)
    - .planning/phases/02-generic-pipeline-orchestrator/02-06-PLAN.md (the prior gap-closure plan in these exact files — match its surgical-edit description style and the inline traceability-comment convention `(CR-0N)`/`(WR-0N)`/`(D-0N)`)
  </read_first>
  <behavior>
    - trimHistory (WR-05): given a `nonSystem` array containing, in order, multiple complete marker groups (each = one `[assistant_tool_calls]`-prefixed message immediately followed by one or more `[tool_result ...]`-prefixed messages) interleaved with plain user/assistant messages, after trimHistory() runs every `[tool_result ...]`-prefixed message that remains in `this.messages` is immediately preceded (still inside the kept array) by its `[assistant_tool_calls]` marker. The flat tail-slice keeps the last `maxNonSystem` (=20) messages, THEN the kept window's start boundary is shifted: if the first kept message is a `[tool_result ...]` marker (its `[assistant_tool_calls]` predecessor was cut), either extend the start backward to include the predecessor marker, or drop the leading run of orphaned `[tool_result ...]` messages until the first kept message is NOT an orphaned tool-result. Choose the "extend backward to include the predecessor" arm (02-REVIEW.md fix #1) so no information is lost: walk backward from the slice start while the message just before it would leave a `[tool_result ...]` stranded, including the `[assistant_tool_calls]` predecessor. The system message (index 0) is still always preserved; plain (non-marker) user/assistant messages trim exactly as before.
    - trimHistory: a history with NO markers at all (only plain user/assistant turns) trims identically to the old behavior — the marker-pair logic is a no-op when no `[tool_result ...]` message sits at the slice boundary. The `maxTurns` default stays `10` (maxNonSystem = 20).
    - mapMessage (WR-06): given `{ role: "user", content: "[assistant_tool_calls] please call the weather tool for me" }`, mapMessage returns `{ role: "user", content: "[assistant_tool_calls] please call the weather tool for me" }` (plain passthrough — the assistant branch is NOT entered because role !== "assistant", so JSON.parse is never called and nothing throws).
    - mapMessage: given `{ role: "assistant", content: "[assistant_tool_calls] <valid-json>" }`, behavior is unchanged from CR-03 — it returns the `{ role: "assistant", content: null, tool_calls: [...] }` wire shape (the role gate admits it, JSON.parse succeeds).
    - mapMessage: given `{ role: "assistant", content: "[assistant_tool_calls] not-valid-json" }`, the try/catch catches the SyntaxError and falls through to plain passthrough `{ role: "assistant", content: "[assistant_tool_calls] not-valid-json" }` instead of throwing.
    - mapMessage: given `{ role: "user", content: "[tool_result id=c1 name=t] sunny" }`, behavior is unchanged — the role==="user" gate admits the existing TOOL_RESULT_PATTERN branch, returning `{ role: "tool", tool_call_id: "c1", content: "sunny" }`.
    - No regression: build stays clean; all existing tests still pass.
  </behavior>
  <action>
    Apply two coordinated edits — one per file. Root causes: (WR-05) the flat tail-slice has no marker-pair awareness; (WR-06) the marker branches have no role gate and no try/catch.

    EDIT A (GenericPipelineProvider.ts — make trimHistory() marker-pair-aware, WR-05):
    Replace the body of `trimHistory(maxTurns = 10)` (lines ~634-642) so that, after computing `const maxNonSystem = maxTurns * 2;` and the candidate tail-slice `nonSystem.slice(-maxNonSystem)`, it ensures the kept window does not begin with a `[tool_result ...]`-marked message whose `[assistant_tool_calls]` predecessor was cut off. Concretely: compute the slice start index `let start = Math.max(0, nonSystem.length - maxNonSystem)`; then while `start > 0` AND `nonSystem[start]` is a `[tool_result ...]`-prefixed message (content starts with `"[tool_result "`) AND `nonSystem[start - 1]` is NOT already the start of this group, shift `start` backward by one (so the preceding `[assistant_tool_calls]` marker — and any earlier contiguous tool-results of the same group — are pulled back into the kept window). Stop shifting once `nonSystem[start]` is an `[assistant_tool_calls]`-prefixed message (the group head is now included) or once `start === 0`. Use `const trimmed = nonSystem.slice(start);` and `this.messages = [...systemMessages, ...trimmed];`. Detect the two markers with simple `String.prototype.startsWith` checks on `.content` against the literal prefixes `"[tool_result "` and `"[assistant_tool_calls]"` (do NOT import the adapter's regex — keep the orchestrator free of adapter coupling; a startsWith check is sufficient and matches how the markers are written at lines 524-527 and 540-543). Update the trimHistory() doc comment (lines 624-633) to document the marker-pair-aware behavior and tag it `(WR-05)`: the slice never strands a `[tool_result ...]` message without its preceding `[assistant_tool_calls]` marker, because every OpenAI-compatible backend requires a role:"tool" message to be immediately preceded by the assistant message bearing the matching tool_calls entry (HTTP 400 otherwise — same invariant CR-03 restored within a turn, now preserved across trimming). Note that the kept window may therefore slightly exceed `maxNonSystem` when a marker group straddles the boundary — that is intentional and bounded by one group's size. Do NOT change the `maxTurns = 10` default, the system-message preservation, or any other method.

    EDIT B (OpenAILLMAdapter.ts — role-gate both marker branches + try/catch the JSON.parse, WR-06):
    Rewrite `mapMessage()` (lines ~165-189) to match the suggested fix shape in 02-REVIEW.md (lines 46-82):
    1. Wrap the existing TOOL_RESULT_PATTERN branch in an `if (message.role === "user") { ... }` gate. Keep the inner logic (`TOOL_RESULT_PATTERN.exec`, the `{ role: "tool", tool_call_id: id, content }` return) byte-for-byte.
    2. Wrap the existing ASSISTANT_TOOL_CALLS_PATTERN branch in an `if (message.role === "assistant") { ... }` gate. Inside, wrap the `JSON.parse(assistantToolCallsMatch[1])` + the `{ role: "assistant", content: null, tool_calls: [...] }` return in a `try { ... } catch { /* malformed marker payload — fall through to passthrough rather than crashing the turn */ }` so a malformed assistant-role marker payload falls through instead of throwing. Keep the `tool_calls` mapping (`{ id: tc.id, type: "function", function: { name: tc.name, arguments: JSON.stringify(tc.args) } }`) and the `Array<{ id: string; name: string; args: Record<string, any> }>` parsed type unchanged.
    3. Keep the final `return { role: message.role, content: message.content };` plain passthrough as the fall-through for: non-matching content, non-`user`/non-`assistant` roles, AND the caught-malformed-marker case.
    4. Update the `mapMessage()` doc comment (lines 159-164) to note the role gates and try/catch fallback, tagged `(WR-06)`: each marker branch is now gated on the message's role so ordinary user text starting with a marker prefix is treated as plain content (not misrouted into JSON.parse), and a malformed assistant-role marker payload passes through rather than throwing.

    Follow the existing 2-space-indent / double-quote / section-divider style across both files. No decorative emoji logging. Do NOT introduce fenced code blocks into source comments. Preserve all existing `(D-0N)`/`(CR-0N)`/`(CORE-06)`/`(RESEARCH Pitfall N)` traceability markers. Do NOT touch the CR-03 assistant-marker push (lines 514-527), the `[tool_result ...]` push (lines 540-543), GAP-02-05's pipelineToolList wiring, or the CR-01/CR-02 abort guards. Do NOT touch any unrelated finding (WR-02, WR-03, WR-04, IN-03).
  </action>
  <verify>
    <automated>cd /Users/whitemalt/Documents/khavee-sdk && pnpm --filter @khaveeai/providers-generic-stt-tts build 2>&1 | tail -6 && grep -c 'message.role === "assistant"' packages/providers/generic-stt-tts/src/adapters/OpenAILLMAdapter.ts && grep -c 'message.role === "user"' packages/providers/generic-stt-tts/src/adapters/OpenAILLMAdapter.ts && grep -c "tool_result " packages/providers/generic-stt-tts/src/GenericPipelineProvider.ts && grep -c "assistant_tool_calls" packages/providers/generic-stt-tts/src/GenericPipelineProvider.ts && pnpm --filter @khaveeai/providers-generic-stt-tts test --run 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @khaveeai/providers-generic-stt-tts build` exits 0 (both edits compile clean under strict mode)
    - `grep -c 'message.role === "assistant"' packages/providers/generic-stt-tts/src/adapters/OpenAILLMAdapter.ts` returns at least `1` — the assistant-role gate exists
    - `grep -c 'message.role === "user"' packages/providers/generic-stt-tts/src/adapters/OpenAILLMAdapter.ts` returns at least `1` — the user-role gate on the tool-result branch exists
    - Reading OpenAILLMAdapter.ts mapMessage() confirms: the TOOL_RESULT_PATTERN branch is inside `if (message.role === "user")`, the ASSISTANT_TOOL_CALLS_PATTERN branch is inside `if (message.role === "assistant")` with the `JSON.parse` wrapped in `try { ... } catch { ... }`, and the final plain passthrough return is unchanged
    - Reading GenericPipelineProvider.ts trimHistory() confirms the slice-start boundary is shifted backward when the first kept message is a `[tool_result ...]`-prefixed message (via `startsWith("[tool_result ")` / `startsWith("[assistant_tool_calls]")` checks), so an `[assistant_tool_calls]` marker and its following tool-results are never split; the system-message preservation and `maxTurns = 10` default are unchanged
    - `grep -c "assistant_tool_calls" packages/providers/generic-stt-tts/src/GenericPipelineProvider.ts` still returns at least `1` — the CR-03 marker push is preserved (NOT regressed); the new trimHistory startsWith check adds another occurrence
    - All existing tests still pass: `pnpm --filter @khaveeai/providers-generic-stt-tts test --run` is green (the CR-03 marker-ordering test, GAP-02-05 tests, and the existing adapter wire-shape test at lines 185-222 are unregressed — new tests are added in Task 2)
    - `pnpm --filter @khaveeai/providers-openai-stt-tts test --run` still passes 13/13 (compatibility constraint: existing provider untouched)
  </acceptance_criteria>
  <done>WR-05 and WR-06 are closed at the source: trimHistory() never strands a `[tool_result ...]` message without its preceding `[assistant_tool_calls]` marker, and mapMessage() gates both marker branches on message.role with the assistant-branch JSON.parse wrapped in try/catch so ordinary user text starting with a marker prefix (and malformed assistant payloads) pass through as plain content instead of crashing. Build is clean and the existing suite is unbroken.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regression tests — trimHistory never strands a tool-result (WR-05) and user-role marker text passes through without throwing (WR-06)</name>
  <files>packages/providers/generic-stt-tts/src/__tests__/GenericPipelineProvider.test.ts, packages/providers/generic-stt-tts/src/__tests__/OpenAILLMAdapter.test.ts</files>
  <read_first>
    - packages/providers/generic-stt-tts/src/__tests__/GenericPipelineProvider.test.ts (lines 1-90: imports, FakeAudioContext polyfill, makeFakeVAD/makeFakeSTT/makeFakeLLM/makeFakeTTS/makeBaseConfig builders — reuse them; lines 435-482: the EXISTING CR-03 test that captures a per-call shallow copy of args.messages — the WR-05 test must use the SAME capture idiom to observe the trimmed array on a later turn's complete() call, since this.messages/trimHistory are protected)
    - packages/providers/generic-stt-tts/src/__tests__/OpenAILLMAdapter.test.ts (lines 1-54: the vi.stubGlobal("fetch")/mockResolvedValueOnce idiom + request-body capture; lines 185-222: the EXISTING assistant_tool_calls wire-shape test — mirror its structure for the new WR-06 test)
    - packages/providers/generic-stt-tts/src/GenericPipelineProvider.ts (the Task 1 trimHistory() fix — the marker-pair-aware slice the WR-05 test asserts)
    - packages/providers/generic-stt-tts/src/adapters/OpenAILLMAdapter.ts (the Task 1 mapMessage() role gates — the passthrough the WR-06 test asserts)
    - .planning/phases/02-generic-pipeline-orchestrator/02-REVIEW.md (lines 36-45, 86-90: WR-05, WR-06, and IN-04 — the exact failure modes and the test shape IN-04 prescribes for WR-05)
  </read_first>
  <behavior>
    - WR-05 regression (GenericPipelineProvider.test.ts): A fake LLM is driven through enough tool-calling rounds in ONE session to push `this.messages` well past the 20-non-system-message trim threshold, with at least two complete marker groups in the trimmed window's vicinity. The test captures a per-call shallow copy of `args.messages` (as the CR-03 test does). On the LAST captured complete() call — the one issued AFTER trimHistory() has run and dropped the earliest messages — the test scans the messages array and asserts: for every message whose content starts with `"[tool_result "`, the IMMEDIATELY-preceding message's content starts with `"[assistant_tool_calls]"`. No stranded tool-result exists. This FAILS against the unfixed flat-slice trimHistory() (which strands the earliest group's tool-result when its assistant marker is cut) and PASSES against the Task 1 fix.
    - WR-06 regression (OpenAILLMAdapter.test.ts): `complete()` is called with a history whose FIRST message is `{ role: "user", content: "[assistant_tool_calls] please call the weather tool for me" }`. The call does NOT reject/throw (no SyntaxError), and the request body's first message is exactly `{ role: "user", content: "[assistant_tool_calls] please call the weather tool for me" }` — plain passthrough, NOT parsed into a `tool_calls` wire shape, `content` is the original string (not `null`). A second assertion in the same or a sibling test confirms an assistant-role marker with malformed JSON (`{ role: "assistant", content: "[assistant_tool_calls] not-json" }`) also passes through as `{ role: "assistant", content: "[assistant_tool_calls] not-json" }` without throwing.
  </behavior>
  <action>
    Append new tests to BOTH existing suites — do NOT modify or delete any existing passing test.

    TEST A — WR-05 orchestrator regression (GenericPipelineProvider.test.ts):
    Add `describe("WR-05: trimHistory keeps [assistant_tool_calls] paired with its [tool_result ...] markers across the trim boundary", ...)` with a test that:
    - Builds a fake LLM whose `complete` captures a per-call SHALLOW COPY of `args.messages` into a module-local array (`captured.push(a.messages.map((m: any) => ({ ...m })))`, same idiom as the CR-03 test at lines 441-448) and returns, across successive calls, a SEQUENCE that forces several tool-calling rounds within a single sendMessage turn AND/OR several sendMessage turns — enough that `this.messages` exceeds 20 non-system entries and trimHistory() runs at least once before a final complete() call. Simplest deterministic approach: drive MULTIPLE `await provider.sendMessage(...)` turns in a loop (e.g. 12 turns), where each turn's first complete() returns one tool call `{ toolCalls: [{ id: \`c\${n}\`, name: "testTool", args: { n } }] }` and the second returns `{ text: "done", toolCalls: [] }`. Each turn appends 1 user + 1 assistant-marker + 1 tool-result + 1 final-assistant = 4 non-system messages, so after ~6 turns history exceeds 20 and trimHistory() begins cutting earlier groups. (trimHistory() runs at line 564 of the provider, once per turn, after the assistant reply is pushed.)
    - Constructs the provider via `makeBaseConfig({ llm, pipelineTools: [{ name: "testTool", description: "a test tool", parameters: { type: "object", properties: {} }, execute: vi.fn(async () => ({ success: true, message: "tool ran" })) }] })`, then `await provider.connect()`.
    - After the loop, takes the LAST captured messages array (`captured[captured.length - 1]`) — the array sent on the final turn's complete(), which reflects the trimmed history — and asserts: `for (let i = 0; i < msgs.length; i++) { if (msgs[i].content.startsWith("[tool_result ")) { expect(i).toBeGreaterThan(0); expect(msgs[i - 1].content.startsWith("[assistant_tool_calls]")).toBe(true); } }`. This is the IN-04-prescribed assertion (every surviving tool-result is immediately preceded by its assistant marker). Also assert the captured array's length is bounded (e.g. `expect(msgs.length).toBeLessThan(30)` — trimming actually happened, history did not grow unbounded).
    - Include the literal substring `WR-05` in the describe title so the grep gate matches.

    TEST B — WR-06 adapter regression (OpenAILLMAdapter.test.ts):
    In the existing `describe("ORCH-02/ORCH-05: ...")` block (or a new sibling describe), add `it("WR-06: a role:user message starting with [assistant_tool_calls] passes through as plain content and does NOT throw", ...)` mirroring the existing assistant_tool_calls test (lines 185-222):
    - `mockFetch.mockResolvedValueOnce(new Response(JSON.stringify({ text: "done" }), { status: 200 }))`.
    - Assert the call resolves without throwing: `await expect(adapter.complete({ messages: [{ role: "user", content: "[assistant_tool_calls] please call the weather tool for me" }] })).resolves.toBeDefined();`.
    - Capture the request body: `const [, calledInit] = mockFetch.mock.calls[0] as [string, RequestInit]; const body = JSON.parse(calledInit.body as string);`.
    - Assert `body.messages[0]` deep-equals `{ role: "user", content: "[assistant_tool_calls] please call the weather tool for me" }` (plain passthrough — `content` is the original string, NOT `null`, and there is NO `tool_calls` field).
    - Add a SECOND assertion (same test or a sibling `it(...)`): a `{ role: "assistant", content: "[assistant_tool_calls] not-json" }` history message maps to a passthrough `{ role: "assistant", content: "[assistant_tool_calls] not-json" }` (malformed assistant marker falls through via try/catch, does not throw). Use a fresh `mockFetch.mockResolvedValueOnce(...)`.
    - Include the literal substring `WR-06` in the test title and the literal substring `assistant_tool_calls` in the test body so the grep gates match.

    Keep both tests deterministic (await sendMessage / await complete; no fixed-tick waits). Use the project's 2-space-indent / double-quote style. Reuse the existing fake builders and fetch-stub idioms. Do NOT alter the existing CR-03 or assistant_tool_calls tests.
  </action>
  <verify>
    <automated>cd /Users/whitemalt/Documents/khavee-sdk && pnpm --filter @khaveeai/providers-generic-stt-tts test --run 2>&1 | tail -20 && grep -c "WR-05" packages/providers/generic-stt-tts/src/__tests__/GenericPipelineProvider.test.ts && grep -c "WR-06" packages/providers/generic-stt-tts/src/__tests__/OpenAILLMAdapter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @khaveeai/providers-generic-stt-tts test --run` passes with TWO more tests than before (prior 33 + 2 = 35, or +1 describe-block test each), zero failures
    - `grep -c "WR-05" packages/providers/generic-stt-tts/src/__tests__/GenericPipelineProvider.test.ts` returns at least `1`
    - `grep -c "WR-06" packages/providers/generic-stt-tts/src/__tests__/OpenAILLMAdapter.test.ts` returns at least `1`
    - The WR-05 test drives enough turns/rounds that the captured final-call messages array is the result of at least one trimHistory() pass (history exceeded 20 non-system messages), and asserts via a loop that EVERY `"[tool_result "`-prefixed message is immediately preceded by an `"[assistant_tool_calls]"`-prefixed message — confirm by reading the test; it must capture per-call shallow copies (not the live this.messages reference) and inspect the LAST call's array
    - The WR-06 test asserts `adapter.complete({ messages: [{ role: "user", content: "[assistant_tool_calls] ..." }] })` resolves (does NOT throw) and that `body.messages[0]` is the plain user passthrough (content is the original string, no `tool_calls` field) — confirm by reading the test
    - No existing test was modified or removed (only appended) — confirm via `git diff` that the prior CR-03 test (lines 435-482) and the assistant_tool_calls wire-shape test (lines 185-222) are untouched
  </acceptance_criteria>
  <done>Two regression tests lock WR-05 and WR-06 closed: the orchestrator test proves the trimmed history never strands a tool-result without its assistant marker (failing against the flat-slice trimHistory), and the adapter test proves a role:user message starting with the marker prefix passes through as plain content without throwing (failing against the un-gated mapMessage). Both pass against the Task 1 fix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Orchestrator (this.messages, after trimHistory) → LLM (llm.complete messages field) | The trimmed conversation history sent to the LLM must remain a wire-protocol-valid OpenAI-compatible message sequence; a trim that strands a tool-result without its assistant predecessor produces an HTTP-400-rejected request |
| External input (user typing / STT transcript) → OpenAILLMAdapter (mapMessage) | Untrusted text that happens to start with a marker prefix crosses into mapMessage's branch-routing; without a role gate it is misrouted and can crash the turn via uncaught JSON.parse |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-17 | Denial of Service | trimHistory()'s flat tail-slice stranding a [tool_result ...] message without its [assistant_tool_calls] predecessor once history exceeds 20 non-system messages — every OpenAI-compatible backend rejects the resulting request with HTTP 400, breaking long tool-calling sessions (WR-05, reintroduces CR-03's failure mode via a different path) | mitigate | Make trimHistory() marker-pair-aware: shift the kept-window start boundary backward so an [assistant_tool_calls] marker and its following [tool_result ...] entries are never split; WR-05 regression test drives history past the 20-message threshold and asserts every surviving tool-result is immediately preceded by its assistant marker |
| T-02-18 | Denial of Service | mapMessage() crashing the turn via uncaught JSON.parse when a role:"user" message (ordinary user text or STT transcript) starts with the literal "[assistant_tool_calls]" prefix — aborts a legitimate user turn with a confusing JSON-syntax onError (WR-06) | mitigate | Gate the ASSISTANT_TOOL_CALLS_PATTERN branch on message.role === "assistant" (so user text is never routed into JSON.parse) and wrap the JSON.parse in try/catch (so a malformed assistant payload falls through to passthrough); also gate the TOOL_RESULT_PATTERN branch on role === "user" for the sibling marker; WR-06 regression test asserts the user-role marker text resolves without throwing and passes through as plain content |
| T-02-19 | Tampering | A user/STT-supplied message starting with a marker prefix being misclassified into the wrong wire shape (user text mapped to a fabricated assistant/tool_calls or tool message), silently corrupting the conversation the LLM sees | mitigate | The role gates ensure only role:"assistant" content is considered for the assistant/tool_calls shape and only role:"user" content for the tool-result shape — a marker-prefixed message in the wrong role passes through as plain content; the WR-06 test asserts the user-role marker text is preserved verbatim as role:"user" content |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies — this gap modifies existing source and tests only; no package-manager install tasks in this plan |
</threat_model>

<verification>
- `pnpm --filter @khaveeai/providers-generic-stt-tts build` exits 0 (both edits compile clean under strict mode)
- `pnpm --filter @khaveeai/providers-generic-stt-tts test --run` passes with two more tests than before, zero failures
- The WR-05 regression test fails against the unfixed flat-slice trimHistory() (a stranded tool-result survives the cut) and passes against the marker-pair-aware fix
- The WR-06 regression test fails against the un-gated mapMessage() (user-role marker text throws SyntaxError) and passes against the role-gated fix
- `grep -c 'message.role === "assistant"' .../adapters/OpenAILLMAdapter.ts` returns at least `1` and `grep -c 'message.role === "user"' .../adapters/OpenAILLMAdapter.ts` returns at least `1` (both role gates present)
- Reading trimHistory() confirms the slice-start boundary is shifted backward when the first kept message is a `[tool_result ...]` marker (startsWith checks), preserving marker pairs
- The CR-03 assistant-marker push, the `[tool_result ...]` push, GAP-02-05's pipelineToolList wiring, and the CR-01/CR-02 abort guards remain unchanged (no regression)
- `pnpm --filter @khaveeai/providers-openai-stt-tts test --run` still passes 13/13 (compatibility constraint: existing provider untouched)
</verification>

<success_criteria>
- WR-05 closed: trimHistory() is marker-pair-aware — once history exceeds the 20-non-system-message trim threshold across multiple tool-calling rounds, the kept window never strands a `[tool_result ...]` message without its preceding `[assistant_tool_calls]` marker, so long tool-calling sessions don't reintroduce CR-03's HTTP-400 protocol violation
- WR-06 closed: OpenAILLMAdapter.mapMessage() gates both marker branches on message.role (assistant for the tool_calls marker, user for the tool-result marker) and wraps the assistant-branch JSON.parse in try/catch — ordinary user text starting with a marker prefix is treated as plain content (no crash), and a malformed assistant-role marker payload passes through instead of throwing
- Both fixes apply the suggested shapes from 02-REVIEW.md verbatim in intent (WR-05 fix #1: extend slice backward to keep pairs; WR-06: role gate + try/catch fallback)
- A WR-05 regression test (overflows the trim threshold, asserts no stranded tool-result) and a WR-06 regression test (user-role marker text passes through without throwing) both fail against the unfixed code and pass against the fix
- Build clean; existing generic-stt-tts suite (incl. CR-03 and GAP-02-05 tests) + openai-stt-tts 13 tests unaffected; the CR-03 push, GAP-02-05 wiring, and CR-01/CR-02 abort guards untouched
- WR-02, WR-03, WR-04, IN-03 (carried-forward findings) left untouched (out of this gap's scope)
</success_criteria>

<output>
Create `.planning/phases/02-generic-pipeline-orchestrator/02-07-SUMMARY.md` when done
</output>
