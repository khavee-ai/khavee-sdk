---
phase: 03-python-backend-services
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/whitemalt/Documents/thonburian-stt/main.py
  - /Users/whitemalt/Documents/thonburian-stt/requirements.txt
  - /Users/whitemalt/Documents/thonburian-stt/README.md
  - /Users/whitemalt/Documents/thonburian-stt/.gitignore
autonomous: true
requirements: [BACK-01, BACK-02, BACK-05]
must_haves:
  truths:
    - "POSTing a Thai-speech WAV utterance to thonburian-stt /transcribe returns Thai transcription text (D-02 contract)"
    - "The Whisper model is loaded exactly once at startup, not per request"
    - "The service auto-detects CUDA, then MPS, then falls back to CPU (D-06)"
    - "A developer can install deps and run the service with plain venv + uvicorn (D-05)"
    - "BACK-02 hallucination rejection and the BACK-05 semaphore half are explicitly deferred per D-01, documented in README"
  artifacts:
    - path: "/Users/whitemalt/Documents/thonburian-stt/main.py"
      provides: "FastAPI app with lifespan model-load + POST /transcribe + GET /health"
      contains: "biodatlab/whisper-th-large-v3-combined"
      min_lines: 40
    - path: "/Users/whitemalt/Documents/thonburian-stt/requirements.txt"
      provides: "Pinned dependency manifest including python-multipart"
      contains: "python-multipart"
    - path: "/Users/whitemalt/Documents/thonburian-stt/README.md"
      provides: "venv + uvicorn run instructions and ffmpeg/first-run prerequisites"
    - path: "/Users/whitemalt/Documents/thonburian-stt/.gitignore"
      provides: "Ignores venv/, __pycache__/, test WAV artifacts"
  key_links:
    - from: "lifespan(app)"
      to: "app.state.asr_pipe"
      via: "transformers.pipeline assignment before yield"
      pattern: "app\\.state\\.asr_pipe\\s*="
    - from: "transcribe handler"
      to: "app.state.asr_pipe"
      via: "request.app.state.asr_pipe(...) call"
      pattern: "request\\.app\\.state\\.asr_pipe"
---

<objective>
Scaffold the `thonburian-stt` FastAPI service at `/Users/whitemalt/Documents/thonburian-stt` (greenfield, empty repo). The service accepts a posted WAV audio utterance and returns Thai transcription text using `biodatlab/whisper-th-large-v3-combined`, loading the model exactly once at startup with CUDA→MPS→CPU auto-detection.

Purpose: Deliver BACK-01 (Thai ASR over HTTP) and the non-deferred model-load-once half of BACK-05. This is one of the two real non-OpenAI vendors that prove the SDK's generic pipeline abstraction in Phase 5.
Output: `main.py`, `requirements.txt`, `README.md`, `.gitignore` in the sibling repo.

Scope notes (per CONTEXT.md D-01):
- BACK-02 (hallucination rejection on short/silent audio) is DEFERRED — do NOT implement silence-trimming or repetition-ratio checks. The service returns whatever the model produces. Document the deferral in the README so it is not silently dropped.
- BACK-05's semaphore/concurrency-gating half is DEFERRED — do NOT add a semaphore or request queue. Only the load-once-at-startup half is implemented here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-python-backend-services/03-CONTEXT.md
@.planning/phases/03-python-backend-services/03-RESEARCH.md
@.planning/phases/03-python-backend-services/03-PATTERNS.md
@.planning/PROJECT.md

<interfaces>
<!-- HTTP contract this service MUST expose (D-02). Phase 4's ThonburianSTTProvider adapter is the future consumer. -->
POST /transcribe
  Request: multipart/form-data with a single file field named `file` (raw WAV audio bytes)
  Response: 200 JSON { "text": string }
GET /health
  Response: 200 JSON { "status": "ok" }

Device selection chain (D-06): "cuda" if torch.cuda.is_available(), else "mps" if torch.backends.mps.is_available(), else "cpu".
Whisper inference call (from model card pattern): asr_pipe(audio_path, generate_kwargs={"language": "<|th|>", "task": "transcribe"}, batch_size=16) returns a dict with a "text" key.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin dependencies and scaffold repo metadata</name>
  <files>/Users/whitemalt/Documents/thonburian-stt/requirements.txt, /Users/whitemalt/Documents/thonburian-stt/.gitignore, /Users/whitemalt/Documents/thonburian-stt/README.md</files>
  <read_first>
    - .planning/phases/03-python-backend-services/03-RESEARCH.md (Standard Stack table, "Installation (thonburian-stt)" block, "Version verification" note, Open Question 3, "Run command" block, Environment Availability table)
    - .planning/phases/03-python-backend-services/03-PATTERNS.md ("thonburian-stt/requirements.txt" section, "Plain venv + uvicorn run convention" shared pattern)
    - .planning/phases/03-python-backend-services/03-CONTEXT.md (D-01, D-05, D-06)
  </read_first>
  <action>
    Create `requirements.txt` listing exactly these five dependencies: `fastapi`, `uvicorn`, `python-multipart`, `transformers`, `torch`. Pin `transformers` to a 4.x range expressed as `transformers>=4.40,<5.0` (the Whisper model-card pipeline usage was written against the 4.x API; 5.x breaking changes are unverified — RESEARCH Open Question 3). Before finalizing pins, run `pip3 index versions transformers torch fastapi uvicorn python-multipart` to confirm a real current 4.x `transformers` release exists, and pin the other four with `>=` floors at their verified current major (do not pin versions recalled from memory — these are fast-moving ML libraries per RESEARCH). `python-multipart` MUST be present (FastAPI raises `RuntimeError: Form data requires "python-multipart" to be installed` lazily at first `/transcribe` request if omitted — RESEARCH Pitfall 3).

    Create `.gitignore` ignoring `venv/`, `__pycache__/`, `*.pyc`, and `*.wav` (test audio artifacts).

    Create `README.md` documenting: (1) the venv + uvicorn run sequence — `python3 -m venv venv`, `source venv/bin/activate`, `pip install -r requirements.txt`, `uvicorn main:app --host 0.0.0.0 --port 8001` (port 8001 reserved for STT; jai-tts uses 8002); (2) `ffmpeg` is a required system prerequisite (not pip-installable, install via Homebrew); (3) first run requires internet access to download the Whisper weights from Hugging Face (cached locally afterward); (4) the `POST /transcribe` and `GET /health` contract from the interfaces block; (5) an explicit "Out of scope (deferred)" note stating that hallucination rejection on short/silent audio (BACK-02) and concurrency gating/semaphore (BACK-05) are deliberately NOT implemented this phase per decision D-01 — the service returns raw model output and has no concurrent-request protection.
  </action>
  <verify>
    <automated>test -f /Users/whitemalt/Documents/thonburian-stt/requirements.txt && grep -q 'python-multipart' /Users/whitemalt/Documents/thonburian-stt/requirements.txt && grep -Eq 'transformers>=4\.[0-9]+,<5\.0' /Users/whitemalt/Documents/thonburian-stt/requirements.txt && test -f /Users/whitemalt/Documents/thonburian-stt/.gitignore && grep -q 'venv/' /Users/whitemalt/Documents/thonburian-stt/.gitignore && grep -qi 'ffmpeg' /Users/whitemalt/Documents/thonburian-stt/README.md && grep -qi '8001' /Users/whitemalt/Documents/thonburian-stt/README.md && grep -Eqi 'BACK-02|BACK-05|defer' /Users/whitemalt/Documents/thonburian-stt/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `requirements.txt` contains the lines `fastapi`, `uvicorn`, `python-multipart`, `transformers` (pinned `>=4.40,<5.0`), and `torch`
    - `.gitignore` contains `venv/`, `__pycache__/`, and `*.wav`
    - `README.md` contains the four-line venv+uvicorn run sequence with port `8001`, names `ffmpeg` as a system prerequisite, names Hugging Face first-run internet requirement, and contains an explicit deferral note referencing BACK-02 and BACK-05 / decision D-01
  </acceptance_criteria>
  <done>requirements.txt, .gitignore, and README.md exist with the pins, ignores, run instructions, ffmpeg/first-run prerequisites, and the BACK-02/BACK-05 deferral note.</done>
</task>

<task type="auto">
  <name>Task 2: Implement main.py — lifespan model load + /transcribe + /health</name>
  <files>/Users/whitemalt/Documents/thonburian-stt/main.py</files>
  <read_first>
    - .planning/phases/03-python-backend-services/03-RESEARCH.md (Pattern 1, Pattern 2, Pattern 4, "Complete minimal thonburian-stt/main.py skeleton" in Code Examples, Pitfall 4, Pitfall 5)
    - .planning/phases/03-python-backend-services/03-PATTERNS.md ("thonburian-stt/main.py" section with all pattern blocks, "Device auto-detection" and "Model-load-once via FastAPI lifespan" shared patterns, "No custom error envelope" shared pattern)
    - /Users/whitemalt/Documents/thonburian-stt/requirements.txt (from Task 1, confirms installed deps)
  </read_first>
  <action>
    Create `main.py` as a single-file FastAPI app (single-file layout is explicitly Claude's discretion per CONTEXT.md and idiomatic for a one-endpoint demo — do NOT build an `app/` package). Structure:

    1. A `select_device() -> str` function implementing the D-06 chain: return `"cuda"` if `torch.cuda.is_available()`, else `"mps"` if `torch.backends.mps.is_available()`, else `"cpu"`.
    2. An `@asynccontextmanager` `lifespan(app)` that computes the device once, then assigns `app.state.asr_pipe = pipeline(task="automatic-speech-recognition", model="biodatlab/whisper-th-large-v3-combined", chunk_length_s=30, device=device)` BEFORE `yield`. The model load MUST happen here, never inside a request handler (RESEARCH anti-pattern: per-request load causes multi-second-to-minute latency). No teardown needed after yield.
    3. `app = FastAPI(lifespan=lifespan)`.
    4. `GET /health` returning `{"status": "ok"}`.
    5. `POST /transcribe` with signature `async def transcribe(request: Request, file: UploadFile = File(...))`: read bytes via `await file.read()`, write to a `tempfile.NamedTemporaryFile(suffix=".wav")`, flush, then call `request.app.state.asr_pipe(tmp.name, generate_kwargs={"language": "<|th|>", "task": "transcribe"}, batch_size=16)` and return `{"text": result["text"]}`.

    Do NOT add try/except error envelopes — rely on FastAPI's automatic 422 validation for malformed/missing multipart input (CONTEXT.md Claude's Discretion + D-01 descoped robustness). Do NOT implement silence-trimming, repetition-ratio checks (BACK-02 deferred), or any semaphore/concurrency guard (BACK-05 semaphore half deferred). Keep `chunk_length_s=30` as-is (RESEARCH Pitfall 4: it is a no-op for short clips, do not tune it).
  </action>
  <verify>
    <automated>cd /Users/whitemalt/Documents/thonburian-stt && python3 -m py_compile main.py && grep -q 'def select_device' main.py && grep -q 'torch.backends.mps.is_available' main.py && grep -q 'biodatlab/whisper-th-large-v3-combined' main.py && grep -q 'app.state.asr_pipe' main.py && grep -q '@app.post("/transcribe")' main.py && grep -q '@app.get("/health")' main.py && ! grep -Eq 'repetition|silence_trim|Semaphore' main.py</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m py_compile main.py` exits 0 (valid Python syntax)
    - `main.py` defines `select_device` with a `torch.cuda.is_available()` → `torch.backends.mps.is_available()` → cpu chain
    - `app.state.asr_pipe` is assigned inside the `lifespan` function (before `yield`), proving model-load-once; it is NOT assigned inside the `/transcribe` handler
    - `main.py` references model id `biodatlab/whisper-th-large-v3-combined`
    - A `@app.post("/transcribe")` handler exists that reads an `UploadFile` and returns `{"text": ...}`, and a `@app.get("/health")` handler exists
    - No `Semaphore`, `repetition`, or `silence_trim` symbols appear (deferred BACK-02/BACK-05 not implemented)
  </acceptance_criteria>
  <done>main.py compiles, loads the Whisper model once in lifespan with device auto-detection, and exposes POST /transcribe (multipart→{"text"}) and GET /health.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /transcribe | Untrusted multipart file upload crosses into the service (arbitrary bytes claimed to be WAV) |
| service → Hugging Face Hub | Model weights downloaded from an external registry at first run |
| pip install → local venv | Third-party packages execute setup code during install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | multipart upload at /transcribe | accept | Demo service, no auth surface; FastAPI/python-multipart parse boundaries safely; malformed input yields 422. Bytes are written to a tempfile and fed to the ASR pipeline only. No file path is taken from user input. |
| T-03-02 | Denial of Service | concurrent /transcribe requests | accept | Concurrency gating (BACK-05 semaphore) explicitly deferred per D-01; demo runs single-user. Documented in README. |
| T-03-03 | Information Disclosure | tempfile handling | mitigate | Use `tempfile.NamedTemporaryFile` (auto-removed on context exit), never echo file paths or raw bytes in responses. |
| T-03-SC | Tampering | pip installs (fastapi, uvicorn, python-multipart, transformers, torch) | mitigate | All five packages were audited `OK`/Approved in RESEARCH Package Legitimacy Audit (PyPI, mature, high-download). No `[ASSUMED]`/`[SUS]` packages in this plan → no blocking human checkpoint required. `python-multipart`'s LLM-bait naming was confirmed established. |
</threat_model>

<verification>
- `main.py` compiles via `python3 -m py_compile`.
- Model is loaded in `lifespan`, not in the request handler (grep for `app.state.asr_pipe` assignment location).
- `requirements.txt` includes `python-multipart` and a 4.x-pinned `transformers`.
- README documents the run sequence, ffmpeg/first-run prerequisites, and the BACK-02/BACK-05 deferral.
- (Manual / Phase 4 follow-up) POSTing a real Thai WAV returns correct Thai text — full model-download + inference smoke test is left to first run with internet/GPU; not gated here because it requires multi-GB weight download.
</verification>

<success_criteria>
- thonburian-stt repo contains main.py, requirements.txt, README.md, .gitignore.
- BACK-01: POST /transcribe exists with the multipart→{"text"} contract using `biodatlab/whisper-th-large-v3-combined`.
- BACK-05 (load-once half): model is loaded exactly once in the FastAPI lifespan with CUDA→MPS→CPU auto-detection.
- BACK-02 and the BACK-05 semaphore half are documented as deferred (D-01), not implemented.
</success_criteria>

<output>
Create `.planning/phases/03-python-backend-services/03-01-SUMMARY.md` when done.
</output>
