---
phase: 03-python-backend-services
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/whitemalt/Documents/jai-tts/main.py
  - /Users/whitemalt/Documents/jai-tts/requirements.txt
  - /Users/whitemalt/Documents/jai-tts/README.md
  - /Users/whitemalt/Documents/jai-tts/.gitignore
  - /Users/whitemalt/Documents/jai-tts/assets/default_voice.wav
  - /Users/whitemalt/Documents/jai-tts/assets/default_voice.txt
  - /Users/whitemalt/Documents/jai-tts/assets/LICENSE-default-voice.txt
autonomous: false
requirements: [BACK-03, BACK-04, BACK-05]
user_setup:
  - service: huggingface-hub
    why: "First run downloads JaiTTS-F5TTS weights and the thonburian-tts flowtts inference code via hf:// + git"
    env_vars: []
    dashboard_config:
      - task: "Ensure internet access for Hugging Face Hub + GitHub on first run"
        location: "local machine network"
must_haves:
  truths:
    - "POSTing Thai text to jai-tts /synthesize returns raw audio/wav bytes"
    - "Calling /synthesize with text alone (no reference audio) succeeds using the bundled default Thai reference voice"
    - "The TTS pipeline is loaded exactly once at startup, not per request"
    - "The service auto-detects CUDA, then MPS, then falls back to CPU"
    - "A permissively-licensed default reference voice clip + transcript + license file are bundled in assets/"
  artifacts:
    - path: "/Users/whitemalt/Documents/jai-tts/main.py"
      provides: "FastAPI app with lifespan TTS-pipeline load + POST /synthesize + GET /health"
      contains: "/synthesize"
      min_lines: 40
    - path: "/Users/whitemalt/Documents/jai-tts/requirements.txt"
      provides: "Dependency manifest including git+thonburian-tts flowtts install"
      contains: "github.com/biodatlab/thonburian-tts"
    - path: "/Users/whitemalt/Documents/jai-tts/assets/default_voice.wav"
      provides: "Bundled default Thai reference voice clip (BACK-04)"
    - path: "/Users/whitemalt/Documents/jai-tts/assets/default_voice.txt"
      provides: "Transcript of the default reference clip"
    - path: "/Users/whitemalt/Documents/jai-tts/assets/LICENSE-default-voice.txt"
      provides: "License/attribution for the sourced clip"
  key_links:
    - from: "lifespan(app)"
      to: "app.state.tts_pipe"
      via: "FlowTTSPipeline load assignment before yield"
      pattern: "app\\.state\\.tts_pipe\\s*="
    - from: "synthesize handler"
      to: "app.state.tts_pipe"
      via: "request.app.state.tts_pipe(...) call against verified signature"
      pattern: "request\\.app\\.state\\.tts_pipe"
    - from: "synthesize handler"
      to: "assets/default_voice.wav"
      via: "default reference voice path constant"
      pattern: "default_voice"
---

<objective>
Scaffold the `jai-tts` FastAPI service at `/Users/whitemalt/Documents/jai-tts` (greenfield, empty repo). The service accepts posted Thai text and returns synthesized WAV audio using the `JTS-AI/JaiTTS-F5TTS` voice-cloning model via the `FlowTTSPipeline` class from the `biodatlab/thonburian-tts` git repo, loading the pipeline once at startup with CUDA→MPS→CPU auto-detection, and ships a bundled default Thai reference voice so callers can synthesize from text alone.

Purpose: Deliver BACK-03 (Thai TTS over HTTP), BACK-04 (bundled default reference voice), and the non-deferred model-load-once half of BACK-05. This is the second real non-OpenAI vendor proving the SDK abstraction in Phase 5.
Output: `main.py`, `requirements.txt`, `README.md`, `.gitignore`, and `assets/{default_voice.wav, default_voice.txt, LICENSE-default-voice.txt}`.

Scope notes (per CONTEXT.md D-01):
- BACK-05's semaphore/concurrency-gating half is DEFERRED — do NOT add a semaphore or request queue. Only the load-once-at-startup half is implemented here.
- BACK-02 does not apply to this TTS service (it concerns STT hallucination).

This plan is NOT autonomous: it contains a trust-boundary checkpoint for the git-installed `flowtts` package and a license-compliance checkpoint for the sourced reference voice.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-python-backend-services/03-CONTEXT.md
@.planning/phases/03-python-backend-services/03-RESEARCH.md
@.planning/phases/03-python-backend-services/03-PATTERNS.md
@.planning/PROJECT.md

<interfaces>
<!-- HTTP contract this service MUST expose (D-03). Phase 4's JaiTTSProvider adapter is the future consumer. -->
POST /synthesize
  Request: JSON { "text": string }
  Response: 200, media_type "audio/wav", body = raw WAV bytes (NO base64, NO JSON envelope)
GET /health
  Response: 200 JSON { "status": "ok" }

Device selection chain (D-06): identical to thonburian-stt — "cuda" / "mps" / "cpu".

FlowTTSPipeline calling convention is UNRESOLVED across two real sources and MUST be verified against the installed package source before the endpoint is finalized (see Task 2). The two candidate signatures observed:
  (A) model card "Quick Usage": pipeline.generate(reference_audio=, reference_text=, gen_text=) -> (audio_ndarray, sample_rate)
  (B) repo example f5tts_thai_example.py: pipeline(text=, ref_voice=, ref_text=, output_file=, speed=, check_duration=) -> file path
WAV encoding (when signature A applies): soundfile.write(io.BytesIO(), audio, sr, format="WAV").
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify flowtts git-install trust boundary, then pin requirements + source default voice</name>
  <files>/Users/whitemalt/Documents/jai-tts/requirements.txt, /Users/whitemalt/Documents/jai-tts/assets/default_voice.wav, /Users/whitemalt/Documents/jai-tts/assets/default_voice.txt, /Users/whitemalt/Documents/jai-tts/assets/LICENSE-default-voice.txt</files>
  <what-built>
    Before any code trusts the `flowtts` package, the executor will:
    1. Create a venv at `/Users/whitemalt/Documents/jai-tts/venv` and run `pip install git+https://github.com/biodatlab/thonburian-tts.git` (plus `fastapi uvicorn soundfile torch`).
    2. Confirm the package imports cleanly: `python -c "from flowtts.inference import FlowTTSPipeline, ModelConfig, AudioConfig; print('flowtts OK')"`.
    3. Write `requirements.txt` with `fastapi`, `uvicorn`, `soundfile`, `torch`, and `git+https://github.com/biodatlab/thonburian-tts.git` (NOT `flowtts` — it is not on PyPI under any name; RESEARCH Pitfall 1).
    4. Source ONE permissively-licensed Thai speech clip (CC0/CC-BY — e.g. Common Voice Thai, or the JaiTTS-F5TTS example reference clip if its license permits), saving it as `assets/default_voice.wav`, its transcript as `assets/default_voice.txt`, and the source/license terms as `assets/LICENSE-default-voice.txt`.
  </what-built>
  <read_first>
    - .planning/phases/03-python-backend-services/03-RESEARCH.md (Package Legitimacy Audit + "Note on flowtts itself" + "accelerate" note, Pitfall 1, Open Question 2, Assumption A2/A5, "Installation (jai-tts)" block)
    - .planning/phases/03-python-backend-services/03-PATTERNS.md ("jai-tts/requirements.txt" section, "default_voice" static-asset section)
    - .planning/phases/03-python-backend-services/03-CONTEXT.md (D-04, D-05)
  </read_first>
  <action>
    Create the jai-tts venv and git-install flowtts BEFORE writing any service code that imports it (it is an `[ASSUMED]` trust boundary — git-installed, not PyPI-auditable). Run `python3 -m venv /Users/whitemalt/Documents/jai-tts/venv`, activate it, then `pip install fastapi uvicorn soundfile torch` and `pip install git+https://github.com/biodatlab/thonburian-tts.git`. Confirm the import succeeds via `python -c "from flowtts.inference import FlowTTSPipeline, ModelConfig, AudioConfig; print('flowtts OK')"`. Do NOT add `accelerate` unless an `ImportError: Accelerate` actually surfaces (it is `[SUS]` per RESEARCH; install only on demand and re-confirm at this checkpoint).

    Write `requirements.txt` listing `fastapi`, `uvicorn`, `soundfile`, `torch`, and the line `git+https://github.com/biodatlab/thonburian-tts.git`. Do NOT list a bare `flowtts` entry — `pip install flowtts` resolves to nothing/something unrelated (RESEARCH Pitfall 1).

    Source exactly ONE single-speaker, permissively-licensed (CC0/CC-BY or equivalent) Thai speech clip and bundle it as `assets/default_voice.wav`; write its matching transcript to `assets/default_voice.txt`; and write the source dataset/URL plus the concrete license terms to `assets/LICENSE-default-voice.txt`. Candidates per RESEARCH Open Question 2: a Common Voice Thai (CC0) clip, or the JaiTTS-F5TTS example reference clip if its license permits redistribution. Then pause for the human checkpoint below.
  </action>
  <how-to-verify>
    Two human checks are required before proceeding to Task 2:

    1. TRUST BOUNDARY (flowtts git install): The `flowtts` package is git-installed from `github.com/biodatlab/thonburian-tts` and is NOT auditable on PyPI (`[ASSUMED]` trust boundary per RESEARCH). Confirm:
       - The repo URL is exactly `https://github.com/biodatlab/thonburian-tts.git` (the legitimate biodatlab org, matching the Whisper model author).
       - `python -c "from flowtts.inference import FlowTTSPipeline, ModelConfig, AudioConfig; print('flowtts OK')"` prints `flowtts OK` with no error.
       - Review that the install did not pull anything unexpected. Approve only if the repo and import look legitimate.
       (If `ImportError: Accelerate` surfaces during a later model-load step, `accelerate` is flagged `[SUS]` by slopcheck — install it ONLY if actually needed and re-confirm here; do not add it speculatively.)

    2. LICENSE COMPLIANCE (default reference voice): Confirm the sourced clip is genuinely permissively licensed for bundling/redistribution:
       - `assets/LICENSE-default-voice.txt` names the source dataset/URL and the license (e.g. CC0, CC-BY 4.0) and is compatible with bundling the clip in this repo.
       - `assets/default_voice.wav` exists and is a single-speaker Thai clip; `assets/default_voice.txt` contains its matching transcript.
       Approve only if the license clearly permits redistribution.
  </how-to-verify>
  <verify>
    <automated>test -f /Users/whitemalt/Documents/jai-tts/requirements.txt && grep -q 'github.com/biodatlab/thonburian-tts' /Users/whitemalt/Documents/jai-tts/requirements.txt && ! grep -Eq '^flowtts$|^flowtts[=<>]' /Users/whitemalt/Documents/jai-tts/requirements.txt && test -s /Users/whitemalt/Documents/jai-tts/assets/default_voice.wav && test -s /Users/whitemalt/Documents/jai-tts/assets/default_voice.txt && test -s /Users/whitemalt/Documents/jai-tts/assets/LICENSE-default-voice.txt</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from flowtts.inference import FlowTTSPipeline, ModelConfig, AudioConfig"` succeeds in the jai-tts venv (flowtts installed from the biodatlab git repo)
    - `requirements.txt` contains the line `git+https://github.com/biodatlab/thonburian-tts.git` and does NOT contain a bare `flowtts` PyPI entry
    - `requirements.txt` also lists `fastapi`, `uvicorn`, `soundfile`, `torch`
    - `assets/default_voice.wav`, `assets/default_voice.txt`, and `assets/LICENSE-default-voice.txt` all exist and are non-empty
    - `LICENSE-default-voice.txt` names a concrete permissive license (CC0/CC-BY or equivalent) and the source of the clip
    - Human approved both the flowtts trust boundary and the voice-clip license compliance
  </acceptance_criteria>
  <done>flowtts imports cleanly from the biodatlab git install, requirements.txt pins the five deps (flowtts via git, not PyPI), the permissively-licensed default voice clip + transcript + license file are bundled in assets/, and the human has approved both the trust boundary and the license.</done>
  <resume-signal>Type "approved" if the flowtts git install imports cleanly AND the default voice clip is permissively licensed, or describe what needs to change (different repo, different clip).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Verify FlowTTSPipeline signature, implement main.py against it, scaffold .gitignore/README</name>
  <files>/Users/whitemalt/Documents/jai-tts/main.py, /Users/whitemalt/Documents/jai-tts/.gitignore, /Users/whitemalt/Documents/jai-tts/README.md</files>
  <read_first>
    - .planning/phases/03-python-backend-services/03-RESEARCH.md (Pattern 1, Pattern 3 + its CRITICAL caveat, Pattern 4, Pitfall 2, Open Question 1, Code Examples, "Run command" block, Assumption A2/A3)
    - .planning/phases/03-python-backend-services/03-PATTERNS.md ("jai-tts/main.py" section incl. the CRITICAL calling-convention caveat, shared "Device auto-detection", "Model-load-once via FastAPI lifespan", "No custom error envelope", "Plain venv + uvicorn run convention")
    - /Users/whitemalt/Documents/jai-tts/requirements.txt (from Task 1)
    - /Users/whitemalt/Documents/jai-tts/assets/default_voice.txt (transcript used as reference_text)
  </read_first>
  <action>
    STEP A — Resolve the FlowTTSPipeline calling convention empirically (do this FIRST, do not assume). In the jai-tts venv run `python -c "from flowtts.inference import FlowTTSPipeline; help(FlowTTSPipeline)"` (and/or read the installed source) to determine the real method signature. The two documented candidates disagree (RESEARCH Open Question 1): signature (A) `pipeline.generate(reference_audio=, reference_text=, gen_text=)` returning `(audio, sr)`, vs signature (B) callable `pipeline(text=, ref_voice=, ref_text=, output_file=, ...)` returning a file path. Also confirm the constructor/`ModelConfig` args and the JaiTTS-F5TTS `hf://` checkpoint/vocab paths actually resolve (RESEARCH Assumption A3). Write the endpoint against the VERIFIED signature only. Record which signature was found in a one-line code comment so reviewers know it was checked.

    STEP B — Create `main.py` as a single-file FastAPI app (single-file is Claude's discretion per CONTEXT.md; do NOT build an `app/` package). Structure:
    1. Module-level constants: `DEFAULT_VOICE_PATH = "assets/default_voice.wav"` and `DEFAULT_VOICE_TEXT` read from `assets/default_voice.txt` (read the transcript file at import or startup).
    2. A `select_device() -> str` function with the D-06 chain (cuda → mps → cpu) — copy the same logic used by thonburian-stt.
    3. An `@asynccontextmanager` `lifespan(app)` that computes the device once and assigns `app.state.tts_pipe = <FlowTTSPipeline loaded with the verified constructor args + device>` BEFORE `yield`. Load MUST happen here, never per-request (RESEARCH anti-pattern).
    4. `app = FastAPI(lifespan=lifespan)`.
    5. `GET /health` returning `{"status": "ok"}`.
    6. A `SynthesizeRequest(BaseModel)` with a single `text: str` field, and `POST /synthesize` with signature `async def synthesize(request: Request, body: SynthesizeRequest)`. Inside: call `request.app.state.tts_pipe` using the VERIFIED signature from STEP A, passing the default reference voice (`DEFAULT_VOICE_PATH`, `DEFAULT_VOICE_TEXT`) and `body.text` as the generation text. If the verified signature returns `(audio, sr)`, encode to WAV via `soundfile.write(io.BytesIO(), audio, sr, format="WAV")` and return `Response(content=buf.getvalue(), media_type="audio/wav")`. If it returns a file path, read those bytes and return them the same way. Either way the response body is raw `audio/wav` bytes — NO base64, NO JSON envelope (D-03).

    Do NOT add try/except error envelopes — rely on FastAPI's automatic 422 for malformed/empty JSON (CONTEXT.md Claude's Discretion + D-01). Do NOT add any semaphore/concurrency guard (BACK-05 semaphore half deferred).

    STEP C — Create `.gitignore` ignoring `venv/`, `__pycache__/`, `*.pyc`, and `*.out.wav` (synthesized test outputs); do NOT ignore `assets/` (the bundled default voice must be committed). Create `README.md` documenting: the venv+uvicorn run sequence with `uvicorn main:app --host 0.0.0.0 --port 8002` (port 8002 for TTS; STT uses 8001); `ffmpeg` system prerequisite; first-run internet requirement for HF weights + the git flowtts install; the `POST /synthesize` (JSON text → raw WAV bytes) and `GET /health` contract; that `flowtts` comes from `git+https://github.com/biodatlab/thonburian-tts.git` (not PyPI); the bundled default voice in `assets/` (BACK-04); and an "Out of scope (deferred)" note that concurrency gating/semaphore (BACK-05) is deliberately NOT implemented per decision D-01.
  </action>
  <verify>
    <automated>cd /Users/whitemalt/Documents/jai-tts && python3 -m py_compile main.py && grep -q 'def select_device' main.py && grep -q 'torch.backends.mps.is_available' main.py && grep -q 'app.state.tts_pipe' main.py && grep -q '@app.post("/synthesize")' main.py && grep -q '@app.get("/health")' main.py && grep -q 'media_type="audio/wav"' main.py && grep -q 'default_voice' main.py && ! grep -Eqi 'base64' main.py && ! grep -q 'Semaphore' main.py && test -f .gitignore && grep -q 'venv/' .gitignore && grep -qi '8002' README.md && grep -qi 'thonburian-tts' README.md</automated>
  </verify>
  <acceptance_criteria>
    - The FlowTTSPipeline signature was verified against the installed package (a one-line comment in main.py records which of signature A/B was found)
    - `python3 -m py_compile main.py` exits 0
    - `main.py` defines `select_device` with the cuda→mps→cpu chain and assigns `app.state.tts_pipe` inside `lifespan` (before `yield`), not in the handler
    - `@app.post("/synthesize")` accepts a `SynthesizeRequest` body with a `text: str` field and returns `Response(..., media_type="audio/wav")` with raw bytes (no `base64`, no JSON audio envelope)
    - The handler references the bundled default reference voice (`default_voice` path constant) so text-only calls succeed (BACK-04)
    - No `Semaphore` symbol appears (BACK-05 semaphore half not implemented)
    - `.gitignore` contains `venv/` and does NOT ignore `assets/`; `README.md` documents port `8002`, the `thonburian-tts` git install, and the BACK-05 deferral
  </acceptance_criteria>
  <done>main.py compiles, loads the JaiTTS-F5TTS FlowTTSPipeline once in lifespan against the empirically-verified signature, and exposes POST /synthesize (JSON text → raw audio/wav bytes using the bundled default voice) and GET /health.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /synthesize | Untrusted JSON text crosses into the service |
| service → github.com/biodatlab/thonburian-tts | `flowtts` inference code is git-installed and executes model-loading code in-process (NOT PyPI-auditable) |
| service → Hugging Face Hub | JaiTTS-F5TTS checkpoint + vocab + reference assets resolved via hf:// at first run |
| bundled asset → repo | A third-party-sourced audio clip is committed and redistributed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | flowtts git install (biodatlab/thonburian-tts) | mitigate | `[ASSUMED]` trust boundary — git-installed code is not PyPI-auditable. Task 1 is a blocking-human checkpoint that verifies the repo URL is the legitimate biodatlab org and that the package imports cleanly before any service trusts it. |
| T-03-05 | Spoofing/IP | bundled default_voice.wav license | mitigate | Task 1 blocking-human checkpoint verifies the clip is permissively licensed (CC0/CC-BY) with a committed LICENSE-default-voice.txt naming source + terms before redistribution. |
| T-03-06 | Denial of Service | concurrent /synthesize requests + GPU OOM | accept | Concurrency gating (BACK-05 semaphore) explicitly deferred per D-01; demo runs single-user. Documented in README. |
| T-03-07 | Tampering | /synthesize JSON body | accept | FastAPI/Pydantic validates the `text: str` field (422 on malformed/missing); text is only passed as gen_text to the pipeline, never used as a file path or shell argument. |
| T-03-SC | Tampering | pip installs | mitigate | fastapi/uvicorn/soundfile/torch audited `OK`/Approved (PyPI, mature). `flowtts` (git) gated by Task 1 blocking-human checkpoint (T-03-04). `accelerate` is `[SUS]` — installed only if an ImportError forces it, and re-confirmed at the Task 1 checkpoint; not added speculatively. Legitimacy checkpoints are never auto-approvable. |
</threat_model>

<verification>
- `main.py` compiles via `python3 -m py_compile`.
- FlowTTSPipeline signature verified empirically before the endpoint was written (comment in main.py).
- TTS pipeline loaded in `lifespan`, not per-request.
- `/synthesize` returns raw `audio/wav` bytes (no base64) and uses the bundled default voice.
- `requirements.txt` git-installs flowtts from biodatlab/thonburian-tts (not a PyPI `flowtts`).
- assets/ contains default_voice.wav, default_voice.txt, LICENSE-default-voice.txt.
- (Manual / Phase 4 follow-up) POSTing Thai text returns audibly-correct Thai WAV — full model-download + synthesis smoke test left to first run with internet/GPU.
</verification>

<success_criteria>
- jai-tts repo contains main.py, requirements.txt, README.md, .gitignore, and assets/{default_voice.wav,default_voice.txt,LICENSE-default-voice.txt}.
- BACK-03: POST /synthesize exists with the JSON-text → raw-WAV-bytes contract (D-03) using JaiTTS-F5TTS via FlowTTSPipeline.
- BACK-04: a permissively-licensed default Thai reference voice is bundled and used when text alone is supplied.
- BACK-05 (load-once half): FlowTTSPipeline is loaded exactly once in lifespan with CUDA→MPS→CPU auto-detection; the semaphore half is documented as deferred (D-01), not implemented.
</success_criteria>

<output>
Create `.planning/phases/03-python-backend-services/03-02-SUMMARY.md` when done.
</output>
