---
phase: 04-vendor-adapters-audio-contract
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - packages/providers/generic-stt-tts/src/index.ts
  - packages/providers/generic-stt-tts/README.md
  - packages/providers/generic-stt-tts/scripts/roundtrip-audio-contract.ts
autonomous: true
requirements: [ADPT-03]
must_haves:
  truths:
    - "ThonburianSTTProvider and JaiTTSProvider (+ their config types) are exported from the package barrel"
    - "The audio wire format for both directions (sample rate, encoding, channels) is documented in one place"
    - "An opt-in round-trip test script exists that hits the real running services and asserts format compatibility, and is excluded from the default pnpm test / CI run"
  artifacts:
    - path: "packages/providers/generic-stt-tts/src/index.ts"
      provides: "Barrel re-exports of the two new providers + config types"
      contains: "ThonburianSTTProvider"
    - path: "packages/providers/generic-stt-tts/README.md"
      provides: "Audio wire-format documentation (both directions) + how to run the opt-in round-trip script"
      contains: "Audio Wire Format"
    - path: "packages/providers/generic-stt-tts/scripts/roundtrip-audio-contract.ts"
      provides: "Opt-in real-service encode→POST→decode→assert round-trip test (D-05/D-06)"
      contains: "roundtrip"
  key_links:
    - from: "packages/providers/generic-stt-tts/src/index.ts"
      to: "./adapters/ThonburianSTTProvider"
      via: "export"
      pattern: "export.*ThonburianSTTProvider"
    - from: "packages/providers/generic-stt-tts/scripts/roundtrip-audio-contract.ts"
      to: "ThonburianSTTProvider, JaiTTSProvider"
      via: "import from ../src/adapters"
      pattern: "import.*adapters"
---

<objective>
Finish Phase 4 by (1) exporting the two new providers from the package barrel so consumers can import them, (2) writing the audio wire format down in one place per direction (the explicit point of ADPT-03), and (3) creating the opt-in round-trip script that proves real byte-format compatibility against the live local services. Covers requirement ADPT-03.

Purpose: ADPT-03 requires the wire format documented in one place AND a passing round-trip test. The barrel export makes Plans 01/02's adapters publicly usable (needed by Phase 5).
Output: Updated `index.ts`, an amended `README.md` with a wire-format section, and a new opt-in `scripts/roundtrip-audio-contract.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-vendor-adapters-audio-contract/04-CONTEXT.md
@.planning/phases/04-vendor-adapters-audio-contract/04-RESEARCH.md
@.planning/phases/04-vendor-adapters-audio-contract/04-PATTERNS.md

<interfaces>
<!-- Confirmed wire-format facts to document (04-CONTEXT.md Specific Ideas). Both directions DIFFER. -->

STT direction (SDK → thonburian-stt, port 8001):
  Format: 16000 Hz, mono, 32-bit IEEE float PCM (WAV format code 3)
  Source: @ricky0123/vad-web utils.encodeWAV() defaults (AudioRecorder.ts)
  Transport: multipart/form-data, field name "file", filename "utterance.wav"
  Response: 200 JSON {"text": string}

TTS direction (jai-tts → SDK, port 8002):
  Format: 24000 Hz, mono, 16-bit PCM (soundfile.write default subtype)
  Source: flowtts FlowTTSPipeline → soundfile.write(..., target_sample_rate=24000)
  Transport: request JSON {"text": string}; response raw audio/wav bytes
  SDK side: AudioContext.decodeAudioData() auto-resamples — no manual resampling needed

<!-- vitest config (confirmed): include is ["src/**/*.test.ts"]. A file under scripts/ (outside src/) is auto-excluded from `pnpm test` and CI with zero config change (D-06). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export the two new providers from the package barrel</name>
  <files>packages/providers/generic-stt-tts/src/index.ts</files>
  <read_first>
    - packages/providers/generic-stt-tts/src/index.ts (current barrel — append to it; preserve existing OpenAI* exports unchanged)
    - packages/providers/generic-stt-tts/src/adapters/ThonburianSTTProvider.ts (confirm the exported class + config type names — from Plan 01)
    - packages/providers/generic-stt-tts/src/adapters/JaiTTSProvider.ts (confirm the exported class + config type names — from Plan 02)
  </read_first>
  <action>
    Append two `export`/`export type` pairs to `index.ts`, matching the existing pattern (lines 7-14 export OpenAISTTAdapter/OpenAITTSAdapter the same way). Add: `export { ThonburianSTTProvider } from "./adapters/ThonburianSTTProvider";` and `export type { ThonburianSTTProviderConfig } from "./adapters/ThonburianSTTProvider";`, then `export { JaiTTSProvider } from "./adapters/JaiTTSProvider";` and `export type { JaiTTSProviderConfig } from "./adapters/JaiTTSProvider";`. Do not modify or remove any existing export lines (GenericPipelineProvider, the four OpenAI* adapters).
  </action>
  <verify>
    <automated>cd packages/providers/generic-stt-tts && npx tsc --noEmit && grep -c "ThonburianSTTProvider\|JaiTTSProvider" src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes
    - `src/index.ts` contains `export { ThonburianSTTProvider }` and `export type { ThonburianSTTProviderConfig }`
    - `src/index.ts` contains `export { JaiTTSProvider }` and `export type { JaiTTSProviderConfig }`
    - All five pre-existing exports (GenericPipelineProvider + 4 OpenAI* adapters) remain present
  </acceptance_criteria>
  <done>The barrel re-exports both new providers and their config types alongside the existing exports; the package type-checks.</done>
</task>

<task type="auto">
  <name>Task 2: Document the audio wire format (both directions) in the package README</name>
  <files>packages/providers/generic-stt-tts/README.md</files>
  <read_first>
    - packages/providers/generic-stt-tts/README.md (current content — append a new section; do not rewrite existing sections)
    - .planning/phases/04-vendor-adapters-audio-contract/04-CONTEXT.md (Specific Ideas section — the verified per-direction format facts that MUST be captured)
    - packages/providers/generic-stt-tts/src/adapters/ThonburianSTTProvider.ts and JaiTTSProvider.ts (the file-header doc comments already note the format per Plans 01/02 — README is the single consolidated home ADPT-03 calls for)
  </read_first>
  <action>
    Append a new `## Audio Wire Format` section to the README documenting BOTH directions explicitly and separately (they differ — do not imply a shared format). Capture these exact, verified facts:

    STT direction (SDK → `thonburian-stt`, default `http://localhost:8001/transcribe`): 16000 Hz, mono, 32-bit IEEE float PCM (WAV format code 3); produced by `@ricky0123/vad-web`'s `encodeWAV()` defaults; sent as `multipart/form-data` with field name `file` (filename `utterance.wav`); response is `200` JSON `{"text": string}`.

    TTS direction (`jai-tts` → SDK, default `http://localhost:8002/synthesize`): 24000 Hz, mono, 16-bit PCM (soundfile default subtype, `target_sample_rate=24000`); request is JSON `{"text": string}`; response is raw `audio/wav` bytes; the SDK decodes via `AudioContext.decodeAudioData()` which auto-resamples, so no manual resampling is required on the SDK side.

    Note that neither service honors per-call `language`/`voice`/`speed` (D-04, hardcoded server-side) and that neither adapter sends auth (D-01, local demo services). Then add a `## Round-trip audio-contract test` subsection documenting how to run the opt-in script (Task 3): start both services locally, then `cd packages/providers/generic-stt-tts && npx vitest run scripts/roundtrip-audio-contract.ts`, and state that this script is NOT part of the default `pnpm test` / CI because it requires both Python services with loaded models running (D-05/D-06).
  </action>
  <verify>
    <automated>cd packages/providers/generic-stt-tts && grep -F "Audio Wire Format" README.md && grep -F "16000" README.md && grep -F "24000" README.md && grep -iF "roundtrip-audio-contract" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains a `## Audio Wire Format` heading
    - README documents the STT direction as 16000 Hz / mono / 32-bit float and field name `file`
    - README documents the TTS direction as 24000 Hz / mono / 16-bit and raw `audio/wav` response
    - README documents the D-04 (no per-call options) and D-01 (no auth) facts
    - README documents how to run the opt-in round-trip script and that it is excluded from default `pnpm test`/CI
    - Existing README sections (Install, Usage, Vendor-neutral interfaces) remain present and unmodified
  </acceptance_criteria>
  <done>The README has a single consolidated wire-format section covering both directions explicitly, plus run instructions for the opt-in script.</done>
</task>

<task type="auto">
  <name>Task 3: Create the opt-in real-service round-trip test script</name>
  <files>packages/providers/generic-stt-tts/scripts/roundtrip-audio-contract.ts</files>
  <read_first>
    - .planning/phases/04-vendor-adapters-audio-contract/04-RESEARCH.md (Code Examples → "Round-trip test script skeleton" lines ~463-501 — use as the structural template for the describe/it shape ONLY; ignore its `/* load fixture WAV */` placeholder — the fixture is built inline per the action below, NOT loaded from a file or a vad-web import)
    - packages/providers/generic-stt-tts/src/adapters/ThonburianSTTProvider.ts and JaiTTSProvider.ts (the classes the script exercises — from Plans 01/02; confirm `transcribe(blob)` accepts a `Blob` and returns `{ text: string }`)
    - packages/providers/generic-stt-tts/vitest.config.ts (confirm include is ["src/**/*.test.ts"] — placing this file under scripts/ keeps it out of the default run; do NOT edit this config)
    - packages/providers/generic-stt-tts/package.json (confirm `@ricky0123/vad-web` is NOT a dependency here — it lives only in `openai-stt-tts`; this is WHY the WAV encoder must be hand-written inline, NOT imported)
  </read_first>
  <action>
    Create `scripts/roundtrip-audio-contract.ts` as a Vitest-shaped file (uses `describe`/`it`/`expect` from "vitest") so it runs via `npx vitest run scripts/roundtrip-audio-contract.ts` but is excluded from `pnpm test` because vitest's `include` only matches `src/**/*.test.ts` (D-06 satisfied structurally — DO NOT modify vitest.config.ts). Import `ThonburianSTTProvider` and `JaiTTSProvider` from `../src/adapters/...`. Do NOT import anything from `@ricky0123/vad-web` — that package is NOT a dependency of `generic-stt-tts` (it is only declared in `openai-stt-tts`), so any import of it would fail to resolve under pnpm's strict isolation and break `tsc --noEmit`. Add a top-of-file comment block stating the manual run steps (start thonburian-stt on :8001, start jai-tts on :8002, then the vitest run command) and that it is intentionally outside `src/`.

    WAV encoder (hand-written, inline — this is the PRIMARY approach, not a fallback): write a small self-contained local function in this script that constructs a valid WAV file from a `Float32Array` of mono samples. It must emit WAV format code 3 (IEEE float PCM), 1 channel, 16000 Hz sample rate, 32-bit bit depth — exactly matching the wire format `ThonburianSTTProvider` is documented to send (CONTEXT.md Specific Ideas: 16000 Hz, mono, 32-bit IEEE float PCM, WAV format code 3). Build the 44-byte RIFF/WAVE header by hand (RIFF chunk, `fmt ` subchunk with audioFormat=3, numChannels=1, sampleRate=16000, bitsPerSample=32, then the `data` subchunk) using a `DataView`/`ArrayBuffer`, then write the samples as little-endian float32 with `DataView.setFloat32(offset, sample, true)`. Return the bytes as a `Blob` (or wrap in `new Blob([buffer], { type: "audio/wav" })`). This is dev-tooling-only code (opt-in script, never shipped, never in default `pnpm test`/CI), so a small inline encoder is correct and lower-risk than adding a cross-package dependency just for a script.

    STT round trip (`describe("ADPT-03: real-service audio wire-format round trip")`): generate a fixture WAV programmatically (no committed binary asset — RESEARCH Open Question 2). Build a short deterministic `Float32Array` (e.g. ~0.5s of a low-amplitude sine wave at 16000 Hz), encode it with the inline WAV-format-code-3 function above into a `Blob`, POST it via `new ThonburianSTTProvider().transcribe(blob)`, and assert the result is an object with a `text` of type string (format/round-trip proof; do NOT assert exact transcript content since synthetic audio yields no meaningful Thai — assertion is structural per RESEARCH).

    TTS round trip: `new JaiTTSProvider()` — since Node has no real `AudioContext`, do NOT call `speak()` (which needs Web Audio). Instead assert the raw HTTP leg directly: `fetch("http://localhost:8002/synthesize", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ text: "สวัสดี" }) })`, then assert `res.ok`, that `res.headers.get("content-type")` includes `"audio/wav"`, and that `(await res.arrayBuffer()).byteLength > 44` (larger than a bare WAV header — proves real audio bytes came back). Add a comment explaining the AudioContext-absence reason for testing the HTTP leg only here.

    Mark the suite/cases so a maintainer understands both services must be running (e.g. a top comment; optionally guard with a clear failure message if the fetch connection is refused).
  </action>
  <verify>
    <automated>cd packages/providers/generic-stt-tts && npx tsc --noEmit && npx vitest run --reporter=json --outputFile=/tmp/04-03-default-run.json; test $? -eq 0 && node -e 'const r=require("/tmp/04-03-default-run.json");const files=r.testResults.map(t=>t.name).filter(Boolean);const base=files.map(f=>f.split("/").pop());const hasRoundtrip=base.some(f=>f.includes("roundtrip"));if(hasRoundtrip){console.error("FAIL: roundtrip script ran in default suite: "+JSON.stringify(base));process.exit(1);}if(!base.includes("ThonburianSTTProvider.test.ts")){console.error("FAIL: ThonburianSTTProvider.test.ts missing from default run: "+JSON.stringify(base));process.exit(1);}if(!base.includes("JaiTTSProvider.test.ts")){console.error("FAIL: JaiTTSProvider.test.ts missing from default run: "+JSON.stringify(base));process.exit(1);}console.log("DEFAULT_RUN_OK: exit 0, both new adapter tests present, roundtrip absent (pre-existing tests permitted)");'</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/roundtrip-audio-contract.ts` exists at the package root under `scripts/` (NOT under `src/`)
    - `npx tsc --noEmit` for the package still passes (the script is valid TypeScript)
    - The script does NOT import from `@ricky0123/vad-web` (not a dependency of this package); the WAV encoder is hand-written inline (format code 3, mono, 16000 Hz, 32-bit float)
    - Running the default `npx vitest run` (no path arg) exits 0 AND the reporter's executed-file list INCLUDES both `ThonburianSTTProvider.test.ts` and `JaiTTSProvider.test.ts` AND contains no file whose name includes `roundtrip` (the opt-in script is absent from the default run); pre-existing package test files (e.g. `GenericPipelineProvider.test.ts`, `OpenAILLMAdapter.test.ts`, `OpenAISTTAdapter.test.ts`, `OpenAITTSAdapter.test.ts`) are permitted in the run and must NOT be required to disappear; vitest.config.ts is unmodified
    - The script imports `ThonburianSTTProvider` and `JaiTTSProvider` from `../src/adapters`
    - The STT case generates a 16kHz/mono/float32 WAV fixture programmatically via the inline encoder and asserts `typeof result.text === "string"`
    - The TTS case asserts the `/synthesize` response is `audio/wav` with a body larger than a bare WAV header
    - A top-of-file comment documents the two-service manual startup steps and the D-06 exclusion rationale
  </acceptance_criteria>
  <done>The opt-in round-trip script exists outside src/, builds its STT fixture with an inline WAV-format-code-3 encoder (no vad-web import), exercises both real services (encode→POST→assert for STT, HTTP-leg byte-format assert for TTS), type-checks, and does not run in the default pnpm test / CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Round-trip script → both local services | The opt-in script (run manually by a developer) sends generated audio/text over localhost HTTP to the two unauthenticated local services. Dev tooling only — never runs in CI or production (D-06). No new runtime trust boundary beyond what Plans 01/02 already established. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Denial of Service | Round-trip script hanging if a service is down | accept | Adapters' own 60s `AbortSignal.timeout` (Plans 01/02) bounds the STT leg; the raw TTS `fetch` is a manual dev diagnostic where a connection-refused failure is the expected, acceptable signal that a service is not running. Low-value local dev tooling. |
| T-04-10 | Information Disclosure | Documentation exposing internal service ports/format | accept | Ports (8001/8002) and formats are local-dev facts already in each service's README; documenting them in the SDK README for ADPT-03 introduces no new exposure (local-only services, no secrets). |

No npm/pip/cargo installs in this plan. The round-trip script uses only the already-installed `vitest` plus a hand-written inline WAV encoder — it deliberately does NOT import `@ricky0123/vad-web` (not a dependency of `generic-stt-tts`). Zero new dependencies (RESEARCH: vitest-as-script-runner chosen over adding `tsx`). No package-legitimacy checkpoint required.
</threat_model>

<verification>
- `npx tsc --noEmit` passes for the package
- `npx vitest run` (default, no path) exits 0, runs both adapter test files from Plans 01/02 (alongside any pre-existing package tests), and the round-trip script is absent from the run
- README has a single `## Audio Wire Format` section covering both directions; index.ts exports both new providers
</verification>

<success_criteria>
- ADPT-03 satisfied: the audio wire format (sample rate, encoding, channels) for both directions is written down in one place (README `## Audio Wire Format`), and a round-trip test (encode → POST → decode/assert format) exists for both services as an opt-in script that passes against the real running services and is excluded from default `pnpm test`/CI
- Both new providers are importable from the package barrel
</success_criteria>

<output>
Create `.planning/phases/04-vendor-adapters-audio-contract/04-03-SUMMARY.md` when done
</output>
