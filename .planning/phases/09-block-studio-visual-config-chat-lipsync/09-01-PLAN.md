---
phase: 09-block-studio-visual-config-chat-lipsync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wordpress-plugin/src/block.json
  - wordpress-plugin/includes/Block/block.json
  - wordpress-plugin/includes/Block/AvatarBlock.php
  - wordpress-plugin/includes/Render/AvatarRenderer.php
  - wordpress-plugin/includes/ConfigSource/WpOptionsConfigSource.php
  - packages/wp-bundle/src/config.ts
  - packages/wp-bundle/src/mount.tsx
  - packages/wp-bundle/build.mjs
autonomous: true
requirements:
  - STUDIO-05

must_haves:
  truths:
    - "All 14 new visual/chat config keys ride the EXISTING data-khaveeai-config JSON contract end-to-end: block.json attributes → AvatarBlock render_callback → wp_parse_args merge over WpOptionsConfigSource defaults → AvatarRenderer public_safe() allowlist → escaped JSON on mount-point div"
    - "A zero/blank block attribute value means 'use admin default' (forward-compatible with existing Phase-8 blocks/shortcodes)"
    - "The preview bundle entry scaffolding exists in build.mjs (emits khaveeai-preview.js alongside khaveeai-bundle.js) and the build-time safety grep assertion runs against the preview output"
    - "No new transport mechanism is introduced — new keys are purely additive to three PHP arrays + one TS interface + one build entry"
  artifacts:
    - path: "wordpress-plugin/src/block.json"
      provides: "14 new block attributes (containerWidth/Height, fullWidth, bgType/Color/Transparent/ImageId, lightIntensity, avatarScale/OffsetX/OffsetY, cameraPreset, chatShow/Placement)"
      contains: "containerWidth"
    - path: "wordpress-plugin/includes/Block/block.json"
      provides: "Mirror of src/block.json attributes (Gutenberg registers from this path)"
      contains: "cameraPreset"
    - path: "wordpress-plugin/includes/Block/AvatarBlock.php"
      provides: "render_callback extended with 13 new keys + bgImageId→URL resolution + type-aware array_filter (preserves 0/0.0/false)"
    - path: "wordpress-plugin/includes/Render/AvatarRenderer.php"
      provides: "public_safe() allowlist emits all new camelCase keys; per-field isset→cast→fallback re-application block extended"
    - path: "wordpress-plugin/includes/ConfigSource/WpOptionsConfigSource.php"
      provides: "get_runtime_config() returns snake_case defaults for the 13 new keys (admin settings-page UI for editing them remains out-of-specret)"
    - path: "packages/wp-bundle/src/config.ts"
      provides: "Extended KhaveeAvatarConfig interface + CAMERA_PRESETS map + LIGHT_INTENSITY constant + resolveSceneDefaults() helper"
    - path: "packages/wp-bundle/src/mount.tsx"
      provides: "KhaveeAvatarConfig re-exported from config.ts (interface moved, signature unchanged for index.ts consumer)"
    - path: "packages/wp-bundle/build.mjs"
      provides: "buildOptions(entry, outfile) factory + second entry for src/preview.ts → khaveeai-preview.js + build-time grep assertion"
  key_links:
    - from: "wordpress-plugin/includes/Block/AvatarBlock.php"
      to: "wordpress-plugin/includes/Render/AvatarRenderer.php"
      via: "$this->renderer->render( $renderer_atts )"
      pattern: "renderer->render"
    - from: "wordpress-plugin/includes/Render/AvatarRenderer.php"
      to: "packages/wp-bundle/src/index.ts"
      via: "esc_attr(wp_json_encode(public_safe($merged))) on data-khaveeai-config"
      pattern: "data-khaveeai-config"
    - from: "packages/wp-bundle/src/config.ts"
      to: "packages/wp-bundle/src/mount.tsx"
      via: "import type { KhaveeAvatarConfig } from './config'"
      pattern: "from ['\"]\\./config['\"]"
---

<objective>
Lay the STUDIO-05 foundation that every other Phase-9 plan depends on. Extend the existing config-transport pipeline (block.json → PHP merge → JSON injection → bundle parse) with the 14 new visual/chat config keys, end-to-end and purely additively. Also scaffold the second esbuild entry (preview bundle) in build.mjs so Wave-2 plans can populate `src/preview.ts` without re-touching the build config. No inspector UI, no preview scene, no ChatBox yet — this plan is the transport spine.

Purpose: STUDIO-05 is the load-bearing requirement — STUDIO-01/02/03/04 all consume config keys that must flow end-to-end before any UI work can be verified. Doing this in Wave 1 unblocks parallel Wave-2 work on inspector / preview / ChatBox.

Output: Three PHP files extended with new keys (block.json × 2, AvatarBlock.php, AvatarRenderer.php, WpOptionsConfigSource.php), one new TS types module (config.ts), build.mjs with the preview entry factory + safety grep assertion. The published page still renders identically to Phase 8 (all new keys default to blank/zero → fall through to Phase-8 hardcoded scene values).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-block-studio-visual-config-chat-lipsync/09-CONTEXT.md
@.planning/phases/09-block-studio-visual-config-chat-lipsync/09-RESEARCH.md
@.planning/phases/09-block-studio-visual-config-chat-lipsync/09-PATTERNS.md
@wordpress-plugin/src/block.json
@wordpress-plugin/includes/Block/block.json
@wordpress-plugin/includes/Block/AvatarBlock.php
@wordpress-plugin/includes/Render/AvatarRenderer.php
@wordpress-plugin/includes/ConfigSource/WpOptionsConfigSource.php
@packages/wp-bundle/src/index.ts
@packages/wp-bundle/src/mount.tsx
@packages/wp-bundle/build.mjs

<interfaces>
<!-- Existing pipeline contracts the executor must extend (NOT replace). -->

From wordpress-plugin/includes/Render/AvatarRenderer.php (the public_safe allowlist — the snake→camel boundary):
```php
// Existing 5-key return shape (lines 138-146). The executor EXTENDS this array
// with the 13 new camelCase keys. The existing esc_attr(wp_json_encode(...))
// at line 93 escapes whatever this returns — new keys benefit automatically.
private function public_safe( array $merged ): array {
    return array(
        'voice'        => isset( $merged['voice'] )        ? (string) $merged['voice']        : '',
        'instructions' => isset( $merged['instructions'] ) ? (string) $merged['instructions'] : '',
        'avatarUrl'    => isset( $merged['avatar_url'] )   ? (string) $merged['avatar_url']   : '',
        'model'        => isset( $merged['model'] )        ? (string) $merged['model']        : '',
        'restUrl'      => rest_url( 'khaveeai/v1/session' ),
    );
}
```

From packages/wp-bundle/src/mount.tsx (the existing KhaveeAvatarConfig interface — to be MOVED to config.ts and extended):
```typescript
export interface KhaveeAvatarConfig {
  voice?: "alloy" | "ash" | "ballad" | "coral" | "echo" | "sage" | "shimmer" | "verse" | "marin" | "cedar";
  instructions?: string;
  avatarUrl?: string;
  model?: string;
  restUrl?: string;
}
```
</interfaces>
</context>

<tasks>

<!-- Execution note (WARNING-3, checker iteration 1): this plan modifies 8 files — all cohesive additive extensions to the config-transport pipeline (no split required). The HIGHEST-RISK single edit is the ARRAY_FILTER_USE_BOTH fix in AvatarBlock.php (Task 2, step 3): if the type-aware callback is mis-applied, scale=0 / offset=0 / chatShow=false will be silently stripped and the per-instance override lost on the published page. Execute that edit first and verify it with the `grep -c ARRAY_FILTER_USE_BOTH` gate before moving to the renderer extension. -->

<task type="auto">
  <name>Task 1: Extend block.json (both copies) with 14 new attributes + add defaults to WpOptionsConfigSource</name>
  <read_first>
    - wordpress-plugin/src/block.json (current 3-attribute shape, lines 9-22)
    - wordpress-plugin/includes/Block/block.json (mirror — must stay byte-identical to src/)
    - wordpress-plugin/includes/ConfigSource/WpOptionsConfigSource.php (lines 28-78: existing constants + get_runtime_config pattern)
    - .planning/phases/09-block-studio-visual-config-chat-lipsync/09-PATTERNS.md (section "wordpress-plugin/src/block.json" — exact 13 new keys with types/defaults; section "WpOptionsConfigSource.php" — DEFAULT constants to add)
    - .planning/phases/09-block-studio-visual-config-chat-lipsync/09-CONTEXT.md (Part A locked knob set — final source of truth for which keys exist)
  </read_first>
  <action>
    Add exactly these 14 attributes to BOTH `wordpress-plugin/src/block.json` and `wordpress-plugin/includes/Block/block.json` inside the existing `attributes` object (alongside voice/instructions/avatar). The two files must be byte-identical after the edit:

    containerWidth (number, default 0), containerHeight (number, default 0), fullWidth (boolean, default false), bgType (string, default ""), bgColor (string, default ""), bgTransparent (boolean, default false), bgImageId (number, default 0), lightIntensity (number, default 0), avatarScale (number, default 0), avatarOffsetX (number, default 0), avatarOffsetY (number, default 0), cameraPreset (string, default ""), chatShow (boolean, default false), chatPlacement (string, default "").

    Numeric defaults are 0 / false / "" so they mean "use admin default" — this mirrors how the existing `avatar: 0` works in Phase 8. Do NOT set "real" defaults like 1.0 here (the real default lives in the PHP layer's wp_parse_args + the bundle's ?? fallback).

    Then in `wordpress-plugin/includes/ConfigSource/WpOptionsConfigSource.php`:
    1. Add new private constants matching CONTEXT.md locked defaults:
       DEFAULT_LIGHT_INTENSITY = 1.0, DEFAULT_AVATAR_SCALE = 1.0, DEFAULT_AVATAR_OFFSET_X = 0.0, DEFAULT_AVATAR_OFFSET_Y = 0.0, DEFAULT_CAMERA_PRESET = 'front', DEFAULT_CHAT_PLACEMENT = 'beside'. (containerWidth/Height/fullWidth/bgType/Color/Transparent/ImageUrl/chatShow have no meaningful admin default — they fall through to bundle-side defaults or "off" — so they do NOT need constants; just emit them as raw defaults in the returned array.)
    2. Extend get_runtime_config()'s returned array (currently 4 keys at lines 72-77) with the 13 new snake_case keys. Each key: read from $settings[<snake_key>] if it exists in wp_options (admin may have set it later), else fall back to the DEFAULT_* constant or a literal. Mirror the existing `isset ? (cast) : DEFAULT` shape at lines 65-67. Note: settings-page UI for editing these is OUT OF SCOPE this phase (CONTEXT deferred); only the defaults must exist so wp_parse_args has a fallback.

    Do NOT add a `bgImageUrl` to wp_options defaults — that is resolved per-instance from `bgImageId` in AvatarBlock.php (next task), not stored globally.
  </action>
  <verify>
    <automated>grep -c '"containerWidth"\|"cameraPreset"\|"chatShow"' wordpress-plugin/src/block.json wordpress-plugin/includes/Block/block.json returns 3 for each file; diff wordpress-plugin/src/block.json wordpress-plugin/includes/Block/block.json shows no differences; grep -c "DEFAULT_LIGHT_INTENSITY\|DEFAULT_CAMERA_PRESET\|DEFAULT_AVATAR_SCALE" wordpress-plugin/includes/ConfigSource/WpOptionsConfigSource.php returns at least 3</automated>
  </verify>
  <acceptance_criteria>
    - wordpress-plugin/src/block.json contains the keys "containerWidth", "containerHeight", "fullWidth", "bgType", "bgColor", "bgTransparent", "bgImageId", "lightIntensity", "avatarScale", "avatarOffsetX", "avatarOffsetY", "cameraPreset", "chatShow", "chatPlacement" each with type number/boolean/string
    - wordpress-plugin/includes/Block/block.json is byte-identical to src/block.json (`diff` exits 0)
    - Existing voice/instructions/avatar attributes are unchanged
    - WpOptionsConfigSource::get_runtime_config() returns an array containing the snake_case keys: container_width, container_height, full_width, bg_type, bg_color, bg_transparent, bg_image_url (empty string default — no admin default for URL), light_intensity (=1.0 when unset), avatar_scale (=1.0), avatar_offset_x (=0.0), avatar_offset_y (=0.0), camera_preset (='front'), chat_show (=false), chat_placement (='beside')
    - No PHP syntax error: `php -l wordpress-plugin/includes/ConfigSource/WpOptionsConfigSource.php` exits 0
  </acceptance_criteria>
  <done>14 new attributes exist in both block.json copies; 13 snake_case defaults returned from WpOptionsConfigSource with the CONTEXT-locked default values (light=1.0, scale=1.0, offset=0.0, cameraPreset='front', chatPlacement='beside', chatShow=false); existing Phase-8 keys unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Extend AvatarBlock::render_callback + AvatarRenderer::public_safe() with the new keys (fix the array_filter 0-stripping bug)</name>
  <read_first>
    - wordpress-plugin/includes/Block/AvatarBlock.php (current 3-key render_callback at lines 68-82; the existing array_filter at line 79 uses `static fn( $v ) => '' !== $v` which is the bug to fix)
    - wordpress-plugin/includes/Render/AvatarRenderer.php (render() at lines 54-95; public_safe() allowlist at lines 138-146; per-field isset→cast→fallback re-application at lines 66-74)
    - .planning/phases/09-block-studio-visual-config-chat-lipsync/09-PATTERNS.md (section "AvatarBlock.php" — the exact safe array_filter callback with ARRAY_FILTER_USE_BOTH; section "AvatarRenderer.php" — the exact public_safe extension with camelCase keys)
    - .planning/phases/09-block-studio-visual-config-chat-lipsync/09-RESEARCH.md ("Pitfall warning" after the AvatarBlock.php code example — explains why `'' !== $v` strips 0/0.0 under PHP loose comparison)
  </read_first>
  <action>
    In `wordpress-plugin/includes/Block/AvatarBlock.php` `render_callback()`:

    1. Resolve bgImageId to a URL exactly as `avatar` is resolved at lines 69-71: `$bg_image_url = isset( $attributes['bgImageId'] ) && $attributes['bgImageId'] > 0 ? wp_get_attachment_url( (int) $attributes['bgImageId'] ) : '';` then coerce with `is_string( $bg_image_url ) ? $bg_image_url : '';`.

    2. Extend the `$renderer_atts` array with the 13 new snake_case keys (container_width, container_height, full_width, bg_type, bg_color, bg_transparent, bg_image_url, light_intensity, avatar_scale, avatar_offset_x, avatar_offset_y, camera_preset, chat_show, chat_placement) — each following the isset→cast→literal-default shape from PATTERNS.md AvatarBlock section. Use the literal defaults documented there: light_intensity=1.0, avatar_scale=1.0, avatar_offset_x=0.0, avatar_offset_y=0.0. Booleans via `! empty( $attributes[<camelKey>] )`.

    3. REPLACE the existing buggy `$renderer_atts = array_filter( $renderer_atts, static fn( $v ) => '' !== $v );` (line 79) with the type-aware callback that preserves 0/0.0/false:
       ```
       $renderer_atts = array_filter(
           $renderer_atts,
           static function ( $v, $k ) {
               if ( is_string( $v ) ) {
                   return '' !== $v;
               }
               return true; // keep numeric + bool as-is (preserves 0, 0.0, false)
           },
           ARRAY_FILTER_USE_BOTH
       );
       ```
       This is the single most important correctness fix in this plan — without it, scale=0 or offset=0 would be silently stripped and the per-instance override lost.

    In `wordpress-plugin/includes/Render/AvatarRenderer.php`:

    4. Extend the per-field isset→cast→fallback re-application block (currently lines 66-74, covering instructions/voice/avatar_url/model) to ALSO cover the 13 new snake_case keys. For each new key: `isset( $merged[<snake>] ) ? (cast) $merged[<snake>] : <literal_default>`, falling back to the same literal defaults as WpOptionsConfigSource (light=1.0, scale=1.0, offset=0.0, camera='front', chatPlacement='beside', chatShow=false, others='' or 0). Reassign into `$merged[<snake>]`. This guards against empty strings surviving wp_parse_args for keys present but blank (the existing comment at lines 61-65 explains why this redundant defense exists).

    5. Extend `public_safe()` (lines 138-146) to emit the 13 new keys in camelCase — the snake→camel translation boundary. Use the exact shape from PATTERNS.md AvatarRenderer section: containerWidth (int), containerHeight (int), fullWidth (bool), bgType (string), bgColor (string), bgTransparent (bool), bgImageUrl (string), lightIntensity (float, default 1.0), avatarScale (float, default 1.0), avatarOffsetX (float, default 0.0), avatarOffsetY (float, default 0.0), cameraPreset (string, default 'front'), chatShow (bool, default false), chatPlacement (string, default 'beside'). Keep the existing 5 keys (voice/instructions/avatarUrl/model/restUrl) unchanged. The existing `esc_attr( wp_json_encode( ... ) )` at line 93 escapes whatever this returns — new keys are automatically XSS-safe.

    Do NOT add the API key or any secret to public_safe() — the existing comment at lines 130-133 documents this constraint and it carries forward unchanged.
  </action>
  <verify>
    <automated>php -l wordpress-plugin/includes/Block/AvatarBlock.php exits 0; php -l wordpress-plugin/includes/Render/AvatarRenderer.php exits 0; grep -c "ARRAY_FILTER_USE_BOTH" wordpress-plugin/includes/Block/AvatarBlock.php returns 1; grep -c "containerWidth\|cameraPreset\|chatShow\|lightIntensity\|bgTransparent" wordpress-plugin/includes/Render/AvatarRenderer.php returns at least 5 (the camelCase keys in public_safe)</automated>
  </verify>
  <acceptance_criteria>
    - AvatarBlock.php render_callback resolves bgImageId → bg_image_url using wp_get_attachment_url (parallel to the avatar resolution at lines 69-71)
    - AvatarBlock.php uses ARRAY_FILTER_USE_BOTH with a type-aware callback that returns true for non-string values (preserving 0/0.0/false) — the old `static fn( $v ) => '' !== $v` is gone
    - AvatarRenderer.php public_safe() return array contains the camelCase keys: containerWidth, containerHeight, fullWidth, bgType, bgColor, bgTransparent, bgImageUrl, lightIntensity, avatarScale, avatarOffsetX, avatarOffsetY, cameraPreset, chatShow, chatPlacement
    - public_safe() never references get_api_key() or any secret-reading method (grep "api_key\|get_api_key" on AvatarRenderer.php returns no hits inside public_safe)
    - Both PHP files pass `php -l` lint
    - The existing 5 keys (voice/instructions/avatarUrl/model/restUrl) are still emitted unchanged
  </acceptance_criteria>
  <done>13 new snake_case keys flow through AvatarBlock::render_callback (with the 0-preserving filter) and AvatarRenderer::public_safe() emits 13 new camelCase keys into the escaped data-khaveeai-config JSON. Existing 5 keys unchanged. No secret reaches public_safe().</done>
</task>

<task type="auto">
  <name>Task 3: Create packages/wp-bundle/src/config.ts (extended KhaveeAvatarConfig + camera presets + light range + resolveSceneDefaults) and refactor build.mjs for multi-entry + safety grep</name>
  <read_first>
    - packages/wp-bundle/src/mount.tsx (current KhaveeAvatarConfig interface at lines 36-52; current AvatarScene hardcoded values at lines 54-64: ambient 1, directional 2.5, camera position [0,0,5], fov 50)
    - packages/wp-bundle/src/index.ts (current consumer of KhaveeAvatarConfig via `import type ... from './mount'` at line 6 — the re-export in mount.tsx must keep this import working)
    - packages/wp-bundle/build.mjs (current single-entry buildOptions at lines 10-22; watch branch at lines 24-28)
    - .planning/phases/09-block-studio-visual-config-chat-lipsync/09-PATTERNS.md (section "packages/wp-bundle/src/config.ts" — exact interface, CAMERA_PRESETS vectors lifted from khavee-app, LIGHT_INTENSITY constant, resolveSceneDefaults helper; section "build.mjs" — buildOptions factory + second entry + grep assertion)
    - .planning/phases/09-block-studio-visual-config-chat-lipsync/09-RESEARCH.md (Code Examples "Camera preset vectors" — the four verbatim preset vectors from khavee-app Preview.tsx:54-87)
  </read_first>
  <action>
    Create `packages/wp-bundle/src/config.ts` with the following exports (lift values VERBATIM from PATTERNS.md / RESEARCH.md — do not invent):

    1. `export interface KhaveeAvatarConfig` — MOVE the existing 5 fields (voice/instructions/avatarUrl/model/restUrl) from mount.tsx, then ADD 14 new optional fields with the exact types from PATTERNS: containerWidth?, containerHeight?, fullWidth?, bgType?: "color" | "image" | "", bgColor?, bgTransparent?, bgImageUrl?, lightIntensity?, avatarScale?, avatarOffsetX?, avatarOffsetY?, cameraPreset?: CameraPreset, chatShow?, chatPlacement?: "beside" | "below". ALSO ADD a 15th editor-only field: `previewTalking?: boolean;` — comment it as EDITOR-ONLY (NOT a persisted block attribute, NOT emitted by PHP public_safe, NOT present on the published-page data-khaveeai-config). It exists ONLY so the preview bundle (Plan 09-03) can read `config.previewTalking` from the editor-emitted data-khaveeai-preview-config JSON without a TypeScript error under tsconfig strict:true (Plan 09-02 emits the flag; Plan 09-03 consumes it). This closes BLOCKER-3 of checker iteration 1.

    2. `export const CAMERA_PRESETS` — the four preset vectors lifted VERBATIM from khavee-app Preview.tsx:54-87, converted to R3F tuple form:
       - front: { position: [0, 1.3, 3.1], target: [0, 0.15, 0] }
       - "left-angle": { position: [-2.05, 1.28, 2.5], target: [0, 0.15, 0] }
       - "right-angle": { position: [2.05, 1.28, 2.5], target: [0, 0.15, 0] }
       - wide: { position: [0, 1.55, 5.2], target: [0, 0.1, 0] }
       Use `as [number, number, number]` on each tuple and `as const` on the object. Export `type CameraPreset = keyof typeof CAMERA_PRESETS`.

    3. `export const LIGHT_INTENSITY = { min: 0, max: 2, step: 0.1, default: 1.0 } as const;` (lift from khavee-app BackgroundPanel.tsx).

    4. `export function resolveSceneDefaults(c: KhaveeAvatarConfig)` — returns an object with: lightIntensity (c.lightIntensity ?? 1.0), avatarScale (c.avatarScale ?? 1.0), avatarOffsetX/Y (c.avatarOffsetX/Y ?? 0.0), cameraPreset (c.cameraPreset ?? "front"), cameraPosition (CAMERA_PRESETS[c.cameraPreset ?? "front"].position), cameraFov (20 — matches khavee-app PreviewModel.tsx:61 and the lifted preset vectors), ambient (1), directional (2.5). The ambient/directional values mirror Phase-8's hardcoded AvatarScene at mount.tsx:59-60 so existing published pages render identically. 

       IMPORTANT — Phase-8 camera-default preservation (WARNING-2, checker iteration 1): `cameraFov` and `cameraPosition` MUST follow this two-mode rule:
       - When `c.cameraPreset` is UNSET, an empty string (`""`), or `undefined` (i.e. the author has NOT chosen a preset, which is the default state of every existing Phase-8 block): `resolveSceneDefaults` returns `cameraPosition: [0, 0, 5]` and `cameraFov: 50` — the EXACT Phase-8 hardcoded values at mount.tsx:58. This makes the must_haves.truth "Existing Phase-8 published pages render IDENTICALLY when no new config keys are set" literally true.
       - When `c.cameraPreset` is one of "front" | "left-angle" | "right-angle" | "wide" (the author explicitly picked a preset): `cameraPosition` is `CAMERA_PRESETS[c.cameraPreset].position` and `cameraFov` is 20 (khavee-app framing, consistent with the preset vectors per RESEARCH A2).
       Implement this as: `const hasPreset = c.cameraPreset && c.cameraPreset in CAMERA_PRESETS;` then `cameraPosition: hasPreset ? CAMERA_PRESETS[c.cameraPreset].position : [0, 0, 5] as [number, number, number],` and `cameraFov: hasPreset ? 20 : 50,`. (Note: `cameraPreset` default in WpOptionsConfigSource is the literal string `"front"` ONLY for the admin-default path — but per-instance block.json defaults to `""` so existing Phase-8 blocks ride the `[0,0,5]/50` path. If the admin has set a preset globally, that IS an explicit choice and the preset vectors apply, which is correct.)

    In `packages/wp-bundle/src/mount.tsx`:
    5. Replace the local `export interface KhaveeAvatarConfig` with `import type { KhaveeAvatarConfig } from "./config"; export type { KhaveeAvatarConfig } from "./config";` so `index.ts`'s existing `import type { KhaveeAvatarConfig } from "./mount"` (line 6) keeps working unchanged. Do NOT change AvatarScene or mountAvatarInstance in this task — Plan 09-05 does the live-side scene wiring.

    In `packages/wp-bundle/build.mjs`:
    6. Refactor the existing single buildOptions object into a `buildOptions(entry, outfile)` factory function. Add the second entry call: `await esbuild.build(buildOptions("src/preview.ts", "../../wordpress-plugin/build/khaveeai-preview.js"));`. The preview entry source file (src/preview.ts) does NOT need to exist yet for this task — but create a stub `packages/wp-bundle/src/preview.ts` containing only `console.log("preview entry stub — populated in plan 09-03");` so esbuild does not fail on a missing entry. (Plan 09-03 overwrites this stub with the real mountAllPreviews implementation.) Apply the same factory refactor to the `watch` branch: build a context for each entry. Keep format: "iife", no globalName, NO external array (D-10 carries forward).

    7. After both esbuild.build() calls in the non-watch branch, add the build-time safety assertion from PATTERNS.md: read `wordpress-plugin/build/khaveeai-preview.js` and fail the build (process.exit(1) with a clear "SAFETY VIOLATION" message) if the output matches any of these regexes: /RealtimeProvider/, /getUserMedia/, /ephemeral/. This is the belt-and-braces guard for STUDIO-02 — the structural entry-point isolation is the primary safety mechanism, but this grep catches accidental imports.

    Do NOT delete or modify the existing khaveeai-bundle.js output path or its buildOptions.
  </action>
  <verify>
    <automated>cd packages/wp-bundle && pnpm exec tsc --noEmit exits 0 (typecheck passes); node build.mjs exits 0 (both bundles build + grep assertion passes against the stub preview entry — stub contains no forbidden tokens); test -f wordpress-plugin/build/khaveeai-preview.js succeeds; grep -c "openai-realtime\|RealtimeProvider\|getUserMedia" wordpress-plugin/build/khaveeai-preview.js returns 0</automated>
  </verify>
  <acceptance_criteria>
    - packages/wp-bundle/src/config.ts exists and exports KhaveeAvatarConfig (with the 5 existing + 14 new optional fields PLUS the editor-only `previewTalking?: boolean` field), CAMERA_PRESETS (4 keys: front/left-angle/right-angle/wide), CameraPreset type, LIGHT_INTENSITY constant (min:0, max:2, step:0.1, default:1.0), and resolveSceneDefaults function
    - The KhaveeAvatarConfig.previewTalking field carries a /** editor-only — emitted in data-khaveeai-preview-config, never in data-khaveeai-config or public_safe */ doc comment so the boundary is explicit at the type level
    - CAMERA_PRESETS.front.position equals [0, 1.3, 3.1] (verbatim from khavee-app)
    - resolveSceneDefaults returns cameraPosition [0,0,5] and cameraFov 50 when config.cameraPreset is unset/empty (Phase-8 identical); returns CAMERA_PRESETS[preset].position and cameraFov 20 only when an explicit preset is set (WARNING-2 option b)
    - packages/wp-bundle/src/mount.tsx no longer declares KhaveeAvatarConfig locally; it imports from ./config and re-exports (so index.ts line 6 import path is unchanged)
    - packages/wp-bundle/src/index.ts is UNCHANGED (still imports from './mount')
    - packages/wp-bundle/build.mjs uses a buildOptions(entry, outfile) factory and calls esbuild.build() twice (once for src/index.ts → khaveeai-bundle.js, once for src/preview.ts → khaveeai-preview.js)
    - packages/wp-bundle/build.mjs contains a post-build grep assertion that calls process.exit(1) if khaveeai-preview.js matches /RealtimeProvider|getUserMedia|ephemeral/
    - `node packages/wp-bundle/build.mjs` exits 0 and produces both wordpress-plugin/build/khaveeai-bundle.js AND wordpress-plugin/build/khaveeai-preview.js
    - wordpress-plugin/build/khaveeai-bundle.js still contains RealtimeProvider (the live bundle legitimately needs it — the assertion is ONLY on the preview output)
    - `pnpm exec tsc --noEmit` (run from packages/wp-bundle) exits 0
  </acceptance_criteria>
  <done>config.ts module owns the extended KhaveeAvatarConfig + camera-preset map + light range + scene-default resolver. mount.tsx re-exports the type from config.ts (index.ts unchanged). build.mjs builds two IIFE entries and gates the preview bundle on a grep assertion that proves it contains no realtime/mic/token code. Both bundles build clean; typecheck passes.</done>
</task>

</tasks>

<threat_model>

> security_enforcement: ON | ASVS tier: L1 | disposition: block-on high

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author (WP admin) → block attributes | Author-supplied visual config values cross from the Gutenberg inspector into persisted block markup, then into PHP render_callback |
| PHP backend → browser (data-khaveeai-config) | Merged config (including author-set bgColor/bgImageUrl/etc.) crosses from PHP into the published-page DOM via the JSON attribute |
| Author → editor iframe (data-khaveeai-preview-config) | Author-supplied config crosses from editor.js into the preview-bundle mount point (wired in Plan 09-06, but the keys established here are what cross) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Tampering / XSS | AvatarRenderer::public_safe() — new config keys (bgColor, bgImageUrl, cameraPreset, etc.) | mitigate | All new keys pass through the existing `esc_attr( wp_json_encode( ... ) )` at AvatarRenderer.php:93 — JSON-encoding plus attribute escaping defeats any `<script>`/`"`/`'` injection attempt. Each key is also cast to its primitive type (int/float/bool/string) in both AvatarBlock::render_callback and public_safe() before emission, so even a malformed stored attribute cannot arrive as an arbitrary string at the DOM. |
| T-09-01-02 | Tampering | AvatarBlock::render_callback — bgImageId / avatar resolution | mitigate | bgImageId is cast `(int)` and resolved to a URL ONLY via `wp_get_attachment_url()` (the same path avatar already uses at lines 69-71). No user-supplied URL string is ever accepted for the background image — only Media Library attachment IDs. Eliminates SSRF/path-traversal via a crafted URL. |
| T-09-01-03 | Tampering | array_filter in render_callback — accidental data loss | mitigate | The buggy `'' !== $v` filter (which strips `0`/`0.0` under PHP loose comparison) is replaced with the explicit ARRAY_FILTER_USE_BOTH callback that returns true for all non-string values. Without this fix, scale=0 or offset=0 would silently vanish and the published scene would render with the wrong defaults — a correctness/security-adjacent bug. |
| T-09-01-04 | Information Disclosure | public_safe() allowlist | mitigate | public_safe() is the single boundary that decides what reaches the browser. The threat model requires NO secret (API key) ever appears in its return. The existing comment at AvatarRenderer.php:130-133 carries forward; the new keys are all non-secret visual config. No code path added in this plan reads get_api_key(). |
| T-09-01-05 | Tampering | build.mjs safety grep (STUDIO-02 foundation) | mitigate | The build-time grep assertion (process.exit(1) on /RealtimeProvider|getUserMedia|ephemeral/ in the preview output) is established here so that Plan 09-03's preview-bundle population cannot accidentally regress the safety property. Structural entry-point isolation is the primary defense; this grep is belt-and-braces. |
| T-09-01-SC | Tampering | npm installs | accept | This plan installs ZERO new packages (RESEARCH.md Package Legitimacy Audit confirms — all dependencies already in repo). No supply-chain risk introduced. |
</threat_model>

<verification>
- `php -l` passes on all three modified PHP files (AvatarBlock.php, AvatarRenderer.php, WpOptionsConfigSource.php)
- `diff wordpress-plugin/src/block.json wordpress-plugin/includes/Block/block.json` exits 0 (mirror integrity)
- `node packages/wp-bundle/build.mjs` exits 0 and emits both khaveeai-bundle.js (with RealtimeProvider, legitimately) and khaveeai-preview.js (without RealtimeProvider/getUserMedia/ephemeral — verified by the embedded grep assertion)
- `pnpm exec tsc --noEmit` from packages/wp-bundle exits 0
- Existing Phase-8 published page renders identically (new keys all default to blank/zero → fall through to Phase-8 hardcoded scene values; no migration needed)
</verification>

<success_criteria>
- All 14 new block attributes exist in both block.json copies with blank/zero defaults
- AvatarBlock::render_callback resolves bgImageId and emits 13 new snake_case keys with the type-aware array_filter (preserving 0/0.0/false)
- AvatarRenderer::public_safe() emits 13 new camelCase keys into the escaped JSON; no secret reaches it
- WpOptionsConfigSource::get_runtime_config() returns 13 new snake_case defaults with CONTEXT-locked values (light=1.0, scale=1.0, offset=0.0, camera='front', chatPlacement='beside')
- packages/wp-bundle/src/config.ts owns the extended KhaveeAvatarConfig + CAMERA_PRESETS + LIGHT_INTENSITY + resolveSceneDefaults; mount.tsx re-exports the type so index.ts is unchanged
- build.mjs builds two IIFE entries and gates the preview bundle on a grep safety assertion
- All typecheck and lint gates pass; both bundles build clean
</success_criteria>

<output>
Create `.planning/phases/09-block-studio-visual-config-chat-lipsync/09-01-SUMMARY.md` when done
</output>
