---
phase: 09-block-studio-visual-config-chat-lipsync
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - packages/wp-bundle/src/preview.ts
  - packages/wp-bundle/src/preview/mountPreview.tsx
  - packages/wp-bundle/src/preview/PreviewScene.tsx
autonomous: true
requirements:
  - STUDIO-02
  - STUDIO-03
  - STUDIO-04

must_haves:
  truths:
    - "The preview bundle entry (src/preview.ts) is a physically separate esbuild entry whose import graph NEVER reaches @khaveeai/providers-openai-realtime — verified by the build-time grep assertion added in Plan 09-01"
    - "The preview renders a real live 3D VRM avatar (via @khaveeai/react's VRMAvatar inside a <KhaveeProvider> with no realtime config), with all Part A visual config applied (camera preset, lighting intensity, avatar scale/offset, background color/image/transparent)"
    - "The preview NEVER calls getUserMedia, NEVER constructs OpenAIRealtimeProvider, NEVER calls the REST token endpoint — safety is structural (entry-point isolation), not a runtime flag"
    - "useVRMExpressions() works in the preview path because <KhaveeProvider> accepts config={realtime: null} (verified at KhaveeProvider.tsx:93-95); useRealtime() is NEVER called from the preview path (it throws at useRealtime.ts:36-40 if realtimeProvider is null)"
    - "A 'Preview talking' no-audio demo loops a setInterval viseme cycler over the five mouth shapes (aa/ih/ou/ee/oh) at ~4Hz so the author sees mouth motion without a live realtime session"
    - "When config.chatShow is true, the editor preview renders the ChatBox chrome ALONGSIDE the avatar (per chatPlacement: --beside or --below inside the preview container) so the author can see how the chat will coexist with the avatar on the published page (STUDIO-03 editor-preview half, BLOCKER-1 checker iteration 1). The preview ChatBox is PRESENTATIONAL ONLY: it imports ChatBoxChrome from ../ui/ChatBox (created by sibling Plan 09-04), passes thinkingEnabled:true, and NEVER calls useRealtime, NEVER constructs a provider, NEVER accesses the mic, NEVER mints a token — so STUDIO-02 safety is preserved."
    - "Config changes from the inspector (via data-khaveeai-preview-config JSON attribute) are applied to the live preview WITHOUT unmounting/remounting the React root — a MutationObserver on the attribute pushes fresh config into React state (Pitfall 3: WebGL context leak avoidance)"
  artifacts:
    - path: "packages/wp-bundle/src/preview.ts"
      provides: "Preview-mode IIFE entry — scans [data-khaveeai-preview-config] mount points, mounts <PreviewScene> via mountEditorPreview, NEVER imports @khaveeai/providers-openai-realtime"
      contains: "data-khaveeai-preview-config"
    - path: "packages/wp-bundle/src/preview/mountPreview.tsx"
      provides: "mountEditorPreview(root, config) + MutationObserver-based config re-sync into React state (no unmount on attribute change)"
    - path: "packages/wp-bundle/src/preview/PreviewScene.tsx"
      provides: "<Canvas> + ambientLight + directionalLight + VRMAvatar (config-driven: camera preset, light intensity, scale, offset, bg color/transparent) + usePreviewTalking viseme cycler + conditional <ChatBoxChrome thinkingEnabled> render when config.chatShow (BLOCKER-1 editor-preview ChatBox)"
  key_links:
    - from: "packages/wp-bundle/src/preview.ts"
      to: "@khaveeai/react (KhaveeProvider, VRMAvatar, GLBAvatar, useVRMExpressions)"
      via: "import { KhaveeProvider, VRMAvatar, GLBAvatar, useVRMExpressions } from '@khaveeai/react'"
      pattern: "from ['\"]@khaveeai/react['\"]"
    - from: "packages/wp-bundle/src/preview.ts (forbidden import)"
      to: "@khaveeai/providers-openai-realtime"
      via: "MUST NOT EXIST — build.mjs grep assertion enforces"
      pattern: "must NOT match providers-openai-realtime"
    - from: "packages/wp-bundle/src/preview/PreviewScene.tsx"
      to: "packages/wp-bundle/src/config.ts (CAMERA_PRESETS, resolveSceneDefaults)"
      via: "import { CAMERA_PRESETS, resolveSceneDefaults } from '../config'"
      pattern: "from ['\"]\\.\\./config['\"]"
    - from: "packages/wp-bundle/src/preview/PreviewScene.tsx"
      to: "packages/wp-bundle/src/ui/ChatBox.tsx (ChatBoxChrome presentational export — created by sibling Plan 09-04, same Wave 2)"
      via: "import { ChatBoxChrome } from '../ui/ChatBox'"
      pattern: "from ['"]\.\./ui/ChatBox['"]"
---

<objective>
Deliver the STUDIO-02 safe preview bundle entry + STUDIO-04's editor-half no-audio talking demo. Populate `src/preview.ts` (stubbed in Plan 09-01) with the real IIFE entry that scans `[data-khaveeai-preview-config]` mount points and mounts a `<PreviewScene>` — a live 3D VRM avatar with all Part A config applied, but with NO realtime provider, NO mic, NO token mint. Add the no-audio "Preview talking" viseme cycler (setInterval over aa/ih/ou/ee/oh) so authors can see mouth motion without a live session.

Purpose: STUDIO-02 is the load-bearing safety property of this phase. The mechanism is structural entry-point isolation (the preview bundle's import graph physically cannot reach @khaveeai/providers-openai-realtime because esbuild tree-shakes per entry). This plan delivers the second half of that mechanism (the entry source itself); Plan 09-01 delivered the first half (build.mjs emits the entry + safety grep). STUDIO-04's published-page lip-sync is automatic reuse (handled in Plan 09-05) — this plan delivers only the editor-side no-audio demo half.

Output: Three new files in packages/wp-bundle/src/preview/. The preview bundle builds clean and contains zero references to RealtimeProvider/getUserMedia/ephemeral (verified by the embedded grep assertion from Plan 09-01). Plan 09-06 wires it into editor.js's mount-point div. Per BLOCKER-1 (checker iteration 1), PreviewScene ALSO renders a presentational disconnected-state ChatBox alongside the avatar when config.chatShow is true — closing the STUDIO-03 "ChatBox visible in editor preview" gap. The ChatBox preview is presentational-only (imports ChatBoxChrome with thinkingEnabled:true from sibling Plan 09-04; no useRealtime, no provider, no mic, no token) so STUDIO-02 safety is preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-block-studio-visual-config-chat-lipsync/09-CONTEXT.md
@.planning/phases/09-block-studio-visual-config-chat-lipsync/09-RESEARCH.md
@.planning/phases/09-block-studio-visual-config-chat-lipsync/09-PATTERNS.md
@.planning/phases/09-block-studio-visual-config-chat-lipsync/09-UI-SPEC.md
@.planning/phases/09-block-studio-visual-config-chat-lipsync/09-01-SUMMARY.md
@packages/wp-bundle/src/index.ts
@packages/wp-bundle/src/preview.ts
@packages/wp-bundle/src/config.ts
@packages/wp-bundle/build.mjs

<interfaces>
<!-- Contracts the executor needs. All verified during research. -->

From packages/wp-bundle/src/config.ts (created in Plan 09-01):
```typescript
export interface KhaveeAvatarConfig {
  voice?: ...; instructions?: string; avatarUrl?: string; model?: string; restUrl?: string;
  containerWidth?: number; containerHeight?: number; fullWidth?: boolean;
  bgType?: "color" | "image" | ""; bgColor?: string; bgTransparent?: boolean; bgImageUrl?: string;
  lightIntensity?: number; avatarScale?: number; avatarOffsetX?: number; avatarOffsetY?: number;
  cameraPreset?: CameraPreset; chatShow?: boolean; chatPlacement?: "beside" | "below";
}
export const CAMERA_PRESETS: { front: {position:[0,1.3,3.1], target:[0,0.15,0]}, "left-angle":..., "right-angle":..., wide:... };
export type CameraPreset = keyof typeof CAMERA_PRESETS;
export const LIGHT_INTENSITY = { min: 0, max: 2, step: 0.1, default: 1.0 } as const;
export function resolveSceneDefaults(c: KhaveeAvatarConfig): { lightIntensity, avatarScale, avatarOffsetX, avatarOffsetY, cameraPreset, cameraPosition, cameraFov, ambient: 1, directional: 2.5 };
// Two-mode (WARNING-2 option b): cameraPosition=[0,0,5] + cameraFov=50 when c.cameraPreset is unset/empty (Phase-8 identical);
// cameraPosition=CAMERA_PRESETS[preset].position + cameraFov=20 only when an explicit preset is set.
```

From packages/react/src/KhaveeProvider.tsx (verified — accepts null realtime):
```typescript
// Line 93-95: const [realtimeProvider, setRealtimeProvider] = useState<RealtimeProvider | null>(
//   config?.realtime || null
// );
// → KhaveeProvider accepts config={realtime: null} OR no config prop at all (both yield null provider).
// → useVRMExpressions() then works (it reads VRM state from useKhavee() context, no provider needed).
// → useRealtime() THROWS if realtimeProvider is null (line 36-40) — preview MUST NOT call useRealtime.
export function KhaveeProvider({ config, children }: KhaveeProviderProps)
```

From packages/react/src/VRMAvatar.tsx (verified — useVRMExpressions export):
```typescript
// Line 577: export function useVRMExpressions()
// Returns { setMultipleExpressions: (state: { aa:number, ih:number, ou:number, ee:number, oh:number }) => void, ... }
// Viseme keys confirmed at useRealtime.ts:61, 73.
```

Camera preset vectors (lift VERBATIM from khavee-app Preview.tsx:54-87 — already in config.ts):
  front [0,1.3,3.1] target [0,0.15,0]; left-angle [-2.05,1.28,2.5] target [0,0.15,0];
  right-angle [2.05,1.28,2.5] target [0,0.15,0]; wide [0,1.55,5.2] target [0,0.1,0]

Viseme cycler values (RESEARCH Code Examples, Claude's discretion per CONTEXT):
  VISEME_SEQUENCE = ["aa", "ih", "ou", "ee", "oh"] (verified viseme keys)
  VISEME_VALUES = { aa: 0.6, ih: 0.4, ou: 0.5, ee: 0.45, oh: 0.55 } (demo intensities)
  Cycle interval: 250ms (~4Hz per UI-SPEC §Interaction-States)
ChatBoxChrome presentational contract (BLOCKER-1 — created by sibling Plan 09-04 in the SAME Wave 2;
the 09-03 executor imports it; final integration verified by Plan 09-06 UAT):
```typescript
// From packages/wp-bundle/src/ui/ChatBox.tsx (Plan 09-04):
interface ChatBoxChromeProps {
  placement: "beside" | "below";
  messages: { role: "user" | "assistant"; text: string }[];
  input: string;
  onInputChange: (v: string) => void;
  onSend: () => void;
  onKeyDown: (e: React.KeyboardEvent<HTMLTextAreaElement>) => void;
  scrollRef: React.RefObject<HTMLDivElement | null>;
  endRef: React.RefObject<HTMLDivElement | null>;
  connected: boolean;
  chatStatus?: string;
  thinkingEnabled?: boolean;  // editor-preview override: true => render only header + "Chat preview" pill (no transcript/input)
}
export function ChatBoxChrome(props: ChatBoxChromeProps): JSX.Element;
// ChatBoxChrome does NOT call useRealtime — it is safe to import from the preview bundle.
// When thinkingEnabled === true, it renders ONLY: outer card + "AI Assistant" header + a centered
// "Chat preview — visible on the published page." pill (className `khaveeai-chat__preview-pill`).
// No transcript, no input row, no send button in that mode.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write preview.ts (IIFE entry) + mountPreview.tsx (mountEditorPreview + MutationObserver config re-sync)</name>
  <read_first>
    - packages/wp-bundle/src/index.ts (the existing view entry's mountAll pattern at lines 19-45 — MIRROR this structure: idempotency guard via dataset.khaveeaiMounted, per-element try/catch around JSON.parse, createRoot + mount call)
    - packages/wp-bundle/src/preview.ts (current stub from Plan 09-01 — to be fully replaced)
    - .planning/phases/09-block-studio-visual-config-chat-lipsync/09-PATTERNS.md (section "packages/wp-bundle/src/preview.ts" — the exact mountAllPreviews shape with data-khaveeai-preview-config attribute, idempotency guard, JSON.parse try/catch; section "IIFE-mount entry pattern" for the shared structure)
    - .planning/phases/09-block-studio-visual-config-chat-lipsync/09-RESEARCH.md (Open Question 3: MutationObserver-based config-sync — the preview bundle MUST use MutationObserver to re-read data-khaveeai-preview-config and update React state, NOT unmount/remount the root which would leak WebGL contexts per Pitfall 3)
  </read_first>
  <action>
    Replace `packages/wp-bundle/src/preview.ts` (currently a stub) with the real IIFE entry. Mirror `index.ts:19-45` exactly but swap three things: (1) selector is `[data-khaveeai-preview-config]` not `[data-khaveeai-config]`, (2) the JSON attribute is `el.dataset.khaveeaiPreviewConfig`, (3) the mount function is `mountEditorPreview(root, config, el)` (imported from `./preview/mountPreview`) — note the third `el` argument so the MutationObserver can be attached to the DOM element. Keep: `import "../styles.css";`, the `document.readyState` gate, the idempotency guard (`el.dataset.khaveeaiMounted === "true"`), the per-element try/catch around JSON.parse, the createRoot call. Add a header comment documenting the STUDIO-02 safety property: this file MUST NOT import from "@khaveeai/providers-openai-realtime" — the build.mjs grep assertion (Plan 09-01) enforces it at build time; the structural entry-point isolation enforces it at the bundler-graph level.

    Create `packages/wp-bundle/src/preview/mountPreview.tsx` exporting `mountEditorPreview(root: Root, initialConfig: KhaveeAvatarConfig, hostEl: HTMLElement): { unmount: () => void }`. The function:
    1. Uses React's `root.render(<PreviewHost initialConfig={initialConfig} hostEl={hostEl} />)` where PreviewHost is a local component.
    2. PreviewHost holds the live config in `useState(initialConfig)` and sets up a `MutationObserver` on `hostEl` watching the `data-khaveeai-preview-config` attribute. When the attribute changes (Gutenberg re-renders edit() and rewrites the JSON on every setAttributes), the observer callback JSON.parses the new value and calls `setConfig(newConfig)` — this pushes fresh config into React state, which re-renders `<PreviewScene>` with the new values WITHOUT unmounting the React root or recreating the WebGL context (Pitfall 3 avoidance). Disconnect the observer in the cleanup returned by the useEffect.
    3. PreviewHost renders `<PreviewScene config={config} />` (PreviewScene is Task 2). The MutationObserver's setConfig call is the ONLY reactivity mechanism — editor.js is responsible for rewriting the JSON attribute on every re-render (Plan 09-02 does this).
    4. Return `{ unmount: () => { root.unmount(); } }` so the caller (or a future Plan 09-06 unmount-on-block-removal observer) can tear down cleanly.

    KhaveeProvider wrapping belongs in PreviewScene (Task 2), not PreviewHost — keep mountPreview.tsx focused on the mount lifecycle + observer. The hostEl argument is passed through so the observer has a stable node to watch (the mount-point div that editor.js owns).

    DO NOT import OpenAIRealtimeProvider anywhere in preview.ts or mountPreview.tsx.
  </action>
  <verify>
    <automated>cd packages/wp-bundle && pnpm exec tsc --noEmit exits 0; grep -c "providers-openai-realtime\|OpenAIRealtimeProvider" packages/wp-bundle/src/preview.ts packages/wp-bundle/src/preview/mountPreview.tsx returns 0 for both; grep -c "MutationObserver\|data-khaveeai-preview-config\|mountEditorPreview" packages/wp-bundle/src/preview/mountPreview.tsx returns at least 3; node build.mjs exits 0 AND the grep safety assertion passes (khaveeai-preview.js contains no RealtimeProvider/getUserMedia/ephemeral)</automated>
  </verify>
  <acceptance_criteria>
    - packages/wp-bundle/src/preview.ts contains a mountAllPreviews() function that scans `[data-khaveeai-preview-config]` elements (not data-khaveeai-config)
    - preview.ts has the idempotency guard (dataset.khaveeaiMounted === "true") and the per-element try/catch around JSON.parse, mirroring index.ts:24-34
    - preview.ts header comment documents that this file MUST NOT import @khaveeai/providers-openai-realtime and references the build.mjs grep assertion
    - Neither preview.ts nor mountPreview.tsx contains the strings "providers-openai-realtime" or "OpenAIRealtimeProvider" (grep returns 0)
    - mountPreview.tsx exports mountEditorPreview(root, initialConfig, hostEl) and uses MutationObserver on hostEl watching the data-khaveeai-preview-config attribute
    - mountPreview.tsx's MutationObserver callback JSON.parses the new attribute value and calls setConfig (does NOT unmount/remount the React root — Pitfall 3)
    - `node packages/wp-bundle/build.mjs` exits 0 (both bundles build, grep assertion passes against preview output)
    - `pnpm exec tsc --noEmit` from packages/wp-bundle exits 0
    - wordpress-plugin/build/khaveeai-preview.js does NOT contain "RealtimeProvider", "getUserMedia", or "ephemeral" (the safety property, verified by the embedded assertion)
  </acceptance_criteria>
  <done>preview.ts is a real IIFE entry mirroring index.ts's mountAll shape (attribute swapped). mountPreview.tsx mounts PreviewScene via React root and uses MutationObserver to push fresh config into state (no unmount on attribute change). No file in the preview entry's import graph references OpenAIRealtimeProvider. Build + grep assertion + typecheck all pass.</done>
</task>

<task type="auto">
  <name>Task 2: Write PreviewScene.tsx (Canvas + lights + VRMAvatar config-driven + KhaveeProvider wrap + usePreviewTalking viseme cycler)</name>
  <read_first>
    - packages/wp-bundle/src/mount.tsx (existing AvatarScene at lines 54-64 — the pattern to extend: <Canvas camera={{position,fov}}><ambientLight/><directionalLight/><VRMAvatar/></Canvas>)
    - packages/react/src/KhaveeProvider.tsx (lines 85-105: KhaveeProvider accepts config={realtime: null} or no config prop — verified; the provider just yields null realtimeProvider which is fine for VRM-only rendering)
    - packages/react/src/VRMAvatar.tsx (line 577: useVRMExpressions export — returns setMultipleExpressions({aa,ih,ou,ee,oh}))
    - .planning/phases/09-block-studio-visual-config-chat-lipsync/09-PATTERNS.md (section "PreviewScene.tsx" — the khavee-app PreviewModel.tsx structure to lift: <Canvas shadows camera={{fov}}><ambientLight intensity={...}/><directionalLight position={[10,10,5]} castShadow/><VRMAvatar src=... position={[offsetX,offsetY,0]}/></Canvas>; Phase-9 deviations: DROP <Environment>, DROP <CameraControls> (preset-only), DROP custom .fbx motion; section also has the exact usePreviewTalking viseme cycler code)
    - .planning/phases/09-block-studio-visual-config-chat-lipsync/09-RESEARCH.md (Code Examples: "Preview-talking no-audio viseme loop" — the setInterval pattern with VISEME_SEQUENCE/VISEME_VALUES; Pitfall 6: transparent-background canvas needs gl={{alpha:true}} + style={{background:'transparent'}} + no scene.background)
  </read_first>
  <action>
    Create `packages/wp-bundle/src/preview/PreviewScene.tsx` exporting `PreviewScene({ config }: { config: KhaveeAvatarConfig })`.

    1. Top-level structure: return `<KhaveeProvider>` (with NO config prop, or config={{}} — KhaveeProvider.tsx:93-95 yields null realtimeProvider which is fine) wrapping a container div + `<Canvas>`. The KhaveeProvider wrap is REQUIRED so `useVRMExpressions()` (used by the viseme cycler) has context to read from. Do NOT call `useRealtime()` anywhere in PreviewScene or its children — it throws when realtimeProvider is null (useRealtime.ts:36-40).

    2. Container div styling (Pitfall 6 — transparent-background): when `config.bgTransparent === true`, the container div has style `{ background: 'transparent' }` and the `<Canvas>` gets `gl={{ alpha: true }}` plus `style={{ background: 'transparent' }}` (so the WebGL clear is transparent and the page shows through). Otherwise apply `config.bgColor` (when bgType==='color') as the container div's CSS background, OR `config.bgImageUrl` (when bgType==='image') as the container div's CSS `backgroundImage: url(...)`. Do NOT set scene.background in three.js — CSS-on-container is cheaper and works for both color and image (Pitfall 6 avoidance). Container width/height: when config.containerWidth/Height > 0, apply as inline style width/height in px; when config.fullWidth is true, width is '100%'.

    3. `<Canvas>` props: `camera={{ position: resolveSceneDefaults(config).cameraPosition, fov: 20 }}` (fov=20 matches khavee-app PreviewModel.tsx:61 and the lifted preset vectors — PATTERNS notes this is the consistent choice). DO NOT add `<CameraControls>` (preset-only, locked by CONTEXT). DO NOT add `<Environment preset="sunset">` (asset dependency, deliberately dropped — RESEARCH).

    4. Inside <Canvas>: `<ambientLight intensity={resolveSceneDefaults(config).lightIntensity} />` (config-driven Part A Lighting) + `<directionalLight position={[10, 10, 5]} intensity={2.5} />` (matches Phase-8 mount.tsx:59-60). When config.bgTransparent is true, DO NOT add any scene-level background — leave the canvas clear color transparent.

    5. VRM/GLB avatar: only render when `config.avatarUrl` is set. Detect GLB via `config.avatarUrl.toLowerCase().endsWith('.glb')` (mirror mount.tsx:55). Render `<VRMAvatar src={config.avatarUrl} position={[resolveSceneDefaults(config).avatarOffsetX, resolveSceneDefaults(config).avatarOffsetY, 0]} scale={resolveSceneDefaults(config).avatarScale} />` (or `<GLBAvatar>` for .glb). When avatarUrl is absent, render the UI-SPEC empty-state copy: a `<div>` overlaying the canvas with title "No avatar selected" and body "Choose an avatar in the Avatar panel, or set a global default in Settings." (UI-SPEC §Copywriting verbatim).

    6. Add the `usePreviewTalking(enabled: boolean)` hook in this file (lift from RESEARCH Code Examples / PATTERNS). It uses `useVRMExpressions()` from `@khaveeai/react` to get `setMultipleExpressions`, and when enabled, runs a `setInterval` cycling through VISEME_SEQUENCE ["aa","ih","ou","ee","oh"] every 250ms, calling `setMultipleExpressions({...zeros, [viseme]: VISEME_VALUES[viseme]})`. When disabled (or on unmount), set all visemes to 0 and clear the interval. VISEME_VALUES = { aa: 0.6, ih: 0.4, ou: 0.5, ee: 0.45, oh: 0.55 } (Claude's discretion per CONTEXT). The hook is called inside PreviewScene (within KhaveeProvider context) with `enabled = config.previewTalking ?? false`. (config.previewTalking is the editor-only flag Plan 09-02 emits in the preview-config JSON.)

    7. When previewTalking is enabled, render a small "Preview talking" pill label overlaying the preview corner (UI-SPEC §Interaction-States "Preview talking ON" row: "A small 'Preview talking' pill label appears overlaid in the preview corner so the author knows the motion is a demo, not a live session"). Use a simple absolutely-positioned div with the pill styling — no icon library.

    Wrap `usePreviewTalking(config.previewTalking ?? false)` AFTER the early-return guards but inside the PreviewScene component body (so the hook order is stable — React rules of hooks). Place the KhaveeProvider as the outermost wrapper so useVRMExpressions (called by usePreviewTalking inside PreviewScene) has context. If structurally cleaner, split into `<PreviewHost>` (KhaveeProvider wrap, from Task 1) and `<PreviewSceneInner>` (the actual scene + cycler) — but a single component is also fine as long as useVRMExpressions is called inside the provider's subtree.
  </action>
  <verify>
    <automated>cd packages/wp-bundle && pnpm exec tsc --noEmit exits 0; grep -c "KhaveeProvider\|VRMAvatar\|GLBAvatar\|useVRMExpressions\|setMultipleExpressions\|setInterval\|aa.*ih.*ou.*ee.*oh" packages/wp-bundle/src/preview/PreviewScene.tsx returns at least 5; grep -c "useRealtime\|OpenAIRealtimeProvider\|providers-openai-realtime" packages/wp-bundle/src/preview/PreviewScene.tsx returns 0; node build.mjs exits 0 AND grep assertion passes (khaveeai-preview.js contains no RealtimeProvider/getUserMedia/ephemeral)</automated>
  </verify>
  <acceptance_criteria>
    - packages/wp-bundle/src/preview/PreviewScene.tsx exports PreviewScene({config}) and wraps its content in <KhaveeProvider> (with no realtime config — config={{}} or no config prop)
    - The file imports KhaveeProvider, VRMAvatar, GLBAvatar, useVRMExpressions from '@khaveeai/react'
    - The file imports CAMERA_PRESETS / resolveSceneDefaults from '../config' and uses resolveSceneDefaults(config).cameraPosition + .lightIntensity + .avatarScale + .avatarOffsetX/Y
    - The <Canvas> uses camera={{ position: <preset>, fov: 20 }} and contains <ambientLight intensity={config.lightIntensity ?? 1.0}/> + <directionalLight position={[10,10,5]} intensity={2.5}/>
    - When config.bgTransparent is true, <Canvas> has gl={{ alpha: true }} and the container div has style background:'transparent'; when false, bgColor or bgImageUrl is applied to the container div CSS (NOT scene.background)
    - The usePreviewTalking hook cycles VISEME_SEQUENCE ["aa","ih","ou","ee","oh"] at 250ms via setInterval, calling setMultipleExpressions
    - The file NEVER calls useRealtime() and NEVER imports OpenAIRealtimeProvider (grep returns 0)
    - A "Preview talking" pill label is rendered when config.previewTalking is true
    - The empty-state copy ("No avatar selected" / "Choose an avatar in the Avatar panel, or set a global default in Settings.") renders when config.avatarUrl is absent
    - `pnpm exec tsc --noEmit` exits 0
    - `node build.mjs` exits 0 and the grep assertion confirms khaveeai-preview.js contains no RealtimeProvider/getUserMedia/ephemeral (the load-bearing STUDIO-02 safety property)
  </acceptance_criteria>
  <done>PreviewScene renders a config-driven 3D VRM preview (camera preset, lighting, scale/offset, bg color/image/transparent) inside a KhaveeProvider-with-no-realtime, with a no-audio viseme cycler for the Preview-talking demo. No realtime provider, no mic, no token — verified by source grep AND build-time bundle grep. Typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 3: Render a presentational disconnected-state ChatBox in PreviewScene when config.chatShow is true (BLOCKER-1, STUDIO-03 editor-preview half)</name>
  <read_first>
    - packages/wp-bundle/src/preview/PreviewScene.tsx (the file produced by Task 2 — this task EXTENDS the PreviewScene component)
    - .planning/phases/09-block-studio-visual-config-chat-lipsync/09-PATTERNS.md (section "ChatBox.tsx" — khavee-app lift table; the dependency-free chrome shape)
    - .planning/phases/09-block-studio-visual-config-chat-lipsync/09-UI-SPEC.md (§Copywriting ChatBox rows — "AI Assistant" header verbatim; §Mutual-exclusivity — preview chat mirrors the live chat's placement semantics)
    - The ChatBoxChrome contract in this plan's &lt;interfaces&gt; block above (created by sibling Plan 09-04; if 09-03 executes before 09-04 in Wave 2, write the import against this documented contract — final integration is verified by Plan 09-06 UAT)
  </read_first>
  <action>
    EXTEND `packages/wp-bundle/src/preview/PreviewScene.tsx` (produced by Task 2) so the preview also shows the ChatBox chrome alongside the avatar when the author has enabled chat. This is the STUDIO-03 "ChatBox visible in editor preview" half (BLOCKER-1, checker iteration 1).

    1. Add the import: `import { ChatBoxChrome } from "../ui/ChatBox";` at the top of PreviewScene.tsx. (ChatBoxChrome — NOT ChatBox. ChatBox calls useRealtime and would throw / pull the realtime path into the preview bundle's import graph, violating STUDIO-02. ChatBoxChrome is presentational-only: no hooks, no provider dependency, no realtime import.)

    2. Inside PreviewScene's render, change the container layout so that when `config.chatShow === true`, the preview shows BOTH the avatar canvas AND a `<ChatBoxChrome>` sibling. Wrap them in a layout div mirroring the published-page layout (Plan 09-05): a `<div className={`khaveeai-layout khaveeai-layout--${config.chatPlacement ?? "beside"}`}>` containing the existing `<Canvas>` block AND `{config.chatShow === true && (<ChatBoxChrome placement={config.chatPlacement ?? "beside"} messages={[]} input="" onInputChange={() => {}} onSend={() => {}} onKeyDown={() => {}} scrollRef={{ current: null }} endRef={{ current: null }} connected={false} thinkingEnabled={true} />)}`. The `thinkingEnabled: true` prop is the key: it makes ChatBoxChrome render ONLY the header + "Chat preview — visible on the published page." pill (no transcript, no input, no send button — see the ChatBoxChrome contract). The empty stub callbacks (no-op onInputChange/onSend/onKeyDown) and the `{{ current: null }}` refs are required by the ChatBoxChromeProps signature but are never invoked in thinkingEnabled mode, so they are inert.

    3. When `config.chatShow` is false (or unset), render ONLY the `<Canvas>` block as Task 2 already does — no layout wrapper, no ChatBoxChrome. The avatar preview fills the container exactly as before.

    4. STUDIO-02 SAFETY (load-bearing — must remain provably true): the ChatBoxChrome import must NOT transitively pull @khaveeai/providers-openai-realtime or any useRealtime call site into PreviewScene.tsx's import graph. Verify by re-running the build.mjs grep assertion after this task: `grep -c "RealtimeProvider\|getUserMedia\|ephemeral" wordpress-plugin/build/khaveeai-preview.js` MUST still return 0. If it returns non-zero, ChatBox.tsx (Plan 09-04) has leaked a realtime import into ChatBoxChrome's definition body — that is a BLOCKER on Plan 09-04, not this plan; flag it and stop. (The acceptance criteria in Plan 09-04 already require `grep "useRealtime" on ChatBoxChrome's definition body returns 0` — this task's grep is the integration-level re-check.)

    Do NOT add a transcript, do NOT add an input row, do NOT add a send button in the editor preview. The author is shown the chat CHROME (card + header + pill) so they understand the chat will appear here on the published page — nothing more. The pill copy "Chat preview — visible on the published page." makes the placeholder nature explicit.
  </action>
  <verify>
    <automated>cd packages/wp-bundle && pnpm exec tsc --noEmit exits 0; grep -c "ChatBoxChrome\|config.chatShow\|khaveeai-layout" packages/wp-bundle/src/preview/PreviewScene.tsx returns at least 3; grep -c "useRealtime\|OpenAIRealtimeProvider\|providers-openai-realtime" packages/wp-bundle/src/preview/PreviewScene.tsx returns 0 (ChatBoxChrome must not pull realtime into the preview graph); node build.mjs exits 0 AND grep assertion passes (grep -c "RealtimeProvider\|getUserMedia\|ephemeral" wordpress-plugin/build/khaveeai-preview.js returns 0 — STUDIO-02 still holds with the ChatBox import added)</automated>
  </verify>
  <acceptance_criteria>
    - packages/wp-bundle/src/preview/PreviewScene.tsx imports ChatBoxChrome (NOT ChatBox) from '../ui/ChatBox'
    - When config.chatShow === true, PreviewScene renders the avatar Canvas AND a ChatBoxChrome sibling inside a `khaveeai-layout khaveeai-layout--${chatPlacement}` wrapper (mirrors Plan 09-05's published layout shape)
    - The ChatBoxChrome instance is rendered with thinkingEnabled={true} (so only the header + "Chat preview — visible on the published page." pill appear — no transcript/input/send in the editor preview)
    - When config.chatShow is false/unset, PreviewScene renders ONLY the Canvas (no layout wrapper, no ChatBoxChrome) — identical to Task 2's behavior
    - PreviewScene.tsx contains ZERO references to useRealtime, OpenAIRealtimeProvider, or providers-openai-realtime (grep returns 0)
    - `grep -c "RealtimeProvider\|getUserMedia\|ephemeral" wordpress-plugin/build/khaveeai-preview.js` returns 0 AFTER this task — the STUDIO-02 safety property still holds with the ChatBoxChrome import added (the load-bearing integration check)
    - `pnpm exec tsc --noEmit` from packages/wp-bundle exits 0
    - `node build.mjs` exits 0 and both bundles build clean
  </acceptance_criteria>
  <done>PreviewScene renders a presentational disconnected-state ChatBox chrome (header + "Chat preview" pill, no transcript/input) alongside the avatar when config.chatShow is true, laid out per chatPlacement. STUDIO-02 safety holds: no useRealtime, no provider, no mic, no token in the preview bundle's import graph (verified at source and build-output grep level). Typecheck + build pass.</done>
</task>

</tasks>

<threat_model>

> security_enforcement: ON | ASVS tier: L1 | disposition: block-on high

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Preview bundle import graph → @khaveeai/providers-openai-realtime | The FORBIDDEN boundary: the preview bundle's import graph must never reach the realtime provider package. This is the load-bearing STUDIO-02 safety property. |
| Editor iframe DOM → preview bundle (via data-khaveeai-preview-config JSON) | Config from editor.js (Plan 09-02) crosses into the preview-bundle mount point as JSON; preview parses into typed KhaveeAvatarConfig |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | DoS / Information Disclosure | Preview bundle → OpenAI token endpoint / mic | mitigate | THREE LAYERS: (1) Structural — preview.ts's import graph excludes @khaveeai/providers-openai-realtime by construction (esbuild tree-shakes per entry; preview.ts never imports it). (2) Build-time — Plan 09-01's build.mjs grep assertion fails the build if khaveeai-preview.js matches /RealtimeProvider\|getUserMedia\|ephemeral/. (3) Source-level — this plan's source grep acceptance criterion (PreviewScene.tsx + preview.ts + mountPreview.tsx contain zero provider references). No single-layer defense — defense in depth. |
| T-09-03-02 | Tampering / XSS | Preview bundle config JSON parsing | mitigate | JSON.parse is wrapped in try/catch (mirroring index.ts:30-34 — malformed config fails gracefully per-element, not uncaught). The parsed object is consumed via resolveSceneDefaults(config) which uses `??` fallbacks — an attacker-controlled bgColor string is applied as CSS background via React's style prop (auto-escaped) — no innerHTML, no eval, no script injection path. |
| T-09-03-03 | Denial of Service | WebGL context leak (Pitfall 3) | mitigate | The MutationObserver in mountPreview.tsx pushes fresh config into React state instead of unmounting/remounting the root. This avoids the "Too many WebGL contexts" failure mode when the author rapidly drags inspector sliders or undoes/redoes block attribute changes. |
| T-09-03-04 | Tampering | useRealtime() called from preview path | mitigate | The plan explicitly forbids useRealtime() in PreviewScene (it throws when realtimeProvider is null per useRealtime.ts:36-40). The source-grep acceptance criterion (grep "useRealtime" returns 0) enforces this. useVRMExpressions() is the ONLY SDK hook used in the preview — it works with a null realtime provider. |
| T-09-03-SC | Tampering | npm installs | accept | Plan installs zero new packages — @khaveeai/react, @react-three/fiber, react-dom are all already in packages/wp-bundle/package.json. No supply-chain risk. |
</threat_model>

<verification>
- `pnpm exec tsc --noEmit` from packages/wp-bundle exits 0
- `node packages/wp-bundle/build.mjs` exits 0 AND the embedded grep assertion passes (khaveeai-preview.js contains no RealtimeProvider/getUserMedia/ephemeral)
- `grep -c "providers-openai-realtime\|OpenAIRealtimeProvider\|useRealtime" packages/wp-bundle/src/preview.ts packages/wp-bundle/src/preview/mountPreview.tsx packages/wp-bundle/src/preview/PreviewScene.tsx` returns 0 for all three files
- `grep -c "RealtimeProvider\|getUserMedia\|ephemeral" wordpress-plugin/build/khaveeai-preview.js` returns 0 (the load-bearing STUDIO-02 safety check)
- Manual UAT (covered in Plan 09-06's checkpoint): preview bundle enqueued in editor (Plan 09-06), block selected, confirm NO mic prompt, NO network call to /wp-json/khaveeai/v1/session, avatar renders with config, Preview-talking animates mouth, AND when Chat Show is enabled the disconnected-state ChatBox chrome (header + "Chat preview" pill) is visible alongside the avatar per chatPlacement (BLOCKER-1)
</verification>

<success_criteria>
- preview.ts is a real IIFE entry mirroring index.ts's mountAll shape with the data-khaveeai-preview-config attribute
- mountPreview.tsx uses MutationObserver (no unmount/remount) to push fresh config into React state
- PreviewScene renders config-driven 3D VRM (camera preset vectors, lighting, scale/offset, bg color/image/transparent) inside KhaveeProvider-with-no-realtime
- usePreviewTalking cycles aa/ih/ou/ee/oh at 250ms via setInterval when config.previewTalking is true
- No file in the preview entry's import graph references OpenAIRealtimeProvider, useRealtime, getUserMedia, or ephemeral
- When config.chatShow is true, PreviewScene renders the presentational ChatBoxChrome (header + "Chat preview" pill) alongside the avatar per chatPlacement (BLOCKER-1, STUDIO-03 editor-preview half); when chatShow is false, only the avatar renders
- The ChatBox preview import does NOT regress STUDIO-02 — the build-time grep on khaveeai-preview.js still returns 0 for RealtimeProvider/getUserMedia/ephemeral after the ChatBoxChrome import is added
- Build + typecheck + grep safety assertion all pass
</success_criteria>

<output>
Create `.planning/phases/09-block-studio-visual-config-chat-lipsync/09-03-SUMMARY.md` when done
</output>
