---
phase: 09-block-studio-visual-config-chat-lipsync
plan: 04
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - packages/wp-bundle/src/ui/ChatBox.tsx
  - packages/wp-bundle/styles.css
autonomous: true
requirements:
  - STUDIO-03

must_haves:
  truths:
    - "A bespoke dependency-free ChatBox component exists at packages/wp-bundle/src/ui/ChatBox.tsx — no HeroUI, no lucide-react, no icon library (the send affordance is a single inline SVG)"
    - "ChatBox consumes useRealtime() from @khaveeai/react for conversation/sendMessage/chatStatus/isConnected — it is the single source of truth, NOT a local useState transcript (Don't Hand-Roll: provider owns transcript state)"
    - "Enter sends the message, Shift+Enter inserts a newline (lifted verbatim from khavee-app ChatBox.tsx:57-62)"
    - "Auto-scroll-to-bottom on new message via scrollIntoView on the last child, BUT only if the visitor is already near the bottom (pinned-to-bottom rule — don't yank if scrolled up to read history)"
    - "User bubbles are right-aligned with accent bg (#2271b1); assistant bubbles are left-aligned with neutral translucent bg — this asymmetry is the strongest speaker-turn visual signal (UI-SPEC §Color)"
    - "Assistant messages render as plain text ONLY (no markdown, no HTML injection) — React auto-escapes; this is the XSS mitigation"
    - "The disconnected state shows helper text 'Click the avatar to start, then type here.' — NOT a 'Connect to AI' button (WP connection is always initiated by the existing ClickToTalkOverlay, the ChatBox must not host a competing connect affordance)"
    - "ChatBox accepts a placement prop ('beside' | 'below') and applies the layout class accordingly"
  artifacts:
    - path: "packages/wp-bundle/src/ui/ChatBox.tsx"
      provides: "Dependency-free chat panel (header + scrollable transcript + input + send) consuming useRealtime()"
      contains: "useRealtime"
    - path: "packages/wp-bundle/styles.css"
      provides: "ChatBox chrome CSS (card, bubbles, input, send button) + light/dark theme via prefers-color-scheme"
      contains: "khaveeai-chat"
  key_links:
    - from: "packages/wp-bundle/src/ui/ChatBox.tsx"
      to: "@khaveeai/react useRealtime()"
      via: "import { useRealtime } from '@khaveeai/react'"
      pattern: "from ['\"]@khaveeai/react['\"]"
    - from: "packages/wp-bundle/src/ui/ChatBox.tsx (sendMessage on Enter)"
      to: "OpenAIRealtimeProvider.sendMessage (existing, unchanged)"
      via: "useRealtime().sendMessage(text)"
      pattern: "sendMessage"
---

<objective>
Deliver the STUDIO-03 ChatBox component half: a bespoke, dependency-free chat panel authored at `packages/wp-bundle/src/ui/ChatBox.tsx`, mirroring khavee-app's ChatBox.tsx interaction shape but with zero HeroUI/lucide dependencies. Consumes `useRealtime()` from `@khaveeai/react` for the transcript + sendMessage — text input drives the SAME realtime session as voice (no second connection, per UI-SPEC). Also adds the ChatBox chrome CSS to `styles.css`.

Purpose: This plan delivers ONLY the component. Plan 09-05 wires it into the published-page mount.tsx (where useRealtime actually has a non-null provider) and Plan 09-02/09-06 surface its disconnected-state placeholder in the editor preview. The ChatBox is the only genuinely NEW component in Phase 9 (everything else is reuse or wiring).

Output: One new component file + extended CSS. After this plan, `<ChatBox placement="beside|below" />` renders a complete chat panel that, when mounted inside a KhaveeProvider-with-realtime (Plan 09-05), shows the live transcript and accepts text input that joins the voice session. The file ALSO exports a presentational `ChatBoxChrome` component (BLOCKER-1, checker iteration 1) that renders the same header + transcript-area + input chrome from explicit props WITHOUT calling useRealtime — Plan 09-03's editor preview imports `ChatBoxChrome` to render a disconnected-state placeholder alongside the avatar when `config.chatShow` is true. The two components share their render shape: `ChatBox` is a thin live wrapper that consumes useRealtime and delegates its render to `ChatBoxChrome`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-block-studio-visual-config-chat-lipsync/09-CONTEXT.md
@.planning/phases/09-block-studio-visual-config-chat-lipsync/09-RESEARCH.md
@.planning/phases/09-block-studio-visual-config-chat-lipsync/09-PATTERNS.md
@.planning/phases/09-block-studio-visual-config-chat-lipsync/09-UI-SPEC.md
@.planning/phases/09-block-studio-visual-config-chat-lipsync/09-01-SUMMARY.md
@packages/wp-bundle/styles.css
@packages/wp-bundle/src/mount.tsx

<interfaces>
<!-- Contracts the executor needs. All verified during research. -->

From packages/react/src/hooks/useRealtime.ts (verified — line 24 state shape, line 138-143 sendMessage):
```typescript
// useRealtime() returns:
//   conversation: Conversation[]      // already { role, text }[] — matches khavee-app's prop shape exactly, no conversion
//   sendMessage: (text: string) => void  // joins the existing realtime session (does NOT open a second connection)
//   chatStatus: ChatStatus             // 'idle' | 'thinking' | 'speaking' | ...
//   isConnected: boolean               // false until the visitor clicks ClickToTalkOverlay and connect() succeeds
// THROWS if realtimeProvider is null (line 36-40) — ChatBox can ONLY be mounted inside a KhaveeProvider-with-realtime (Plan 09-05)
```

From packages/core/src/types/conversation.ts (the Conversation type — already {role, text}[]):
```typescript
// role is "user" | "assistant" (string literal union); text is the message string
// → ChatBox just renders {msg.text} per bubble — React auto-escapes (XSS mitigation, no markdown rendering per UI-SPEC)
```

UI-SPEC locked tokens (use VERBATIM):
  Copywriting: header "AI Assistant"; empty-state heading "Start the conversation" + body "Type a message below or click the avatar to talk."; input placeholder "Type a message…"; send button aria-label "Send message" (icon-only); disconnected-state "Click the avatar to start, then type here."
  Color: user bubble bg #2271b1 (accent); assistant bubble bg rgba(255,255,255,0.92) light / rgba(30,30,30,0.92) dark via prefers-color-scheme; ChatBox card itself rgba(255,255,255,0.92) / rgba(30,30,30,0.92)
  Spacing: bubble inner padding 16px (md); gap between bubbles 8px (sm); input-row gaps 4px (xs); ChatBox card outer padding 24px (lg); touch-target floor 44px (send button min-height)
  Typography: body 14px/400/1.5; label 16px/600/1.2 (header title); font-family: inherit on every text node
  Icon: single inline SVG paper-plane (no icon library) — viewBox="0 0 24 24"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ChatBox.tsx — ChatBox (live wrapper consuming useRealtime) + ChatBoxChrome (presentational inner, reusable by the editor preview)</name>
  <read_first>
    - /Users/whitemalt/Documents/khavee-app/apps/web/src/components/settings/preview/ChatBox.tsx (the 175-line reference — lift INTERACTION PATTERNS only: handleKeyPress lines 57-62 [Enter sends, Shift+Enter newline], scrollToBottom lines 41-48 [scrollIntoView on lastElementChild], bubble alignment lines 118-133 [user right accent, assistant left neutral], empty-state lines 96-114). DO NOT lift HeroUI/lucide imports — re-author dependency-free.
    - packages/react/src/hooks/useRealtime.ts (lines 24, 138-143 — the return shape and sendMessage wrapper)
    - .planning/phases/09-block-studio-visual-config-chat-lipsync/09-PATTERNS.md (section "ChatBox.tsx" — the khavee-app lift table; section also has the deviation list: drop "Connect to AI" button, header is "AI Assistant" not "Khavee AI Assistant", plain-text-only assistant messages, the wiring snippet)
    - .planning/phases/09-block-studio-visual-config-chat-lipsync/09-UI-SPEC.md (§Copywriting bundle runtime rows — every ChatBox string is locked VERBATIM; §Interaction-States published-side rows — send-text, scrollback, shift+Enter behaviors)
  </read_first>
  <action>
    Create `packages/wp-bundle/src/ui/ChatBox.tsx` exporting `ChatBox({ placement }: { placement: 'beside' | 'below' })`.

    1. Import `useRealtime` from `@khaveeai/react` and `useState`/`useEffect`/`useRef` from `react`. Do NOT import any HeroUI, lucide, or other UI-library components.

    2. Define and EXPORT the presentational inner component `ChatBoxChrome` FIRST (BLOCKER-1, checker iteration 1). Signature:
       ```
       interface ChatBoxChromeProps {
         placement: "beside" | "below";
         messages: { role: "user" | "assistant"; text: string }[];
         input: string;
         onInputChange: (v: string) => void;
         onSend: () => void;
         onKeyDown: (e: React.KeyboardEvent<HTMLTextAreaElement>) => void;
         scrollRef: React.RefObject<HTMLDivElement | null>;
         endRef: React.RefObject<HTMLDivElement | null>;
         connected: boolean;          // false => disconnected-state helper text, no input row
         chatStatus?: string;          // optional className hook only
         thinkingEnabled?: boolean;    // editor-preview override: when true, the input row + transcript are HIDDEN and a "Preview" pill replaces them (see Plan 09-03); when undefined, normal live behavior
       }
       export function ChatBoxChrome(props: ChatBoxChromeProps)
       ```
       ChatBoxChrome contains ALL the JSX from steps 7-9 below (outer card, header, body modes, input row, send button, SVG, scroll anchor). It calls NO SDK hooks — only renders from props. This is what makes it safe to import from the editor preview bundle (Plan 09-03): no useRealtime() call site exists in this component, so it works when there is no realtime provider, and it does not pull any realtime path into the preview bundle's import graph.

    3. Define the LIVE wrapper `ChatBox({ placement }: { placement: "beside" | "below" })`. Inside, destructure from useRealtime(): `const { conversation, sendMessage, chatStatus, isConnected } = useRealtime();`. The `conversation` array is already `{role: "user"|"assistant", text: string}[]` — no shape conversion needed. Local state: `const [ input, setInput ] = useState('')`; `const scrollRef = useRef<HTMLDivElement>(null)`; `const endRef = useRef<HTMLDivElement>(null)`. Then implement handleKeyPress/handleSend (steps 5-6) and the auto-scroll effect (step 4), and render `<ChatBoxChrome placement={placement} messages={conversation} input={input} onInputChange={setInput} onSend={handleSend} onKeyDown={handleKeyPress} scrollRef={scrollRef} endRef={endRef} connected={isConnected} chatStatus={chatStatus} />` (no thinkingEnabled prop — the live path uses normal mode). Export `ChatBox` as the default-named export consumers use.

    4. Auto-scroll effect (lift khavee-app ChatBox.tsx:41-48 + RESEARCH pinned-to-bottom rule): a useEffect on `[conversation]` that checks `if (scrollRef.current && (scrollRef.current.scrollHeight - scrollRef.current.scrollTop - scrollRef.current.clientHeight < 80)) { endRef.current?.scrollIntoView({ behavior: 'smooth', block: 'end' }); }`. The 80px threshold means: only auto-scroll if the user is already near the bottom — if they've scrolled up to read history, don't yank them down (UI-SPEC §Interaction-States "ChatBox — scrollback" row).

    5. handleKeyPress (lift khavee-app ChatBox.tsx:57-62 verbatim): on the textarea's onKeyDown, if `e.key === 'Enter' && !e.shiftKey` then `e.preventDefault(); handleSend();`. Plain Enter sends; Shift+Enter inserts a newline (default behavior preserved by NOT preventing it).

    6. handleSend: `const text = input.trim(); if (!text || !isConnected) return; sendMessage(text); setInput('');` — guards against sending empty messages or sending when the realtime session is not connected.

    7. Render structure — implement this JSX INSIDE `ChatBoxChrome` (className-based — CSS lives in styles.css Task 2). ChatBoxChrome is the only place this JSX exists; `ChatBox` (step 3) just delegates to it:
       - Outer div className={`khaveeai-chat khaveeai-chat--${placement} ${chatStatus ? `khaveeai-chat--${chatStatus}` : ""}`} (the card; placement drives flex-direction via CSS).
       - Header div className="khaveeai-chat__header" containing the title text "AI Assistant" (UI-SPEC verbatim — NOT "Khavee AI Assistant"; the WP embed is white-label).
       - EDITOR-PREVIEW OVERRIDE (BLOCKER-1): when `props.thinkingEnabled === true`, ChatBoxChrome renders ONLY the outer card + header + a small "Chat preview" pill (className `khaveeai-chat__preview-pill`) explaining the chat is a placeholder in the editor. It does NOT render the transcript, the input row, or the send button in this mode. This is the branch Plan 09-03 uses so the editor preview shows the chat box chrome without any live transcript/input that would imply a connected session.
       - Body — THREE modes (skipped entirely when thinkingEnabled === true):
         - When `!props.connected`: render the disconnected-state helper text "Click the avatar to start, then type here." (UI-SPEC verbatim — NOT a "Connect to AI" button; in WP, connection is always initiated by the existing ClickToTalkOverlay from Phase 8, and the ChatBox must not host a competing connect affordance).
         - When `props.connected` and `props.messages.length === 0`: render the empty-state heading "Start the conversation" + body "Type a message below or click the avatar to talk." (UI-SPEC verbatim).
         - When `props.connected` and props.messages has entries: render a scrollable transcript div (ref=props.scrollRef) mapping `props.messages.map((msg, i) => <div key={i} className={`khaveeai-chat__bubble khaveeai-chat__bubble--${msg.role}`}>{msg.text}</div>)`. The bubble className suffix `--user` vs `--assistant` drives alignment + bg via CSS. Each bubble's text is rendered as `{msg.text}` (plain text — React auto-escapes; UI-SPEC explicitly forbids markdown/HTML rendering in bubbles, the XSS mitigation).
         - At the end of the transcript div, an empty `<div ref={props.endRef} />` anchor for scrollIntoView.
       - Input row (only rendered when props.connected AND thinkingEnabled !== true): a `<textarea>` (value=props.input, onChange={e => props.onInputChange(e.target.value)}, onKeyDown=props.onKeyDown, placeholder="Type a message…", className="khaveeai-chat__input", rows=1, aria-label="Message") + a send button (className="khaveeai-chat__send", onClick=props.onSend, aria-label="Send message", min-height 44px via CSS). The send button contains an inline SVG: `<svg viewBox="0 0 24 24" width="20" height="20" aria-hidden="true">...</svg>` shaped as a paper-plane glyph (UI-SPEC §Design System: "single inline SVG, no icon library").

    8. The `chatStatus` value from useRealtime can optionally drive a subtle "thinking" indicator on the header (UI-SPEC §Interaction-States "Active — listening / thinking") but this is not strictly required — leave a className hook (e.g. `khaveeai-chat--${chatStatus}`) on the outer div so CSS can show/hide an indicator later.

    Do NOT add any `dangerouslySetInnerHTML`, `innerHTML`, or markdown rendering anywhere — assistant messages are plain text only.
  </action>
  <verify>
    <automated>cd packages/wp-bundle && pnpm exec tsc --noEmit exits 0; grep -c "useRealtime\|sendMessage\|handleKeyPress\|scrollIntoView\|khaveeai-chat\|ChatBoxChrome" packages/wp-bundle/src/ui/ChatBox.tsx returns at least 6; grep -c "heroui\|lucide\|@heroui\|from ['\"]lucide" packages/wp-bundle/src/ui/ChatBox.tsx returns 0 (no forbidden UI-lib imports); grep -c "dangerouslySetInnerHTML\|innerHTML" packages/wp-bundle/src/ui/ChatBox.tsx returns 0 (no XSS path)</automated>
  </verify>
  <acceptance_criteria>
    - packages/wp-bundle/src/ui/ChatBox.tsx exports BOTH `ChatBox({placement}: {placement: 'beside' | 'below'})` (live wrapper) AND `ChatBoxChrome(props: ChatBoxChromeProps)` (presentational inner)
    - ChatBox imports useRealtime from '@khaveeai/react' and uses conversation, sendMessage, chatStatus, isConnected from it (NO local useState transcript — provider is source of truth); ChatBox delegates its render to `<ChatBoxChrome ... connected={isConnected} ... />`
    - ChatBoxChrome does NOT import useRealtime (grep "useRealtime" on ChatBoxChrome's definition body returns 0 — only the ChatBox wrapper imports it). This is the BLOCKER-1 safety property: the editor preview can import ChatBoxChrome without pulling any realtime path into the preview bundle.
    - ChatBoxChrome renders the "Preview" pill branch (className `khaveeai-chat__preview-pill`) when thinkingEnabled === true, skipping the transcript + input row
    - handleKeyPress implements: Enter (no shift) prevents default + calls handleSend; Shift+Enter is left as default (newline)
    - handleSend guards: empty/whitespace text is not sent; sendMessage is not called when !isConnected
    - Auto-scroll effect uses the 80px pinned-to-bottom threshold (only scrolls if user near bottom)
    - Disconnected state renders "Click the avatar to start, then type here." (verbatim — NO Connect button)
    - Empty connected state renders "Start the conversation" + "Type a message below or click the avatar to talk." (verbatim)
    - Input placeholder is "Type a message…" (verbatim); send button aria-label is "Send message" (verbatim)
    - Header text is "AI Assistant" (NOT "Khavee AI Assistant" — white-label deviation)
    - Assistant/user bubbles render `{msg.text}` (plain text, no markdown, no HTML) — grep dangerouslySetInnerHTML returns 0
    - Send button contains an inline SVG (no icon-library import) — grep for heroui/lucide returns 0
    - `pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>ChatBox.tsx renders a complete dependency-free chat panel (header + transcript + input + send). Consumes useRealtime for transcript/sendMessage (single source of truth). Enter sends, Shift+Enter newlines, auto-scroll pinned-to-bottom. Disconnected state shows the WP-specific helper text (no Connect button). All copy is UI-SPEC-verbatim. No XSS path. Typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add ChatBox CSS to styles.css (card, bubbles, input, send, light/dark theme, placement)</name>
  <read_first>
    - packages/wp-bundle/styles.css (existing .khaveeai-* classes lines 11-67; the existing #2271b1 accent token at line 49 on .khaveeai-cta-button; the existing 44px min-height on .khaveeai-cta-button — carry both into the send button)
    - .planning/phases/09-block-studio-visual-config-chat-lipsync/09-UI-SPEC.md (§Color — accent #2271b1 for user bubble + send button; assistant bubble rgba(255,255,255,0.92) light / rgba(30,30,30,0.92) dark via prefers-color-scheme; §Spacing — md=16px bubble padding, sm=8px bubble gap, xs=4px input gap, lg=24px card padding, 44px touch-target; §Typography — body 14px/400/1.5, label 16px/600/1.2)
    - .planning/phases/09-block-studio-visual-config-chat-lipsync/09-PATTERNS.md (section "styles.css" — the token references to carry forward)
  </read_first>
  <action>
    Append ChatBox styles to `packages/wp-bundle/styles.css`. Use the existing `.khaveeai-*` class naming convention. Implement the UI-SPEC tokens exactly:

    1. `.khaveeai-chat` (the card): translucent background `rgba(255,255,255,0.92)` (light); padding 24px (lg); border-radius matches Phase 8's existing overlays; `font-family: inherit` (UI-SPEC §Design System). Use `@media (prefers-color-scheme: dark) { .khaveeai-chat { background: rgba(30,30,30,0.92); color: #f0f0f0; } }`.

    2. `.khaveeai-chat--beside` and `.khaveeai-chat--below` (placement modifiers): `--beside` makes the chat a flex sibling of the avatar canvas (e.g. `display: flex; flex-direction: column; width: 320px;` — the layout root in Plan 09-05 decides parent flex-direction); `--below` makes it full-width below the canvas. The exact pixel widths can be tuned, but the modifier hooks must exist so Plan 09-05 can choose the layout.

    3. `.khaveeai-chat__header`: padding 16px (md) vertical; font-size 16px; font-weight 600 (label token); `font-family: inherit`.

    4. `.khaveeai-chat__transcript`: `overflow-y: auto; flex: 1;` scrollable; display flex column; gap 8px (sm) between bubbles.

    5. `.khaveeai-chat__bubble`: padding 16px (md) all sides; border-radius 12px; font-size 14px (body); line-height 1.5; `font-family: inherit`; word-break: break-word; max-width: 80%.

    6. `.khaveeai-chat__bubble--user`: align-self: flex-end (right-aligned); background #2271b1 (accent); color #fff.

    7. `.khaveeai-chat__bubble--assistant`: align-self: flex-start (left-aligned); background rgba(255,255,255,0.92) light / rgba(30,30,30,0.92) dark (via prefers-color-scheme — note: slightly different from the card bg so the bubble is distinguishable; consider rgba(0,0,0,0.05) over the card bg in light mode for subtle contrast).

    8. `.khaveeai-chat__input-row`: display flex; gap 4px (xs); align-items flex-end.

    9. `.khaveeai-chat__input`: flex 1; resize none; min-height 44px (touch-target floor); padding 8px 12px; font-size 14px; `font-family: inherit`; border 1px solid rgba(0,0,0,0.1); border-radius 8px; background rgba(255,255,255,0.6) (light). Dark-mode overrides via prefers-color-scheme.

    10. `.khaveeai-chat__send`: min-height 44px; min-width 44px (touch-target floor); background #2271b1 (accent); color #fff; border none; border-radius 8px; cursor pointer; display inline-flex; align-items center; justify-content center. The inline SVG inside inherits `color` via `fill="currentColor"`.

    11. `.khaveeai-chat__send:hover`: slightly darker accent (e.g. #135e96). `.khaveeai-chat__send:focus-visible`: 2px solid #2271b1 outline (UI-SPEC §Color — the input focus ring is the one allowed accent extension).

    12. `.khaveeai-chat__disconnected`: padding 16px (md); font-size 14px (body); color rgba(0,0,0,0.55) (light) / rgba(255,255,255,0.55) (dark); text-align center. Holds the "Click the avatar to start, then type here." helper text.

    13. `.khaveeai-chat__empty-state`: similar to disconnected but holds the empty-connected-state heading + body.

    14. `.khaveeai-chat__preview-pill` (BLOCKER-1, editor-preview-only): a small centered pill inside the chat card body used ONLY when ChatBoxChrome is rendered with `thinkingEnabled: true` from the editor preview (Plan 09-03). Style it as a subtle muted pill: `display: inline-block; margin: 16px auto; padding: 8px 16px; border-radius: 999px; background: rgba(0,0,0,0.05); color: rgba(0,0,0,0.55); font-size: 13px; text-align: center;` with `@media (prefers-color-scheme: dark)` overrides to `background: rgba(255,255,255,0.08); color: rgba(255,255,255,0.55);`. The pill copy is "Chat preview — visible on the published page." (plain-language, UI-SPEC §Copywriting tone — explains to the author that the chat is a placeholder in the editor and functional on the published page). This class is NEVER used by the live ChatBox (the live path does not set thinkingEnabled).

    Do NOT use `!important` anywhere. Do NOT scope rules to a specific WP theme — these styles apply inside the .khaveeai-root container only.
  </action>
  <verify>
    <automated>cd packages/wp-bundle && node build.mjs exits 0 (CSS bundles cleanly); grep -c "khaveeai-chat__bubble--user\|khaveeai-chat__bubble--assistant\|khaveeai-chat__send\|prefers-color-scheme" packages/wp-bundle/styles.css returns at least 4; grep -c "!important" packages/wp-bundle/styles.css returns 0 (no important abuse)</automated>
  </verify>
  <acceptance_criteria>
    - styles.css contains rules for .khaveeai-chat, .khaveeai-chat--beside, .khaveeai-chat--below, .khaveeai-chat__header, .khaveeai-chat__transcript, .khaveeai-chat__bubble, .khaveeai-chat__bubble--user, .khaveeai-chat__bubble--assistant, .khaveeai-chat__input-row, .khaveeai-chat__input, .khaveeai-chat__send, .khaveeai-chat__disconnected, .khaveeai-chat__empty-state, .khaveeai-chat__preview-pill (BLOCKER-1 editor-preview-only class)
    - .khaveeai-chat__bubble--user has background #2271b1 and align-self flex-end (UI-SPEC accent + right alignment)
    - .khaveeai-chat__bubble--assistant has align-self flex-start (left alignment) with a neutral bg differing between light/dark via prefers-color-scheme
    - .khaveeai-chat__send has min-height 44px and min-width 44px (touch-target floor) and background #2271b1
    - A @media (prefers-color-scheme: dark) block exists that overrides the card + assistant bubble + input backgrounds for dark mode
    - Every text-bearing rule has `font-family: inherit`
    - No `!important` declarations
    - `node packages/wp-bundle/build.mjs` exits 0 (CSS loads via esbuild's css loader)
  </acceptance_criteria>
  <done>ChatBox CSS implements the UI-SPEC color/spacing/typography tokens with light/dark theme support and the placement modifiers. Send button + user bubble use the #2271b1 accent. Touch-target floor 44px on input + send. No !important. Build succeeds.</done>
</task>

</tasks>

<threat_model>

> security_enforcement: ON | ASVS tier: L1 | disposition: block-on high

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenAI assistant reply → ChatBox bubble | The assistant's text response (potentially containing model-influenced output from the admin-configured instructions) renders into the published-page DOM via ChatBox bubbles |
| Visitor text input → realtime session | Visitor-typed text in the ChatBox crosses into the existing OpenAIRealtimeProvider.sendMessage path (unchanged from Phase 8) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Tampering / XSS | Assistant chat message rendering | mitigate | Assistant messages render as `{msg.text}` (plain text). React auto-escapes all string interpolation — `<script>` tags or HTML in the assistant's reply become inert text. UI-SPEC §Conversation explicitly forbids markdown rendering and HTML injection. No `dangerouslySetInnerHTML`, no `innerHTML`, no markdown library — grep acceptance criterion enforces this. |
| T-09-04-02 | Tampering | Visitor text input | accept | Visitor-typed text is sent to the existing OpenAIRealtimeProvider.sendMessage (unchanged, Phase-8-tested path). It is NOT rendered into the DOM as HTML — user bubbles also use `{msg.text}` (auto-escaped). No new attack surface introduced. |
| T-09-04-03 | Information Disclosure | ChatBox transcript visibility | accept | The transcript shows only the visitor's own conversation with the avatar (already a Phase-8 capability via the realtime session). No new disclosure path. The disconnected-state helper text reveals no secrets. |
| T-09-04-SC | Tampering | npm installs | accept | Plan installs zero new packages — the inline SVG send icon replaces what would otherwise be a lucide-react dependency. No supply-chain risk. |
</threat_model>

<verification>
- `pnpm exec tsc --noEmit` from packages/wp-bundle exits 0
- `node packages/wp-bundle/build.mjs` exits 0 (ChatBox component + CSS both bundle cleanly)
- ChatBox source contains no heroui/lucide imports, no dangerouslySetInnerHTML/innerHTML
- All UI-SPEC-locked copy strings are present verbatim in ChatBox.tsx
- Manual UAT (covered in Plan 09-05's checkpoint once ChatBox is wired into the live mount): published page shows transcript + input + send works + scroll behavior
</verification>

<success_criteria>
- ChatBox.tsx is dependency-free, consumes useRealtime for transcript/sendMessage (single source of truth)
- Enter sends, Shift+Enter inserts newline, auto-scroll respects pinned-to-bottom rule
- Disconnected state shows the WP-specific helper text (no Connect button — no competing connect affordance with ClickToTalkOverlay)
- All UI-SPEC copy verbatim ("AI Assistant", "Type a message…", "Send message", "Click the avatar to start, then type here.", etc.)
- Assistant messages render as plain text only (XSS-safe via React auto-escaping)
- styles.css implements the locked color/spacing/typography tokens with light/dark theme + placement modifiers
- Typecheck + build pass; no UI-library imports; no !important
</success_criteria>

<output>
Create `.planning/phases/09-block-studio-visual-config-chat-lipsync/09-04-SUMMARY.md` when done
</output>
