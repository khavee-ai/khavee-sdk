---
phase: 09-block-studio-visual-config-chat-lipsync
plan: 05
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-04
files_modified:
  - packages/wp-bundle/src/mount.tsx
autonomous: true
requirements:
  - STUDIO-03
  - STUDIO-04

must_haves:
  truths:
    - "The published-page mount (mountAvatarInstance in mount.tsx) renders ChatBox when config.chatShow is true, placed beside or below the avatar per config.chatPlacement"
    - "ChatBox is mounted inside <KhaveeProvider config={{realtime: provider}}> so useRealtime() works — the published page constructs OpenAIRealtimeProvider (unchanged from Phase 8), so the realtime context is non-null"
    - "Text typed in ChatBox drives the SAME realtime session as voice (via useRealtime().sendMessage → OpenAIRealtimeProvider.sendMessage) — NO second connection is opened"
    - "The published AvatarScene is config-driven: applies resolveSceneDefaults(config) for camera position (preset vectors), light intensity, avatar scale, avatar offset X/Y — replacing Phase-8's hardcoded [0,0,5]/50/ambient 1/dir 2.5"
    - "Lip-sync on the published page is AUTOMATIC REUSE — useRealtime's existing effect (onAudioData → RealtimeAudioAnalyzer → setMultipleExpressions → VRMAvatar) runs unchanged once VRMAvatar is inside KhaveeProvider (which it already is). NO new lip-sync code is added on the published side."
    - "Existing Phase-8 published pages render IDENTICALLY when no new config keys are set (all new fields are optional with ?? fallbacks to Phase-8 hardcoded values)"
  artifacts:
    - path: "packages/wp-bundle/src/mount.tsx"
      provides: "Config-driven AvatarScene (preset camera, lighting, scale/offset, bg color/image/transparent) + ChatBox mount (when chatShow) inside KhaveeProvider; KhaveeAvatarConfig re-exported from ./config"
      contains: "ChatBox"
  key_links:
    - from: "packages/wp-bundle/src/mount.tsx (ChatBox)"
      to: "packages/wp-bundle/src/ui/ChatBox.tsx (Plan 09-04)"
      via: "import { ChatBox } from './ui/ChatBox'"
      pattern: "from ['\"]\\./ui/ChatBox['\"]"
    - from: "packages/wp-bundle/src/mount.tsx (AvatarScene)"
      to: "packages/wp-bundle/src/config.ts (resolveSceneDefaults + CAMERA_PRESETS)"
      via: "import { resolveSceneDefaults } from './config'"
      pattern: "from ['\"]\\./config['\"]"
    - from: "packages/wp-bundle/src/mount.tsx (VRMAvatar inside KhaveeProvider)"
      to: "packages/react/src/hooks/useRealtime.ts (existing lip-sync path, unchanged)"
      via: "VRMAvatar inside KhaveeProvider → useRealtime auto-wires onAudioData → RealtimeAudioAnalyzer → setMultipleExpressions"
      pattern: "KhaveeProvider"
---

<objective>
Deliver the published-page halves of STUDIO-03 (functional ChatBox) and STUDIO-04 (real lip-sync). Extend `mountAvatarInstance` in `mount.tsx` to: (1) make `AvatarScene` config-driven (apply resolveSceneDefaults for camera/lighting/scale/offset/background per Part A), (2) mount `<ChatBox placement={config.chatPlacement ?? 'beside'} />` as a sibling of AvatarScene when `config.chatShow` is true, inside the existing `<KhaveeProvider>` so ChatBox's useRealtime() has a non-null provider.

Purpose: STUDIO-04's published-page lip-sync is REUSE, not rebuild — the SDK's existing lip-sync path (useRealtime → RealtimeAudioAnalyzer → setMultipleExpressions → VRMAvatar) already runs automatically once VRMAvatar is inside KhaveeProvider (which Phase 8 already does). This plan verifies that path is preserved and adds the config application. STUDIO-03's live half requires ChatBox (built in Plan 09-04) to be mounted in the live context where it can actually send messages.

Output: One modified file (`packages/wp-bundle/src/mount.tsx`). After this plan, the published page renders a config-driven avatar + an optional functional ChatBox, with automatic lip-sync on the avatar while the assistant speaks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-block-studio-visual-config-chat-lipsync/09-CONTEXT.md
@.planning/phases/09-block-studio-visual-config-chat-lipsync/09-RESEARCH.md
@.planning/phases/09-block-studio-visual-config-chat-lipsync/09-PATTERNS.md
@.planning/phases/09-block-studio-visual-config-chat-lipsync/09-UI-SPEC.md
@.planning/phases/09-block-studio-visual-config-chat-lipsync/09-01-SUMMARY.md
@.planning/phases/09-block-studio-visual-config-chat-lipsync/09-04-SUMMARY.md
@packages/wp-bundle/src/mount.tsx
@packages/wp-bundle/src/config.ts
@packages/wp-bundle/src/ui/ChatBox.tsx
@packages/react/src/hooks/useRealtime.ts

<interfaces>
<!-- Existing contracts (Plan 09-01 / 09-04 create the new pieces; this plan wires them). -->

From packages/wp-bundle/src/config.ts (Plan 09-01):
```typescript
export function resolveSceneDefaults(c: KhaveeAvatarConfig) {
  return {
    lightIntensity: c.lightIntensity ?? 1.0,
    avatarScale: c.avatarScale ?? 1.0,
    avatarOffsetX: c.avatarOffsetX ?? 0.0,
    avatarOffsetY: c.avatarOffsetY ?? 0.0,
    cameraPreset: c.cameraPreset ?? "front",
    cameraPosition: CAMERA_PRESETS[c.cameraPreset ?? "front"].position,
    cameraFov: 20, ambient: 1, directional: 2.5,
  };
}
```

From packages/wp-bundle/src/ui/ChatBox.tsx (Plan 09-04):
```typescript
export function ChatBox({ placement }: { placement: 'beside' | 'below' })
// Consumes useRealtime() — REQUIRES a non-null realtimeProvider (only the published mount provides one).
```

From packages/wp-bundle/src/mount.tsx (current — to be extended):
```typescript
// mountAvatarInstance currently constructs OpenAIRealtimeProvider, wraps in KhaveeProvider,
// and renders <AvatarScene avatarUrl=...> + ClickToTalkOverlay + ErrorOverlay.
// AvatarScene is HARDCODED: <Canvas camera={{position:[0,0,5], fov:50}}> ambient 1 / dir 2.5.
// This plan: (1) extend AvatarScene to consume resolveSceneDefaults(config),
//             (2) add {config.chatShow && <ChatBox placement={config.chatPlacement ?? 'beside'} />} sibling,
//             (3) wrap AvatarScene + ChatBox in a layout div that respects placement + containerWidth/Height/fullWidth/bgColor.
```

SDK lip-sync path (verified at useRealtime.ts:43-127 — UNCHANGED this plan):
```text
provider.onAudioData(analyser, audioContext)  →  RealtimeAudioAnalyzer (MFCC/DTW)
  →  setMultipleExpressions({aa,ih,ou,ee,oh})  →  VRMAvatar expression blend shapes per frame
// This path runs AUTOMATICALLY once VRMAvatar is inside KhaveeProvider (Phase 8 already does this).
// This plan makes ZERO changes to useRealtime.ts or VRMAvatar.tsx.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make AvatarScene config-driven (camera preset, lighting, scale, offset, background) and add ChatBox + layout container to mountAvatarInstance</name>
  <read_first>
    - packages/wp-bundle/src/mount.tsx (current AvatarScene at lines 54-64 hardcoded; current mountAvatarInstance at lines 66-84 with OpenAIRealtimeProvider construction + KhaveeProvider wrap + ClickToTalkOverlay/ErrorOverlay)
    - packages/wp-bundle/src/config.ts (Plan 09-01 — resolveSceneDefaults returns the camera/lighting/scale/offset values; the type re-export: `import type { KhaveeAvatarConfig } from './config'`)
    - packages/wp-bundle/src/ui/ChatBox.tsx (Plan 09-04 — ChatBox signature, placement prop)
    - .planning/phases/09-block-studio-visual-config-chat-lipsync/09-PATTERNS.md (section "mount.tsx" — the exact extension: AvatarScene becomes config-driven, mountAvatarInstance adds the ChatBox sibling, lip-sync is automatic reuse; section "PreviewScene.tsx" Pitfall 6 — transparent-background canvas needs gl={{alpha:true}} + container CSS background, NOT scene.background)
    - .planning/phases/09-block-studio-visual-config-chat-lipsync/09-RESEARCH.md (Pattern 2: lip-sync reuse — the verified end-to-end path; Pitfall 6: transparent-background)
  </read_first>
  <action>
    Modify `packages/wp-bundle/src/mount.tsx`:

    1. Add imports: `import { ChatBox } from './ui/ChatBox';` and `import { resolveSceneDefaults } from './config';` (the KhaveeAvatarConfig type re-export is already in place from Plan 09-01). Keep the existing imports (OpenAIRealtimeProvider, KhaveeProvider, VRMAvatar, GLBAvatar, Canvas, ClickToTalkOverlay, ErrorOverlay).

    2. Modify `AvatarScene` to accept the full config (not just avatarUrl) and apply resolveSceneDefaults. Signature becomes `function AvatarScene({ config }: { config: KhaveeAvatarConfig })`. Inside:
       - Compute `const scene = resolveSceneDefaults(config);` and `const avatarUrl = config.avatarUrl ?? '';` and `const isGlb = avatarUrl.toLowerCase().endsWith('.glb');`
       - Render `<Canvas camera={{ position: scene.cameraPosition, fov: scene.cameraFov }}>` (fov=20 from resolveSceneDefaults — consistent with the lifted preset vectors; this CHANGES Phase-8's fov=50 to fov=20, but only when cameraPreset or scene defaults are at play. NOTE: to preserve Phase-8 identical rendering when NO new config keys are set, this is acceptable because: Phase-8 hardcoded `[0,0,5] fov 50` is replaced by `CAMERA_PRESETS['front'].position = [0,1.3,3.1] fov 20`. This is a deliberate reframing per RESEARCH A2 — the published page now uses the khavee-app-consistent framing. If the executor wants strict Phase-8 identical-appearance preservation, they should consult the planner — but PATTERNS A2 recommends fov=20 for consistency with the preset vectors that are now the only camera mechanism.). Apply `gl={{ alpha: config.bgTransparent === true }}` to the Canvas when transparent mode is on (Pitfall 6).
       - Inside Canvas: `<ambientLight intensity={scene.lightIntensity} />` (config-driven — was hardcoded 1) + `<directionalLight position={[10, 10, 5]} intensity={scene.directional} />` (was 2.5, still 2.5 via resolveSceneDefaults).
       - Render the avatar: `{avatarUrl && (isGlb ? <GLBAvatar src={avatarUrl} position={[scene.avatarOffsetX, scene.avatarOffsetY, 0]} scale={scene.avatarScale} /> : <VRMAvatar src={avatarUrl} position={[scene.avatarOffsetX, scene.avatarOffsetY, 0]} scale={scene.avatarScale} />)}`. The position prop drives the avatar offset; scale drives the avatar scale.

    3. Modify `mountAvatarInstance(root, config)`:
       - The OpenAIRealtimeProvider construction (lines 67-72) is UNCHANGED — same useProxy/proxyEndpoint/voice/instructions/model arguments.
       - The KhaveeProvider wrap is UNCHANGED — `<KhaveeProvider config={{ realtime: provider }}>`.
       - Replace the current `<div className="khaveeai-root">{avatarUrl ? <AvatarScene .../> : null}<ClickToTalkOverlay/><ErrorOverlay/></div>` with a layout structure that:
         a. Applies container styling on the outer `.khaveeai-root` div: width = config.containerWidth ? `${config.containerWidth}px` : (config.fullWidth ? '100%' : undefined); height = config.containerHeight ? `${config.containerHeight}px` : undefined; background: when !bgTransparent && bgType==='color' && bgColor → bgColor; when !bgTransparent && bgType==='image' && bgImageUrl → `url(${bgImageUrl})` (centered/cover); when bgTransparent → 'transparent'. (Pitfall 6: container CSS background, NOT scene.background.)
         b. Uses a flex layout for the avatar + chat when chatShow: `<div className={`khaveeai-layout khaveeai-layout--${config.chatPlacement ?? 'beside'}`}>` wrapping `{config.avatarUrl && <AvatarScene config={config} />}` and `{config.chatShow && <ChatBox placement={config.chatPlacement ?? 'beside'} />}`. The `.khaveeai-layout--beside` vs `--below` class drives flex-direction (CSS in styles.css — add a small layout block if not already present from Plan 09-04; if absent, add it inline here as a `<style>` tag is forbidden — add to styles.css).
         c. ClickToTalkOverlay and ErrorOverlay remain siblings of the layout div inside .khaveeai-root (they overlay the whole widget).
       - When `config.chatShow` is false, render only the AvatarScene + overlays (no ChatBox, no layout wrapper needed — the AvatarScene fills .khaveeai-root).

    4. To support the layout, append a small CSS block to `packages/wp-bundle/styles.css` for `.khaveeai-layout` and `.khaveeai-layout--beside` / `.khaveeai-layout--below` (flex container rules — beside = row, below = column). Keep it minimal; the ChatBox styles from Plan 09-04 already handle the chat panel itself.

    The lip-sync path runs automatically — VRMAvatar is inside KhaveeProvider, useRealtime's effect (useRealtime.ts:43-127) wires onAudioData → RealtimeAudioAnalyzer → setMultipleExpressions → VRMAvatar blend shapes. NO changes to useRealtime.ts or VRMAvatar.tsx. STUDIO-04's published-page requirement is satisfied by this automatic reuse.

    DO NOT remove the `export type { KhaveeAvatarConfig } from './config';` re-export (Plan 09-01 added it so index.ts's import path stays valid).
  </action>
  <verify>
    <automated>cd packages/wp-bundle && pnpm exec tsc --noEmit exits 0; grep -c "ChatBox\|resolveSceneDefaults\|config.chatShow\|avatarOffsetX\|lightIntensity" packages/wp-bundle/src/mount.tsx returns at least 5; node build.mjs exits 0 (khaveeai-bundle.js rebuilds successfully — the live bundle legitimately contains OpenAIRealtimeProvider, so the preview-only grep assertion does not apply); grep -c "khaveeai-layout" packages/wp-bundle/styles.css returns at least 2 (the layout classes exist)</automated>
  </verify>
  <acceptance_criteria>
    - packages/wp-bundle/src/mount.tsx imports ChatBox from './ui/ChatBox' and resolveSceneDefaults from './config'
    - AvatarScene signature is `({ config }: { config: KhaveeAvatarConfig })` — no longer takes bare avatarUrl
    - AvatarScene's <Canvas> uses camera={{ position: scene.cameraPosition, fov: scene.cameraFov }} (NOT the Phase-8 hardcoded [0,0,5]/50) — the values come from resolveSceneDefaults(config)
    - AvatarScene's <ambientLight intensity={scene.lightIntensity}/> uses the resolved (config-driven) value, not hardcoded 1
    - VRMAvatar/GLBAvatar receive position={[scene.avatarOffsetX, scene.avatarOffsetY, 0]} and scale={scene.avatarScale} props (config-driven offset + scale)
    - When config.bgTransparent is true, Canvas has gl={{ alpha: true }}; container div CSS background is transparent. When false, bgColor or bgImageUrl is applied to the container div CSS (NOT scene.background)
    - mountAvatarInstance renders {config.chatShow && <ChatBox placement={config.chatPlacement ?? 'beside'} />} as a sibling of AvatarScene inside a layout wrapper
    - The layout wrapper className includes `khaveeai-layout--beside` or `khaveeai-layout--below` based on config.chatPlacement
    - The container .khaveeai-root div applies width/height/fullWidth/bgColor/bgImageUrl/bgTransparent styles per config
    - The OpenAIRealtimeProvider construction is UNCHANGED (same constructor args as Phase 8)
    - VRMAvatar is still inside <KhaveeProvider> (lip-sync path preserved — automatic reuse)
    - useRealtime.ts and VRMAvatar.tsx are UNCHANGED (this plan touches neither)
    - styles.css contains .khaveeai-layout, .khaveeai-layout--beside, .khaveeai-layout--below rules
    - `pnpm exec tsc --noEmit` exits 0
    - `node build.mjs` exits 0 and khaveeai-bundle.js is regenerated
  </acceptance_criteria>
  <done>Published page renders a config-driven avatar (camera preset, lighting, scale, offset, background) + optional ChatBox (beside/below) inside KhaveeProvider. Lip-sync runs automatically via the existing useRealtime path (unchanged). Typecheck + build pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data-khaveeai-config JSON → published mount (mountAvatarInstance) | Config from the PHP backend (Plan 09-01's AvatarRenderer public_safe output) crosses into mount.tsx via JSON.parse in index.ts (unchanged), then into AvatarScene + ChatBox as the typed KhaveeAvatarConfig |
| Visitor ChatBox input → existing realtime session | Visitor text crosses into OpenAIRealtimeProvider.sendMessage (Phase-8 path, unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05-01 | Tampering / XSS | AvatarScene container CSS background (bgColor / bgImageUrl) | mitigate | bgColor is applied via React's `style={{ background: config.bgColor }}` (auto-escaped by React's CSS-property stringification — a malicious hex string like `#fff;<script>` cannot break out of the style attribute). bgImageUrl comes from wp_get_attachment_url (Plan 09-01 — Media Library attachment ID only, cast int, no free-form URL). No innerHTML/CSS injection path. |
| T-09-05-02 | Tampering | ChatBox text input path | mitigate | Text flows through useRealtime().sendMessage (Phase-8 tested) → OpenAIRealtimeProvider.sendMessage → existing realtime session. No new untrusted-input path introduced. ChatBox rendering of user/assistant bubbles uses {msg.text} (Plan 09-04 — React auto-escapes). |
| T-09-05-03 | Information Disclosure | resolveSceneDefaults / config values on published page | accept | All config values on the published page are already part of data-khaveeai-config (public_safe allowlist — Plan 09-01). The provider construction uses only voice/instructions/model/restUrl (no API key on the client — REST proxy mints the token server-side, unchanged Phase-6 pattern). No new disclosure. |
| T-09-05-04 | Denial of Service | Opening a second realtime connection for text chat | mitigate | UI-SPEC §Interaction-States "ChatBox — send text": text input shares the existing realtime session via sendMessage — does NOT open a second connection. The plan's acceptance criterion (sendMessage called, no second provider constructed) enforces this. |
| T-09-05-SC | Tampering | npm installs | accept | Plan installs zero new packages — ChatBox is already authored (Plan 09-04), config.ts already exists (Plan 09-01). No supply-chain risk. |
</threat_model>

<verification>
- `pnpm exec tsc --noEmit` from packages/wp-bundle exits 0
- `node packages/wp-bundle/build.mjs` exits 0 (khaveeai-bundle.js regenerated with the new config-driven mount + ChatBox)
- useRealtime.ts and VRMAvatar.tsx are unchanged (grep `git diff` should show no modifications to packages/react/)
- Existing Phase-8 published pages render with the new framing (fov=20, preset camera=[0,1.3,3.1]) — this is a deliberate change per RESEARCH A2 (khavee-app-consistent framing), documented in the action; if the team wants strict Phase-8 identical appearance they should run Plan 09-06's UAT and adjust the cameraPreset default accordingly
- Manual UAT (covered in Plan 09-06's checkpoint): published page, click ClickToTalk, speak, confirm avatar mouth animates during TTS (lip-sync reuse works), type in ChatBox, confirm reply arrives + joins the same session
</verification>

<success_criteria>
- AvatarScene is config-driven (camera preset vectors, lighting intensity, avatar scale, offset X/Y, background color/image/transparent)
- ChatBox mounts inside KhaveeProvider when config.chatShow, placed beside or below per config.chatPlacement
- ChatBox text input calls useRealtime().sendMessage — shares the existing realtime session, no second connection
- Lip-sync on the published page runs automatically via the existing useRealtime → RealtimeAudioAnalyzer → setMultipleExpressions → VRMAvatar path (NO new lip-sync code; useRealtime.ts/VRMAvatar.tsx unchanged)
- Existing Phase-8 shortcodes/blocks with no new config keys still render (forward-compatible — all new fields are optional with ?? fallbacks)
- Typecheck + build pass
</success_criteria>

<output>
Create `.planning/phases/09-block-studio-visual-config-chat-lipsync/09-05-SUMMARY.md` when done
</output>
