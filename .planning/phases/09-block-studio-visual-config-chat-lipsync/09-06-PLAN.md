---
phase: 09-block-studio-visual-config-chat-lipsync
plan: 06
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
  - 09-03
files_modified:
  - wordpress-plugin/includes/Plugin.php
autonomous: false
requirements:
  - STUDIO-02

must_haves:
  truths:
    - "The preview bundle (khaveeai-preview.js, built in Plans 09-01/09-03) is registered for enqueue ONLY via the enqueue_block_editor_assets hook (editor-only) — NEVER via wp_enqueue_scripts (Pitfall 5: would load the 400KB+ preview bundle on published pages)"
    - "The view bundle (khaveeai-bundle.js) is NOT enqueued in the editor — it stays on the Phase-8 AssetManager::enqueue() path called only from AvatarRenderer::render() on published pages"
    - "When an author opens the Gutenberg editor and selects the khaveeai/avatar block, the preview bundle mounts into the data-khaveeai-preview-config div from editor.js (Plan 09-02) and renders a live 3D VRM avatar — NO mic permission prompt fires, NO network request to /wp-json/khaveeai/v1/session appears in the editor's network tab (STUDIO-02 load-bearing safety property — live behavioral verification)"
    - "When the author publishes and views the page, the live SPA loads (Phase-8 view bundle), ClickToTalk fires the mic prompt + token mint NOW (not before), and the avatar + ChatBox (if enabled) are fully functional"
  artifacts:
    - path: "wordpress-plugin/includes/Plugin.php"
      provides: "Preview-bundle registration via wp_enqueue_script on enqueue_block_editor_assets (editor-only); view-bundle path unchanged"
      contains: "enqueue_block_editor_assets"
  key_links:
    - from: "wordpress-plugin/includes/Plugin.php (enqueue_block_editor_assets hook)"
      to: "wordpress-plugin/build/khaveeai-preview.js (the preview IIFE)"
      via: "wp_enqueue_script('khaveeai-preview', plugins_url('build/khaveeai-preview.js', KHAVEEAI_PLUGIN_FILE), ...)"
      pattern: "khaveeai-preview"
---

<objective>
Close the integration loop and verify the entire phase end-to-end with a live WordPress install. Wire the preview bundle (Plans 09-01/09-03) into the editor via `enqueue_block_editor_assets` in `Plugin.php`, then run the full UAT script from RESEARCH.md as a blocking human-verification checkpoint — with special attention to the load-bearing STUDIO-02 safety property (no mic prompt, no token mint in the editor).

Purpose: This is the only plan in the phase with a human-verify checkpoint. The bulk of Phase-9 acceptance is visual/runtime (RESEARCH.md Validation Architecture confirms no automated test framework for wp-bundle/wordpress-plugin). The single most valuable automated check (the build-time grep assertion, Plan 09-01) is already in place — this plan adds the BEHAVIORAL confirmation that the safety property holds in a real browser.

Output: One modified PHP file (Plugin.php — preview-bundle enqueue). Plus a blocking checkpoint where the human confirms the 8-step UAT script passes against a live WP install.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-block-studio-visual-config-chat-lipsync/09-CONTEXT.md
@.planning/phases/09-block-studio-visual-config-chat-lipsync/09-RESEARCH.md
@.planning/phases/09-block-studio-visual-config-chat-lipsync/09-PATTERNS.md
@.planning/phases/09-block-studio-visual-config-chat-lipsync/09-UI-SPEC.md
@.planning/phases/09-block-studio-visual-config-chat-lipsync/09-01-SUMMARY.md
@.planning/phases/09-block-studio-visual-config-chat-lipsync/09-02-SUMMARY.md
@.planning/phases/09-block-studio-visual-config-chat-lipsync/09-03-SUMMARY.md
@.planning/phases/09-block-studio-visual-config-chat-lipsync/09-05-SUMMARY.md
@wordpress-plugin/includes/Plugin.php
@wordpress-plugin/includes/Assets/AssetManager.php

<interfaces>
<!-- Existing AssetManager::enqueue() (the VIEW bundle's registration — UNCHANGED this plan).
     Source: wordpress-plugin/includes/Assets/AssetManager.php:47-73 -->

From AssetManager.php (the pattern to mirror for the preview bundle, with hook swap):
```php
// VIEW bundle (khaveeai-bundle.js) — enqueued ONLY by AvatarRenderer::render() on published pages.
// This plan does NOT touch this path.
const HANDLE = 'khaveeai-bundle';
wp_enqueue_script(
    self::HANDLE,
    plugins_url( 'build/khaveeai-bundle.js', KHAVEEAI_PLUGIN_FILE ),
    array(), // Deliberately empty — D-10 full isolation
    $version,
    array( 'in_footer' => true )
);

// PREVIEW bundle (khaveeai-preview.js) — this plan registers a parallel handle
// but enqueues it via add_action('enqueue_block_editor_assets', ...) in Plugin.php,
// NOT via AvatarRenderer. Editor-only.
```

UI-SPEC editor-preview banner copy (Plan 09-02 emits this in editor.js as the pre-mount fallback):
  Title: "Khavee AI Avatar — preview"
  Subtitle: "Live preview — view the published page to talk."

RESEARCH.md UAT script (8 steps — the human-checkpoint basis):
  1. Build all three artifacts (editor.js, khaveeai-bundle.js, khaveeai-preview.js)
  2. Activate the plugin in a local WP
  3. Add the block to a page; confirm NO mic prompt, NO network call to /wp-json/khaveeai/v1/session in the editor's network tab
  4. Drag each inspector slider; confirm the preview updates within one frame
  5. Toggle "Preview talking"; confirm mouth animates on a timer
  6. Publish; on the published page click "Click to talk"; confirm mic prompt + token mint fire NOW (not before)
  7. Type in the ChatBox; confirm assistant reply streams; confirm mouth animates during TTS
  8. Verify a second block instance on the same page renders independently
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the preview bundle for editor-only enqueue in Plugin.php (mirror AssetManager's pattern but on enqueue_block_editor_assets)</name>
  <read_first>
    - wordpress-plugin/includes/Plugin.php (the boot() method at lines 43-86 — the composition root; the existing add_action calls at lines 66, 73, 85; where to insert the new add_action for enqueue_block_editor_assets)
    - wordpress-plugin/includes/Assets/AssetManager.php (lines 47-73 — the existing view-bundle enqueue pattern: idempotency check via wp_script_is, filemtime-based version, deliberately-empty dependency array for D-10 full isolation)
    - .planning/phases/09-block-studio-visual-config-chat-lipsync/09-RESEARCH.md (Pitfall 5: "Conditional enqueue — preview bundle loading on published pages (or vice versa)" — the preview bundle MUST be enqueued ONLY via enqueue_block_editor_assets, NEVER via wp_enqueue_scripts; Open Question 1: enqueue_block_editor_assets is the recommended path over block.json editorScript array because it decouples the wp-scripts and esbuild build pipelines)
    - .planning/phases/09-block-studio-visual-config-chat-lipsync/09-PATTERNS.md (section "AssetManager enqueue shape" — the parallel-handle pattern for the preview bundle)
  </read_first>
  <action>
    Modify `wordpress-plugin/includes/Plugin.php`:

    1. Inside `Plugin::boot()`, after the existing block registration (`add_action( 'init', array( $block, 'register' ) );` at line 85), add a new `add_action( 'enqueue_block_editor_assets', array( __CLASS__, 'enqueue_preview_bundle' ) );` call. Using `__CLASS__` (the static method pattern) matches the existing static boot() style.

    2. Add a new public static method `enqueue_preview_bundle()` to the Plugin class. The method mirrors AssetManager::enqueue() but for the preview bundle, with these specifics:
       - Idempotency: `if ( wp_script_is( 'khaveeai-preview', 'enqueued' ) ) { return; }`
       - Version: `$preview_path = plugin_dir_path( KHAVEEAI_PLUGIN_FILE ) . 'build/khaveeai-preview.js'; $version = file_exists( $preview_path ) ? (string) filemtime( $preview_path ) : KHAVEEAI_VERSION;`
       - Enqueue: `wp_enqueue_script( 'khaveeai-preview', plugins_url( 'build/khaveeai-preview.js', KHAVEEAI_PLUGIN_FILE ), array(), $version, array( 'in_footer' => false ) );` — note `in_footer: false` for the preview (it should load in the editor head/body, not foot, so the preview mounts promptly when the block is selected). The dependency array is deliberately empty (D-10 full isolation — preview bundle bundles its own React 19, does NOT depend on wp-element).
       - Optional: register the preview bundle's CSS too if the build emits a separate khaveeai-preview.css (the build.mjs in Plan 09-01 may emit CSS inline via the css loader — check; if so, no separate style enqueue needed). If esbuild emits khaveeai-bundle.css that covers both entries (likely, since both import "../styles.css"), then the existing view-side CSS path is fine and no preview-side CSS enqueue is needed.

    3. Add a brief docblock to the new method explaining: this enqueues the preview bundle (built by esbuild from packages/wp-bundle/src/preview.ts) ONLY in the block editor, that the bundle is the safe-preview IIFE whose import graph excludes OpenAIRealtimeProvider (STUDIO-02), and that it is intentionally NOT enqueued on wp_enqueue_scripts (Pitfall 5 — would load the 400KB+ preview bundle on published pages, a site-wide perf regression).

    Do NOT touch the existing AssetManager, AvatarRenderer, or the view-bundle enqueue path — those stay on the Phase-8 published-page-only path.
  </action>
  <verify>
    <automated>php -l wordpress-plugin/includes/Plugin.php exits 0; grep -c "enqueue_block_editor_assets\|khaveeai-preview\|enqueue_preview_bundle" wordpress-plugin/includes/Plugin.php returns at least 3; grep -c "wp_enqueue_scripts" wordpress-plugin/includes/Plugin.php returns 0 for the preview-bundle lines (the view bundle uses AssetManager, not wp_enqueue_scripts directly — confirm no NEW wp_enqueue_scripts call is added for the preview)</automated>
  </verify>
  <acceptance_criteria>
    - wordpress-plugin/includes/Plugin.php contains a new public static method enqueue_preview_bundle()
    - boot() contains `add_action( 'enqueue_block_editor_assets', array( __CLASS__, 'enqueue_preview_bundle' ) );`
    - enqueue_preview_bundle() calls wp_enqueue_script with handle 'khaveeai-preview', pointing at plugins_url('build/khaveeai-preview.js', KHAVEEAI_PLUGIN_FILE)
    - The dependency array passed to wp_enqueue_script is empty (D-10 full isolation — preview bundles its own React)
    - enqueue_preview_bundle() does NOT call wp_enqueue_scripts and is NOT registered on wp_enqueue_scripts (grep "wp_enqueue_scripts" returns 0 — only enqueue_block_editor_assets is used)
    - The method has an idempotency guard (wp_script_is check) and uses filemtime-based version
    - The existing AssetManager path (view bundle) is unchanged
    - `php -l wordpress-plugin/includes/Plugin.php` exits 0
  </acceptance_criteria>
  <done>Preview bundle is registered for editor-only enqueue via enqueue_block_editor_assets in Plugin.php. View bundle path unchanged. PHP lint passes. The preview bundle will load in the block editor and nowhere else.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The full Phase-9 Block Studio is wired end-to-end. Plans 09-01 through 09-05 + this plan's Task 1 deliver: (1) 14 new visual/chat config keys flowing through the existing data-khaveeai-config pipeline, (2) a 7-panel inspector with the locked Tier-1 knob set, (3) a structurally-isolated safe preview bundle that renders a live 3D VRM avatar in the editor with NO mic/token, (4) a dependency-free ChatBox functional on the published page, (5) automatic SDK lip-sync reuse on the published page + a no-audio viseme-cycler demo in the editor. This checkpoint confirms the load-bearing STUDIO-02 safety property behaviorally AND the full UAT passes.
  </what-built>
  <how-to-verify>
    Prerequisites: a local WordPress install (wp-env or equivalent) with the khavee-sdk plugin activated and a real OpenAI API key configured in Settings. Build all three artifacts before starting:
    - `cd wordpress-plugin && pnpm exec wp-scripts build` (regenerates assets/editor.js)
    - `cd packages/wp-bundle && node build.mjs` (regenerates build/khaveeai-bundle.js + build/khaveeai-preview.js + the safety grep assertion must pass)
    - Confirm `wordpress-plugin/build/khaveeai-preview.js` exists and `grep -c "RealtimeProvider\|getUserMedia\|ephemeral" wordpress-plugin/build/khaveeai-preview.js` returns 0 (the automated safety check).

    Then run the RESEARCH.md UAT script (8 steps) against the live WP install:

    **STUDIO-02 SAFETY (load-bearing — Step 3 is the critical check):**
    1. In WP admin, create/edit a page, add the Khavee AI Avatar block.
    2. SELECT the block. Open the browser DevTools Network tab. Filter for "session".
    3. **CRITICAL**: Confirm NO mic permission prompt appears. Confirm NO network request to `/wp-json/khaveeai/v1/session` appears in the network tab. (If EITHER fires, the safety property is broken — stop and report. The structural entry-point isolation + build-time grep assertion have failed somewhere.)
    4. Confirm the block shows a LIVE 3D VRM avatar (not a static placeholder). If no avatar is configured globally, set one in Settings first.

    **STUDIO-01 inspector:**
    5. Drag each inspector slider (container width/height, light intensity, avatar scale/offset X/Y). Confirm the preview updates within one frame (WYSIWYG-reactive). Confirm the camera preset dropdown (Front/Left Angle/Right Angle/Wide) reframes the avatar. Confirm the mutual-exclusivity rules: toggle Transparent Background ON → color + image pickers grey out; set Background type to Image → color picker greys out; toggle Chat Show OFF → placement dropdown greys out.
    6. Confirm the inspector has exactly 7 panels: Layout, Background, Lighting, Avatar, Camera, Voice & Behavior, Chat Box. Confirm all labels match UI-SPEC (e.g. "Container width (px)", "Light intensity", "Camera preset").

    **STUDIO-04 editor preview-talking:**
    7. In the Chat Box panel, toggle "Preview talking" ON. Confirm the avatar's mouth animates on a timer (viseme cycling at ~4Hz). Confirm a "Preview talking" pill label appears in the preview corner. Toggle OFF — mouth returns to neutral.

    **STUDIO-03 + STUDIO-04 published (live):**
    8. Enable Chat Show in the inspector. Publish the page. Open the published page in a new tab (or as a logged-out visitor).
    9. Confirm the live SPA loads. Confirm NO mic prompt fires on page load (only on click).
    10. Click "Click to talk". Confirm the mic permission prompt fires NOW. Confirm a network request to `/wp-json/khaveeai/v1/session` fires NOW (the token mint). Speak — confirm the avatar replies.
    11. While the assistant is speaking, confirm the avatar's mouth animates (real lip-sync — SDK reuse).
    12. Type a message in the ChatBox. Press Enter. Confirm: (a) your message appears as a right-aligned accent-bg bubble immediately, (b) the assistant's reply streams in as a left-aligned neutral-bg bubble, (c) the transcript auto-scrolls to bottom. Confirm Shift+Enter inserts a newline (does not send).
    13. Scroll up in the transcript. Have the assistant send another reply (e.g. speak to it). Confirm the transcript does NOT yank you to the bottom (pinned-to-bottom rule respected).
    14. Add a SECOND Khavee AI Avatar block to the same page (different settings). Publish. Confirm both blocks render and behave independently (no shared state).

    Report: if all 14 steps pass, type "approved" and the phase is complete. If any step fails, describe which step + observed behavior — the plan set will be revised (likely a gap-closure plan).
  </how-to-verify>
  <resume-signal>Type "approved" or describe the failing step + observed behavior</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Editor iframe → published page (preview bundle must NOT leak to published) | The preview bundle (khaveeai-preview.js) is registered for editor-only enqueue via enqueue_block_editor_assets — it must NEVER appear on a published page (Pitfall 5) |
| Editor iframe → OpenAI token endpoint (STUDIO-02 load-bearing) | The preview bundle's import graph must never reach the token endpoint. Structural isolation (Plan 09-03) + build-time grep (Plan 09-01) + this plan's behavioral UAT (Step 3) are the three layers of defense. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06-01 | DoS / Information Disclosure | Preview bundle enqueued on published pages (Pitfall 5) | mitigate | enqueue_preview_bundle() is registered ONLY on `enqueue_block_editor_assets` (WP core fires this hook exclusively in the block editor, never on the front-end). The acceptance criterion (grep wp_enqueue_scripts returns 0 for preview lines) plus the human-verify UAT Step 8 (published page network tab should NOT show khaveeai-preview.js) enforce this. The view bundle stays on the Phase-8 AssetManager path. |
| T-09-06-02 | DoS / Information Disclosure | Preview bundle fires mic/token in editor (STUDIO-02) | mitigate | THREE LAYERS verified by this plan's checkpoint: (1) Structural — Plan 09-03's preview.ts import graph excludes the provider. (2) Build-time — Plan 09-01's grep assertion fails the build if khaveeai-preview.js matches /RealtimeProvider\|getUserMedia\|ephemeral/. (3) Behavioral — UAT Step 3 confirms no mic prompt + no /session network call when the block is selected in the editor. Any layer catching a violation blocks the phase. |
| T-09-06-03 | Denial of Service | Editor undo/redo WebGL context leak (Pitfall 3) | mitigate | Plan 09-03's MutationObserver pushes config into React state instead of unmounting/remounting. UAT Step 5 (rapid slider drags) + Step 14 (two block instances) exercise this — if contexts leak, the canvas goes black and the step fails. |
| T-09-06-SC | Tampering | npm installs | accept | Plan installs zero new packages. No supply-chain risk. |
</threat_model>

<verification>
- `php -l wordpress-plugin/includes/Plugin.php` exits 0
- `grep -c "enqueue_block_editor_assets" wordpress-plugin/includes/Plugin.php` returns at least 1
- `grep -c "wp_enqueue_scripts" wordpress-plugin/includes/Plugin.php` returns 0 for the preview-bundle code path (the existing view-bundle path uses AssetManager, which is called from AvatarRenderer::render — neither path uses wp_enqueue_scripts directly for the khaveeai bundles)
- Build artifacts regenerated: wordpress-plugin/assets/editor.js, wordpress-plugin/build/khaveeai-bundle.js, wordpress-plugin/build/khaveeai-preview.js
- `grep -c "RealtimeProvider\|getUserMedia\|ephemeral" wordpress-plugin/build/khaveeai-preview.js` returns 0 (the STUDIO-02 automated safety check)
- Human-verify checkpoint: all 14 UAT steps pass against a live WP install (the behavioral safety + functional verification)
</verification>

<success_criteria>
- Preview bundle registered for editor-only enqueue (enqueue_block_editor_assets); view bundle path unchanged
- The human-verify checkpoint passes: in the editor, NO mic prompt and NO /session network call fire when the block is selected (STUDIO-02 load-bearing)
- Live 3D VRM preview renders in the editor; inspector sliders are WYSIWYG-reactive; preview-talking animates mouth
- Published page: ClickToTalk fires mic+token on click (not before); ChatBox text input works; lip-sync animates mouth during TTS; two block instances behave independently
- The full Phase-9 success criteria (ROADMAP Criteria 1-5) are satisfied end-to-end
</success_criteria>

<output>
Create `.planning/phases/09-block-studio-visual-config-chat-lipsync/09-06-SUMMARY.md` when done
</output>
