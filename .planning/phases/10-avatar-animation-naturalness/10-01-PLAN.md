---
phase: 10-avatar-animation-naturalness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/react/src/VRMAvatar.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "When chatStatus becomes 'ready', an animation key named 'idle' (if present) auto-plays"
    - "When chatStatus becomes 'listening' or 'thinking', a matching animation key auto-plays if present"
    - "When chatStatus transitions into 'speaking', a random key containing 'speak'/'talk'/'gesture' is chosen and played"
    - "A new speaking variant is picked only on transition INTO 'speaking', not on every render while speaking"
    - "If no matching key exists for a status, the current animation continues with no error thrown"
    - "Changing the animations prop reference does not cause the mapping to read stale keys"
  artifacts:
    - path: "packages/react/src/VRMAvatar.tsx"
      provides: "chatStatus-to-animation auto-mapping useEffect and speaking-variety picker"
      contains: "prevChatStatusRef"
  key_links:
    - from: "packages/react/src/VRMAvatar.tsx"
      to: "useKhavee().chatStatus"
      via: "context destructure"
      pattern: "chatStatus"
    - from: "packages/react/src/VRMAvatar.tsx"
      to: "useKhavee().animate"
      via: "animate(key) call inside chatStatus effect"
      pattern: "animate\\("
---

<objective>
Add built-in `chatStatus` → animation auto-mapping to `VRMAvatar`, including the speaking-variety random picker. Developers who name animation keys `idle`/`listening`/`thinking`/`speaking` (or include `speak`/`talk`/`gesture` in a key) get sensible playback with zero manual `useEffect` wiring.

Purpose: Removes boilerplate wiring so the avatar reacts to conversation state out of the box (D-01, D-02, D-03).
Output: New refs + one `useEffect` in `VRMAvatar.tsx`. Pure logic, no bone access — testable in isolation and independent of the procedural layer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-avatar-animation-naturalness/10-CONTEXT.md
@.planning/phases/10-avatar-animation-naturalness/10-RESEARCH.md

<interfaces>
<!-- From packages/core/src/types/conversation.ts — ChatStatus has NO 'idle' value -->
type ChatStatus = 'ready' | 'speaking' | 'listening' | 'thinking' | 'stopped' | 'starting'

<!-- From packages/react/src/KhaveeProvider.tsx — context surface already exposes these -->
useKhavee(): { chatStatus: ChatStatus; animate: (name: string) => void; currentAnimation: string | null; ... }

<!-- From packages/react/src/VRMAvatar.tsx line 187 — existing destructure to extend -->
const { setVrm, expressions, currentAnimation, animate } = useKhavee();
<!-- animations prop is AnimationConfig = { [name: string]: string } -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read chatStatus from context and add stale-safe refs</name>
  <read_first>packages/react/src/VRMAvatar.tsx (lines 178-210 — props signature and useKhavee destructure)</read_first>
  <files>packages/react/src/VRMAvatar.tsx</files>
  <action>
    Extend the `useKhavee()` destructure at line ~187 to also pull `chatStatus`.
    Add three refs near the existing blink refs (~line 196): `prevChatStatusRef` initialized to `null` (typed `ChatStatus | null`), `animationsRef` initialized to `animations`, and `currentSpeakingAnimRef` initialized to `null` (string | null) to remember the chosen variant for the active speaking turn.
    Import the `ChatStatus` type from `@khaveeai/core`. Keep a `useEffect([animations])` that assigns `animationsRef.current = animations` so the mapping reads the latest keys (Pitfall 3 stale-closure guard).
  </action>
  <verify>grep -n "chatStatus" packages/react/src/VRMAvatar.tsx | grep -v '^\s*\*'</verify>
  <acceptance_criteria>
    `chatStatus` is destructured from `useKhavee()`. `prevChatStatusRef`, `animationsRef`, `currentSpeakingAnimRef` exist as `useRef`. A `useEffect` with dep `[animations]` syncs `animationsRef.current`. `ChatStatus` is imported from `@khaveeai/core`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement chatStatus→animation mapping effect with speaking variety</name>
  <read_first>packages/react/src/VRMAvatar.tsx (lines 313-345 — existing currentAnimation crossfade effect, to mirror lookup semantics)</read_first>
  <files>packages/react/src/VRMAvatar.tsx</files>
  <action>
    Add a `useEffect` with deps `[chatStatus, animate]`. Early-return if `chatStatus === prevChatStatusRef.current`. Capture the prior value, then set `prevChatStatusRef.current = chatStatus`.
    Read `const animKeys = Object.keys(animationsRef.current || {})`.
    If `chatStatus === "speaking"`: filter keys with `/speak|talk|gesture/i.test(key)`; if the array is non-empty, pick `variants[Math.floor(Math.random() * variants.length)]`, store it in `currentSpeakingAnimRef.current`, and call `animate(pick)` (D-02). Re-roll happens only here because the effect only runs on a real transition.
    Else (non-speaking): clear `currentSpeakingAnimRef.current = null`; compute `targetKey = chatStatus === "ready" ? "idle" : chatStatus` (D-01, 'idle' maps to 'ready' per research — ChatStatus has no 'idle'); if `animKeys.includes(targetKey)` call `animate(targetKey)`, otherwise do nothing (D-03, no throw).
  </action>
  <verify>cd packages/react && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "VRMAvatar" || echo "no VRMAvatar type errors"</verify>
  <acceptance_criteria>
    Effect early-returns when chatStatus is unchanged (guards Pitfall 5). Speaking branch uses case-insensitive `speak|talk|gesture` substring match and random index. Non-speaking branch maps `ready`→`idle` and only calls `animate` when the key exists. No code path throws when a key is absent. `tsc --noEmit` reports no new errors in VRMAvatar.tsx.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure in-process React/Three.js animation logic; no network, no user input parsing, no untrusted data crosses a boundary in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Denial of Service | chatStatus effect re-running | accept | Effect early-returns on unchanged status; runs at most once per turn transition, negligible cost |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new packages installed this phase (RESEARCH: no installs); nothing to audit |
</threat_model>

<verification>
- `npx tsc --noEmit` in `packages/react` passes with no new VRMAvatar errors.
- Manual trace: setting `animations={{ idle, listening, talk1, gesture_wave }}` and cycling chatStatus ready→listening→speaking→ready calls `animate` with `idle`, `listening`, one of `talk1|gesture_wave`, `idle` respectively.
- No `throw` added; missing keys are silent.
</verification>

<success_criteria>
- chatStatus transitions drive `animate()` per D-01/D-02/D-03.
- Speaking variant re-rolls only on transition into `speaking`.
- animations prop changes are read fresh via `animationsRef`.
</success_criteria>

<output>
Create `.planning/phases/10-avatar-animation-naturalness/10-01-SUMMARY.md` when done.
</output>
