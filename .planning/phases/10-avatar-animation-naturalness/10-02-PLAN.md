---
phase: 10-avatar-animation-naturalness
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - packages/react/src/VRMAvatar.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "The avatar breathes: spine and chest bones oscillate subtly with no FBX file required"
    - "The avatar's head drifts with smooth multi-octave noise while any state is active"
    - "The avatar's eye gaze shifts naturally via a drifting invisible lookAt target"
    - "The avatar's fingers curl subtly with per-finger phase-offset noise"
    - "Each procedural layer is additive on top of the playing FBX animation and does not freeze it"
    - "Each layer is individually togglable via a boolean prop defaulting to true"
    - "Missing optional bones (fingers, chest) are skipped without throwing"
  artifacts:
    - path: "packages/react/src/VRMAvatar.tsx"
      provides: "Procedural bone deltas + eye gaze applied between mixer.update and vrm.update"
      contains: "getNormalizedBoneNode"
  key_links:
    - from: "packages/react/src/VRMAvatar.tsx useFrame"
      to: "vrm.humanoid.getNormalizedBoneNode"
      via: "quaternion.multiply delta after mixer.update, before vrm.update"
      pattern: "getNormalizedBoneNode\\("
    - from: "packages/react/src/VRMAvatar.tsx"
      to: "vrm.lookAt.target"
      via: "invisible Object3D added to vrm.scene, drifted each frame"
      pattern: "lookAt.target"
---

<objective>
Add the procedural bone/gaze life layer to `VRMAvatar`: breathing (spine/chest), head micro-movement, eye gaze via `lookAt.target`, and per-finger curl noise. All applied inside the existing `useFrame` loop AFTER `mixer.update(delta)` and BEFORE `vrm.update(delta)`, additive on top of FBX.

Purpose: Make the avatar feel continuously alive even when no FBX clip is playing (D-04, D-05, D-06, D-08).
Output: New refs, one gaze-target `useEffect`, and delta-application blocks in `useFrame` of `VRMAvatar.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-avatar-animation-naturalness/10-RESEARCH.md

<interfaces>
<!-- Verified @pixiv/three-vrm 3.4.2 API (RESEARCH: VRM API Reference) -->
vrm.humanoid.getNormalizedBoneNode(boneName: string): THREE.Object3D | null   // USE THIS (mixer drives normalized bones)
vrm.lookAt.target: THREE.Object3D | null      // set to drifting invisible Object3D
vrm.lookAt.autoUpdate: boolean                // true by default; vrm.update() applies it

<!-- Bone name strings (camelCase, verified): "spine","chest","head" -->
<!-- Finger proximal bones: left/right + Index/Middle/Ring/Little/Thumb + "Proximal" -->

<!-- Existing useFrame order in VRMAvatar.tsx lines 379-436:
     mixer.update(delta) → expression lerp → blinking → vrm.update(delta)
     Insert procedural bone deltas AFTER mixer.update, BEFORE vrm.update. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add procedural props, per-frame refs, and pre-allocated scratch objects</name>
  <read_first>packages/react/src/VRMAvatar.tsx (lines 19-26 props interface; 178-197 signature + blink refs)</read_first>
  <files>packages/react/src/VRMAvatar.tsx</files>
  <action>
    Add optional boolean props to `VRMAvatarProps`: `enableBreathing`, `enableHeadMovement`, `enableEyeGaze`, `enableHandGestures` (each `?: boolean`). Default all to `true` in the destructure (D-04). Leave `enableMicroExpressions` for plan 10-03.
    Add refs (all `useRef`, per Pattern 5 — never useState): `breathTimeRef`, `headTimeRef`, `gazeTimeRef`, `fingerTimeRef` (each `useRef(0)`), and `gazeTargetRef` (`useRef<THREE.Object3D | null>(null)`).
    Add MODULE-LEVEL scratch constants above the component to avoid per-frame GC (RESEARCH anti-pattern): reusable `THREE.Vector3` axes (X = (1,0,0), Y = (0,1,0)) and reusable `THREE.Quaternion` instances for breath/head/finger. Also define module-level constant arrays of the 5 left + 5 right finger proximal bone names (`leftFingerBones`, `rightFingerBones`) and a `fingerPhases` array `[0, 1.1, 2.3, 0.7, 1.9]` — camelCase per CLAUDE.md (lookup/template constants, not test fixtures).
  </action>
  <verify>grep -n "enableBreathing\|enableEyeGaze\|getNormalizedBoneNode\|fingerPhases" packages/react/src/VRMAvatar.tsx | grep -v '^\s*\*'</verify>
  <acceptance_criteria>
    Four new boolean props exist in `VRMAvatarProps` and default to `true`. Time refs and `gazeTargetRef` exist. Scratch Vector3/Quaternion objects and finger-bone-name arrays are declared at module scope (not inside useFrame). No `new THREE.Quaternion()` or `new THREE.Vector3()` appears inside the `useFrame` callback body.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire eye-gaze lookAt target lifecycle</name>
  <read_first>packages/react/src/VRMAvatar.tsx (lines 347-362 — existing currentVrm setup effect)</read_first>
  <files>packages/react/src/VRMAvatar.tsx</files>
  <action>
    Add a `useEffect` with deps `[currentVrm, enableEyeGaze]`. Guard: return early if `!currentVrm` or `!enableEyeGaze`. Create `const gazeObj = new THREE.Object3D()`, set `gazeObj.position.set(0, 1.6, 2.0)`, `currentVrm.scene.add(gazeObj)` (Pitfall 6 — add to vrm.scene), assign `gazeTargetRef.current = gazeObj`, `currentVrm.lookAt.target = gazeObj`, `currentVrm.lookAt.autoUpdate = true` (D-05).
    Return a cleanup that runs `currentVrm.scene.remove(gazeObj)`, sets `currentVrm.lookAt.target = null`, and `gazeTargetRef.current = null` (Open Question 3 — cleanup tied to currentVrm change).
  </action>
  <verify>grep -n "lookAt.target\|scene.add(gazeObj)\|scene.remove(gazeObj)" packages/react/src/VRMAvatar.tsx</verify>
  <acceptance_criteria>
    A `useEffect([currentVrm, enableEyeGaze])` creates an Object3D, adds it to `currentVrm.scene`, assigns it to `vrm.lookAt.target`, and its cleanup removes it and nulls the target. No manual `vrm.lookAt.lookAt()` call is present (autoUpdate handles it).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Apply breathing, head, gaze, and finger deltas in useFrame</name>
  <read_first>packages/react/src/VRMAvatar.tsx (lines 379-436 — full useFrame body and existing order)</read_first>
  <files>packages/react/src/VRMAvatar.tsx</files>
  <action>
    Inside `useFrame`, immediately AFTER `mixerRef.current.update(delta)` and BEFORE `currentVrm.update(delta)` (Pitfall 1 — order is fixed), insert guarded blocks. All read bones via `vrm.humanoid.getNormalizedBoneNode(name)` and null-check before `.quaternion.multiply(...)` (Pitfall 4).
    Breathing (if `enableBreathing`): advance `breathTimeRef`, `offset = Math.sin(breathTimeRef.current * 1.2) * 0.020`, set the breath scratch quaternion via `setFromAxisAngle(X, offset)`, multiply into `spine` and `chest` quaternions (D-06).
    Head (if `enableHeadMovement`): advance `headTimeRef`; compute two-octave `headX` (~0.018+0.009) and `headY` (~0.022+0.011) sine sums (Pattern 4 frequencies 0.19–0.67); build X and Y scratch quaternions and multiply both into `head` (D-06, ±0.02–0.04 rad).
    Gaze (if `enableEyeGaze` and `gazeTargetRef.current`): advance `gazeTimeRef`; set `gazeTargetRef.current.position.x`/`.y` from slow sine sums around base (0, 1.6, 2.0) (D-05, Pattern 2). No bone writes here — vrm.update applies it.
    Fingers (if `enableHandGestures`): advance `fingerTimeRef`; iterate the combined 10 finger bone names, per index use `fingerPhases[i % 5]` and freq `0.7 + (i % 3) * 0.17`, `offset = Math.sin(t*freq+phase) * 0.018`, set the finger scratch quaternion and multiply into each present bone (D-08).
  </action>
  <verify>cd packages/react && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "VRMAvatar" || echo "no VRMAvatar type errors"</verify>
  <acceptance_criteria>
    All four delta blocks live between `mixer.update(delta)` and `currentVrm.update(delta)`. Every bone access is null-guarded. Each block is gated on its `enable*` prop. Gaze block only moves the target Object3D (no eye-bone writes). `tsc --noEmit` passes with no new VRMAvatar errors.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Per-frame Three.js scene-graph math only; no network or untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Denial of Service | useFrame @ 60fps | mitigate | Pre-allocate scratch Vector3/Quaternion at module scope; zero allocations per frame (RESEARCH anti-pattern guard) |
| T-10-03 | Tampering | Missing optional bones | mitigate | Null-check every getNormalizedBoneNode result before quaternion writes (Pitfall 4) |
| T-10-SC | Tampering | npm installs | accept | Zero new packages this phase |
</threat_model>

<verification>
- `npx tsc --noEmit` in `packages/react` passes, no new VRMAvatar errors.
- Delta blocks are physically located between `mixerRef.current.update(delta)` and `currentVrm.update(delta)`.
- Setting any `enable*` prop to `false` removes that layer (block is gated).
- On a VRM lacking finger bones, no runtime throw (null guards).
- No `new THREE.*` allocations inside `useFrame`.
</verification>

<success_criteria>
- Breathing, head, gaze, finger layers run additively over FBX (Success Criteria 1-3, 6).
- Each layer individually togglable, defaults true (D-04).
- Correct normalized-bone / order-of-operations per D-06.
</success_criteria>

<output>
Create `.planning/phases/10-avatar-animation-naturalness/10-02-SUMMARY.md` when done.
</output>
