---
phase: 10-avatar-animation-naturalness
plan: 03
type: execute
wave: 3
depends_on:
  - 10-02
files_modified:
  - packages/react/src/VRMAvatar.tsx
  - packages/react/src/index.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "The avatar plays subtle curated micro-expressions that vary by chatStatus"
    - "Micro-expressions change every 3-8 seconds with a smooth lerp"
    - "Micro-expressions blend additively with developer-set expressions, capped at 1.0 per name"
    - "Speaking status produces no micro-expressions (audio drives expression during speech)"
    - "enableMicroExpressions toggles the layer; defaults to true"
    - "VRMAvatarProps and JSDoc document all new props; the barrel still exports the public surface"
  artifacts:
    - path: "packages/react/src/VRMAvatar.tsx"
      provides: "Micro-expression scheduler in useFrame + updated props type and JSDoc"
      contains: "enableMicroExpressions"
  key_links:
    - from: "packages/react/src/VRMAvatar.tsx useFrame"
      to: "vrm.expressionManager.setValue"
      via: "lerp toward scheduled targets, before vrm.update"
      pattern: "expressionManager"
---

<objective>
Add the built-in micro-expression scheduler (D-07) to `VRMAvatar`, finalize the `VRMAvatarProps` type and JSDoc for all new props, and confirm the barrel export surface. Micro-expressions blend additively with developer-set expressions and are capped at 1.0 per expression name (Open Question 2 resolution).

Purpose: Perceived personality between blinks without dramatic swings; complete the public API surface for the phase.
Output: A micro-expression block + refs in `VRMAvatar.tsx`, updated props/JSDoc, verified `packages/react/src/index.ts` exports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-avatar-animation-naturalness/10-RESEARCH.md

<interfaces>
<!-- Verified @pixiv/three-vrm 3.4.2 -->
vrm.expressionManager.setValue(name: string, weight: number): void
vrm.expressionManager.getValue(name: string): number | null   // null if model lacks the expression

<!-- Existing helper in VRMAvatar.tsx line 364 — reuse pattern -->
lerpExpression(name, value, lerpFactor)  // reads current, lerps toward value, calls setValue

<!-- D-07 schedule (effectiveStatus maps 'ready' -> 'idle'):
     idle      -> { relaxed: 0.06-0.10 }
     listening -> { happy: 0.10-0.15, surprised: 0.04-0.06 }
     thinking  -> { neutral: 0.08-0.12 }
     speaking / other -> {} (none) -->

<!-- Existing useFrame applies developer expressions (lines 388-392) BEFORE blinking, BEFORE vrm.update.
     Micro-expressions must be applied in the same window (before vrm.update, Pitfall 2). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add enableMicroExpressions prop and scheduler refs</name>
  <read_first>packages/react/src/VRMAvatar.tsx (lines 19-26 props; 178-197 signature; 388-392 expression apply block)</read_first>
  <files>packages/react/src/VRMAvatar.tsx</files>
  <action>
    Add `enableMicroExpressions?: boolean` to `VRMAvatarProps`, default `true` in the destructure (D-04).
    Add refs (all useRef): `microExprTimeRef` (`useRef(0)`), `nextExprChangeRef` (`useRef(3 + Math.random() * 5)` — seconds until first change), and `currentExprTargetsRef` (`useRef<Record<string, number>>({})`).
  </action>
  <verify>grep -n "enableMicroExpressions\|currentExprTargetsRef\|nextExprChangeRef" packages/react/src/VRMAvatar.tsx | grep -v '^\s*\*'</verify>
  <acceptance_criteria>
    `enableMicroExpressions` prop exists and defaults to `true`. The three scheduler refs exist as `useRef`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement micro-expression scheduler in useFrame</name>
  <read_first>packages/react/src/VRMAvatar.tsx (lines 379-436 — useFrame body; 364-377 lerpExpression)</read_first>
  <files>packages/react/src/VRMAvatar.tsx</files>
  <action>
    Inside `useFrame`, after the developer-expression apply loop and BEFORE `currentVrm.update(delta)` (Pitfall 2), add a block gated on `enableMicroExpressions`.
    Advance `microExprTimeRef`. When `microExprTimeRef.current >= nextExprChangeRef.current`, set `nextExprChangeRef.current = microExprTimeRef.current + 3 + Math.random() * 5` (3-8s) and re-roll `currentExprTargetsRef.current` from the D-07 schedule using `effectiveStatus = chatStatus === "ready" ? "idle" : chatStatus`: idle→`{relaxed}` (0.06-0.10), listening→`{happy, surprised}` (0.10-0.15 / 0.04-0.06), thinking→`{neutral}` (0.08-0.12), else `{}`.
    Each frame, iterate `currentExprTargetsRef.current`; for each `name` guard `expressionManager.getValue(name) !== null`; compute additive base = current developer value from `expressions[name]` (or 0), target the micro-value ADDED on top, and lerp the manager value toward `Math.min(1, base + microTarget)` (Open Question 2 — additive, capped at 1.0). Use a slow lerp factor (e.g. `delta * 0.8`) for smooth transitions. Do NOT setValue for names outside the current target set (let developer/blink values stand).
  </action>
  <verify>cd packages/react && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "VRMAvatar" || echo "no VRMAvatar type errors"</verify>
  <acceptance_criteria>
    Block is gated on `enableMicroExpressions` and sits before `currentVrm.update(delta)`. Schedule re-rolls on a 3-8s interval. `effectiveStatus` maps `ready`→`idle`; `speaking` yields no targets. Each expression is null-guarded and the composed value is capped at 1.0 additively over the developer value. `tsc --noEmit` passes with no new VRMAvatar errors.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update JSDoc, props documentation, and verify barrel exports</name>
  <read_first>packages/react/src/VRMAvatar.tsx (lines 76-186 component JSDoc + props); packages/react/src/index.ts (full file)</read_first>
  <files>packages/react/src/VRMAvatar.tsx, packages/react/src/index.ts</files>
  <action>
    Update the `VRMAvatar` component JSDoc: remove/replace the stale `@param enableTalkingAnimations` line (that prop does not exist), and add `@param` entries for `enableBreathing`, `enableHeadMovement`, `enableEyeGaze`, `enableMicroExpressions`, `enableHandGestures` — each noting "Default: true" and one-line behavior. Add a `/** ... */` single-line doc comment above each new field in the `VRMAvatarProps` interface (units/defaults per project convention).
    Add a brief section-divider comment `// ── Procedural life layer ──` before the new refs group for readability (large-class convention).
    Verify `packages/react/src/index.ts` still exports `VRMAvatar` and the `AnimationConfig` type. If `VRMAvatarProps` is not exported and would be useful to consumers, add `export type { VRMAvatarProps }` to the VRMAvatar export block — but only after exporting the interface (add `export` keyword to `interface VRMAvatarProps` if missing).
  </action>
  <verify>grep -n "enableBreathing\|enableMicroExpressions\|enableHandGestures" packages/react/src/VRMAvatar.tsx | grep '\*' ; grep -n "VRMAvatar\|AnimationConfig" packages/react/src/index.ts</verify>
  <acceptance_criteria>
    Component JSDoc has `@param` lines for all five new props and no longer references the nonexistent `enableTalkingAnimations`. Each new `VRMAvatarProps` field has a `/** */` doc comment. `index.ts` exports `VRMAvatar` and `AnimationConfig`; if `VRMAvatarProps` is exported it is also marked `export` at its declaration. `tsc --noEmit` passes.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Per-frame expression math on the local VRM; no network or untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Tampering | Model lacking expression names | mitigate | Guard `expressionManager.getValue(name) !== null` before setValue (A1 risk) |
| T-10-05 | Elevation of Privilege | Micro-expr overriding developer intent | mitigate | Additive over developer value, capped at 1.0; speaking yields none (D-07) |
| T-10-SC | Tampering | npm installs | accept | Zero new packages this phase |
</threat_model>

<verification>
- `npx tsc --noEmit` in `packages/react` passes, no new VRMAvatar errors.
- Micro-expression block sits before `currentVrm.update(delta)` and is gated on `enableMicroExpressions`.
- Setting a developer `happy = 0.9` plus a listening micro `happy` stays capped at 1.0.
- `chatStatus === 'speaking'` clears micro targets to `{}`.
- `index.ts` exports remain intact; no removed public symbols.
</verification>

<success_criteria>
- Curated micro-expression schedule per D-07, subtle (≤0.15), additive and capped (Open Question 2).
- All five new boolean props documented and defaulting true (D-04).
- Public API surface (barrel) intact / extended cleanly (Success Criteria 4).
</success_criteria>

<output>
Create `.planning/phases/10-avatar-animation-naturalness/10-03-SUMMARY.md` when done.
</output>
