---
phase: 10-shared-animation-architecture-crossfade-engine
plan: 03
type: execute
wave: 3
depends_on: ["10-01", "10-02"]
files_modified:
  - packages/react/src/VRMAvatar.tsx
  - packages/react/src/GLBAvatar.tsx
  - src/app/glb-avatar-test/page.tsx
  - src/app/vrm-avatar-test/page.tsx
autonomous: true
requirements: [ANIM-01, ANIM-02, ANIM-03, XFADE-01]

must_haves:
  truths:
    - "VRMAvatar and GLBAvatar both drive chatStatus-triggered animation through the shared useAnimationController (one code path, not two)"
    - "VRMAvatar's fixed-duration fadeIn/fadeOut crossfade effect and inline blink block are gone"
    - "GLBAvatar's setTimeout loop-back, dead second AnimationMixer, and linear fadeIn/fadeOut crossfade are all gone"
    - "GLBAvatar's getMixer() returns drei's real mixer, not a local mixerRef"
    - "useLoadVRM/useGLTF model loading paths are unchanged"
  artifacts:
    - path: "packages/react/src/VRMAvatar.tsx"
      provides: "VRM avatar consuming the shared module via a VRM format adapter"
      contains: "useAnimationController"
    - path: "packages/react/src/GLBAvatar.tsx"
      provides: "GLB avatar consuming the shared module via a GLB format adapter on drei's real mixer"
      contains: "useAnimationController"
    - path: "src/app/glb-avatar-test/page.tsx"
      provides: "verification surface mounting the SDK GLBAvatar with happy.glb inside KhaveeProvider"
      contains: "GLBAvatar"
    - path: "src/app/vrm-avatar-test/page.tsx"
      provides: "verification surface mounting the SDK VRMAvatar with the bundled Idle/talking/talking1 FBX clips (D-03) inside KhaveeProvider — the VRM analog of glb-avatar-test, so VRM crossfade is human-verifiable in 10-04"
      contains: "VRMAvatar"
  key_links:
    - from: "packages/react/src/VRMAvatar.tsx"
      to: "packages/react/src/animation/AnimationStateEngine.ts"
      via: "useAnimationController"
      pattern: "useAnimationController"
    - from: "packages/react/src/GLBAvatar.tsx"
      to: "drei useAnimations mixer"
      via: "getMixer returns useDreiAnimations().mixer"
      pattern: "mixer"
    - from: "src/app/glb-avatar-test/page.tsx"
      to: "@khaveeai/react GLBAvatar + KhaveeProvider"
      via: "import"
      pattern: "GLBAvatar"
    - from: "src/app/vrm-avatar-test/page.tsx"
      to: "@khaveeai/react VRMAvatar + KhaveeProvider"
      via: "import + animations prop"
      pattern: "VRMAvatar"
---

<objective>
Rewire both avatar components to consume the shared module built in 10-01/10-02, and delete the old format-specific switching code (ANIM-02). `VRMAvatar` gets a VRM format adapter and hands its `useFrame` off to `useAnimationController.update(delta)` (removing its linear `fadeIn`/`fadeOut` crossfade effect and its inline blink block); `GLBAvatar` gets a GLB adapter wired to drei's REAL mixer (removing its `setTimeout` loop-back, its dead second `AnimationMixer`, and its linear crossfade). Add two small Next.js test pages that actually mount the SDK avatars with multiple clips — one for `GLBAvatar` (`happy.glb`'s embedded clips) and one for `VRMAvatar` (the bundled `Idle`/`talking`/`talking1` FBX clips per D-03; nothing currently passes an `animations` prop to the SDK's `VRMAvatar`) — so BOTH migrated crossfade paths are exercisable for the 10-04 checkpoint. Model loading (`useLoadVRM`/`useGLTF`) stays untouched (ANIM-03).

Purpose: This is where ANIM-01 (one shared path) and ANIM-02 (old patterns removed) become true, and where XFADE-01's eased adaptive crossfade replaces both components' fixed 0.3s fades.
Output: modified `VRMAvatar.tsx` + `GLBAvatar.tsx`, new `src/app/glb-avatar-test/page.tsx` and `src/app/vrm-avatar-test/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-shared-animation-architecture-crossfade-engine/10-RESEARCH.md
@.planning/phases/10-shared-animation-architecture-crossfade-engine/10-PATTERNS.md
@.planning/phases/10-shared-animation-architecture-crossfade-engine/10-02-SUMMARY.md
@packages/react/src/animation/types.ts
@packages/react/src/animation/AnimationStateEngine.ts

<interfaces>
<!-- Shared module surface to consume (from 10-01/10-02): -->
  AvatarFormatAdapter { getMixer(); getBoneNode(name); getExpressionManager(): VRMExpressionManager | null }
  useAnimationController({ adapter, chatStatus, currentAnimation, availableNames, getAction, getRoot, enableBlinking }): { update(delta) }

<!-- happy.glb REAL embedded clip names (verified by parsing the GLB — use these EXACT strings,
     not the "Idle"/"Taking"/"listening" shorthand): -->
  'Pose', 'State 1 Idle (loop)', 'State 2 present (loop)', 'State 3 Welcome (loop)',
  'State 4 Taking (loop)', 'State 5 listening (loop)', 'Walk', 'Walk.001'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire VRMAvatar to the shared module; remove its linear crossfade effect and inline blink</name>
  <files>packages/react/src/VRMAvatar.tsx</files>
  <read_first>
    - packages/react/src/VRMAvatar.tsx (read in full — you are editing it; key ranges: props destructure ~294-302, mixer/action refs 304-305, blink refs 308-317, crossfade effect 428-467, useFrame 501-557 incl. inline blink 516-553, stale JSDoc 204-206/217)
    - packages/react/src/animation/AnimationStateEngine.ts (useAnimationController params + resolveBaseClip)
    - packages/react/src/animation/blink.ts (confirm blink now lives in the module)
    - .planning/phases/10-shared-animation-architecture-crossfade-engine/10-PATTERNS.md (VRMAvatar.tsx section: adapter wiring, useFrame ordering to preserve, stale-JSDoc correction, exact code-to-remove ranges)
    - .planning/phases/10-shared-animation-architecture-crossfade-engine/10-RESEARCH.md (Pitfall 3 stale JSDoc, Pitfall 5 clip identity/no frame-0 snap, Pitfall 6 frame ordering)
  </read_first>
  <action>
    In `packages/react/src/VRMAvatar.tsx`:
    1. Construct a `vrmAdapter: AvatarFormatAdapter` (import from `./animation/types`): `getMixer: () => mixerRef.current!`, `getBoneNode: (name) => scene?.getObjectByName(name) ?? null`, `getExpressionManager: () => currentVrm?.expressionManager ?? null`. The existing `mixerRef` (line 304, set at 391) is the real mixer — keep it.
    2. Call `useAnimationController` with: `adapter: vrmAdapter`, `chatStatus` (add `chatStatus` to the `useKhavee()` destructure at line 303), `currentAnimation`, `availableNames: availableAnimations` (from context) — or derive names from `processedClips.map(c => c.name)` if that is the more stable source, `getAction: (name) => { const clip = processedClips.find(c => c?.name === name); return clip && mixerRef.current ? mixerRef.current.clipAction(clip) : null; }`, `getRoot: () => currentVrm?.scene ?? scene ?? null`, `enableBlinking`.
    3. DELETE the entire "Handle animation switching with proper crossfading" effect (lines 428-467) — its `fadeOut(0.3)`/`fadeIn(0.3)` linear crossfade is replaced by the controller. Keep the mixer-init effect (389-410) and the clip-add effect (413-426) — those manage clip/action registration, not crossfading (do not regress Pitfall 5 clip identity).
    4. In the `useFrame` (501-557): keep `mixerRef.current.update(delta)` first, keep the expression-lerp loop, then REPLACE the inline blink block (516-553) with a single `controller.update(delta)` call (which runs the crossfade ramp + blink step), keeping `currentVrm.update(delta)` LAST. Preserve the documented ordering: mixer.update -> controller.update -> vrm.update (Pitfall 6 insertion point for Phase 11).
    5. DELETE the four inline blink refs (308-317) now that blink lives in `animation/blink.ts` (via the controller). Keep `enableBlinking` prop and pass it to the controller so external behavior is unchanged.
    6. Correct the stale JSDoc (204-206, 217): remove/replace the description of the never-implemented `enableTalkingAnimations` auto-switching with an accurate description of the new shared-module chatStatus-driven behavior. Do not search for `enableTalkingAnimations` code to delete — it was never destructured (Pitfall 3).
    Leave `useLoadVRM`, `useAnimationFiles`, `processedClips`, and all model-loading code untouched (ANIM-03).
  </action>
  <verify>
    <automated>cd /Users/whitemalt/Documents/khavee-sdk && pnpm --filter @khaveeai/react exec tsc --noEmit && grep -q "useAnimationController" packages/react/src/VRMAvatar.tsx && ! grep -Eq "fadeIn\(0\.3\)|fadeOut\(0\.3\)" packages/react/src/VRMAvatar.tsx && ! grep -q "blinkAnimationRef" packages/react/src/VRMAvatar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `VRMAvatar.tsx` imports and calls `useAnimationController` and constructs an `AvatarFormatAdapter`
    - No `fadeIn(0.3)` / `fadeOut(0.3)` remains in `VRMAvatar.tsx` (`grep -c` === 0)
    - The inline blink refs and per-frame blink block are gone (`grep -c "blinkAnimationRef" VRMAvatar.tsx` === 0)
    - The `useFrame` still calls `mixerRef.current.update(delta)` before, and `currentVrm.update(delta)` after, `controller.update(delta)`
    - `useLoadVRM`/`useAnimationFiles`/`processedClips` code is unchanged
    - `pnpm --filter @khaveeai/react exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>VRMAvatar drives all animation through the shared controller with eased adaptive crossfades and module-owned blink; its old linear-crossfade effect and inline blink are removed; loading is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Wire GLBAvatar to drei's real mixer via the shared module; delete dead mixer, setTimeout loop-back, and linear crossfade</name>
  <files>packages/react/src/GLBAvatar.tsx</files>
  <read_first>
    - packages/react/src/GLBAvatar.tsx (read in full — you are editing it; dead mixer 97/206-224, talking-refs 100-103, linear crossfade effect 138-163, setTimeout loop-back 165-203, drei useAnimations 107)
    - packages/react/src/VRMAvatar.tsx (POST-migration, from Task 1 — use its adapter + useFrame + controller wiring as the consistency template)
    - packages/react/src/animation/AnimationStateEngine.ts (useAnimationController params)
    - .planning/phases/10-shared-animation-architecture-crossfade-engine/10-PATTERNS.md (GLBAvatar.tsx section: exact code-to-delete ranges, "MUST use drei's real mixer" adapter wiring)
    - .planning/phases/10-shared-animation-architecture-crossfade-engine/10-RESEARCH.md (Pitfall 2 dead second mixer — getMixer must return drei's mixer or setEffectiveWeight is a silent no-op)
  </read_first>
  <action>
    In `packages/react/src/GLBAvatar.tsx`:
    1. Change the drei call to also capture the mixer: `const { mixer, actions, names } = useDreiAnimations(gltf.animations, groupRef);` (currently line 107 destructures only `{ actions, names }`).
    2. Construct a `glbAdapter: AvatarFormatAdapter`: `getMixer: () => mixer` (drei's REAL mixer — NOT a new/local `mixerRef`; see Pitfall 2), `getBoneNode: (name) => groupRef.current?.getObjectByName(name) ?? null`, `getExpressionManager: () => null` (GLB has no expression system).
    3. Call `useAnimationController` with: `adapter: glbAdapter`, `chatStatus` (already destructured from `useKhavee()` at line 95), `currentAnimation`, `availableNames: names`, `getAction: (name) => actions[name] ?? null`, `getRoot: () => groupRef.current`, `enableBlinking: true` (harmless no-op on GLB since the adapter returns null).
    4. Add a `useFrame((_, delta) => controller.update(delta));` — drei's `useAnimations` already runs `mixer.update(delta)` internally in its own earlier-registered `useFrame`, so the controller's ramp step runs after it (do NOT add a manual `mixer.update`).
    5. DELETE the dead second mixer entirely: the `mixerRef` ref (line 97), its update `useFrame` (206-210), and its init `useEffect` (213-224).
    6. DELETE the linear crossfade effect (138-163) — replaced by the controller.
    7. DELETE the `setTimeout` talking-animation loop-back effect (165-203) AND its supporting refs (`animationTimeout` 101, `availableTalkingAnimations` 102, `lastTalkingAnimationIndex` 103). Talk-clip cycling returns properly (loop-boundary-driven, timer-free) in Phase 11 (TALK-01) — this phase only removes the timer.
    Keep the `useGLTF(src)` load (106), the auto-play effect (118-136), and the `setAvailableAnimations(names)` effect (110-115) unchanged (ANIM-03). Remove now-unused imports (e.g. `THREE` if no longer referenced after the dead mixer is gone — verify before deleting).
  </action>
  <verify>
    <automated>cd /Users/whitemalt/Documents/khavee-sdk && pnpm --filter @khaveeai/react exec tsc --noEmit && grep -q "useAnimationController" packages/react/src/GLBAvatar.tsx && ! grep -Eq "setTimeout|setInterval" packages/react/src/GLBAvatar.tsx && ! grep -q "new THREE.AnimationMixer" packages/react/src/GLBAvatar.tsx && ! grep -Eq "fadeIn\(0\.3\)|fadeOut\(0\.3\)" packages/react/src/GLBAvatar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `GLBAvatar.tsx` imports/calls `useAnimationController` and builds an `AvatarFormatAdapter` whose `getMixer` returns drei's `mixer` (from `useDreiAnimations`)
    - No `new THREE.AnimationMixer` remains in `GLBAvatar.tsx` (dead second mixer deleted) — `grep -c` === 0
    - No `setTimeout`/`setInterval` remains (`grep -c` === 0)
    - No `fadeIn(0.3)`/`fadeOut(0.3)` remains (`grep -c` === 0)
    - `useGLTF(src)` load and `setAvailableAnimations` effect are unchanged
    - `pnpm --filter @khaveeai/react exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>GLBAvatar drives animation through the same shared controller on drei's real mixer; its dead mixer, setTimeout loop-back, and linear crossfade are all removed; loading is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Add a GLBAvatar verification page exercising happy.glb through the migrated path</name>
  <files>src/app/glb-avatar-test/page.tsx</files>
  <read_first>
    - src/app/glb/page.tsx (existing standalone drei demo — analog for Canvas/OrbitControls setup + button UI; NOTE it does NOT use the SDK GLBAvatar)
    - src/app/generic-demo/page.tsx (existing page that wraps avatars in KhaveeProvider — reference for provider wiring)
    - packages/react/src/GLBAvatar.tsx (POST-migration props: src, position, scale, autoPlayAnimation)
    - packages/react/src/KhaveeProvider.tsx (animate()/currentAnimation seam the buttons drive)
  </read_first>
  <action>
    Create `src/app/glb-avatar-test/page.tsx` — a client-component Next.js page that mounts the SDK's `GLBAvatar` (from `@khaveeai/react`) inside `<KhaveeProvider>` and a `<Canvas>` with `<OrbitControls>` and basic lighting, loading `/models/happy.glb`. Add buttons that call `useVRMAnimations().animate(name)` (or `useKhavee().animate`) for each of happy.glb's REAL clip names — use the exact strings `'State 1 Idle (loop)'`, `'State 4 Taking (loop)'`, `'State 5 listening (loop)'`, `'State 3 Welcome (loop)'` (verified names, not shorthand) — so a human can trigger transitions and watch the eased pose-gap-adaptive crossfade. This page is the manual-verification surface for the 10-04 checkpoint (criterion 3: duration visibly scales with pose gap; and confirming no setTimeout loop-back). Keep it minimal — this is a dev/test page, not shipped SDK surface. Mirror `src/app/glb/page.tsx`'s Canvas/OrbitControls/button styling for consistency.
  </action>
  <verify>
    <automated>cd /Users/whitemalt/Documents/khavee-sdk && grep -q "GLBAvatar" src/app/glb-avatar-test/page.tsx && grep -q "KhaveeProvider" src/app/glb-avatar-test/page.tsx && grep -q "State 4 Taking (loop)" src/app/glb-avatar-test/page.tsx && pnpm exec tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/glb-avatar-test/page.tsx` mounts `<GLBAvatar src="/models/happy.glb" />` inside `<KhaveeProvider>` + `<Canvas>`
    - Page exposes buttons wired to `animate(name)` using the exact happy.glb clip strings (incl. `'State 4 Taking (loop)'` and `'State 5 listening (loop)'`)
    - Root `pnpm exec tsc --noEmit` (Next app typecheck) exits 0
  </acceptance_criteria>
  <done>A runnable page mounts the migrated SDK GLBAvatar against happy.glb so crossfade behavior can be human-verified in 10-04.</done>
</task>

<task type="auto">
  <name>Task 4: Add a VRMAvatar verification page exercising the bundled Idle/talking/talking1 FBX clips through the migrated path (D-03)</name>
  <files>src/app/vrm-avatar-test/page.tsx</files>
  <read_first>
    - src/app/glb-avatar-test/page.tsx (the GLB verification page from Task 3 — mirror its Canvas/OrbitControls/KhaveeProvider/button structure for consistency)
    - src/app/generic-demo/page.tsx (existing page mounting the SDK `VRMAvatar` inside `KhaveeProvider` — reference for provider wiring; NOTE it mounts `<VRMAvatar src="/models/male.vrm" />` with NO `animations` prop, which is exactly why it cannot exercise crossfade — do not reuse it as the verification surface)
    - packages/react/src/VRMAvatar.tsx (POST-migration props: `src`, `animations?: AnimationConfig`, `enableBlinking`; the `AnimationConfig` shape is `{ [name: string]: string }` mapping a clip name -> FBX/GLB URL, so the config keys ARE the animate() names)
    - packages/react/src/KhaveeProvider.tsx / useVRMAnimations (the `animate(name)`/`currentAnimation`/`availableAnimations` seam the buttons drive)
  </read_first>
  <action>
    Create `src/app/vrm-avatar-test/page.tsx` — a client-component Next.js page that mounts the SDK's `VRMAvatar` (from `@khaveeai/react`) inside `<KhaveeProvider>` and a `<Canvas>` with `<OrbitControls>` and basic lighting. Load a bundled VRM model (`/models/male.vrm`, matching generic-demo) and pass an `animations` prop wiring the three bundled Mixamo FBX fixtures named in D-03: `{ idle: '/models/animations/Idle.fbx', talking: '/models/animations/talking.fbx', talking1: '/models/animations/talking1.fbx' }` (paths verified present under `public/models/animations/`). The SDK's `useAnimationFiles`/`processedClips` path (untouched by ANIM-03) loads and Mixamo-remaps these via `remapMixamoAnimationToVrm`, so the config KEYS (`idle`/`talking`/`talking1`) become the clip names. Add buttons that call `useVRMAnimations().animate(name)` for each of `'idle'`, `'talking'`, `'talking1'` so a human can trigger transitions between distinct poses and watch the eased pose-gap-adaptive VRM crossfade (a DIFFERENT code path than GLB: VRM format adapter, `currentVrm.scene` root, Mixamo bone remapping). Optionally render buttons dynamically from `useVRMAnimations().availableAnimations` once clips finish loading. This page is the VRM manual-verification surface for the 10-04 checkpoint (criterion 3 duration-scales-with-pose-gap, plus VRM blink + no-regression). Keep it minimal — a dev/test page, not shipped SDK surface. Mirror `src/app/glb-avatar-test/page.tsx`'s Canvas/OrbitControls/button styling for consistency.
  </action>
  <verify>
    <automated>grep -q "VRMAvatar" src/app/vrm-avatar-test/page.tsx && grep -q "KhaveeProvider" src/app/vrm-avatar-test/page.tsx && grep -q "animations" src/app/vrm-avatar-test/page.tsx && grep -Eq "Idle\\.fbx|talking\\.fbx|talking1\\.fbx" src/app/vrm-avatar-test/page.tsx && test -f public/models/animations/Idle.fbx && test -f public/models/animations/talking.fbx && test -f public/models/animations/talking1.fbx && pnpm exec tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/vrm-avatar-test/page.tsx` mounts `<VRMAvatar src="/models/male.vrm" animations={...} />` inside `<KhaveeProvider>` + `<Canvas>`
    - The `animations` prop references all three bundled fixtures: `Idle.fbx`, `talking.fbx`, `talking1.fbx` (D-03)
    - Page exposes buttons wired to `animate(name)` for the config keys (`idle`/`talking`/`talking1`) so VRM crossfade transitions are triggerable
    - Root `pnpm exec tsc --noEmit` (Next app typecheck) exits 0
  </acceptance_criteria>
  <done>A runnable page mounts the migrated SDK VRMAvatar with the bundled D-03 FBX clips so VRM crossfade behavior (distinct from GLB) can be human-verified in 10-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Client-side R3F component refactor + a dev test page. No network, user input, persistence, or auth surface introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Tampering (Input Validation) | animation-name string lookups (`actions[name]`, `getObjectByName`) in both components | accept | Names originate from developer-authored props/bundled clips, not untrusted end-user input; unknown names resolve to null/undefined and are guarded by early returns. No new validation required (RESEARCH Security Domain). |
| T-10-07 | Information Disclosure | `src/app/glb-avatar-test/page.tsx` and `src/app/vrm-avatar-test/page.tsx` dev/test routes shipped in the Next app | accept | Static client pages loading public bundled models/clips with no secrets, tokens, mic, or API calls — same exposure class as the existing `src/app/glb/page.tsx` and `src/app/generic-demo/page.tsx` demo routes. No sensitive data reachable. |
</threat_model>

<verification>
- `pnpm --filter @khaveeai/react exec tsc --noEmit` clean (both components)
- `pnpm --filter @khaveeai/react test` still green (module unit tests unaffected)
- Root `pnpm exec tsc --noEmit` clean (both GLB and VRM test pages typecheck)
- `grep` gates confirm removal of `fadeIn(0.3)`/`fadeOut(0.3)`, `setTimeout`/`setInterval`, and `new THREE.AnimationMixer` from GLBAvatar
- `packages/react/src/index.ts` still does not export the animation module
</verification>

<success_criteria>
- Both avatar components consume the single shared `useAnimationController` (ANIM-01)
- VRM's fixed-duration crossfade + inline blink removed; GLB's setTimeout loop-back + dead mixer + linear crossfade removed (ANIM-02)
- GLB's `getMixer()` returns drei's real mixer (crossfade actually blends, not a silent no-op)
- Model loading paths untouched (ANIM-03); crossfades are eased + pose-gap-adaptive (XFADE-01)
- Two verification pages exist mounting the migrated avatars with multiple clips: GLBAvatar (`happy.glb`) and VRMAvatar (bundled `Idle`/`talking`/`talking1` FBX per D-03), so BOTH crossfade paths are human-verifiable
</success_criteria>

<output>
Create `.planning/phases/10-shared-animation-architecture-crossfade-engine/10-03-SUMMARY.md` when done
</output>
