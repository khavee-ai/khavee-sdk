---
phase: 10-shared-animation-architecture-crossfade-engine
plan: 04
type: execute
wave: 4
depends_on: ["10-03"]
files_modified: []
autonomous: false
requirements: [XFADE-01, ANIM-02, ANIM-01, ANIM-03]

must_haves:
  truths:
    - "A human confirms crossfade blend duration visibly scales with pose gap (large-pose-change transition is visibly slower than a small one)"
    - "A human confirms no fixed-interval/jump-cut animation switching remains (setTimeout loop-back gone)"
    - "A human confirms VRM and GLB models still load and render as before (no loading regression)"
  artifacts: []
  key_links: []
---

<objective>
Human-verify the observable, inherently-visual success criteria that automated tests cannot fully prove: that the eased crossfade's duration visibly scales with pose gap (XFADE-01 criterion 3), that the old timer-driven switching is gone (ANIM-02 criterion 2), and that model loading/rendering did not regress (ANIM-03 criterion 4). The math is already unit-tested (10-01) and the code-removal is already grep-gated (10-03) — this checkpoint confirms the on-screen result of the whole shared-module refactor before Phase 10 is considered done.

Purpose: Phase 10's value is smoother, non-robotic transitions with zero visual regression; only a running build confirms that. (Note: the full subjective per-state naturalness review, VERIFY-02, is Phase 13 scope and depends on Phase 11/12 procedural behaviors that do not exist yet — this checkpoint is scoped to Phase 10's four criteria only.)
Output: human approval (or a punch list of issues to feed back into 10-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-shared-animation-architecture-crossfade-engine/10-03-SUMMARY.md
@.planning/phases/wayfinder-map-1-animation-architecture/VERIFICATION-CHECKLIST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Boot the dev server and run the objective code-level gates</name>
  <files>(none — runs tests + dev server)</files>
  <read_first>
    - .planning/phases/wayfinder-map-1-animation-architecture/VERIFICATION-CHECKLIST.md (the objective items applicable to Phase 10: old patterns removed, max-not-average pose-gap, no live-clock interrupts)
    - src/app/glb-avatar-test/page.tsx (the GLB verification page from 10-03)
    - src/app/vrm-avatar-test/page.tsx (the VRM verification page from 10-03 — mounts VRMAvatar with the bundled Idle/talking/talking1 FBX clips)
  </read_first>
  <action>
    Run the objective Phase-10 gates and report their output for the human's reference before the visual check: (1) `pnpm --filter @khaveeai/react test` (crossfade max-not-average + resolveBaseClip green); (2) confirm zero live-clock interrupts and zero old-crossfade calls in the shipped avatar/animation code via `grep -REn "setTimeout|setInterval|fadeIn\(0\.3\)|fadeOut\(0\.3\)|new THREE.AnimationMixer" packages/react/src/VRMAvatar.tsx packages/react/src/GLBAvatar.tsx packages/react/src/animation/` (expect no matches); (3) confirm `packages/react/src/index.ts` does not export the animation module. Then start the Next dev server with `pnpm dev` (background) so the human can open the test pages. Report the local URLs.
  </action>
  <verify>
    <automated>cd /Users/whitemalt/Documents/khavee-sdk && pnpm --filter @khaveeai/react test && ! grep -REn "setTimeout|setInterval|fadeIn\(0\.3\)|fadeOut\(0\.3\)|new THREE.AnimationMixer" packages/react/src/VRMAvatar.tsx packages/react/src/GLBAvatar.tsx packages/react/src/animation/ && ! grep -q "animation" packages/react/src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @khaveeai/react test` passes
    - `grep -REn "setTimeout|setInterval|fadeIn\(0\.3\)|fadeOut\(0\.3\)|new THREE.AnimationMixer"` over the two components + `animation/` returns no matches
    - `packages/react/src/index.ts` contains no reference to `animation`
    - Dev server is running and the test-page URL is reported
  </acceptance_criteria>
  <done>Objective gates pass and the dev server is up for visual verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of adaptive crossfade + no regression</name>
  <action>Pause for the developer to run the six-step visual verification below against the running dev server (both the GLB test page and the VRM test page) and report the result. Do not auto-approve — animation smoothness and no-regression are visual judgements only the human can make.</action>
  <what-built>
    A shared internal animation module now drives BOTH VRMAvatar and GLBAvatar: chatStatus/`animate()` -> resolved base clip -> an `easeInOutCubic`-eased crossfade whose duration scales (0.3-0.9s) with the MAX per-bone pose gap. GLBAvatar's old `setTimeout` talk loop-back, its dead second AnimationMixer, and both components' fixed-0.3s `fadeIn`/`fadeOut` crossfades were removed. Blink migrated into the module (VRM unchanged; GLB gets it wired but as a no-op).
  </what-built>
  <how-to-verify>
    1. Open the GLB test page at http://localhost:3000/glb-avatar-test (the page added in 10-03, using `/models/happy.glb`).
    2. Click 'State 1 Idle (loop)', let it settle, then click 'State 5 listening (loop)'. This is the prototype's measured large-gap transition (~113 degrees max). Watch the blend: it should take clearly closer to ~0.9s and look smooth/eased, NOT a fixed short 0.3s snap.
    3. Now click between two poses that are similar (e.g. 'State 1 Idle (loop)' -> 'State 3 Welcome (loop)' if visually close, or repeatedly toggle back). A small-gap transition should visibly resolve FASTER (closer to ~0.3s) than the large-gap one in step 2. The key confirmation: different transitions take visibly different durations (adaptive), and none jump-cuts.
    4. Leave the avatar on any clip for ~10 seconds and confirm nothing auto-switches on a timer (the old 3-5s setTimeout loop-back is gone — the clip should just keep looping).
    5. Open the VRM test page at http://localhost:3000/vrm-avatar-test (the page added in 10-03, mounting `<VRMAvatar>` with the bundled `Idle`/`talking`/`talking1` FBX clips per D-03). Click between the 'idle', 'talking', and 'talking1' buttons and confirm: the VRM model loads and renders as before, blinks periodically, and each switch crossfades smoothly on the eased pose-gap-adaptive curve (no fixed 0.3s snap, no bind-pose/T-pose snap, no console errors). This exercises VRM's crossfade path specifically — a different code path than GLB (VRM format adapter, `currentVrm.scene` root, Mixamo bone remapping).
    6. Confirm no regression: both models load, no WebGL/console errors, framerate feels unchanged.
  </how-to-verify>
  <resume-signal>Type "approved" if all six checks pass, or describe which check failed (e.g. "step 3: all transitions look the same duration") so 10-03 can be revised.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification-only plan: runs tests + a local dev server for visual inspection. No code changes, no network/auth/persistence surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08 | (n/a) | Local `pnpm dev` server for manual verification | accept | Localhost-only dev server for human inspection; not a deployed surface. No new attack surface introduced by this plan. |
</threat_model>

<verification>
- Objective gates (Task 1) pass
- Human confirms (Task 2): adaptive crossfade duration visible, no timer switching, no loading/render regression on VRM and GLB
</verification>

<success_criteria>
- Human approval recorded that Phase 10's four success criteria hold on a running build
- Any visual defects captured as a punch list for a 10-03 revision (if not approved)
</success_criteria>

<output>
Create `.planning/phases/10-shared-animation-architecture-crossfade-engine/10-04-SUMMARY.md` when done
</output>
