---
phase: 11-bone-masked-upper-body-animation-layering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/react/src/utils/filterClipTracksByBoneSet.ts
autonomous: true
requirements: [BONE-01]
# NOTE: ROADMAP Phase 11 Requirements are "TBD" (no formal REQ IDs defined for this
# phase). BONE-01..BONE-05 are planner-assigned, phase-local traceability IDs derived
# from the CONTEXT.md decisions (D-01..D-06) and the three resolved RESEARCH open
# questions. BONE-01 = bone-set track-filtering utility + bone-split constants (D-01/D-02).

must_haves:
  truths:
    - "A THREE.AnimationClip can be filtered down to only the tracks belonging to a named set of VRM humanoid bones, resolved per the currently-loaded VRM model (D-01, D-02)"
    - "The base/lower bone set (D-01) and upper bone set (D-02) together cover every distinct value in mixamoVRMRigMap (no bone silently dropped from both sets)"
    - "The filtered sub-clip keeps the original source clip's duration (never auto-recomputed from the track subset)"
  artifacts:
    - path: "packages/react/src/utils/filterClipTracksByBoneSet.ts"
      provides: "filterClipTracksByBoneSet() + BASE_LOWER_BONES + UPPER_BONES exports"
      exports: ["filterClipTracksByBoneSet", "BASE_LOWER_BONES", "UPPER_BONES"]
      contains: "getNormalizedBoneNode"
  key_links:
    - from: "packages/react/src/utils/filterClipTracksByBoneSet.ts"
      to: "vrm.humanoid.getNormalizedBoneNode"
      via: "per-VRM bone-name → node-name resolution (never string-match track names)"
      pattern: "getNormalizedBoneNode"
---

<objective>
Create the pure track-filtering utility `filterClipTracksByBoneSet.ts` in `packages/react/src/utils/` that produces a bone-masked `THREE.AnimationClip` (only the tracks for a given set of VRM humanoid bones) from an already-remapped source clip, plus the two bone-set constants (`BASE_LOWER_BONES`, `UPPER_BONES`) that partition the full VRM humanoid rig per CONTEXT.md D-01/D-02.

Purpose: This is the foundational, self-contained building block Plan 02 consumes to derive the always-on base-lower action and the crossfading upper-body layer. It is a structural sibling of the existing `remapMixamoAnimationToVrm.ts` — same directory, same track-manipulation technique, same `track.name.split(".")` convention.

Output: One new file, `packages/react/src/utils/filterClipTracksByBoneSet.ts`, with three named exports. No changes to any other file this plan.

Resolves (made explicit for the checker): the D-01/D-02 bone split is implemented as two exported constant arrays whose union equals all 52 distinct `mixamoVRMRigMap` values; Pitfall 4 (duration drift) is prevented by passing the original `clip.duration` explicitly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-bone-masked-upper-body-animation-layering/11-CONTEXT.md
@.planning/phases/11-bone-masked-upper-body-animation-layering/11-RESEARCH.md
@.planning/phases/11-bone-masked-upper-body-animation-layering/11-PATTERNS.md

<interfaces>
<!-- The exact analog to mirror. Extracted from the codebase — the executor should
     replicate this file's import style, track-name-parsing convention, and
     original-duration-preservation, NOT explore the codebase for these. -->

From packages/react/src/utils/remapMixamoAnimationToVrm.ts:
- Imports: `import * as THREE from "three";`
- Track-name parsing convention (line 34): `const trackSplitted = track.name.split(".");` then `trackSplitted[0]` is the leading node name.
- Per-VRM bone resolution (line 37): `const vrmNodeName = vrm.humanoid?.getNormalizedBoneNode(vrmBoneName)?.name;`
- Clip construction with ORIGINAL duration passed explicitly (line 100): `return new THREE.AnimationClip("vrmAnimation", clip.duration, tracks);`

From @pixiv/three-vrm (already a dependency of packages/react):
- `import type { VRM } from "@pixiv/three-vrm";`
- `vrm.humanoid?.getNormalizedBoneNode(boneName: VRMHumanoidBoneName): THREE.Object3D | null` — resolved node `.name` is `"Normalized_" + <raw skeleton node name>`, which VARIES per loaded VRM file (load-bearing correction from RESEARCH.md — never hardcode a fixed prefix).

Source of truth for the bone-name union (packages/react/src/utils/mixamoVRMRigMap.ts):
- 52 distinct VRM humanoid bone-name values. BASE_LOWER_BONES (10) ∪ UPPER_BONES (42) MUST equal this set exactly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create filterClipTracksByBoneSet utility + bone-split constants</name>
  <files>packages/react/src/utils/filterClipTracksByBoneSet.ts</files>
  <read_first>
    - packages/react/src/utils/remapMixamoAnimationToVrm.ts (the exact analog: import style, `track.name.split(".")` convention, `getNormalizedBoneNode(...)?.name` resolution, and passing `clip.duration` explicitly into `new THREE.AnimationClip(...)`)
    - packages/react/src/utils/mixamoVRMRigMap.ts (source of truth for the full 52-value bone-name union that BASE_LOWER_BONES ∪ UPPER_BONES must cover)
    - .planning/phases/11-bone-masked-upper-body-animation-layering/11-RESEARCH.md (Patterns 1 & 2, Pitfall 4, and the "Bone-set constants" code example)
    - .planning/phases/11-bone-masked-upper-body-animation-layering/11-PATTERNS.md (the "filterClipTracksByBoneSet.ts" analog section with the concrete implementation shape)
  </read_first>
  <action>
    Create `packages/react/src/utils/filterClipTracksByBoneSet.ts` with these exact named exports:

    1. `BASE_LOWER_BONES: string[]` — the 10 D-01 base/lower bones exactly: `hips`, `spine`, `leftUpperLeg`, `leftLowerLeg`, `leftFoot`, `leftToes`, `rightUpperLeg`, `rightLowerLeg`, `rightFoot`, `rightToes`. Add a `// D-01` header comment.

    2. `UPPER_BONES: string[]` — the 42 D-02 upper bones exactly: `chest`, `upperChest`, `neck`, `head`, `leftShoulder`, `leftUpperArm`, `leftLowerArm`, `leftHand`, the 15 left finger bones (`leftThumbMetacarpal`, `leftThumbProximal`, `leftThumbDistal`, `leftIndexProximal`, `leftIndexIntermediate`, `leftIndexDistal`, `leftMiddleProximal`, `leftMiddleIntermediate`, `leftMiddleDistal`, `leftRingProximal`, `leftRingIntermediate`, `leftRingDistal`, `leftLittleProximal`, `leftLittleIntermediate`, `leftLittleDistal`), `rightShoulder`, `rightUpperArm`, `rightLowerArm`, `rightHand`, and the 15 right finger bones (same finger pattern with `right` prefix). Add a `// D-02` header comment noting chest/upperChest/neck are deliberately in the upper set (not base) per D-02 rationale. These names must match `mixamoVRMRigMap` values exactly (they are the VRM humanoid bone names, NOT the mixamorig keys).

    3. A private helper `getBoneTrackNodeNames(vrm: VRM, boneNames: string[]): Set<string>` that iterates `boneNames`, calls `vrm.humanoid?.getNormalizedBoneNode(boneName as VRMHumanoidBoneName)` (cast to the humanoid-bone-name type, or `as any` matching the analog's loose typing), and adds each resolved `node?.name` to a `Set<string>`. This resolves the per-model `"Normalized_..."` node names — do NOT string-match bone names against track names directly (RESEARCH anti-pattern: `track.name.startsWith("head")` matches zero tracks).

    4. The exported `filterClipTracksByBoneSet(clip: THREE.AnimationClip, vrm: VRM, boneNames: string[], newName: string): THREE.AnimationClip` that: builds the node-name set via the helper; filters `clip.tracks` keeping only tracks whose `track.name.split(".")[0]` is in the set; and returns `new THREE.AnimationClip(newName, clip.duration, filteredTracks)` — passing the ORIGINAL `clip.duration` explicitly (Pitfall 4: never call `resetDuration()` or pass `-1`, which would let base-lower and idle-upper sub-clips drift out of phase over long idle loops).

    Import `* as THREE from "three"` and `import type { VRM } from "@pixiv/three-vrm"` (VRM type already used across `VRMAvatar.tsx`). Follow the analog's no-throw contract: the function returns a valid (possibly zero-track) clip; the CALLER (Plan 02's memo) is responsible for the empty-track fallback — do NOT throw or add try/catch here. Do NOT include any React/hooks code — this is a pure utility.
  </action>
  <verify>
    <automated>pnpm --filter @khaveeai/react build</automated>
  </verify>
  <acceptance_criteria>
    - `packages/react/src/utils/filterClipTracksByBoneSet.ts` exists and exports `filterClipTracksByBoneSet`, `BASE_LOWER_BONES`, and `UPPER_BONES` (verify: `grep -E "export (const|function) (filterClipTracksByBoneSet|BASE_LOWER_BONES|UPPER_BONES)" packages/react/src/utils/filterClipTracksByBoneSet.ts` returns 3 matches).
    - `BASE_LOWER_BONES` has exactly 10 entries and `UPPER_BONES` has exactly 42 entries; their union equals the 52 distinct values in `mixamoVRMRigMap.ts` with zero overlap and zero omission (source assertion against `mixamoVRMRigMap.ts`).
    - The file resolves bones via `vrm.humanoid.getNormalizedBoneNode(...)` and does NOT contain any literal string-prefix match like `.startsWith("head"` or `.startsWith("hips"` against track names (verify: `grep -c "getNormalizedBoneNode" packages/react/src/utils/filterClipTracksByBoneSet.ts` is >= 1; `grep -c 'startsWith(' packages/react/src/utils/filterClipTracksByBoneSet.ts` is 0).
    - The `new THREE.AnimationClip(...)` construction passes `clip.duration` explicitly and the file does NOT call `.resetDuration(` (verify: `grep -c "clip.duration" file` >= 1 and `grep -c "resetDuration" file` is 0).
    - `pnpm --filter @khaveeai/react build` (tsc) completes with no type errors.
  </acceptance_criteria>
  <done>The utility file exists, type-checks clean under the package `tsc` build, exports the three symbols, and the two constant arrays partition the full mixamoVRMRigMap bone-name set exactly (10 + 42 = 52, disjoint).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None introduced. This plan produces a pure client-side utility that transforms in-memory `THREE.AnimationClip` objects derived from developer-supplied animation files. No network, no auth, no persistence, no untrusted-input parsing.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-NA | (none applicable) | `filterClipTracksByBoneSet.ts` | accept | Per RESEARCH.md Security Domain: pure client-side animation-blending change, no ASVS L1 category meaningfully applies (V5 input is the developer's own trusted `animations` prop, not runtime/user input). No new external packages installed → Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
- `pnpm --filter @khaveeai/react build` passes (tsc, no emit errors).
- `grep` structural checks in the acceptance criteria confirm the three exports, the bone-count invariants, the `getNormalizedBoneNode` resolution path, and the absence of the two anti-patterns (`startsWith` bone matching, `resetDuration`).
</verification>

<success_criteria>
- `filterClipTracksByBoneSet.ts` created with `filterClipTracksByBoneSet`, `BASE_LOWER_BONES`, `UPPER_BONES` exports.
- BASE_LOWER_BONES (10) ∪ UPPER_BONES (42) = all 52 mixamoVRMRigMap values, disjoint.
- Original `clip.duration` preserved; no `resetDuration`; no direct string-prefix track matching.
- Package `tsc` build is green.
</success_criteria>

<output>
Create `.planning/phases/11-bone-masked-upper-body-animation-layering/11-01-SUMMARY.md` when done.
</output>
