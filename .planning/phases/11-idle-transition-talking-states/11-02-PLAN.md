---
phase: 11-idle-transition-talking-states
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/react/src/KhaveeProvider.tsx
autonomous: true
requirements: [TALK-02]
must_haves:
  truths:
    - "currentVolume is readable from useKhavee() context and updates during a live conversation"
    - "An out-of-range volume value from the provider is clamped to [0,1] before it reaches consumers"
  artifacts:
    - path: "packages/react/src/KhaveeProvider.tsx"
      provides: "currentVolume context field + onVolumeChange subscription + [0,1] clamp"
      contains: "currentVolume"
  key_links:
    - from: "packages/react/src/KhaveeProvider.tsx"
      to: "realtimeProvider.onVolumeChange"
      via: "useEffect subscription mirroring onChatStatusChange"
      pattern: "onVolumeChange"
---

<objective>
Thread the live `currentVolume` signal from the realtime provider through `KhaveeProvider`'s React context so `VRMAvatar`/`GLBAvatar` (which read state via `useKhavee()`, not `useRealtime()`) can consume it for TALK-02's audio-reactive procedural amplitude.

Purpose: `currentVolume` currently exists ONLY as local `useState` inside `useRealtime()` and is absent from `KhaveeContextType` entirely (RESEARCH Summary #2 / Pitfall 5, verified). TALK-02's amplitude scaling runs inside the avatars' `useFrame`, which can only see context values — so the volume must be lifted into `KhaveeProvider`, subscribed the same way `chatStatus` already is, and clamped defensively.
Output: One new context field (`currentVolume: number`), one subscription effect, one clamp — no consumers wired yet (Plan 05 does that).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-idle-transition-talking-states/11-RESEARCH.md
@.planning/phases/11-idle-transition-talking-states/11-PATTERNS.md

<interfaces>
<!-- KhaveeContextType shape to extend (KhaveeProvider.tsx:6-25). Add currentVolume: number. -->
<!-- Existing subscription template (KhaveeProvider.tsx:107-112):
     useEffect(() => { if (realtimeProvider) { realtimeProvider.onChatStatusChange = setChatStatus; } }, [realtimeProvider]);
-->
<!-- RealtimeProvider exposes an onVolumeChange?: (volume: number) => void callback
     (see packages/core/src/types/realtime.ts and useRealtime.ts:106 which assigns it). -->
<!-- Existing scalar clamp convention (KhaveeProvider.tsx setExpression, ~lines 138-141):
     Math.max(0, Math.min(1, value)) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add currentVolume to KhaveeContextType with a subscription and clamp</name>
  <read_first>
    - packages/react/src/KhaveeProvider.tsx (full — KhaveeContextType at lines 6-25, chatStatus state+subscription at lines ~96 and 107-112, setExpression clamp at ~138-141, context value object at ~249-268)
    - packages/react/src/hooks/useRealtime.ts (lines 67-71 and ~106 — shows the bare, non-chaining onVolumeChange assignment and the chaining precaution used for onChatStatusChange)
    - packages/core/src/types/realtime.ts (confirm the onVolumeChange callback signature on RealtimeProvider/RealtimeEvents)
  </read_first>
  <files>packages/react/src/KhaveeProvider.tsx</files>
  <action>
    (1) Add `currentVolume: number` to the `KhaveeContextType` interface. (2) Add `const [currentVolume, setCurrentVolume] = useState(0);` alongside the existing `chatStatus` state. (3) Add a `useEffect` keyed on `[realtimeProvider]` that assigns `realtimeProvider.onVolumeChange = (volume) => setCurrentVolume(Math.max(0, Math.min(1, volume)));` — apply the `Math.max(0, Math.min(1, value))` clamp exactly as `setExpression` already does, so a malformed/out-of-range value from the provider cannot drive extreme procedural motion (per D-02 security note and RESEARCH Security Domain V5). Mirror the existing `onChatStatusChange` subscription effect's structure directly above/below it. (4) Add `currentVolume` to the context value object passed to the Provider, following the flat-spread shape already used there. Do NOT alter the existing `onChatStatusChange` wiring. Add an inline comment noting the known coexistence collision risk: this assignment is bare/non-chaining, matching `chatStatus`'s existing pattern; if a consumer also calls `useRealtime()` in the same tree the callbacks overwrite (pre-existing behavior, RESEARCH Pitfall 5) — flag it, do not attempt to fix `useRealtime.ts` in this phase.
  </action>
  <verify>
    <automated>pnpm --filter @khaveeai/react build && grep -n "onVolumeChange\|currentVolume" packages/react/src/KhaveeProvider.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "currentVolume" packages/react/src/KhaveeProvider.tsx` returns at least 3 (type field, state, context value)
    - `grep -n "onVolumeChange" packages/react/src/KhaveeProvider.tsx` shows an assignment inside a useEffect keyed on realtimeProvider
    - The volume setter applies `Math.max(0, Math.min(1, ...))` before storing
    - `pnpm --filter @khaveeai/react build` exits 0
  </acceptance_criteria>
  <done>currentVolume is a clamped context field subscribed to realtimeProvider.onVolumeChange; tsc green; no consumers wired yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| realtimeProvider.onVolumeChange -> KhaveeProvider | A `number` scalar derived from decoded audio crosses from the provider package into React context. Already-trusted, already-shipped data path; this plan newly persists it into context. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Tampering / Input Validation (ASVS V5) | currentVolume ingestion in KhaveeProvider | mitigate | Clamp incoming volume to [0,1] via `Math.max(0, Math.min(1, value))` before storing, matching the codebase's existing setExpression clamp — prevents an out-of-range value producing extreme/broken procedural amplitude downstream. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. No supply-chain surface introduced. |
</threat_model>

<verification>
- `pnpm --filter @khaveeai/react build` compiles with the new context field.
- `useKhavee()` consumers can type-safely read `currentVolume: number`.
- Clamp is present on the ingestion path.
</verification>

<success_criteria>
`currentVolume` is exposed on `KhaveeContextType`, fed by a `realtimeProvider.onVolumeChange` subscription mirroring the existing `onChatStatusChange` pattern, and clamped to `[0,1]` on ingest.
</success_criteria>

<output>
Create `.planning/phases/11-idle-transition-talking-states/11-02-SUMMARY.md` when done
</output>
