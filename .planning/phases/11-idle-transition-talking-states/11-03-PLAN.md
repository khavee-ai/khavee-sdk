---
phase: 11-idle-transition-talking-states
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - packages/react/src/animation/breathing.ts
  - packages/react/src/animation/breathing.test.ts
  - packages/react/src/animation/sway.ts
  - packages/react/src/animation/sway.test.ts
  - packages/react/src/animation/expressionDrift.ts
autonomous: true
requirements: [IDLE-01, IDLE-02, PERF-01]
must_haves:
  truths:
    - "breathing writes an additive quaternion delta to chest and spine bones every frame via multiply(), never set()"
    - "sway writes an independent-period additive delta to hips and spine via multiply()"
    - "expression drift runs on VRM (expression manager present) and is a silent no-op on GLB (null manager)"
    - "expression drift only targets a documented non-emotion rest-state allow-list and skips any expression already non-zero this frame, so it never overwrites an app-driven setExpression value"
    - "breathing and sway amplitude scale with an injected amplitudeScale argument"
  artifacts:
    - path: "packages/react/src/animation/breathing.ts"
      provides: "useBreathing() ref-driven chest/spine sine stepper"
      contains: "multiply"
    - path: "packages/react/src/animation/sway.ts"
      provides: "useSway() ref-driven hip/spine sine stepper"
      contains: "multiply"
    - path: "packages/react/src/animation/expressionDrift.ts"
      provides: "useExpressionDrift() VRM-only rest-state drift stepper"
      contains: "getExpressionManager"
  key_links:
    - from: "packages/react/src/animation/breathing.ts"
      to: "adapter.getHumanoidBoneNode"
      via: "chest/spine bone resolution"
      pattern: "getHumanoidBoneNode"
    - from: "packages/react/src/animation/expressionDrift.ts"
      to: "adapter.getExpressionManager"
      via: "VRM-only gating null-check"
      pattern: "getExpressionManager"
---

<objective>
Create the three always-on idle procedural delta systems — breathing (chest/spine), weight-shift sway (hips/spine, independent period), and VRM-only expression rest-state drift — each as a ref-driven stepper mirroring `blink.ts`'s exact shape.

Purpose: IDLE-01 requires randomized-range procedural breathing and an independent sway cycle; IDLE-02 requires 1-2 VRM-only expression drift values with GLB getting none. PERF-01 requires that bones touched by more than one system compose via additive delta-quaternion `multiply()`, never `.set()`/overwrite. These modules produce the deltas; Plan 05 composes and bounds them in the controller's fixed order.
Output: `breathing.ts`, `sway.ts`, `expressionDrift.ts` (all internal, none exported from `index.ts`), plus unit tests for the pure delta math.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-idle-transition-talking-states/11-RESEARCH.md
@.planning/phases/11-idle-transition-talking-states/11-PATTERNS.md

<interfaces>
<!-- AvatarFormatAdapter after Plan 01 (packages/react/src/animation/types.ts):
     getMixer(), getBoneNode(name), getHumanoidBoneNode(role), getExpressionManager() -->
<!-- blink.ts is the reference shape (read in full):
     useBlink(): { step(adapter, enabled): void }  — refs only, never useState;
     gating: const em = adapter.getExpressionManager(); if (!em) return; -->
<!-- Allocation-reuse precedent (crossfade.ts:45-46): scratch THREE.Quaternion declared once, reused; never `new` inside per-frame path. -->
<!-- Additive write (net-new this phase, PERF-01):
     _deltaQuat.setFromAxisAngle(axis, Math.sin(phase)*amp); bone.quaternion.multiply(_deltaQuat); // NOT .set() -->
<!-- App-driven expressions: KhaveeProvider exposes setExpression / useKhavee().expressions; VRMAvatar applies them
     each frame via lerpExpression BEFORE controller.update(delta) runs. expressionDrift.step (inside controller.update)
     therefore runs AFTER the app's write, and em.setValue is an ABSOLUTE overwrite — drift must not target or clobber
     an actively app-driven expression (see Task 3). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: breathing.ts — chest/spine additive sine stepper</name>
  <read_first>
    - packages/react/src/animation/blink.ts (full — copy the hook/ref/file-header shape and the "never useState" comment rationale)
    - packages/react/src/animation/crossfade.ts (lines 41-59 — scratch-quaternion allocation-reuse pattern to mirror)
    - packages/react/src/animation/types.ts (post-Plan-01 — getHumanoidBoneNode signature)
    - packages/react/src/animation/crossfade.test.ts (test-file structure/conventions for the colocated .test.ts)
  </read_first>
  <files>packages/react/src/animation/breathing.ts, packages/react/src/animation/breathing.test.ts</files>
  <behavior>
    - Given a bone at identity and a positive amplitude, one step at sine-phase peak produces a bone quaternion whose angleTo(identity) is > 0 and <= the configured amplitude
    - The write is via quaternion.multiply (additive): stepping a bone that already holds a non-identity base orientation preserves the base and adds the delta on top (result != the delta alone, != the base alone)
    - amplitudeScale=0 produces (near) zero net delta; amplitudeScale=2 produces roughly double the delta of amplitudeScale=1 at the same phase
    - When adapter.getHumanoidBoneNode("chest") returns null, step() returns without throwing (early-return gate)
  </behavior>
  <action>
    Create `breathing.ts` exporting `useBreathing(): { step(adapter: AvatarFormatAdapter, delta: number, amplitudeScale?: number): void }`. Follow `blink.ts` verbatim in shape: file-header block comment naming IDLE-01, stating "internal helper, not exported from index.ts," and explaining the fixed-order/composition note (breathing runs before sway; both compose on the shared spine bone via multiply — see AnimationStateEngine, PERF-01). Hold phase/period as `useRef` (never useState — cite blink.ts's reasoning). Randomize the breathing period once at init within a documented band (default ~4.0-6.0s) so different avatar instances are not phase-locked (IDLE-01 "randomized-range"). Advance an internal phase accumulator by `delta`. Resolve chest and spine via `adapter.getHumanoidBoneNode("chest")` / `("spine")` — NEVER the literal `getBoneNode` (RESEARCH Pitfall 1). Early-return if a needed bone is null (defensive gate, no try/catch). Apply the delta additively: a module-scoped scratch `THREE.Quaternion` set via `setFromAxisAngle` on a documented axis (e.g. X for a chest-rise nod) with angle `Math.sin(phase) * amplitude * (amplitudeScale ?? 1)`, then `bone.quaternion.multiply(scratch)`. Default amplitude is Claude's discretion within a small-degree band (~0.02-0.04 rad); document the chosen constants inline per blink.ts's convention. Export a pure helper (e.g. `breathingDeltaAngle(phase, amplitude, amplitudeScale)`) so the sine math is unit-testable without a scene. Do NOT add this file to `index.ts`. Write `breathing.test.ts` covering the four behaviors above using a THREE.Object3D stub scene and a stub adapter.
  </action>
  <verify>
    <automated>pnpm --filter @khaveeai/react exec vitest run src/animation/breathing.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\.multiply(" packages/react/src/animation/breathing.ts` >= 1 and `grep -c "\.quaternion\.set(" packages/react/src/animation/breathing.ts` returns 0
    - `grep -c "useState" packages/react/src/animation/breathing.ts` returns 0
    - `grep -c "getHumanoidBoneNode" packages/react/src/animation/breathing.ts` >= 1; `grep -c "getBoneNode(" packages/react/src/animation/breathing.ts` returns 0
    - `grep -c "breathing" packages/react/src/index.ts` returns 0 (not publicly exported)
    - `pnpm --filter @khaveeai/react exec vitest run src/animation/breathing.test.ts` passes all assertions
  </acceptance_criteria>
  <done>breathing.ts is a ref-driven additive chest/spine stepper with randomized period and amplitudeScale support; tests green; not exported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: sway.ts — hips/spine independent-period additive sine stepper</name>
  <read_first>
    - packages/react/src/animation/breathing.ts (Task 1 output — sway mirrors it; only bone roles and period band differ)
    - packages/react/src/animation/blink.ts (reference shape)
    - packages/react/src/animation/crossfade.test.ts (test conventions)
  </read_first>
  <files>packages/react/src/animation/sway.ts, packages/react/src/animation/sway.test.ts</files>
  <behavior>
    - One step at sine peak produces a non-identity additive delta on hips and spine bounded by the configured amplitude
    - The write is additive via multiply (base orientation preserved), not set/copy
    - sway's period is independent of breathing's (not phase-locked): its default period band is different (~7-10s)
    - Null hips or spine bone -> step() early-returns without throwing
  </behavior>
  <action>
    Create `sway.ts` exporting `useSway(): { step(adapter, delta, amplitudeScale?): void }`, structurally identical to `breathing.ts` (same header/ref/gating/allocation-reuse/additive-multiply rules). Differences: target `adapter.getHumanoidBoneNode("hips")` and `("spine")`; use an INDEPENDENT randomized period band (default ~7.0-10.0s) so sway and breathing are not phase-locked (IDLE-01 "independent cycles"); apply the sway delta on a documented axis appropriate for a weight-shift (e.g. Z-roll / lateral). Export a pure `swayDeltaAngle(...)` helper for testing. Additive `.multiply()` only, never `.set()`. Not exported from `index.ts`. Write `sway.test.ts` covering the four behaviors above.
  </action>
  <verify>
    <automated>pnpm --filter @khaveeai/react exec vitest run src/animation/sway.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\.multiply(" packages/react/src/animation/sway.ts` >= 1 and `grep -c "\.quaternion\.set(" packages/react/src/animation/sway.ts` returns 0
    - `grep -c "useState" packages/react/src/animation/sway.ts` returns 0
    - sway's default period band differs from breathing's (documented inline; not equal to breathing's constants)
    - `grep -c "getHumanoidBoneNode" packages/react/src/animation/sway.ts` >= 1
    - `pnpm --filter @khaveeai/react exec vitest run src/animation/sway.test.ts` passes
  </acceptance_criteria>
  <done>sway.ts is a ref-driven additive hips/spine stepper with an independent period and amplitudeScale support; tests green; not exported.</done>
</task>

<task type="auto">
  <name>Task 3: expressionDrift.ts — VRM-only rest-state expression drift</name>
  <read_first>
    - packages/react/src/animation/blink.ts (lines 43-74 — the getExpressionManager() null-gate and the setValue write pattern to copy verbatim)
    - packages/react/src/animation/types.ts (getExpressionManager signature)
    - packages/react/src/KhaveeProvider.tsx (setExpression / expressions — the app-driven expression path drift must not clobber)
  </read_first>
  <files>packages/react/src/animation/expressionDrift.ts</files>
  <action>
    Create `expressionDrift.ts` exporting `useExpressionDrift(): { step(adapter: AvatarFormatAdapter, delta: number): void }`. Gate on `const em = adapter.getExpressionManager(); if (!em) return;` — this is IDLE-02's exact required behavior (VRM gets drift, GLB's null manager makes it an automatic no-op; no format branching, RESEARCH). Drift 1-2 expression rest-state values on a slow independent sine (period Claude's discretion, e.g. ~8-12s; amplitude small, e.g. 0-0.15). Use `useRef` for phase state (never useState). File-header comment naming IDLE-02 and stating internal-only.

    NON-CLOBBER REQUIREMENT (key_links warning): `em.setValue(name, value)` is an ABSOLUTE overwrite, and `expressionDrift.step` runs inside `controller.update(delta)` — i.e. AFTER the app's own per-frame `lerpExpression` write from `useKhavee().expressions`. Drift must therefore never overwrite an actively app-driven expression. Enforce this two ways:
      (a) Restrict drift candidates to a documented allow-list of subtle NON-EMOTION rest-state expressions that consuming apps do NOT typically drive — explicitly EXCLUDE the standard app-driven VRM emotion presets (`happy`, `angry`, `sad`, `surprised`, and `relaxed` if the app drives it) and all mouth/viseme presets (`aa`, `ih`, `ou`, `ee`, `oh`) and `blink`/`blinkLeft`/`blinkRight`. Prefer neutral micro-expressions unlikely to appear in `useKhavee().expressions`. Document the chosen allow-list inline.
      (b) Additionally guard each write at runtime: before drifting a candidate, read its current manager value (`em.getValue(name)`); if it is already non-zero this frame (i.e. actively set by the app), SKIP drifting that expression this frame so drift never clobbers an app-driven value. Only call `em.setValue(name, value)` for allow-listed candidates that are actually present on the manager (check the manager's available expression names before writing) so it degrades cleanly on VRMs lacking a given expression.

    Note inline that expression values are additive-by-convention via setValue on manager-owned scalars, so PERF-01's bone-quaternion composition rule does NOT apply here (this is not a bone writer). Not exported from `index.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @khaveeai/react build && grep -c "getExpressionManager" packages/react/src/animation/expressionDrift.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "getExpressionManager" packages/react/src/animation/expressionDrift.ts` shows a null-gate early-return before any setValue call
    - A documented allow-list of non-emotion rest-state candidate expressions exists, explicitly excluding app-driven emotion/viseme/blink presets
    - Each write is guarded by a current-value check (e.g. `em.getValue(name)`) that skips drifting any expression already non-zero this frame (no clobber of app-driven setExpression values)
    - `grep -c "useState" packages/react/src/animation/expressionDrift.ts` returns 0
    - `grep -c "expressionDrift" packages/react/src/index.ts` returns 0 (not exported)
    - `pnpm --filter @khaveeai/react build` exits 0
  </acceptance_criteria>
  <done>expressionDrift.ts drifts 1-2 present VRM rest-state expressions from a non-emotion allow-list on a slow sine, skips any expression actively set by the app, no-ops on GLB via the null-manager gate; tsc green; not exported.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure client-side per-frame rendering math on already-loaded scene objects; no external input, network, or parsing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Denial of Service (frame-budget) | per-frame procedural steppers | mitigate | Allocation-reuse (module-scoped scratch quaternions, never `new` inside step) per crossfade.ts precedent, keeping per-frame cost negligible (~1-2ms full stack, RESEARCH PERFORMANCE-BUDGET). |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. |
</threat_model>

<verification>
- `pnpm --filter @khaveeai/react test` (all animation tests) passes.
- breathing/sway use `.multiply()` additive writes and `getHumanoidBoneNode`, never `.set()` or literal `getBoneNode`.
- expressionDrift gates on the null expression manager, targets only a non-emotion allow-list, and skips app-driven (non-zero) expressions.
- None of the three files appear in `packages/react/src/index.ts`.
</verification>

<success_criteria>
Three internal ref-driven idle procedural modules exist: breathing (chest/spine, additive, randomized period), sway (hips/spine, additive, independent period), expressionDrift (VRM-only via null-gate, non-clobbering of app-driven expressions). All follow the blink.ts pattern, use allocation-reused scratch quaternions, and are unit-tested for their pure delta math.
</success_criteria>

<output>
Create `.planning/phases/11-idle-transition-talking-states/11-03-SUMMARY.md` when done
</output>
