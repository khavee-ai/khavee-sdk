---
phase: 11-idle-transition-talking-states
plan: 05
type: execute
wave: 3
depends_on: ["11-01", "11-02", "11-03", "11-04"]
files_modified:
  - packages/react/src/animation/AnimationStateEngine.ts
  - packages/react/src/animation/AnimationStateEngine.test.ts
  - packages/react/src/VRMAvatar.tsx
  - packages/react/src/GLBAvatar.tsx
autonomous: true
requirements: [IDLE-01, IDLE-02, TRANS-01, TRANS-02, TALK-01, TALK-02, PERF-01]
must_haves:
  truths:
    - "update(delta) runs breathing, sway, expression drift, talk-cycle, and audio-reactive amplitude in one fixed, documented order"
    - "breathing and sway compose on the spine bone via multiply() and the combined spine delta is clamped to a documented bound"
    - "starting and stopped transitions apply a ~1.2s minimum-duration floor via beginCrossfade's floorSeconds"
    - "the talk cycle triggers real crossfades to the next variant during speaking, and audio volume scales procedural amplitude only while speaking"
    - "both avatars read currentVolume from useKhavee() and pass it into the controller"
  artifacts:
    - path: "packages/react/src/animation/AnimationStateEngine.ts"
      provides: "controller integration of all Phase 11 procedural steps + floorSeconds + currentVolume param"
      contains: "floorSeconds"
    - path: "packages/react/src/VRMAvatar.tsx"
      provides: "currentVolume threaded into useAnimationController"
      contains: "currentVolume"
    - path: "packages/react/src/GLBAvatar.tsx"
      provides: "currentVolume threaded into useAnimationController"
      contains: "currentVolume"
  key_links:
    - from: "packages/react/src/animation/AnimationStateEngine.ts"
      to: "beginCrossfade(..., floorSeconds)"
      via: "starting/stopped duration floor"
      pattern: "floorSeconds|floor"
    - from: "packages/react/src/animation/AnimationStateEngine.ts"
      to: "breathing/sway/expressionDrift/talkCycle/audioAmplitude"
      via: "update(delta) fixed-order composition"
      pattern: "breathing|sway|talkCycle"
---

<objective>
Integrate every Phase 11 system into the single shared controller: extend `useAnimationController` to run breathing → sway → (spine clamp) → expression drift → talk-cycle in a fixed documented order inside `update(delta)`, apply audio-reactive amplitude during `speaking`, wire the `floorSeconds` minimum-duration floor for `starting`/`stopped` (TRANS-01/02), and thread `currentVolume` from `useKhavee()` through both avatars into the controller.

Purpose: This is the wiring wave — it activates IDLE-01/02, TRANS-01/02, TALK-01/02, and enforces PERF-01's fixed composition order + bounded combined magnitude on the shared spine bone. All the building blocks (bone resolution, procedural modules, talk cycle, amplitude helper, currentVolume context) already exist from Waves 1-2; this plan connects them at the one call site both avatars route through.
Output: Modified `AnimationStateEngine.ts` (+ its test), and minimal `currentVolume` wiring in both avatars.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-idle-transition-talking-states/11-RESEARCH.md
@.planning/phases/11-idle-transition-talking-states/11-PATTERNS.md
@.planning/phases/wayfinder-map-1-animation-architecture/VERIFICATION-CHECKLIST.md

<interfaces>
<!-- After Waves 1-2, available to import from ./breathing ./sway ./expressionDrift ./talkCycle ./audioAmplitude: -->
useBreathing(): { step(adapter, delta, amplitudeScale?): void }
useSway(): { step(adapter, delta, amplitudeScale?): void }
useExpressionDrift(): { step(adapter, delta): void }
useTalkCycle(): { step({ chatStatus, currentAction, currentClipName, speakingVariants, delta }): string | null }
volumeToAmplitudeScale(currentVolume: number, chatStatus: ChatStatus): number

<!-- Current controller (AnimationStateEngine.ts): params object at lines 82-91; crossfade-trigger effect
     at 112-127 calls beginCrossfade(currentActionRef.current, toAction, root) with NO 4th arg;
     update(delta) body at 129-134 (stepCrossfade + blink.step) — Phase 11 steps go here. -->
<!-- crossfade.ts: beginCrossfade(from, to, scene, floorSeconds?) already accepts floorSeconds (no change needed). -->
<!-- STATUS_CLIP_PATTERNS (AnimationStateEngine.ts:32-38): speaking:/talk|gesture|speak/i, listening:/listen/i,
     thinking:/think/i, starting:/welcome|greet|hello|intro/i, stopped:/stop|bye|goodbye|outro/i (no "ready" entry). -->
<!-- VRMAvatar useKhavee() destructure ~line 307; controller call ~452-463. GLBAvatar useKhavee() ~line 97; controller call ~148-156. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire floorSeconds, a ready-idle pattern, the GLB speaking pattern, and the currentVolume param</name>
  <read_first>
    - packages/react/src/animation/AnimationStateEngine.ts (full — params signature 82-91, STATUS_CLIP_PATTERNS 32-38, crossfade-trigger effect 110-127)
    - packages/react/src/animation/crossfade.ts (poseGapToDuration/beginCrossfade floorSeconds param, lines 74 & 110-117)
    - .planning/phases/11-idle-transition-talking-states/11-RESEARCH.md (Pitfalls 2 and 3 — the "Taking" regex mismatch and the missing "ready" idle pattern)
  </read_first>
  <files>packages/react/src/animation/AnimationStateEngine.ts</files>
  <action>
    (1) Add `currentVolume?: number` to `useAnimationController`'s params object (following the existing flat-params convention) and destructure it. (2) In the crossfade-trigger effect (lines ~112-127), compute `const floor = (chatStatus === "starting" || chatStatus === "stopped") ? 1.2 : undefined;` and pass it as the 4th argument: `beginCrossfade(currentActionRef.current, toAction, root, floor)` — this satisfies TRANS-01/02's ~1.0-1.5s minimum-duration floor on top of pose-gap-adaptive timing. Use 1.2 (within the locked 1.0-1.5 range; Claude's discretion). Add `chatStatus` to the effect's dependency array since `floor` now reads it. (3) DECISION (Pitfall 3, locked here): add a `ready` entry to `STATUS_CLIP_PATTERNS` = `/idle|ready|rest/i`, with an inline comment that this lets a conventionally-named idle-loop clip (e.g. GLB's `"State 1 Idle (loop)"`) resolve as the moving idle base so IDLE-01's breathing/sway layer on motion rather than a frozen "Pose". (4) DECISION (Pitfall 2, locked here): extend the `speaking` pattern to also match the bundled GLB placeholder typo — change it to `/talk|gesture|speak|taking/i`, with an inline comment explaining this is a documented one-off accommodation for `happy.glb`'s `"State 4 Taking (loop)"` placeholder (spelled "Taking", not "Talking"), per D-01's placeholder-asset strategy. Do not touch crossfade.ts.
  </action>
  <verify>
    <automated>pnpm --filter @khaveeai/react build && grep -nE "floor|/idle\|ready\|rest/|taking" packages/react/src/animation/AnimationStateEngine.ts</automated>
  </verify>
  <acceptance_criteria>
    - `beginCrossfade(` in AnimationStateEngine.ts is called with a 4th argument that is 1.2 for starting/stopped and undefined otherwise
    - `STATUS_CLIP_PATTERNS` contains a `ready` key with regex `/idle|ready|rest/i`
    - The `speaking` regex includes `taking` (i.e. `/talk|gesture|speak|taking/i`) with an explanatory inline comment
    - `useAnimationController` params include `currentVolume?: number`
    - `pnpm --filter @khaveeai/react build` exits 0
  </acceptance_criteria>
  <done>floorSeconds floor is applied for starting/stopped, ready + GLB-taking clip patterns resolve, and currentVolume is a controller param; tsc green.</done>
</task>

<task type="auto">
  <name>Task 2: Compose all procedural steps in update(delta) with PERF-01 fixed order and bounded spine delta</name>
  <read_first>
    - packages/react/src/animation/AnimationStateEngine.ts (update(delta) body lines 129-134; the "Phase 11 adds ... here" marker comment)
    - packages/react/src/animation/breathing.ts, sway.ts, expressionDrift.ts, talkCycle.ts, audioAmplitude.ts (Wave-2 outputs — exact step signatures)
    - packages/react/src/animation/crossfade.ts (beginCrossfade + the shared blend-state pattern; the trigger helper this task reuses for talk-cycle switches)
    - .planning/phases/11-idle-transition-talking-states/11-RESEARCH.md (Open Question 2 — post-composition spine clamp recommendation)
  </read_first>
  <files>packages/react/src/animation/AnimationStateEngine.ts, packages/react/src/animation/AnimationStateEngine.test.ts</files>
  <action>
    Instantiate the new steppers once in the hook body via their hooks: `useBreathing()`, `useSway()`, `useExpressionDrift()`, `useTalkCycle()` (all ref-driven, never useState — matches this file's existing convention). Extract the crossfade-trigger logic (beginCrossfade + updating `currentActionRef`/`currentClipNameRef`/`blendRef`) into a single local `switchToClip(name)` helper reused by BOTH the existing chatStatus-driven effect AND the talk-cycle switch below, so blend state stays single-owner.

    Rewrite `update(delta)` to run, in this EXACT fixed and inline-documented order (PERF-01 "fixed, documented order"):
    1. `stepCrossfade(blendRef.current)` if active (existing).
    2. `blink.step(adapter, enableBlinking)` (existing).
    3. Compute `const amplitudeScale = volumeToAmplitudeScale(currentVolume ?? 0, chatStatus);`.
    4. Capture the spine bone's post-mixer base orientation: resolve `const spine = adapter.getHumanoidBoneNode("spine")`; if present, copy `spine.quaternion` into a module-scoped scratch `THREE.Quaternion` BEFORE any procedural bone write (needed for the PERF-01 clamp).
    5. `breathing.step(adapter, delta, amplitudeScale)` (writes chest + spine additively).
    6. `sway.step(adapter, delta, amplitudeScale)` (writes hips + spine additively).
    7. PERF-01 bounded magnitude: if `spine` was captured, measure the combined delta angle between the captured base and the now-composed `spine.quaternion` (via `.angleTo`); if it exceeds a documented `MAX_COMBINED_SPINE_DELTA_RAD` constant (Claude's discretion, e.g. ~0.12 rad), `slerp` the spine quaternion from the captured base toward the composed result so the net delta is capped at the bound. Document inline WHY breathing precedes sway and why the clamp is post-composition.
    8. `expressionDrift.step(adapter, delta)` (VRM-only; no bone/spine interaction).
    9. Talk-cycle: compute `speakingVariants` = `availableNames.filter(n => STATUS_CLIP_PATTERNS.speaking.test(n))`; call `talkCycle.step({ chatStatus, currentAction: currentActionRef.current, currentClipName: currentClipNameRef.current, speakingVariants, delta })`; if it returns a non-null next-variant name, resolve its action via `getAction(name)` and call `switchToClip(name)` — this is the ONLY place talk variants advance, driven by loop boundary + dwell, never a timer (TALK-01). The talk-cycle switch must NOT be gated on `amplitudeScale` — audio only scales procedural amplitude, never selection (TALK-02).

    Update `AnimationStateEngine.test.ts`: add assertions that (a) `resolveBaseClip("ready", null, ["State 1 Idle (loop)", "Pose"])` now resolves the idle-loop clip via the new `ready` pattern, and (b) `resolveBaseClip("speaking", null, ["State 4 Taking (loop)"])` resolves that clip via the extended speaking regex. Keep existing tests green.
  </action>
  <verify>
    <automated>pnpm --filter @khaveeai/react exec vitest run src/animation/AnimationStateEngine.test.ts && grep -Ec "setInterval|setTimeout" packages/react/src/animation/AnimationStateEngine.ts</automated>
  </verify>
  <acceptance_criteria>
    - `update(delta)` calls breathing.step, sway.step, expressionDrift.step, and talkCycle.step, in that documented order, after blink.step
    - A `MAX_COMBINED_SPINE_DELTA_RAD` constant exists and the spine's combined delta is clamped against the captured base after breathing+sway
    - `grep -Ec "setInterval|setTimeout" packages/react/src/animation/AnimationStateEngine.ts` returns 0 (TALK-01 no live-clock)
    - `switchToClip` (or an equivalently-named single helper) is used by both the chatStatus effect and the talk-cycle switch (blend state single-owner)
    - New tests assert `ready` resolves an idle-loop clip and `speaking` resolves the `"Taking"` clip; `pnpm --filter @khaveeai/react exec vitest run src/animation/AnimationStateEngine.test.ts` passes all
  </acceptance_criteria>
  <done>update(delta) composes breathing→sway→spine-clamp→drift→talk-cycle in a fixed documented order with a bounded spine delta and timer-free talk cycling; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Thread currentVolume from useKhavee() into both avatars' controller calls</name>
  <read_first>
    - packages/react/src/VRMAvatar.tsx (useKhavee() destructure ~line 307; useAnimationController call ~452-463; useFrame body ~480-502 — MUST remain unchanged in structure)
    - packages/react/src/GLBAvatar.tsx (useKhavee() destructure ~line 97; useAnimationController call ~148-156; useFrame ~158-163)
    - packages/react/src/KhaveeProvider.tsx (confirm currentVolume is on the context after Plan 02)
  </read_first>
  <files>packages/react/src/VRMAvatar.tsx, packages/react/src/GLBAvatar.tsx</files>
  <action>
    In `VRMAvatar.tsx`, add `currentVolume` to the existing `useKhavee()` destructure and pass `currentVolume` into the `useAnimationController({...})` params object (alongside `chatStatus`, `currentAnimation`, etc.) — following the same "pull from useKhavee(), pass straight into controller params" shape already used for `chatStatus`. Do the same in `GLBAvatar.tsx`. Do NOT add any new lines to either avatar's `useFrame` body — all procedural steps live inside `controller.update(delta)` (frame-ordering contract, RESEARCH Pitfall 6). Do NOT re-touch the `getHumanoidBoneNode` adapter methods added in Plan 01. This is purely threading the one new value through.
  </action>
  <verify>
    <automated>pnpm --filter @khaveeai/react build && grep -c "currentVolume" packages/react/src/VRMAvatar.tsx packages/react/src/GLBAvatar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "currentVolume" packages/react/src/VRMAvatar.tsx` shows it in both the useKhavee() destructure and the useAnimationController params
    - `grep -n "currentVolume" packages/react/src/GLBAvatar.tsx` shows the same
    - Neither avatar's `useFrame` body gains new procedural-step lines (steps remain inside controller.update)
    - `pnpm --filter @khaveeai/react build` exits 0
  </acceptance_criteria>
  <done>Both avatars read currentVolume from context and pass it into the shared controller; tsc green; frame-ordering contract preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| currentVolume (context) -> controller amplitude | Volume consumed to scale procedural amplitude; already clamped at ingest (Plan 02) and re-clamped in audioAmplitude (Plan 04). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Tampering / Input Validation (ASVS V5) | volumeToAmplitudeScale usage in update(delta) | mitigate | currentVolume is clamped twice (KhaveeProvider ingest + audioAmplitude) before scaling; `currentVolume ?? 0` guards undefined. |
| T-11-07 | Denial of Service (frame-budget) | combined per-frame procedural stack | mitigate | Module-scoped scratch quaternions, no per-frame allocation, and a bounded spine delta cap; talk-cycle uses timer-free polling. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. |
</threat_model>

<verification>
- `pnpm --filter @khaveeai/react test` passes the full animation suite including new AnimationStateEngine assertions.
- No `setInterval`/`setTimeout` in AnimationStateEngine.ts.
- Both avatars thread `currentVolume`; `pnpm --filter @khaveeai/react build` green.
</verification>

<success_criteria>
The shared controller composes breathing, sway, expression drift, talk-cycle, and audio-reactive amplitude in one fixed documented order with a bounded spine delta and timer-free variant cycling; starting/stopped get a ~1.2s duration floor; both avatars feed currentVolume through. All animation tests pass and tsc is clean.
</success_criteria>

<output>
Create `.planning/phases/11-idle-transition-talking-states/11-05-SUMMARY.md` when done
</output>
