---
phase: 11-idle-transition-talking-states
plan: 06
type: execute
wave: 4
depends_on: ["11-05"]
files_modified: []
autonomous: false
requirements: [IDLE-01, IDLE-02, TRANS-01, TRANS-02, TALK-01, TALK-02, PERF-01]
must_haves:
  truths:
    - "Idle for 30+s shows continuous breathing + independent sway on both avatars"
    - "VRM shows expression drift in idle; GLB shows none"
    - "starting and stopped hold their dedicated clip for a visible minimum duration"
    - "speaking cycles 2+ talk variants only at loop boundaries, never on a fixed interval, min ~2s dwell"
    - "louder/quieter speech visibly changes procedural amplitude but not clip choice or timing"
    - "no live-clock timers and no .set() overwrite remain in the animation module"
  artifacts:
    - path: ".planning/phases/11-idle-transition-talking-states/11-06-SUMMARY.md"
      provides: "sign-off record of the per-state human verification"
  key_links: []
---

<objective>
Verify the Phase 11 behavior on a running build against the wayfinder per-state checklist, and gate the code-level invariants (no live-clock timers, additive-not-overwrite spine composition) with objective greps.

Purpose: IDLE/TRANS/TALK requirements are visual/behavioral — they need a human watching a real avatar, plus machine-checkable proof that the timer-free and additive-composition invariants hold. This is the phase's acceptance gate.
Output: Objective grep-gate results + a blocking human-verify sign-off recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-idle-transition-talking-states/11-RESEARCH.md
@.planning/phases/wayfinder-map-1-animation-architecture/VERIFICATION-CHECKLIST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Objective invariant gates (timer-free + additive composition + full test suite)</name>
  <read_first>
    - packages/react/src/animation/AnimationStateEngine.ts
    - packages/react/src/animation/talkCycle.ts
    - packages/react/src/animation/breathing.ts
    - packages/react/src/animation/sway.ts
  </read_first>
  <files></files>
  <action>
    Run the objective code-level gates from the wayfinder checklist's objective section for this phase's scope. Specifically: (1) assert no live-clock timers anywhere in the animation module — `grep -REc "setInterval|setTimeout" packages/react/src/animation/` must total 0 across `talkCycle.ts`, `AnimationStateEngine.ts`, `breathing.ts`, `sway.ts`, `expressionDrift.ts` (TALK-01). (2) assert breathing/sway write additively, never overwrite: in `breathing.ts` and `sway.ts`, `grep -c "\.quaternion\.set("` returns 0 and `grep -c "\.multiply("` >= 1 (PERF-01). (3) assert the internal-only convention held: `grep -Ec "breathing|sway|expressionDrift|talkCycle|audioAmplitude" packages/react/src/index.ts` returns 0. (4) run the full package test suite: `pnpm --filter @khaveeai/react test`. Record each gate's pass/fail in the SUMMARY. If any gate fails, stop and report before the human-verify task — do not present a checkpoint on top of a failing invariant.
  </action>
  <verify>
    <automated>cd packages/react && grep -REc "setInterval|setTimeout" src/animation/ | grep -v ':0$' | grep -c . ; pnpm --filter @khaveeai/react test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -REc "setInterval|setTimeout" packages/react/src/animation/` shows 0 matches in every file (TALK-01)
    - `grep -c "\.quaternion\.set(" packages/react/src/animation/breathing.ts` and `.../sway.ts` both return 0; `.multiply(` present in both (PERF-01)
    - `grep -Ec "breathing|sway|expressionDrift|talkCycle|audioAmplitude" packages/react/src/index.ts` returns 0
    - `pnpm --filter @khaveeai/react test` reports all suites passing
  </acceptance_criteria>
  <done>All objective invariant gates pass and the full react test suite is green, recorded in the SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Per-state human verification against the wayfinder checklist</name>
  <action>Pause for the developer to run the running dev build and verify the seven per-state behaviors below against `.planning/phases/wayfinder-map-1-animation-architecture/VERIFICATION-CHECKLIST.md`, then report the result. Do not auto-approve — idle liveliness, transition floors, talk-cycle naturalness, and audio-reactive amplitude are visual/behavioral judgements only the human can make.</action>
  <what-built>
    The full Phase 11 natural-motion stack is wired into both avatars: procedural breathing + independent sway idle base, VRM-only expression drift, starting/stopped dedicated clips with a ~1.2s minimum-duration floor, loop-boundary talk-clip cycling (~2s min dwell, no timers), and audio-reactive procedural amplitude scaled by live volume during speaking only.
  </what-built>
  <how-to-verify>
    Run the demo dev server (`pnpm dev` at repo root) and open the VRM avatar test page (http://localhost:3000/openai-avatar-test) and the GLB test page (http://localhost:3000/glb-avatar-test). Working against the per-state prompts in `.planning/phases/wayfinder-map-1-animation-architecture/VERIFICATION-CHECKLIST.md`, confirm each of the following:
    1. IDLE-01: Leave the avatar untouched in `ready` for 30+ seconds — verify continuous, non-robotic breathing (chest/spine rise-fall) AND a slower, independent weight-shift sway (not phase-locked to breathing) on BOTH the VRM and GLB avatars.
    2. IDLE-02: On the VRM avatar, verify subtle expression rest-state drift (a slow shift in 1-2 expression values) during idle; on the GLB avatar, verify NO expression drift.
    3. TRANS-01: Start a session — verify the `starting` state holds its greeting clip visibly for at least ~1-1.5s even when the pose gap is small (the floor, not an instant snap).
    4. TRANS-02: End a session — verify a distinct `stopped` goodbye/settling clip plays (visibly different from the idle base) with the same minimum-duration floor.
    5. TALK-01: Speak continuously for 60+ seconds — verify the talk clip cycles between 2+ variants, switching only at natural loop completions (never on an obvious fixed interval), with no variant playing for less than ~2s.
    6. TALK-02: Speak louder, then quieter — verify the procedural motion amplitude visibly changes with volume in real time, while the clip selection and cycle timing do NOT change.
    7. PERF-01: During states where breathing and sway both act on the spine, verify the motion is smooth and bounded (no jitter, no one system visibly cancelling the other, no runaway amplitude).
    Note: per D-01, VRM has no dedicated starting/stopped/idle-distinct clips today (placeholder-asset gap, issue #17) — for VRM, verify the duration-FLOOR mechanism works even if the visual clip is the idle fallback; GLB has real placeholder candidates for starting/idle/speaking.
  </how-to-verify>
  <resume-signal>Type "approved" if all seven behaviors verify, or describe which state(s) failed and how (gap closure will follow).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification-only plan; no code changes, no new input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | Input Validation (ASVS V5) | live-volume amplitude path (verification) | accept | Clamps already enforced in Plans 02/04; this plan visually confirms no extreme/broken motion occurs under loud speech. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. |
</threat_model>

<verification>
- Objective grep gates all pass (timer-free, additive composition, internal-only).
- Full react test suite green.
- Human confirms all seven per-state behaviors on a running VRM and GLB build.
</verification>

<success_criteria>
Every Phase 11 requirement (IDLE-01/02, TRANS-01/02, TALK-01/02, PERF-01) is confirmed both by objective code gates and by a human running the build against the wayfinder per-state checklist, with sign-off recorded.
</success_criteria>

<output>
Create `.planning/phases/11-idle-transition-talking-states/11-06-SUMMARY.md` when done
</output>
