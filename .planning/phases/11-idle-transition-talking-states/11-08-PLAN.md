---
phase: 11-idle-transition-talking-states
plan: 08
type: execute
wave: 2
depends_on: ["11-07"]
files_modified: []
autonomous: false
gap_closure: true
requirements: [IDLE-01, IDLE-02, TRANS-01, TRANS-02, TALK-01, TALK-02, PERF-01]
user_setup: []

must_haves:
  truths:
    - "The four behaviors 11-06 reported failing (IDLE-02, TRANS-01, TRANS-02, TALK-01) now pass a live human per-state re-check"
    - "The three behaviors 11-06 did not re-confirm (IDLE-01, TALK-02, PERF-01) still pass, since 11-07 touched shared composition/effect code paths"
    - "The timer-free / additive-not-overwrite / internal-only objective invariants still hold after the 11-07 fixes"
  artifacts:
    - path: ".planning/phases/11-idle-transition-talking-states/11-08-SUMMARY.md"
      provides: "sign-off record of the gap-closure re-verification"
  key_links: []
---

<objective>
Re-verify Phase 11 on a running build after the 11-07 fixes: re-run the objective code-level invariant gates plus the new fix-specific greps, then have a human re-check the seven per-state behaviors against the wayfinder checklist — with focus on the four that failed in 11-06 (IDLE-02, TRANS-01, TRANS-02, TALK-01) and a quick re-check of the three that were not re-confirmed (IDLE-01, TALK-02, PERF-01) because 11-07 edited shared composition/effect code.

Purpose: this is the acceptance gate that decides whether Phase 11's gap closure is complete. The failing behaviors are visual/behavioral — a human must watch a real avatar.
Output: objective gate results + a blocking human-verify sign-off recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-idle-transition-talking-states/11-06-SUMMARY.md
@.planning/phases/11-idle-transition-talking-states/11-07-PLAN.md
@.planning/phases/wayfinder-map-1-animation-architecture/VERIFICATION-CHECKLIST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Objective invariant + fix-landed gates (timer-free, additive, internal-only, + 11-07 fixes) and full test suite</name>
  <read_first>
    - packages/react/src/animation/AnimationStateEngine.ts
    - packages/react/src/animation/expressionDrift.ts
    - packages/react/src/VRMAvatar.tsx
    - packages/react/src/GLBAvatar.tsx
  </read_first>
  <files></files>
  <action>
    Re-run the objective gates from 11-06 (they must still hold after 11-07) plus new greps proving the 11-07 fixes landed. Record each result in the SUMMARY; if ANY gate fails, stop and report before presenting the human checkpoint — do not put a checkpoint on top of a failing invariant.

    Invariant gates (unchanged from 11-06, must still pass):
    1. No live-clock timers anywhere in the animation module: `grep -REc "setInterval|setTimeout" packages/react/src/animation/` totals 0 across every file (TALK-01).
    2. Breathing/sway write additively, never overwrite: in `breathing.ts` and `sway.ts`, filtered `grep -v '^ *[*/]' | grep -c '\.quaternion\.set('` returns 0 and `\.multiply(` appears >= 1 in each (PERF-01).
    3. Internal-only convention: `grep -Ec "breathing|sway|expressionDrift|talkCycle|audioAmplitude" packages/react/src/index.ts` returns 0.

    Fix-landed gates (new, prove 11-07's changes are present):
    4. `grep -c "targetName, getAction, chatStatus" packages/react/src/animation/AnimationStateEngine.ts` returns 0 (getAction dropped from the effect deps — TALK-01/TRANS-01).
    5. `grep -c "SETTLE_RAMP_SECONDS" packages/react/src/animation/AnimationStateEngine.ts` returns >= 2 and `easeInOutCubic` appears in that file (eased settle — TRANS-02).
    6. `grep -c "lastWritten" packages/react/src/animation/expressionDrift.ts` returns >= 1 and `"relaxed"` appears in the candidate list (drift ownership + visible candidates — IDLE-02).
    7. `useCallback` appears in both `VRMAvatar.tsx` and `GLBAvatar.tsx`.

    Then run the full package test suite: `pnpm --filter @khaveeai/react test` (must include the new `expressionDrift.test.ts`).
  </action>
  <verify>
    <automated>cd packages/react && grep -REc "setInterval|setTimeout" src/animation/ | grep -v ':0$' | grep -c . | grep -qx 0 && grep -c "targetName, getAction, chatStatus" src/animation/AnimationStateEngine.ts | grep -qx 0 && grep -q "SETTLE_RAMP_SECONDS" src/animation/AnimationStateEngine.ts && grep -q "lastWritten" src/animation/expressionDrift.ts && pnpm --filter @khaveeai/react test</automated>
  </verify>
  <acceptance_criteria>
    - Gates 1-3 (timer-free, additive-not-overwrite, internal-only) all pass exactly as in 11-06
    - Gates 4-7 confirm the four/three fixes landed (deps change, settle ramp, drift ownership + candidates, memoized accessors)
    - `pnpm --filter @khaveeai/react test` reports all suites passing, including `expressionDrift.test.ts`
    - Each gate's pass/fail is recorded in `11-08-SUMMARY.md`
  </acceptance_criteria>
  <done>All objective + fix-landed gates pass and the full react test suite is green, recorded in the SUMMARY. If any gate fails, the plan stops here with a report instead of presenting the checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Per-state human re-verification of the four fixed + three unconfirmed behaviors</name>
  <action>Pause for the developer to run the dev build and re-check the seven per-state behaviors against `.planning/phases/wayfinder-map-1-animation-architecture/VERIFICATION-CHECKLIST.md`, with priority on the four that failed in 11-06, then report the result. Do not auto-approve — idle liveliness, transition smoothness, talk-cycle naturalness, and audio-reactive amplitude are visual/behavioral judgements only the human can make.</action>
  <what-built>
    Plan 11-07 fixed the three diagnosed root causes behind the four failing behaviors: (1) the crossfade-trigger effect no longer re-fires on unrelated re-renders (getAction dropped from deps + memoized in both avatars) and talkCycle is now the sole owner of talk-variant selection during speaking (fixes TALK-01 snap-back and TRANS-01 start double-trigger); (2) the stopped-settle scale eases over ~1.2s via easeInOutCubic in both directions instead of an instant binary cut (fixes TRANS-02 stop-snap, smooths TRANS-01 start); (3) expression drift now targets present, visible VRoid candidates (relaxed/happy, not the absent browInnerUp or the visually-inert neutral) and uses drift-ownership tracking instead of a self-defeating non-zero guard that froze it after one frame (fixes IDLE-02).
  </what-built>
  <how-to-verify>
    Run the demo dev server (`pnpm dev` at repo root) and open the VRM avatar test page (http://localhost:3000/openai-avatar-test) and the GLB test page (http://localhost:3000/glb-avatar-test). Working against the per-state prompts in the wayfinder VERIFICATION-CHECKLIST.md, confirm each behavior. The four marked PREVIOUSLY FAILED are the ones this gap closure must now pass:

    1. IDLE-01 (re-check): Leave the avatar untouched in `ready` for 30+ seconds — confirm continuous non-robotic breathing AND a slower, independent weight-shift sway on BOTH VRM and GLB. (Re-checked because 11-07 changed the shared `update()` settle path.)
    2. IDLE-02 (PREVIOUSLY FAILED — "no expression drift"): On the VRM avatar, confirm a subtle, continuous rest-state expression drift is now VISIBLE during idle (a slow shift in the relaxed/happy rest values), not frozen. On the GLB avatar, confirm NO expression drift.
    3. TRANS-01 (PREVIOUSLY FAILED — "it snaps, not smooth"): Start a session — confirm the transition into the session is SMOOTH (no snap): the greeting/idle base holds for at least the ~1.2s floor and the idle motion eases back to full amplitude rather than popping. (GLB has a real greeting candidate, `State 3 Welcome (loop)`; VRM uses the idle fallback — verify the FLOOR + eased amplitude on both.)
    4. TRANS-02 (PREVIOUSLY FAILED — "it snaps, not smooth"): End a session — confirm the `stopped` settle is now SMOOTH: breathing/sway idle motion EASES toward stillness over ~1.2s (avatar settling to rest, facial drift damping with it), not an instant amplitude drop. This is the D-01 placeholder settle cue on BOTH formats (no dedicated goodbye clip yet — issue #17 / ASSET-01).
    5. TALK-01 (PREVIOUSLY FAILED — "snaps to second variant, jitters, snaps back to first"): Speak continuously for 60+ seconds — on the VRM avatar (2 variants: `talking.fbx` / `talking1.fbx`), confirm the talk clip cycles between variants ONLY at natural loop completions with NO snap-back to the first variant and no jitter at the boundary, each variant playing >= ~2s. On GLB (single placeholder talk clip `State 4 Taking (loop)`, no 2nd variant — issue #17 / ASSET-04), confirm the single talk clip plays steadily during speaking with no snap.
    6. TALK-02 (re-check): Speak louder then quieter — confirm procedural amplitude visibly tracks volume in real time while clip selection and cycle timing do NOT change. (Re-checked because 11-07 touched the amplitude/settle compose path.)
    7. PERF-01 (re-check): During states where breathing and sway both act on the spine, confirm motion is smooth and bounded — no jitter, no one system cancelling the other, no runaway amplitude. (Re-checked because 11-07 changed the settle scaling feeding the spine composition.)

    Placeholder-asset note (unchanged, D-01 / issue #17): VRM still has no dedicated greeting/goodbye clips and GLB still lacks a 2nd talk variant; verify the mechanisms (duration floor, eased settle cue, 2-variant VRM cycling) against the placeholders, not bespoke greeting/goodbye animations.
  </how-to-verify>
  <resume-signal>Type "approved" if all seven behaviors verify — especially the four previously-failing ones (IDLE-02, TRANS-01, TRANS-02, TALK-01) now reading smooth/visible — or describe which state(s) still fail and how (a further gap-closure pass will follow).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification-only plan; no code changes, no new input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-11 | Input Validation (ASVS V5) | live-volume amplitude path (verification) | accept | Clamps already enforced in Plans 11-02/11-04; this plan visually confirms no extreme/broken motion under loud speech. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. |
</threat_model>

<verification>
- Objective invariant gates (timer-free, additive, internal-only) plus fix-landed greps all pass.
- Full react test suite green, including the new `expressionDrift.test.ts`.
- Human confirms the four previously-failing behaviors now pass and the three unconfirmed ones still pass, against the wayfinder per-state checklist, accounting for the documented placeholder-asset gaps (issue #17).
</verification>

<success_criteria>
Every Phase 11 requirement (IDLE-01/02, TRANS-01/02, TALK-01/02, PERF-01) is confirmed by objective code gates AND a human running the build, with the four behaviors that failed in 11-06 now passing, and sign-off recorded in the SUMMARY. If any behavior still fails, it is reported as a remaining gap rather than silently approved.
</success_criteria>

<output>
Create `.planning/phases/11-idle-transition-talking-states/11-08-SUMMARY.md` when done.
</output>
