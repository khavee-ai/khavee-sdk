---
phase: 11-idle-transition-talking-states
plan: 09
type: execute
wave: 1
depends_on: ["11-07"]
files_modified:
  - packages/react/src/animation/expressionDrift.ts
  - packages/react/src/animation/expressionDrift.test.ts
  - packages/react/src/animation/AnimationStateEngine.ts
  - packages/react/src/animation/AnimationStateEngine.test.ts
autonomous: true
gap_closure: true
requirements: [IDLE-02]
user_setup: []

must_haves:
  truths:
    - "On the connected (ready-state) VRM avatar, rest-state expression drift is written at a perceptible peak weight (not the ~0.018 the stopped-settle ramp currently damps it to, and not a peak so subtle a human cannot see it) — a genuinely NEW root cause vs 11-07's candidate/ownership fix (IDLE-02)"
    - "The dominant IDLE-02 root cause is confirmed by a recorded runtime diagnostic (present-expression enumeration + effective-amplitude computation + optional in-app instrumentation), not assumed — the fix targets the confirmed cause, not a repeat of 11-07"
    - "The avatar no longer accumulates an abnormal rotation ('spins weird') during the first-mount window before the base clip drives the skeleton — procedural bone writes (breathing/sway) are guarded until a base action is actually posing the bones, or the spin is root-caused and explicitly triaged/deferred with stated reasoning"
    - "GLB expression drift remains an automatic no-op; the TRANS-02 stopped-settle facial damping still holds after the amplitude change; steady-state breathing/sway (IDLE-01, already approved) is unchanged"
  artifacts:
    - path: "packages/react/src/animation/expressionDrift.ts"
      provides: "perceptible drift amplitude after runtime-confirmed diagnosis + updated header recording the amplitude/settle-damping root cause"
      contains: "DEFAULT_AMPLITUDE"
    - path: "packages/react/src/animation/AnimationStateEngine.ts"
      provides: "first-mount guard so breathing/sway additive bone writes do not accumulate before a base action drives the skeleton (spin-bug fix) OR a documented triage decision"
    - path: "packages/react/src/animation/expressionDrift.test.ts"
      provides: "effective-peak-weight assertion proving drift is perceptible at ready-state settleScale and damped at stopped-state settleScale"
  key_links:
    - from: "packages/react/src/animation/AnimationStateEngine.ts"
      to: "packages/react/src/animation/expressionDrift.ts"
      via: "expressionDrift.step(adapter, delta, settleScale)"
      pattern: "expressionDrift\\.step"
    - from: "packages/react/src/animation/AnimationStateEngine.ts"
      to: "packages/react/src/animation/breathing.ts"
      via: "guarded breathing.step call (skipped before base action drives bones)"
      pattern: "breathing\\.step"
---

<objective>
Second gap-closure pass for Phase 11. Two open code items from 11-08-SUMMARY.md:

1. **IDLE-02 — VRM expression drift still not visible.** 11-07 fixed a real code bug (candidate targeting `relaxed`/`happy` + `lastWritten` ownership tracking) but the human still sees zero expression change over 30s. This pass must DIAGNOSE the actual remaining cause at runtime before fixing — NOT re-apply 11-07's approach. The leading, statically-confirmed suspect is amplitude perceptibility: `DEFAULT_AMPLITUDE` (0.12) multiplied by the stopped-state `SETTLE_SCALE` (0.15) yields a ~0.018 peak weight, and the `openai-avatar-test` page sits in `chatStatus === "stopped"` until the human clicks Connect (default `KhaveeProvider` state is `"stopped"`), so the observed drift is damped to imperceptibility — and even the full `ready`-state 0.12 peak may be too subtle to notice. The task confirms which of the three hypotheses (present-names / amplitude / downstream-overwrite) dominates, then fixes it.

2. **First-load "spins weird" bug (untracked).** Not one of Phase 11's 7 requirements. Strong hypothesis: `breathing.ts`/`sway.ts` do `bone.quaternion.multiply(delta)` every frame assuming the mixer re-establishes the base pose, but during the first-mount window (before `currentActionRef.current` is set / while the initial crossfade weight ramps from 0) the mixer is not yet driving chest/spine/hips, so the additive deltas accumulate into a growing rotation, then settle once the clip drives the bones. IDLE-01 (steady-state breathing/sway) was approved in 11-08, isolating this to first-mount. Investigate and fix-with-confidence, or explicitly triage/defer with stated reasoning if diagnosis shows a deeper cause.

Purpose: make idle expression drift genuinely visible on a live connected build and stop the first-load spin, so the 11-10 human re-verification can pass without failing a THIRD time for the same undiagnosed reason.
Output: perceptible, diagnosed expression drift in `expressionDrift.ts` (+ test), a first-mount procedural-write guard (or documented triage) in `AnimationStateEngine.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-idle-transition-talking-states/11-CONTEXT.md
@.planning/phases/11-idle-transition-talking-states/11-PATTERNS.md
@.planning/phases/11-idle-transition-talking-states/11-07-PLAN.md
@.planning/phases/11-idle-transition-talking-states/11-07-SUMMARY.md
@.planning/phases/11-idle-transition-talking-states/11-08-SUMMARY.md

<empirical_findings>
Established directly this planning session against the current code — the executor should confirm (not blindly trust) these during the diagnostic, and must keep the model-agnostic presence/ownership checks intact:

- `expressionDrift.ts`: `DEFAULT_AMPLITUDE = 0.12` (line ~103), `DRIFT_CANDIDATES = ["relaxed", "happy", "browInnerUp"]` (line ~88), `MAX_ACTIVE_CANDIDATES = 2`. `stepExpressionDrift(state, adapter, delta, amplitudeScale = 1)` multiplies the sine weight by `amplitudeScale`. 11-07's `lastWritten` ownership guard and per-candidate `phaseOffsets` are present and unit-tested (expressionDrift.test.ts, passing).
- `AnimationStateEngine.ts`: `SETTLE_SCALE = 0.15` (line ~46). In `update()` step 3, `settleTarget = chatStatus === "stopped" ? SETTLE_SCALE : 1`, eased via `easeInOutCubic` over `SETTLE_RAMP_SECONDS = 1.2` into `ramp.current`, then `const settleScale = ramp.current`. Step 8 calls `expressionDrift.step(adapter, delta, settleScale)` (line ~371) — so in `stopped`, drift peak = `0.12 * 0.15 = 0.018`; in `ready`/idle-connected, drift peak = `0.12 * 1 = 0.12`. `settleRampRef` initial value is `{ current: 1, from: 1, target: 1, elapsed: 0 }` (line ~202); on first mount with default `chatStatus === "stopped"` it ramps 1 → 0.15.
- `openai-avatar-test/page.tsx` wires `OpenAIRealtimeProvider` and shows `Status: {chatStatus}`; chatStatus is `"stopped"` until the human clicks Connect (then transitions through `starting` → `ready`). The `ready` idle state the VERIFICATION-CHECKLIST names is only reached AFTER connecting.
- `breathing.ts` writes `chest.quaternion.multiply(_scratchDelta)` and `spine.quaternion.multiply(_scratchDelta)` (lines ~112-113); `sway.ts` writes `hips.quaternion.multiply(_scratchDelta)` and `spine.quaternion.multiply(_scratchDelta)` (lines ~101-102). Both rely on "the mixer/pose already wrote to these bones this frame" (their own inline comments) — i.e. they assume the base clip resets the bone each frame. Neither captures/restores a base itself.
- `VRMAvatar.tsx` frame order (lines ~494-516): `mixer.update(delta)` → app-expression `lerpExpression` over `expressions` (default map is EMPTY — `useState<Record<string,number>>({})` in KhaveeProvider, so nothing writes `relaxed`/`happy` unless the app calls `setExpression`) → `controller.update(delta)` (blink, breathing, sway, expressionDrift) → `currentVrm.update(delta)`.
- `volumeToAmplitudeScale` (audioAmplitude.ts) is already objectively covered by `audioAmplitude.test.ts` (monotonic increase while speaking, clamps out-of-range, returns 1 for non-speaking) — TALK-02's math is proven; no code change for TALK-02 in this plan.
</empirical_findings>

<interfaces>
<!-- Contracts the executor needs. Extracted from the current codebase. Use directly; no exploration needed. -->

AvatarFormatAdapter (packages/react/src/animation/types.ts):
  getMixer(): THREE.AnimationMixer
  getBoneNode(name): THREE.Object3D | null
  getHumanoidBoneNode(role): THREE.Object3D | null    // "hips"|"spine"|"chest"|"upperChest"|"neck"|"head"
  getExpressionManager(): VRMExpressionManager | null  // null on GLB

VRMExpressionManager (subset):
  getExpression(name): VRMExpression | null            // presence check
  getValue(name): number | null
  setValue(name, weight): void                         // ABSOLUTE overwrite
  expressions: VRMExpression[]                          // enumerate present expressions (each has .expressionName)

useAnimationController params object already includes: adapter, chatStatus, currentAnimation,
availableNames, getAction, getRoot, enableBlinking, currentVolume. Internal refs:
currentActionRef (null until first switchToClip), blendRef ({ active, ... }), settleRampRef.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Diagnose then fix invisible VRM expression drift (IDLE-02) — amplitude/settle-damping root cause, NOT a repeat of 11-07</name>
  <read_first>
    - packages/react/src/animation/expressionDrift.ts (full file — DEFAULT_AMPLITUDE, DRIFT_CANDIDATES, MAX_ACTIVE_CANDIDATES, the lastWritten ownership guard, stepExpressionDrift's amplitudeScale multiply, the header doc block)
    - packages/react/src/animation/expressionDrift.test.ts (existing 5 tests — the convention to extend, especially the amplitudeScale/phaseOffsets pinning)
    - packages/react/src/animation/AnimationStateEngine.ts (SETTLE_SCALE ~46, update() step 3 settle ramp ~307-320, step 8 expressionDrift.step call ~371)
    - packages/react/src/VRMAvatar.tsx (useFrame ~494-516 and lerpExpression ~479-492 — confirm nothing overwrites drift's write between controller.update and currentVrm.update; the default-empty expressions map)
    - src/app/openai-avatar-test/page.tsx (confirm chatStatus is "stopped" until Connect, and the state the human observes)
    - The <empirical_findings> and <interfaces> blocks above
  </read_first>
  <files>packages/react/src/animation/expressionDrift.ts, packages/react/src/animation/expressionDrift.test.ts</files>
  <behavior>
    - Test A (perceptible at ready): drifting with amplitudeScale=1 (ready-state settleScale) produces a peak written weight >= a documented PERCEPTIBLE_MIN threshold you set (>= 0.15 recommended; whatever value the diagnosis lands on must be asserted here as the new floor of visibility) and stays within [0, new DEFAULT_AMPLITUDE].
    - Test B (still damped at stopped): drifting with amplitudeScale=0.15 (the stopped-state settleScale = SETTLE_SCALE) produces a peak weight strictly LESS than the ready-state peak — proving the TRANS-02 settle damping still applies and the amplitude change did not remove it.
    - Test C (continuity preserved): across several frames the written weight still changes each frame and never freezes (the 11-07 lastWritten ownership behavior is intact after the amplitude change).
    - Test D (yield-to-app preserved): if an external writer sets a drifted candidate to a value drift did not write, the next step skips it (11-07 behavior unchanged).
    - Test E (GLB no-op preserved): null expression manager → step writes nothing, no throw.
  </behavior>
  <action>
    DIAGNOSE FIRST, then fix. Do NOT jump straight to changing a constant.

    (A) Runtime diagnostic — resolve which of the three hypotheses dominates, and RECORD the finding in the SUMMARY:
      - H1 (present names): confirm at runtime which expression names the loaded VRMs actually expose. MANDATORY: write a throwaway node/vitest script that loads `public/models/male.vrm` (and `public/models/female/blue-female.vrm`) via three's `GLTFLoader` + `@pixiv/three-vrm`'s `VRMLoaderPlugin`, reads `vrm.expressionManager.expressions.map(e => e.expressionName)`, and records the actual present-name list against `DRIFT_CANDIDATES` in the SUMMARY — this is the "actual finding, not an assumption" the diagnosis hinges on. Only if headless VRM load throws an unresolvable environment error (e.g. missing WebGL/canvas shim in node) is a fallback permitted, and that fallback MUST be explicitly justified in the SUMMARY (the specific error, why it's unresolvable, and why the 11-07 empirical finding is trusted instead) — it is not a default option. ALSO add TEMPORARY, off-by-default instrumentation to `stepExpressionDrift` — behind a module const `const DRIFT_DEBUG = false;` (or a `?driftDebug=1` URL-param / localStorage check) — that logs once per instance: `em.expressions.map(e => e.expressionName)`, the subset of `DRIFT_CANDIDATES` present, the computed peak `weight`, and `amplitudeScale`, so 11-10's human can cross-check live in devtools.
      - H2 (amplitude/settle damping — leading suspect): compute and record the effective peak weight in each observed state: stopped = `DEFAULT_AMPLITUDE * SETTLE_SCALE` = 0.12 * 0.15 = 0.018; ready = `DEFAULT_AMPLITUDE * 1` = 0.12. Note that the test page idles in `stopped` pre-connect, so the human most likely observed the 0.018 (imperceptible) peak, and that even 0.12 is a plausibly-too-subtle relaxed/happy weight.
      - H3 (downstream overwrite): confirm the frame order does NOT clobber drift's write — `lerpExpression` only iterates the app `expressions` map (empty by default), and `currentVrm.update(delta)` applies (does not zero) manager weights. Record whether any overwrite path exists.

    (B) Fix the confirmed dominant cause. Given the static analysis, the expected fix is amplitude perceptibility — but only apply it AFTER (A) confirms names are present (H1) and no overwrite steals the write (H3):
      - Raise `DEFAULT_AMPLITUDE` from 0.12 to a documented, empirically-perceptible value (Claude's Discretion per CONTEXT.md — pick within ~0.25-0.45 for relaxed/happy on VRoid rigs and document why; a mild rest smile at ~0.3 is clearly visible yet still subtle). Keep weights within [0,1]. Do NOT touch the `lastWritten` ownership guard, the present-name `getExpression(name) !== null` check, `MAX_ACTIVE_CANDIDATES`, the `phaseOffsets`, or the `DRIFT_CANDIDATES` order — those 11-07 fixes are correct and must remain.
      - Do NOT remove the stopped-state settle damping (TRANS-02). The idle visibility comes from raising the base amplitude so the FULL (ready) peak is clearly visible; the stopped state still damps proportionally.
      - If (A) instead proves H1 (a candidate is absent at runtime) or H3 (an overwrite exists) is the true dominant cause, fix THAT instead and document the pivot — do not force the amplitude fix on a wrong diagnosis.

    (C) Update the file-header doc block to record the NEW diagnosed root cause (amplitude imperceptible: 0.12*0.15=0.018 in stopped, and 0.12 too subtle even at full) distinct from 11-07's candidate/ownership fix, the new DEFAULT_AMPLITUDE value and rationale, and that the stopped-settle damping is intentionally retained.

    (D) Extend `expressionDrift.test.ts` with Tests A-E above (perceptible-at-ready floor, still-damped-at-stopped, continuity, yield-to-app, GLB no-op). Pin `phaseOffsets` where needed to make peak-weight assertions deterministic, per 11-07's existing test convention.

    Remove any temporary headless diagnostic script before committing; if you keep the in-app `DRIFT_DEBUG` instrumentation, it MUST default to off and be documented as a dev-only diagnostic aid in the header (it is an internal, non-exported module).
  </action>
  <verify>
    <automated>cd packages/react && grep -q "DEFAULT_AMPLITUDE" src/animation/expressionDrift.ts && grep -Eq "DEFAULT_AMPLITUDE *= *0\.(1[3-9]|[2-9])" src/animation/expressionDrift.ts && grep -q "lastWritten" src/animation/expressionDrift.ts && grep -q '"relaxed"' src/animation/expressionDrift.ts && pnpm --filter @khaveeai/react test</automated>
  </verify>
  <acceptance_criteria>
    - The diagnostic is RECORDED in 11-09-SUMMARY.md: the present-expression finding (H1), the computed effective peak weights per state (H2: 0.018 stopped / raised value ready), and the overwrite check (H3) — with an explicit statement of the dominant cause. It is NOT a restatement of 11-07's candidate/ownership diagnosis.
    - `DEFAULT_AMPLITUDE` is no longer 0.12; it is a documented perceptible value (grep `DEFAULT_AMPLITUDE *= *0\.(1[3-9]|[2-9])` matches, i.e. >= ~0.13), OR — if the diagnosis pivoted to H1/H3 — the pivot fix is applied and the pivot is documented in the SUMMARY.
    - The 11-07 fixes are intact: `lastWritten` present, `DRIFT_CANDIDATES` still leads with `"relaxed"`/`"happy"`, present-name check and `phaseOffsets` unchanged.
    - `expressionDrift.test.ts` includes the perceptible-at-ready floor assertion (Test A) and the still-damped-at-stopped assertion (Test B), and all react suites pass under `pnpm --filter @khaveeai/react test`.
    - Any temporary headless diagnostic is removed; any retained in-app instrumentation defaults to off and is documented as dev-only.
  </acceptance_criteria>
  <done>The dominant IDLE-02 root cause is diagnosed at runtime and recorded, the fix targets that confirmed cause (expected: raised, perceptible DEFAULT_AMPLITUDE while retaining the 11-07 candidate/ownership fixes and the TRANS-02 stopped-settle damping), and the test suite proves drift is perceptible at ready-state amplitude and still damped at stopped-state amplitude.</done>
</task>

<task type="auto">
  <name>Task 2: Root-cause and fix (or triage) the first-load "spins weird" avatar motion</name>
  <read_first>
    - packages/react/src/animation/AnimationStateEngine.ts (update() steps 4-7 spine base capture + breathing/sway + spine clamp ~322-362; switchToClip ~212-232; the crossfade-trigger effect ~234-272; currentActionRef/blendRef declarations ~180-202)
    - packages/react/src/animation/breathing.ts (full — the `chest.quaternion.multiply` / `spine.quaternion.multiply` additive writes and the "preserves whatever the mixer/pose already wrote" assumption in its header)
    - packages/react/src/animation/sway.ts (full — the `hips.quaternion.multiply` / `spine.quaternion.multiply` additive writes)
    - packages/react/src/animation/crossfade.ts (beginCrossfade signature — what it does when the `from` action is null, and how blend weight ramps from 0)
    - packages/react/src/VRMAvatar.tsx (useFrame ~494-516: mixer.update → controller.update ordering) and packages/react/src/GLBAvatar.tsx (useFrame ~158-163)
    - packages/react/src/animation/AnimationStateEngine.test.ts (existing controller tests — the convention to extend)
    - 11-08-SUMMARY.md "New observation" (~line 104) — the exact reported symptom
  </read_first>
  <files>packages/react/src/animation/AnimationStateEngine.ts, packages/react/src/animation/AnimationStateEngine.test.ts</files>
  <action>
    DIAGNOSE FIRST, then fix or triage. Symptom (11-08): on first load the avatar "moves strangely... spins weird," then settles. IDLE-01 (steady-state breathing/sway) is already approved, so the problem is confined to the first-mount window.

    (A) Confirm the mechanism. Leading hypothesis: `breathing.step`/`sway.step` write `bone.quaternion.multiply(delta)` every frame assuming the base clip already re-established the bone's rest orientation THIS frame. On first mount that assumption is false: `currentActionRef.current` is null until the crossfade-trigger `useEffect` fires post-mount, and even after `beginCrossfade(null, toAction, root, floor)` starts, the action's weight ramps from 0 over the ~1.2s floor — so for the first frames the mixer is not (fully) driving chest/spine/hips, and the additive `multiply()` deltas compound into a growing rotation until the clip weight reaches full and starts overwriting the bones each frame (matching "spins then settles"). Verify by reasoning through `beginCrossfade`'s null-`from` behavior and the blend-weight ramp; if a cheap unit reproduction is feasible (step the controller across N frames with a stub adapter whose bones are NOT reset between frames, asserting the spine quaternion angle grows unbounded), add it.

    (B) Fix-with-confidence if (A) holds: guard the additive procedural BONE writes (breathing + sway, steps 5-6, and the spine base-capture/clamp in steps 4/7) so they run ONLY once a base action is actually driving the skeleton — e.g. skip breathing/sway when `currentActionRef.current === null`, and additionally require the active blend to have ramped past a small weight threshold (or `blendRef.current` no longer being in its initial pre-first-clip state) so the deltas never compound onto un-driven bones. Document the guard inline referencing the first-mount accumulation cause. Do NOT gate blink or expression drift (they write manager scalars, not accumulating bone quaternions, so they are not part of this bug). Preserve the PERF-01 fixed composition order and the spine clamp for the steady-state path.

    (C) If (A) does NOT hold — e.g. the spin traces to the null→clip first crossfade pose blend (a large bind-pose→idle-pose blend that reads as a spin) or an effect re-fire — then fix that specific cause if it is low-risk and confidently root-caused, OR triage/defer: record the confirmed root cause, why a safe fix is out of reach this pass, and file it as a follow-up (a GitHub issue via `gh` or a ROADMAP note), per the gap-summary's explicit allowance to scope this as its own follow-up rather than block the plan. Do not silently absorb it into IDLE-02.

    Add/extend an `AnimationStateEngine.test.ts` case covering whichever fix you apply (e.g. procedural bone writes are skipped while `currentActionRef.current` is null). If you triage instead of fix, no test is required but the SUMMARY must record the root cause and the follow-up reference.
  </action>
  <verify>
    <automated>cd packages/react && grep -q "breathing.step" src/animation/AnimationStateEngine.ts && grep -q "sway.step" src/animation/AnimationStateEngine.ts && pnpm --filter @khaveeai/react test</automated>
  </verify>
  <acceptance_criteria>
    - The first-load spin is root-caused with a recorded mechanism in 11-09-SUMMARY.md (not a guess): either confirmed as first-mount additive-delta accumulation on un-driven bones, or an alternative confirmed cause.
    - If fixed: breathing/sway additive bone writes are guarded so they do not run before a base action drives the skeleton (grep-confirm the guard references `currentActionRef` or the blend-active state around the `breathing.step`/`sway.step` calls); a test covers the guard; steady-state IDLE-01 breathing/sway is unchanged (existing breathing/sway tests still pass).
    - If triaged/deferred: the SUMMARY records the confirmed root cause, the reason a safe fix is deferred, and a concrete follow-up reference (issue number or ROADMAP entry) — the item is not silently dropped.
    - `pnpm --filter @khaveeai/react test` passes.
  </acceptance_criteria>
  <done>The first-load spin is diagnosed with a recorded mechanism and either fixed with a first-mount procedural-write guard (with a regression test and IDLE-01 steady-state unchanged) or explicitly triaged/deferred with stated reasoning and a follow-up reference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Client-side 3D animation state only. No new network, auth, storage, or user-input boundary. `currentVolume` (the only externally-sourced scalar in scope) is already clamped in `KhaveeProvider.tsx` (11-02) and `audioAmplitude.ts` (11-04); this plan adds no new ingest path. Any temporary `DRIFT_DEBUG` instrumentation defaults off and logs only local scene-graph names, no secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-12 | Tampering (expression clobber) | raised `DEFAULT_AMPLITUDE` in `expressionDrift.ts` | mitigate | The 11-07 `lastWritten` ownership guard and present-name check are retained unchanged; raising base amplitude does not weaken the yield-to-app behavior (Test D asserts it still holds). |
| T-11-13 | Denial of Service (runaway rotation) | first-mount breathing/sway accumulation | mitigate | The first-mount guard prevents additive deltas compounding on un-driven bones; the existing PERF-01 spine clamp continues to bound the steady-state combined delta. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. |
</threat_model>

<verification>
- `pnpm --filter @khaveeai/react test` green after both tasks (existing suite + new expressionDrift perceptible/damped assertions + any spin-guard test).
- Objective greps confirm `DEFAULT_AMPLITUDE` raised, 11-07 guards intact, and the breathing/sway calls still present (guarded, not removed).
- The runtime IDLE-02 diagnosis and the spin-bug root cause are recorded in 11-09-SUMMARY.md.
- Visual confirmation (drift now visible on a connected build, spin gone) is deferred to the 11-10 human-verify checkpoint — a grep/unit test cannot make that judgement.
</verification>

<success_criteria>
IDLE-02's remaining invisibility is diagnosed at runtime (amplitude/settle-damping expected) and fixed with a perceptible, still-app-yielding drift amplitude that retains 11-07's candidate/ownership fixes and the TRANS-02 stopped-settle damping; the first-load spin is root-caused and either fixed with a first-mount procedural-write guard or explicitly triaged; the react test suite is green and both diagnoses are recorded for the 11-10 re-verification.
</success_criteria>

<output>
Create `.planning/phases/11-idle-transition-talking-states/11-09-SUMMARY.md` when done, recording BOTH the IDLE-02 runtime diagnosis (H1/H2/H3 findings + dominant cause) and the first-load spin root cause + disposition (fixed or triaged).
</output>
