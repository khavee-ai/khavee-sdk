---
phase: 11-idle-transition-talking-states
plan: 10
type: execute
wave: 2
depends_on: ["11-09"]
files_modified: []
autonomous: false
gap_closure: true
requirements: [IDLE-02, TALK-02]
user_setup: []

must_haves:
  truths:
    - "On a CONNECTED (ready-state) VRM avatar, a human sees a visible, continuous rest-state expression drift over 30+ seconds (IDLE-02 finally passes)"
    - "A human gives a DECISIVE (not 'maybe') verdict on TALK-02 via an explicit loud-vs-quiet side-by-side comparison, confirming procedural amplitude visibly tracks volume while clip selection/timing do not change"
    - "The first-load 'spins weird' motion is gone (or its deferral is confirmed acceptable by the human)"
    - "The 11-09 fixes landed in code (raised DEFAULT_AMPLITUDE, 11-07 guards intact, breathing/sway guarded or spin triaged) and the full react test suite is green"
  artifacts:
    - path: ".planning/phases/11-idle-transition-talking-states/11-10-SUMMARY.md"
      provides: "sign-off record of the second gap-closure re-verification (IDLE-02 + TALK-02 + spin disposition)"
  key_links: []
---

<objective>
Third re-verification for Phase 11, after the 11-09 fixes. Two requirements remain open from 11-08-SUMMARY.md: IDLE-02 (expression drift was still invisible) and TALK-02 (audio-reactive amplitude was "maybe pass" — unconfirmed). Plus the untracked first-load spin bug 11-09 root-caused.

This plan: (1) re-run the objective + fix-landed gates proving 11-09's changes are present and the react suite is green; then (2) a blocking human re-check that is DECISIVE this time — IDLE-02 judged only after CONNECTING (so the avatar is in `ready`, not the pre-connect `stopped` state that deliberately damps drift), and TALK-02 judged via an explicit loud-then-quiet side-by-side comparison rather than a vague glance. The five already-passing requirements (IDLE-01, TRANS-01, TRANS-02, TALK-01, PERF-01) are NOT re-litigated — only spot-checked for regression since 11-09 touched shared composition code.

Purpose: this is the acceptance gate that decides whether Phase 11 gap closure is finally complete. The open items are visual/behavioral — a human must watch a real avatar.
Output: objective gate results + a blocking human-verify sign-off recorded in 11-10-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-idle-transition-talking-states/11-08-SUMMARY.md
@.planning/phases/11-idle-transition-talking-states/11-09-PLAN.md
@.planning/phases/wayfinder-map-1-animation-architecture/VERIFICATION-CHECKLIST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix-landed gates + full react test suite (prove 11-09 landed before the human check)</name>
  <read_first>
    - packages/react/src/animation/expressionDrift.ts (confirm raised DEFAULT_AMPLITUDE + retained lastWritten/relaxed)
    - packages/react/src/animation/AnimationStateEngine.ts (confirm the breathing/sway first-mount guard if 11-09 fixed the spin, or the triage note)
    - .planning/phases/11-idle-transition-talking-states/11-09-SUMMARY.md (read the recorded IDLE-02 diagnosis and spin disposition so the checkpoint framing below matches what 11-09 actually did)
  </read_first>
  <files></files>
  <action>
    Re-run the objective invariant gates (must still hold after 11-09) plus new greps proving 11-09's fixes landed. Record each result in 11-10-SUMMARY.md. If ANY gate fails, STOP and report before presenting the human checkpoint — do not put a checkpoint on top of a failing invariant.

    Invariant gates (must still pass):
    1. No live-clock timers in the animation module: `grep -REc "setInterval|setTimeout" packages/react/src/animation/` totals 0 across every file.
    2. Breathing/sway additive-not-overwrite: in `breathing.ts` and `sway.ts`, `grep -v '^ *[*/]' | grep -c '\.quaternion\.set('` returns 0, and `\.multiply(` appears >= 1 in each.
    3. Internal-only: `grep -Ec "breathing|sway|expressionDrift|talkCycle|audioAmplitude" packages/react/src/index.ts` returns 0.

    Fix-landed gates (prove 11-09 is present):
    4. `expressionDrift.ts` `DEFAULT_AMPLITUDE` is raised above 0.12: `grep -Eq "DEFAULT_AMPLITUDE *= *0\.(1[3-9]|[2-9])" packages/react/src/animation/expressionDrift.ts` matches (IDLE-02 perceptibility fix).
    5. 11-07 guards intact: `grep -c "lastWritten" packages/react/src/animation/expressionDrift.ts` >= 1 AND `"relaxed"` still appears in the candidate list.
    6. Spin disposition landed: EITHER a first-mount guard exists around the breathing/sway calls in `AnimationStateEngine.ts` (grep for the guard referencing `currentActionRef` / blend-active state near `breathing.step`) — confirm against what 11-09-SUMMARY records — OR, if 11-09 triaged the spin, confirm the follow-up reference (issue/ROADMAP entry) exists. Record which path 11-09 took.

    Then run the full package test suite: `pnpm --filter @khaveeai/react test` (must include `expressionDrift.test.ts` with the new perceptible-at-ready / still-damped-at-stopped assertions).
  </action>
  <verify>
    <automated>cd packages/react && grep -REc "setInterval|setTimeout" src/animation/ | grep -v ':0$' | grep -c . | grep -qx 0 && grep -Eq "DEFAULT_AMPLITUDE *= *0\.(1[3-9]|[2-9])" src/animation/expressionDrift.ts && grep -q "lastWritten" src/animation/expressionDrift.ts && grep -q '"relaxed"' src/animation/expressionDrift.ts && pnpm --filter @khaveeai/react test</automated>
  </verify>
  <acceptance_criteria>
    - Gates 1-3 (timer-free, additive-not-overwrite, internal-only) still pass.
    - Gate 4 confirms DEFAULT_AMPLITUDE raised above 0.12; Gate 5 confirms the 11-07 lastWritten/relaxed fixes remain; Gate 6 confirms the spin fix OR the triage follow-up, matching 11-09-SUMMARY.
    - `pnpm --filter @khaveeai/react test` reports all suites passing, including the extended `expressionDrift.test.ts`.
    - Each gate's pass/fail and the spin disposition are recorded in `11-10-SUMMARY.md`.
  </acceptance_criteria>
  <done>All objective + fix-landed gates pass and the react test suite is green, recorded in the SUMMARY; if any gate fails, the plan stops here with a report instead of presenting the checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Decisive human re-verification of IDLE-02 (connected) + TALK-02 (loud/quiet side-by-side) + first-load spin</name>
  <action>Pause for the developer to run the dev build and re-check the two open behaviors plus the first-load spin, then report the result. Do NOT auto-approve — expression-drift visibility, audio-reactive amplitude, and spin-free load are visual/behavioral judgements only the human can make. Before presenting this checkpoint, start the dev server yourself (per the automation-first protocol; 11-08 used `next dev --turbopack --port 3002` from within the worktree and confirmed both routes return 200) so the human is looking at THIS worktree's exact code, and tell them the port.</action>
  <what-built>
    Plan 11-09 diagnosed the remaining IDLE-02 cause at runtime (leading cause: expression-drift amplitude was imperceptible — 0.12 base weight damped to ~0.018 by the stopped-state settle ramp, and the test page idles in `stopped` until Connect) and raised `DEFAULT_AMPLITUDE` to a perceptible value while KEEPING 11-07's candidate/ownership fixes and the TRANS-02 stopped-settle damping. It also root-caused the first-load "spins weird" motion (first-mount additive breathing/sway deltas compounding on bones the mixer was not yet driving) and either guarded the procedural bone writes until a base clip drives the skeleton, or triaged it as a follow-up — see 11-09-SUMMARY.md for which.
  </what-built>
  <how-to-verify>
    Open the VRM avatar test page (http://localhost:{PORT}/openai-avatar-test) and the GLB page (http://localhost:{PORT}/glb-avatar-test). Check these in order:

    1. FIRST-LOAD SPIN (untracked bug): Hard-reload the VRM page and WATCH THE FIRST ~2 SECONDS. Confirm the avatar eases into its idle pose WITHOUT an abnormal spin/rotation/lurch. (If 11-09 triaged rather than fixed this, confirm the residual motion is acceptable to ship and the follow-up is filed.)

    2. IDLE-02 (PREVIOUSLY FAILED — "no expression changes") — MUST judge in the `ready` state, not pre-connect:
       a. Click Connect and wait until the on-page `Status:` reads `ready` (NOT `stopped` — the stopped state deliberately damps facial drift for the TRANS-02 settle cue, so drift is intentionally near-invisible there; judging IDLE-02 pre-connect is what made 11-08 ambiguous).
       b. Leave the avatar untouched in `ready` for 30+ seconds and watch the face closely. Confirm a subtle but VISIBLE, continuous rest-state expression drift (a slow shift in the relaxed/happy rest values — the mouth/brow easing in and out), not a frozen face.
       c. If it still looks frozen: open devtools console and (if 11-09 left the `DRIFT_DEBUG` aid in) enable it per 11-09-SUMMARY's note, then report the logged present-expression names + written weights so the next pass has runtime data instead of another blind guess.
       d. On the GLB page, confirm NO expression drift (GLB has no expression system — this must remain a no-op).

    3. TALK-02 (PREVIOUSLY "maybe pass" — make it DECISIVE via explicit loud/quiet contrast):
       a. With the session connected and the AI speaking (or speak into the mic to drive `currentVolume`), first speak/play LOUDLY and note how much the body's procedural idle motion (breathing/sway amplitude) swells.
       b. Then speak/play QUIETLY (near-silent) and note the amplitude shrink back toward the neutral idle baseline.
       c. Decisive pass = the procedural motion amplitude VISIBLY tracks loud→large / quiet→small in real time, AND the talk CLIP selection and cycle timing do NOT change with volume (only amplitude scales, per TALK-02). Objective backing: `audioAmplitude.test.ts` already proves the volume→amplitude map is monotonic, clamped, and speaking-only — you are confirming that proven math is actually wired to visible motion. Report a clear pass/fail, not "maybe."

    4. REGRESSION SPOT-CHECK (5 already-passing requirements, since 11-09 touched shared composition/amplitude code): quickly confirm IDLE-01 (breathing+sway still alive), TRANS-01/TRANS-02 (start/stop still smooth, no snap), TALK-01 (talk-cycle still no snap-back), PERF-01 (spine motion smooth/bounded, no jitter). Report any regression.

    Placeholder-asset note (unchanged, D-01 / issue #17): VRM has no dedicated greeting/goodbye clips and GLB lacks a 2nd talk variant — verify the mechanisms against the placeholders, not bespoke greeting/goodbye animations.
  </how-to-verify>
  <resume-signal>Type "approved" if IDLE-02 now reads as visible drift in the `ready` state, TALK-02 gives a decisive loud/quiet amplitude-tracking pass, the first-load spin is gone (or its deferral is acceptable), and no regression in the five previously-passing behaviors — otherwise describe exactly which item still fails and how (including any DRIFT_DEBUG console output for IDLE-02), and a further gap-closure pass will follow.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification-only plan; no code changes, no new input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-14 | Input Validation (ASVS V5) | live-volume amplitude path (verification) | accept | Clamps already enforced in 11-02/11-04 and covered by `audioAmplitude.test.ts`; this plan visually confirms no extreme/broken motion under loud speech. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. |
</threat_model>

<verification>
- Objective invariant gates (timer-free, additive, internal-only) plus fix-landed greps (raised DEFAULT_AMPLITUDE, retained lastWritten/relaxed, spin fix/triage) all pass.
- Full react test suite green, including the extended `expressionDrift.test.ts`.
- Human confirms IDLE-02 visible in `ready`, TALK-02 decisive loud/quiet pass, first-load spin gone (or deferral accepted), and no regression in IDLE-01/TRANS-01/TRANS-02/TALK-01/PERF-01.
</verification>

<success_criteria>
Phase 11 gap closure is complete: IDLE-02 and TALK-02 are confirmed by a human on a running build (IDLE-02 judged in the connected `ready` state, TALK-02 via an explicit loud/quiet contrast), the first-load spin is resolved or acceptably deferred, the five previously-passing requirements show no regression, and sign-off is recorded in 11-10-SUMMARY.md. If any item still fails, it is reported as a remaining gap rather than silently approved.
</success_criteria>

<output>
Create `.planning/phases/11-idle-transition-talking-states/11-10-SUMMARY.md` when done.
</output>
