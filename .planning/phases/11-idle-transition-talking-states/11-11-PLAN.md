---
phase: 11-idle-transition-talking-states
plan: 11
type: execute
wave: 1
depends_on: ["11-09"]
files_modified:
  - packages/react/src/animation/AnimationStateEngine.ts
  - packages/react/src/animation/AnimationStateEngine.test.ts
  - packages/react/src/animation/crossfade.ts
  - packages/react/src/animation/crossfade.test.ts
autonomous: true
gap_closure: true
requirements: [TALK-01, TRANS-01]
user_setup: []

must_haves:
  truths:
    - "The root cause of BOTH G1 (T-pose stuck on load) and G2 (idle->talking snap) is confirmed by recorded runtime/headless evidence (a headless frame-stepping diagnostic against the real male.vrm scene graph + real Idle/talking clips, and off-by-default in-render instrumentation) — NOT assumed from either of the two leading hypotheses; the SUMMARY states whether G1 and G2 share one cause or are independent."
    - "G1 fixed: on first load / pre-connect (chatStatus 'stopped'), the VRM avatar holds or plays a live idle pose (base-clip motion and/or gated procedural breathing/sway) instead of sitting in a static bind/T-pose — verified by a headless frame-stepping assertion that the humanoid spine/chest/hips orientation is non-static across frames (or the confirmed base-clip drive is present), not merely by re-reading code."
    - "G2 fixed: the idle->talking transition crossfades over its pose-gap-adaptive/floor duration (non-zero, multi-frame eased ramp) rather than snapping in a single frame — verified by a headless assertion on the crossfade duration / eased weight progression for the idle->talk switch."
    - "The original 11-09 first-load 'spins weird' bug is NOT reintroduced: the existing `AnimationStateEngine.test.ts` first-mount compounding repro still holds its invariant, and a recorded check confirms procedural breathing+sway deltas do not accumulate into unbounded spine rotation under this fix."
    - "IDLE-02 (raised DEFAULT_AMPLITUDE, 11-07 lastWritten/relaxed guards) and TALK-02 (audioAmplitude map) — both confirmed passing in 11-10 — remain intact; the full @khaveeai/react test suite stays green."
  artifacts:
    - path: "packages/react/src/animation/AnimationStateEngine.ts"
      provides: "corrected first-mount / gate / base-pose-composition logic that fixes G1 (and G2 if shared) without reintroducing the 11-09 spin, with an updated header doc block recording the 11-10 G1/G2 diagnosis"
      contains: "shouldRunProceduralBoneWrites"
    - path: "packages/react/src/animation/AnimationStateEngine.test.ts"
      provides: "regression test(s) reproducing G1 (frozen/T-pose or suppressed idle motion) and/or G2 (snap) failure-then-fix, plus retention of the 11-09 spin-compounding invariant"
      contains: "describe("
    - path: "packages/react/src/animation/crossfade.ts"
      provides: "unchanged unless G2 is diagnosed as a pose-gap-duration defect in the crossfade engine; if touched, the change is documented against the recorded G2 root cause"
  key_links:
    - from: "packages/react/src/animation/AnimationStateEngine.ts"
      to: "packages/react/src/animation/breathing.ts"
      via: "gated breathing.step call in update()"
      pattern: "breathing\\.step"
    - from: "packages/react/src/animation/AnimationStateEngine.ts"
      to: "packages/react/src/animation/crossfade.ts"
      via: "beginCrossfade/stepCrossfade for the idle->talk transition"
      pattern: "beginCrossfade|stepCrossfade"
---

<objective>
Third gap-closure pass for Phase 11 — a REGRESSION FIX, not new-requirement work. All 7 tracked Phase-11 requirements (IDLE-01/02, TRANS-01/02, TALK-01/02, PERF-01) are currently marked Complete; IDLE-02 and TALK-02 were confirmed passing by the 11-10 human checkpoint. But 11-09's two fixes (raised expression-drift amplitude; the new `shouldRunProceduralBoneWrites` first-mount gate) introduced TWO NEW regressions the 11-10 checkpoint caught:

- **G1 — Avatar stuck in T-pose on first load.** 11-09 added `shouldRunProceduralBoneWrites(action)` (`AnimationStateEngine.ts:126-129`) gating `update()`'s breathing/sway/spine-clamp block (steps 4-7) on the base action's effective weight being >= `MIN_BASE_ACTION_WEIGHT` (0.05). The first-load spin is gone, but the avatar now visibly sits in a frozen T-pose-like state instead of playing/holding an idle pose.
- **G2 — Idle->talking transition now snaps** instead of crossfading smoothly. This previously worked (confirmed as recently as 11-08, TRANS-01/TALK-01 territory). 11-09 did NOT touch `switchToClip`, the crossfade-trigger `useEffect`, or `crossfade.ts` — only `expressionDrift.ts` and the new gate.

**This plan diagnoses BOTH regressions with recorded runtime evidence BEFORE fixing, then applies a targeted fix that does NOT reintroduce the 11-09 spin.** This is the SECOND time a "fix" in this small code area has introduced a new regression (11-09 fixed IDLE-02 + the spin but broke G1/G2). A fix that "seems reasonable" without recorded evidence is exactly the pattern that has caused two regressions already — so the diagnostic (Task 1) is a hard gate on the fix (Task 2).

Purpose: resolve G1 and G2 so the 11-12 human re-verification can finally close Phase 11, without a THIRD regression.
Output: a recorded G1/G2 root-cause diagnosis (headless frame-stepping evidence + off-by-default in-render instrumentation) and a targeted fix in `AnimationStateEngine.ts` (and/or `crossfade.ts` only if the diagnosis lands there), with regression tests that also prove the 11-09 spin is not reintroduced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-idle-transition-talking-states/11-CONTEXT.md
@.planning/phases/11-idle-transition-talking-states/11-PATTERNS.md
@.planning/phases/11-idle-transition-talking-states/11-09-SUMMARY.md
@.planning/phases/11-idle-transition-talking-states/11-10-SUMMARY.md

<empirical_findings>
Established directly this planning session against the current code — the executor should CONFIRM (not blindly trust) these during the diagnostic:

- **11-09 is commit `97b7cb2`** ("fix(11-09): guard first-mount procedural bone writes to fix spin-on-load bug") — the ONLY commit that changed `AnimationStateEngine.ts` in this round. It added `MIN_BASE_ACTION_WEIGHT = 0.05`, the exported pure gate `shouldRunProceduralBoneWrites(action)`, and wrapped `update()` steps 4-7 in `if (shouldRunProceduralBoneWrites(currentActionRef.current)) { ... }`. It did NOT touch `resolveBaseClip`, `switchToClip`, the crossfade-trigger `useEffect`, or `crossfade.ts`. G1/G2 are therefore either a direct effect of this gate OR something the gate UNMASKED.
- `stepCrossfade` (`crossfade.ts:143-154`) runs UNGATED as `update()`'s step 1, every frame. It ramps the base action's `setEffectiveWeight` via `easeInOutCubic`. Per 11-09's own header comment, the 0.05 threshold is reached at t≈0.23 of the crossfade duration. So the gate by itself should NOT prevent the base action's weight from ramping to 1 within under a second — meaning if the avatar is a persistent (not just first-second) T-pose, either the base action is NOT actually driving the skeleton, or `getEffectiveWeight()` is not returning what the gate expects at runtime. BOTH must be checked, not assumed.
- **VRM test page (`src/app/openai-avatar-test/page.tsx`)** mounts `male.vrm` with `animations={{ idle: '/models/animations/Idle.fbx', talking: '/models/animations/talking.fbx', talking1: '/models/animations/talking1.fbx' }}`, `enableBlinking`, and does NOT call `animate()` — so `currentAnimation` is null. Pre-connect `chatStatus === "stopped"`. `resolveBaseClip("stopped", null, ["idle","talking","talking1"])`: the `stopped` pattern `/stop|bye|goodbye|outro/i` matches none of these clips, so it falls through to `currentAnimation ?? availableNames[0] ?? null` = `"idle"`. So the VRM base clip pre-connect IS the moving `Idle.fbx`, NOT a frozen "Pose" — meaning IF the mixer drives that base action at weight ~1, the skeleton should be posed by a live idle, not a T-pose. A persistent T-pose therefore points at the base `Idle` action not actually driving `male.vrm`'s bones (e.g. FBX->VRM remap coverage — see `remapMixamoAnimationToVrm.ts`), which the ungated pre-11-09 breathing/sway motion could have MASKED (it wrote the only visible motion, misread as the "spins weird" symptom). This is the leading G1 lead — VERIFY it, do not assume it.
- `beginCrossfade(null, toAction, root, floor)` (first switch, `crossfade.ts:110-136`) measures `computePoseGapAngle(root, toAction.getClip())` against the LIVE pose to pick a 0.3-0.9s duration (plus optional floor). For the idle->talk switch (`chatStatus === "speaking"`, no floor), the duration is the raw 0.3-0.9s pose-gap value. G2's leading lead: if G1 means the live pre-talk pose is now genuinely static (vs. the pre-11-09 spin-perturbed pose), the measured pose gap — and thus the duration — may differ, producing a shorter/snappier blend even though the crossfade code is unchanged. VERIFY whether the idle->talk duration actually collapsed toward the 0.3s floor (or to ~0), or whether G2 is independent.
- **`AnimationStateEngine.test.ts` already contains a headless frame-stepping repro** ("first-mount procedural-write accumulation repro (11-09)", lines 142-196) that steps `stepBreathing`/`stepSway` over 60 frames against stub `THREE.Object3D` bones, comparing a base-pose-reset-each-frame run vs. no-reset, asserting the no-reset run accumulates a larger spine angle. This is the exact pattern to EXTEND for the G1/G2 headless diagnostic and regression tests — the harness for headless frame-stepping already exists.
- **11-09's headless VRM-load precedent** (`11-09-SUMMARY.md`): a throwaway Node script loaded a bundled VRM via `GLTFLoader.parse()` + `@pixiv/three-vrm`'s `VRMCoreLoaderPlugin` (NOT the full `VRMLoaderPlugin`, whose MToon material loader crashes headless in Node — no canvas/Image backend), after polyfilling `globalThis.self = globalThis`. `THREE.AnimationMixer` is pure math and runs headless. This is the sanctioned route for loading `male.vrm` headless to confirm whether the base `Idle` action drives its humanoid bones.
</empirical_findings>

<interfaces>
<!-- Contracts the executor needs. Extracted from the current codebase. Use directly; no exploration needed. -->

From packages/react/src/animation/AnimationStateEngine.ts:
  export function shouldRunProceduralBoneWrites(action: THREE.AnimationAction | null): boolean
  export function resolveBaseClip(chatStatus, currentAnimation, availableNames): string | null
  export function useAnimationController(params): { update: (delta: number) => void }
  const MIN_BASE_ACTION_WEIGHT = 0.05;   // gate threshold
  // internal refs: currentActionRef (null until first switchToClip), currentClipNameRef, blendRef, settleRampRef
  // switchToClip(name) -> beginCrossfade(currentActionRef.current, toAction, root, floor); floor = starting|stopped ? 1.2 : undefined
  // update() step order: 1 stepCrossfade -> 2 blink -> 3 settle/amplitude scale -> [gate] 4 capture spine base / 5 breathing / 6 sway / 7 spine clamp -> 8 expressionDrift -> 9 talkCycle

From packages/react/src/animation/crossfade.ts:
  export function easeInOutCubic(t: number): number
  export function computePoseGapAngle(scene: THREE.Object3D, toClip: THREE.AnimationClip): number
  export function poseGapToDuration(maxAngleRad: number, floorSeconds?: number): number  // 0.3-0.9s, floor optional
  export function beginCrossfade(fromAction, toAction, scene, floorSeconds?): BlendState
  export function stepCrossfade(blend: BlendState): void   // ramps setEffectiveWeight via easeInOutCubic
  interface BlendState { active; from; to; startTime; duration; }

From packages/react/src/animation/types.ts:
  AvatarFormatAdapter.getHumanoidBoneNode(role: "hips"|"spine"|"chest"|"upperChest"|"neck"|"head"): THREE.Object3D | null
  AvatarFormatAdapter.getMixer(): THREE.AnimationMixer
  AvatarFormatAdapter.getExpressionManager(): VRMExpressionManager | null   // null on GLB

From packages/react/src/animation/breathing.ts / sway.ts:
  createBreathingState() / stepBreathing(state, adapter, delta, amplitudeScale?)   // additive bone.quaternion.multiply, writes chest + spine
  createSwayState() / stepSway(state, adapter, delta, amplitudeScale?)             // additive bone.quaternion.multiply, writes hips + spine
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose G1 (T-pose on load) and G2 (idle->talking snap) at runtime — record evidence, do NOT fix yet</name>
  <read_first>
    - packages/react/src/animation/AnimationStateEngine.ts (FULL file — especially the 11-09 header diagnosis block, `MIN_BASE_ACTION_WEIGHT`/`shouldRunProceduralBoneWrites` lines 103-129, `resolveBaseClip` ~189-200, `switchToClip` ~274-294, the crossfade-trigger `useEffect` ~296-334, and `update()` steps 1-9 ~336-469)
    - packages/react/src/animation/crossfade.ts (FULL — `beginCrossfade` null-`from` behavior ~110-136, `stepCrossfade` weight ramp ~143-154, `computePoseGapAngle` ~41-59, `poseGapToDuration` ~74-81)
    - packages/react/src/animation/AnimationStateEngine.test.ts (the existing headless frame-stepping repro at lines 142-196 and the `shouldRunProceduralBoneWrites` gate tests at 121-140 — the conventions to extend)
    - packages/react/src/VRMAvatar.tsx (useFrame order ~494-515: mixer.update -> controller.update -> currentVrm.update; processedClips build ~320, getAction ~458-465, availableNames ~472; currentVolume/chatStatus destructure ~307)
    - packages/react/src/utils/remapMixamoAnimationToVrm.ts (how FBX Mixamo clips are remapped onto VRM normalized humanoid bones — relevant to whether the base `Idle` action actually drives `male.vrm`)
    - src/app/openai-avatar-test/page.tsx (VRM page wiring: male.vrm + idle/talking/talking1, no animate(), pre-connect chatStatus "stopped")
    - .planning/phases/11-idle-transition-talking-states/11-09-SUMMARY.md (the headless VRMCoreLoaderPlugin load recipe + the recorded spin mechanism this fix must not reintroduce)
    - .planning/phases/11-idle-transition-talking-states/11-10-SUMMARY.md (the authoritative G1/G2 gap report)
    - The <empirical_findings> and <interfaces> blocks above
  </read_first>
  <files>packages/react/src/animation/AnimationStateEngine.ts</files>
  <action>
    DIAGNOSE BOTH regressions with recorded evidence. Do NOT change production behavior in this task beyond adding OFF-BY-DEFAULT instrumentation. The two leading hypotheses below are LEADS (like 11-09's H1/H2/H3), not conclusions — resolve which actually holds for G1 and G2, and whether they share one cause.

    (A) G1 — resolve which cause dominates, with runtime evidence:
      - G1-a (UNMASKING / base-clip-not-driving): the persistent T-pose was ALWAYS the true base pose because the base `Idle` action never actually drove `male.vrm`'s humanoid bones (e.g. FBX->VRM remap coverage gap), and the pre-11-09 UNGATED breathing/sway writes were the only visible motion (misread as the "spins weird" symptom). 11-09's gate correctly stopped that motion, revealing the frozen base.
      - G1-b (GATE / weight-ramp bug): `getEffectiveWeight()` on the live `currentActionRef.current` does not behave as the gate assumes in the real render loop (e.g. never crosses 0.05, or the ramped action is not the one `currentActionRef` points at), so procedural writes are permanently suppressed — independent of whether the base clip drives the bones.
      - MANDATORY runtime evidence — do BOTH:
        1. **Headless frame-stepping diagnostic** (extend the existing repro harness pattern, and the 11-09 `VRMCoreLoaderPlugin` VRM-load recipe): load `public/models/male.vrm` headless (GLTFLoader.parse + VRMCoreLoaderPlugin, `globalThis.self = globalThis`), load + remap `Idle.fbx` onto its humanoid bones the same way `VRMAvatar`/`remapMixamoAnimationToVrm` does, create a real `THREE.AnimationMixer`, `clipAction(idle).play()`, and step it ~30-60 frames. RECORD: (i) does the idle action's `getEffectiveWeight()` ramp from 0 past 0.05 to ~1 (does the gate open)? (ii) do the humanoid `spine`/`chest`/`hips` quaternions actually change across frames from the mixer ALONE (base clip drives the skeleton) or stay at the bind pose (T-pose confirmed as base)? If headless VRM load hits an unresolvable Node environment error, that fallback must be explicitly justified in the SUMMARY (specific error, why unresolvable) — it is not a default.
        2. **Off-by-default in-render instrumentation** in `AnimationStateEngine.ts`: behind a module const `const GATE_DEBUG = false;` (or a `?gateDebug=1` URL-param / localStorage check, mirroring 11-09's `DRIFT_DEBUG`), log once per second per instance: `currentClipNameRef.current`, `currentActionRef.current?.getEffectiveWeight()`, `shouldRunProceduralBoneWrites(currentActionRef.current)`, and the live `spine` quaternion's angle-from-identity — so the 11-12 human can capture decisive live runtime data in devtools. This must default OFF and be documented as a dev-only diagnostic aid.
      - RECORD in the SUMMARY: the headless weight-ramp + bone-drive findings, and the explicit statement of whether G1 is G1-a, G1-b, or a combination.

    (B) G2 — resolve whether it shares G1's cause or is independent:
      - G2-shared: G1's fix (whatever makes the pre-talk pose correct) also fixes the snap, because `computePoseGapAngle(root, talkClip)` now measures a correct live pose->talk gap and picks a normal 0.3-0.9s duration again.
      - G2-independent: the snap traces to something else (e.g. the pose-gap measured against a frozen/bind pose collapses `poseGapToDuration` toward its 0.3s minimum, or the crossfade-trigger effect fires in a way that skips the ramp).
      - MANDATORY runtime evidence: in the SAME headless harness, simulate the idle->talk switch — measure `computePoseGapAngle` and `poseGapToDuration` for `idle`->`talking` (a) when the live pose is the frozen bind/base pose (the current buggy state) and (b) when the base idle is actually driving the pose. RECORD both durations and whether the buggy one collapses toward 0.3s (or produces a single-frame-visible snap). State whether G2 is shared with G1 or independent.

    (C) Update the `AnimationStateEngine.ts` file-header doc block with the recorded 11-10 G1/G2 diagnosis (distinct from the existing 11-09 spin block), naming the confirmed cause(s) and whether G1/G2 share a root cause. Keep the 11-09 block intact for provenance.

    Remove any throwaway headless diagnostic SCRIPT before committing (keep only the retained off-by-default `GATE_DEBUG` instrumentation and any diagnostic assertions you fold into the test file). Commit this task's instrumentation + header update atomically.
  </action>
  <verify>
    <automated>cd packages/react && grep -q "GATE_DEBUG" src/animation/AnimationStateEngine.ts && grep -Eq "GATE_DEBUG *= *false" src/animation/AnimationStateEngine.ts && grep -q "shouldRunProceduralBoneWrites" src/animation/AnimationStateEngine.ts && pnpm --filter @khaveeai/react test</automated>
  </verify>
  <acceptance_criteria>
    - 11-11-SUMMARY.md RECORDS the G1 diagnosis with runtime evidence: whether the base `Idle` action's effective weight ramps past 0.05 (gate opens) AND whether the base clip alone drives `male.vrm`'s spine/chest/hips bones — with an explicit G1-a vs G1-b (or combined) determination. It is NOT a restatement of a hypothesis as fact without the headless/instrumented evidence.
    - 11-11-SUMMARY.md RECORDS the G2 diagnosis: the measured idle->talk `poseGapToDuration` in the buggy vs. corrected pose state, and an explicit "shared with G1" or "independent" determination.
    - `GATE_DEBUG` (or equivalent) instrumentation exists in `AnimationStateEngine.ts`, defaults to OFF (grep `GATE_DEBUG *= *false` matches), logs the gate/weight/clip/spine-angle data, and is documented as a dev-only aid in the header.
    - Any throwaway headless diagnostic script is removed; `git status --short` is clean of it before committing. The full `pnpm --filter @khaveeai/react test` suite still passes (no production behavior changed yet).
    - The `AnimationStateEngine.ts` header records the new 11-10 G1/G2 diagnosis without deleting the 11-09 spin diagnosis block.
  </acceptance_criteria>
  <done>G1 and G2 are each root-caused with recorded runtime/headless evidence (weight-ramp + bone-drive for G1; measured crossfade duration for G2), the shared-vs-independent question is answered, off-by-default in-render instrumentation is added for the 11-12 human, and no production behavior is changed yet.</done>
</task>

<task type="auto">
  <name>Task 2: Fix G1 (and G2) targeting the confirmed cause — without reintroducing the 11-09 spin</name>
  <read_first>
    - packages/react/src/animation/AnimationStateEngine.ts (the gate at 126-129, the gated block in update() steps 4-7 ~397-440, switchToClip ~274-294, the crossfade-trigger effect ~296-334 — AND Task 1's recorded diagnosis in 11-11-SUMMARY.md)
    - packages/react/src/animation/breathing.ts (FULL — the `chest.quaternion.multiply`/`spine.quaternion.multiply` additive writes and the "assumes the mixer re-established the base pose this frame" assumption in its header)
    - packages/react/src/animation/sway.ts (FULL — the `hips.quaternion.multiply`/`spine.quaternion.multiply` additive writes)
    - packages/react/src/animation/crossfade.ts (only if Task 1 diagnosed G2 as a pose-gap-duration defect here)
    - packages/react/src/animation/AnimationStateEngine.test.ts (existing gate tests 121-140 and the first-mount compounding repro 142-196 — the invariant that must still hold, and the convention to extend)
    - The Task 1 diagnosis recorded in 11-11-SUMMARY.md (the fix MUST target the confirmed cause, not a guess)
  </read_first>
  <files>packages/react/src/animation/AnimationStateEngine.ts, packages/react/src/animation/AnimationStateEngine.test.ts, packages/react/src/animation/crossfade.ts, packages/react/src/animation/crossfade.test.ts</files>
  <action>
    Apply the fix that targets the cause Task 1 CONFIRMED. Do NOT reintroduce the 11-09 "spins weird" bug — the existing first-mount compounding repro (`AnimationStateEngine.test.ts:142-196) invariant must continue to hold after your change. Pick the strategy that matches the recorded diagnosis:

    - If G1 = G1-a (UNMASKING / base clip does not drive the bones, so simply un-gating would just bring the spin back): the correct fix is to make the procedural layer compose on a KNOWN, per-frame-reset base so it produces bounded idle motion WITHOUT compounding — e.g. capture each targeted bone's orientation at the START of the frame (before breathing/sway) and restore-then-apply-additive-delta each frame, so the deltas oscillate around a stable base regardless of whether the mixer drove the bone. This yields visible idle micro-motion on load (fixing the frozen T-pose) while structurally preventing the accumulation the 11-09 gate was working around. Removing or loosening the gate is acceptable ONLY if paired with such a per-frame base-reset that provably keeps the compounding-repro invariant satisfied. Do NOT simply lower `MIN_BASE_ACTION_WEIGHT` back toward 0 (that re-enables the exact compounding 11-09 fixed).
    - If G1 = G1-b (GATE / weight-ramp misbehavior): correct the gate so it opens once the base action is genuinely driving the skeleton in the real loop (fix the `getEffectiveWeight`/`currentActionRef` mismatch Task 1 identified), while keeping the compounding guard intact for the true near-zero-weight window.
    - If G2 = shared with G1: confirm (via the headless harness) that the G1 fix restores a normal idle->talk `poseGapToDuration`. If G2 = independent (e.g. pose-gap collapses to the 0.3s floor against a static pose): apply the specific low-risk fix Task 1 identified (in `AnimationStateEngine.ts` and/or `crossfade.ts`), documenting the change against the recorded root cause. Do NOT touch `crossfade.ts` unless Task 1 localized G2 there.

    Guardrails on the fix (regardless of strategy):
      - Blink (step 2) and expression drift (step 8) remain UNGATED (they write manager scalars, not accumulating bone quaternions) — do not add gating to them.
      - Preserve the PERF-01 fixed composition order (breathing -> sway -> spine clamp -> drift -> talkCycle) and the `MAX_COMBINED_SPINE_DELTA_RAD` spine clamp for the steady-state path.
      - Preserve IDLE-02 (do not touch `expressionDrift.ts`'s raised amplitude / lastWritten / relaxed) and TALK-02 (do not touch `audioAmplitude.ts`) — both were confirmed passing in 11-10.
      - Keep the off-by-default `GATE_DEBUG` instrumentation from Task 1 (dev-only aid), OR remove it if the fix makes it redundant — state which in the SUMMARY.

    Tests (extend `AnimationStateEngine.test.ts`, and `crossfade.test.ts` only if G2's fix lands in crossfade):
      - G1 regression test: assert that under the fix, procedural idle motion IS produced on first mount / with a base action that is not (yet) driving the bones — i.e. the spine/chest/hips orientation is non-static across frames (the avatar is NOT frozen), using the existing headless frame-stepping harness.
      - Spin-not-reintroduced test: assert the fix STILL bounds procedural accumulation — extend or re-assert the existing "first-mount procedural-write accumulation repro" so that under the fix the net spine rotation stays bounded (does NOT grow unbounded across many frames). This is the explicit "the original 11-09 spin is not reintroduced" gate.
      - G2 test (if applicable): assert the idle->talk crossfade duration / eased weight ramps over multiple frames (non-snap) under the fix, using `poseGapToDuration`/`stepCrossfade` on a realistic pose gap.

    Update the `AnimationStateEngine.ts` header to record the fix applied and WHY it does not reintroduce the spin. Commit atomically (test + fix).
  </action>
  <verify>
    <automated>cd packages/react && grep -q "breathing.step" src/animation/AnimationStateEngine.ts && grep -q "sway.step" src/animation/AnimationStateEngine.ts && grep -Eq "MAX_COMBINED_SPINE_DELTA_RAD" src/animation/AnimationStateEngine.ts && grep -q "DEFAULT_AMPLITUDE" src/animation/expressionDrift.ts && grep -Eq "DEFAULT_AMPLITUDE *= *0\.(1[3-9]|[2-9])" src/animation/expressionDrift.ts && grep -REc "setInterval|setTimeout" src/animation/ | grep -v ':0$' | grep -c . | grep -qx 0 && pnpm --filter @khaveeai/react test</automated>
  </verify>
  <acceptance_criteria>
    - The fix targets the cause recorded in Task 1's diagnosis (not a blind guess) and is documented inline + in 11-11-SUMMARY.md.
    - A G1 regression test proves procedural idle motion is produced on first mount (non-frozen spine/chest/hips across frames) under the fix.
    - A spin-not-reintroduced test proves procedural accumulation stays BOUNDED under the fix (the 11-09 compounding invariant still holds) — `MIN_BASE_ACTION_WEIGHT` is NOT simply lowered toward 0 without a compensating per-frame base reset.
    - If G2 was independent, a crossfade-duration/non-snap test covers the specific fix; if shared, the SUMMARY records the headless confirmation that the G1 fix restores a normal idle->talk duration.
    - IDLE-02 and TALK-02 invariants intact (grep confirms raised DEFAULT_AMPLITUDE and the PERF-01 spine clamp remain; expressionDrift/audioAmplitude untouched); no `setInterval`/`setTimeout` introduced; full `pnpm --filter @khaveeai/react test` green.
  </acceptance_criteria>
  <done>G1 (and G2, shared or independently) is fixed against the confirmed cause, procedural idle motion is restored without reintroducing the 11-09 spin (proven by a bounded-accumulation test), IDLE-02/TALK-02/PERF-01 invariants are intact, and the react test suite is green — leaving only the 11-12 human runtime confirmation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Client-side 3D animation state only. No new network, auth, storage, or user-input boundary. `currentVolume` (the only externally-sourced scalar in scope) is already clamped in `KhaveeProvider.tsx` (11-02) and `audioAmplitude.ts` (11-04); this plan adds no new ingest path. The off-by-default `GATE_DEBUG` instrumentation logs only local scene-graph names, action weights, and a bone angle — no secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-15 | Denial of Service (runaway rotation regression) | G1/G2 fix in `AnimationStateEngine.ts` (removing/loosening the 11-09 gate) | mitigate | The fix must keep the first-mount compounding-repro invariant satisfied — a dedicated "spin-not-reintroduced" test asserts procedural accumulation stays bounded; the PERF-01 `MAX_COMBINED_SPINE_DELTA_RAD` spine clamp is retained. |
| T-11-16 | Tampering (regression to already-passing behavior) | shared composition/crossfade code touched by the fix | mitigate | Gate/greps confirm IDLE-02 (raised DEFAULT_AMPLITUDE), PERF-01 (spine clamp), and timer-free invariants remain; the 11-12 human re-verification spot-checks all 7 requirements. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. |
</threat_model>

<verification>
- `pnpm --filter @khaveeai/react test` green after both tasks (existing suite + new G1 non-frozen test + spin-not-reintroduced bounded-accumulation test + any G2 non-snap test).
- Objective greps confirm the 11-09/IDLE-02/PERF-01 invariants intact (raised DEFAULT_AMPLITUDE, spine clamp, timer-free) and the breathing/sway calls still present.
- The G1 and G2 root-cause diagnoses (headless weight-ramp/bone-drive evidence + measured crossfade duration) are recorded in 11-11-SUMMARY.md, with the shared-vs-independent determination.
- Decisive visual confirmation (avatar holds a live idle on load, idle->talk crossfades smoothly, no spin, no regression across the 7 requirements) is deferred to the 11-12 human-verify checkpoint — a grep/unit test cannot make that judgement.
</verification>

<success_criteria>
G1 (T-pose on load) and G2 (idle->talking snap) are each diagnosed at runtime with recorded evidence and fixed against the confirmed cause, procedural idle motion is restored WITHOUT reintroducing the 11-09 spin (proven by a bounded-accumulation regression test), IDLE-02/TALK-02/PERF-01 and the timer-free invariant remain intact, and the react test suite is green — ready for the 11-12 human runtime re-verification.
</success_criteria>

<output>
Create `.planning/phases/11-idle-transition-talking-states/11-11-SUMMARY.md` when done, recording BOTH the G1 runtime diagnosis (weight-ramp + bone-drive evidence, G1-a vs G1-b) and the G2 diagnosis (measured idle->talk crossfade duration, shared vs independent), the fix applied, and the explicit confirmation that the 11-09 spin is not reintroduced.
</output>
