---
phase: 11-idle-transition-talking-states
plan: 12
type: execute
wave: 2
depends_on: ["11-11"]
files_modified: []
autonomous: false
gap_closure: true
requirements: [IDLE-01, IDLE-02, TRANS-01, TRANS-02, TALK-01, TALK-02, PERF-01]
user_setup: []

must_haves:
  truths:
    - "On first load / pre-connect, a human sees the VRM avatar hold or play a LIVE idle pose (breathing/sway/idle-clip motion) with NO static T-pose and NO abnormal spin (G1 resolved)."
    - "A human confirms the idle->talking transition crossfades SMOOTHLY over its blend duration rather than snapping in a single frame (G2 resolved)."
    - "The 11-11 fix landed in code (G1/G2 fix present, 11-09 spin NOT reintroduced, IDLE-02/PERF-01/timer-free invariants intact) and the full react test suite is green — proven by objective gates BEFORE the human checkpoint."
    - "A full regression spot-check across ALL 7 original Phase-11 requirements (IDLE-01, IDLE-02, TRANS-01, TRANS-02, TALK-01, TALK-02, PERF-01) shows NO new regression — mandatory this round because regressions have now been introduced twice (11-07 era and 11-09)."
  artifacts:
    - path: ".planning/phases/11-idle-transition-talking-states/11-12-SUMMARY.md"
      provides: "sign-off record of the third gap-closure re-verification (G1 + G2 + full 7-requirement regression pass)"
  key_links: []
---

<objective>
Fourth (and hopefully final) re-verification for Phase 11, after the 11-11 fixes for G1 (T-pose stuck on load) and G2 (idle->talking snap). The 11-10 checkpoint confirmed IDLE-02 and TALK-02 are genuinely fixed but caught these two NEW regressions from 11-09's `shouldRunProceduralBoneWrites` gate.

This plan: (1) re-run the objective invariant + fix-landed gates proving 11-11's fix is present, the 11-09 spin is NOT reintroduced, and the react suite is green; then (2) a blocking human re-check that is DECISIVE on G1 and G2 AND performs a FULL regression spot-check of all 7 original Phase-11 requirements. Because this is the third gap-closure round and a "fix" has introduced a new regression twice now, the human check does NOT trust the five previously-"green" requirements on faith — it explicitly re-verifies each one, since the 11-11 fix touched the shared composition/crossfade code every requirement routes through.

Purpose: this is the acceptance gate that decides whether Phase 11 gap closure is finally complete. The open items are visual/behavioral — a human must watch a real avatar.
Output: objective gate results + a decisive blocking human-verify sign-off recorded in 11-12-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-idle-transition-talking-states/11-09-SUMMARY.md
@.planning/phases/11-idle-transition-talking-states/11-10-SUMMARY.md
@.planning/phases/11-idle-transition-talking-states/11-11-PLAN.md
@.planning/phases/wayfinder-map-1-animation-architecture/VERIFICATION-CHECKLIST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix-landed + invariant gates + full react test suite (prove 11-11 landed before the human check)</name>
  <read_first>
    - packages/react/src/animation/AnimationStateEngine.ts (confirm the 11-11 G1/G2 fix; confirm blink/drift still ungated; confirm the PERF-01 spine clamp and gate/base-reset strategy present; read the header's recorded 11-10 G1/G2 diagnosis + fix)
    - packages/react/src/animation/AnimationStateEngine.test.ts (confirm the new G1 non-frozen test and the spin-not-reintroduced bounded-accumulation test exist)
    - packages/react/src/animation/crossfade.ts (only if 11-11 recorded G2 as a crossfade-engine fix)
    - packages/react/src/animation/expressionDrift.ts (confirm IDLE-02's raised DEFAULT_AMPLITUDE + lastWritten/relaxed still present, untouched by 11-11)
    - .planning/phases/11-idle-transition-talking-states/11-11-SUMMARY.md (read the recorded G1/G2 diagnosis + fix strategy so the checkpoint framing below matches what 11-11 actually did — shared vs independent, and how the spin is prevented)
  </read_first>
  <files></files>
  <action>
    Re-run the objective invariant gates (must still hold after 11-11) plus greps proving 11-11's fixes landed and the 11-09 spin is not reintroduced. Record each result in 11-12-SUMMARY.md. If ANY gate fails, STOP and report before presenting the human checkpoint — do not put a checkpoint on top of a failing invariant.

    Invariant gates (must still pass):
    1. No live-clock timers in the animation module: `grep -REc "setInterval|setTimeout" packages/react/src/animation/` totals 0 across every file.
    2. Breathing/sway additive-not-overwrite: in `breathing.ts` and `sway.ts`, `grep -v '^ *[*/]' | grep -c '\.quaternion\.set('` returns 0, and `\.multiply(` appears >= 1 in each.
    3. Internal-only: `grep -Ec "breathing|sway|expressionDrift|talkCycle|audioAmplitude" packages/react/src/index.ts` returns 0.
    4. IDLE-02 intact: `grep -Eq "DEFAULT_AMPLITUDE *= *0\.(1[3-9]|[2-9])" packages/react/src/animation/expressionDrift.ts` matches AND `lastWritten` and `"relaxed"` still present in `expressionDrift.ts`.
    5. PERF-01 intact: `MAX_COMBINED_SPINE_DELTA_RAD` still present in `AnimationStateEngine.ts` and the breathing/sway `.step` calls remain (guarded, not deleted).

    Fix-landed gates (prove 11-11 is present):
    6. G1/G2 fix present: the `AnimationStateEngine.ts` header records the 11-10 G1/G2 diagnosis and fix (grep for the recorded diagnosis marker, e.g. `11-10` or `G1`/`G2` in the header), and the fix Task 1's SUMMARY described (per-frame base reset and/or corrected gate, or crossfade-duration fix) is present in code. Confirm against what 11-11-SUMMARY records.
    7. Spin NOT reintroduced: `MIN_BASE_ACTION_WEIGHT` was NOT simply lowered toward 0 without a compensating base reset — confirm the 11-11 approach against the SUMMARY, AND confirm the "first-mount procedural-write accumulation" repro plus the new "spin-not-reintroduced" / G1-non-frozen test names exist in `AnimationStateEngine.test.ts`.

    Then run the full package test suite: `pnpm --filter @khaveeai/react test` (must include the new G1 non-frozen + spin-bounded assertions). Record the suite pass count in the SUMMARY.
  </action>
  <verify>
    <automated>cd packages/react && grep -REc "setInterval|setTimeout" src/animation/ | grep -v ':0$' | grep -c . | grep -qx 0 && grep -Eq "DEFAULT_AMPLITUDE *= *0\.(1[3-9]|[2-9])" src/animation/expressionDrift.ts && grep -q "lastWritten" src/animation/expressionDrift.ts && grep -q '"relaxed"' src/animation/expressionDrift.ts && grep -q "MAX_COMBINED_SPINE_DELTA_RAD" src/animation/AnimationStateEngine.ts && grep -q "breathing.step" src/animation/AnimationStateEngine.ts && pnpm --filter @khaveeai/react test</automated>
  </verify>
  <acceptance_criteria>
    - Gates 1-5 (timer-free, additive-not-overwrite, internal-only, IDLE-02 intact, PERF-01 intact) all pass.
    - Gates 6-7 confirm the 11-11 G1/G2 fix landed and the 11-09 spin is NOT reintroduced (the compounding-repro invariant plus the new G1/spin tests exist), matching 11-11-SUMMARY.
    - `pnpm --filter @khaveeai/react test` reports all suites passing, including the new G1 non-frozen and spin-bounded assertions; the pass count is recorded.
    - Each gate's pass/fail is recorded in `11-12-SUMMARY.md`.
  </acceptance_criteria>
  <done>All invariant + fix-landed gates pass and the react test suite is green, recorded in the SUMMARY; if any gate fails, the plan stops here with a report instead of presenting the checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Decisive human re-verification of G1 + G2 + full 7-requirement regression pass</name>
  <action>Pause for the developer to run the dev build and re-check G1, G2, and ALL 7 original Phase-11 requirements, then report the result. Do NOT auto-approve — T-pose-vs-live-idle on load, snap-vs-smooth transition, and per-requirement regression are visual/behavioral judgements only the human can make; `workflow.auto_advance` does NOT apply. Before presenting this checkpoint, start the dev server yourself (per the automation-first protocol; prior rounds used `next dev --turbopack --port 3002` from within the worktree and confirmed both routes return 200) so the human is looking at THIS worktree's exact code, and tell them the port. If 11-11 kept the off-by-default `GATE_DEBUG` aid, tell the human how to enable it (module const or `?gateDebug=1`) so they can capture live weight/gate/spine-angle data if G1 still looks wrong.</action>
  <what-built>
    Plan 11-11 diagnosed BOTH 11-10 regressions at runtime (headless frame-stepping against the real male.vrm + Idle/talking clips, plus off-by-default in-render instrumentation): G1 (avatar stuck in T-pose on load) and G2 (idle->talking transition snaps). It recorded whether G1 was the base `Idle` clip never actually driving the skeleton (the pre-11-09 ungated breathing/sway motion having masked a frozen base) or a gate/weight-ramp bug, and whether G2 shared that cause or was independent — see 11-11-SUMMARY.md for which. It then applied a targeted fix that restores live idle motion on load and a smooth idle->talk crossfade WITHOUT reintroducing the 11-09 "spins weird" bug (proven by a bounded-accumulation regression test), keeping IDLE-02, TALK-02, and the PERF-01 spine clamp intact.
  </what-built>
  <how-to-verify>
    Open the VRM avatar test page (http://localhost:{PORT}/openai-avatar-test) and the GLB page (http://localhost:{PORT}/glb-avatar-test). Check these in order:

    1. G1 — FIRST-LOAD POSE (the regression that blocked 11-10): Hard-reload the VRM page and WATCH THE FIRST ~3 SECONDS and then the settled state. Confirm BOTH: (a) NO abnormal spin/rotation/lurch (the 11-09 fix still holds), AND (b) the avatar holds/plays a LIVE idle pose (subtle breathing/sway + idle-clip motion) — it must NOT sit in a static T-pose / arms-out bind pose. If it still looks frozen: enable `GATE_DEBUG` per 11-11-SUMMARY's note and report the logged base-action weight, `shouldRunProceduralBoneWrites` value, and spine angle so the next pass has runtime data instead of a guess.

    2. G2 — IDLE->TALKING TRANSITION (the other 11-10 regression): Connect and get the AI speaking (or speak into the mic). WATCH the moment chatStatus goes idle->speaking. Confirm the avatar CROSSFADES smoothly into the talk clip over a visible fraction of a second — it must NOT snap/pop instantly to the talk pose. Then watch speaking->idle back too. Report smooth vs snap decisively.

    3. FULL REGRESSION PASS — all 7 original Phase-11 requirements (mandatory this round; the 11-11 fix touched shared composition/crossfade code, and a "fix" has introduced a regression twice already). Judge each explicitly, not on faith:
       a. IDLE-01 (breathing + sway): in `ready`, the body has continuous, alive breathing/sway micro-motion (not frozen, not jittery).
       b. IDLE-02 (expression drift): CONNECT and wait until `Status:` reads `ready` (NOT pre-connect `stopped`, which deliberately damps drift). Over 30s the face shows a subtle, continuous rest-state expression drift. On the GLB page, confirm NO expression drift (GLB has no expression system — must stay a no-op).
       c. TRANS-01 (start transition): the idle->speaking / starting transitions read as smooth, deliberate blends (this overlaps G2 — confirm both the start and the talk transitions are smooth, no snap).
       d. TRANS-02 (stop/settle): entering `stopped` eases the body/face toward a damped rest (settle cue), not an instant cut.
       e. TALK-01 (talk-cycle): while speaking, talk-clip variants cycle on loop boundaries without snapping back to variant 1 mid-speech.
       f. TALK-02 (audio-reactive amplitude): speak LOUD then QUIET — the procedural body amplitude visibly swells/shrinks with volume, while clip selection/timing do NOT change with volume.
       g. PERF-01 (bounded/smooth spine): under loud speech and during transitions, spine motion stays smooth and bounded — no over-bending, jitter, or visible clamp popping.

    Placeholder-asset note (unchanged, D-01 / issue #17): VRM has no dedicated greeting/goodbye clips and GLB lacks a 2nd talk variant — verify the mechanisms against the placeholders, not bespoke greeting/goodbye animations.
  </how-to-verify>
  <resume-signal>Type "approved" if: G1 is resolved (live idle on load, no T-pose, no spin), G2 is resolved (idle->talking crossfades smoothly, no snap), AND all 7 original requirements (IDLE-01/02, TRANS-01/02, TALK-01/02, PERF-01) show no regression. Otherwise describe EXACTLY which item still fails and how (including any GATE_DEBUG console output for G1), and a further gap-closure pass will follow.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification-only plan; no code changes, no new input surface. The optional `GATE_DEBUG` aid (if the human enables it) logs only local scene-graph names, action weights, and a bone angle — no secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-17 | Tampering (undetected regression) | shared animation composition/crossfade code touched by 11-11 | mitigate | This plan's human check explicitly re-verifies ALL 7 requirements (not just G1/G2), and Task 1's objective gates confirm IDLE-02/PERF-01/timer-free invariants and the spin-not-reintroduced tests before the checkpoint. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. |
</threat_model>

<verification>
- Objective invariant gates (timer-free, additive, internal-only, IDLE-02 intact, PERF-01 intact) plus fix-landed greps (G1/G2 fix present, spin-not-reintroduced tests exist) all pass.
- Full react test suite green, including the new G1 non-frozen and spin-bounded assertions.
- Human confirms G1 resolved (live idle on load, no T-pose, no spin), G2 resolved (smooth idle->talk crossfade), and no regression across all 7 original Phase-11 requirements.
</verification>

<success_criteria>
Phase 11 gap closure is complete: G1 (T-pose on load) and G2 (idle->talking snap) are confirmed resolved by a human on a running build, the 11-09 spin is not reintroduced, and all 7 original Phase-11 requirements pass a full regression spot-check with no new breakage, recorded in 11-12-SUMMARY.md. If any item still fails, it is reported as a remaining gap rather than silently approved, and a further gap-closure pass follows.
</success_criteria>

<output>
Create `.planning/phases/11-idle-transition-talking-states/11-12-SUMMARY.md` when done.
</output>
