---
phase: 11-idle-transition-talking-states
plan: 13
type: execute
wave: 8
gap_closure: true
depends_on: ["11-11"]
files_modified:
  - packages/react/src/animation/AnimationStateEngine.ts
  - packages/react/src/animation/AnimationStateEngine.test.ts
autonomous: true
requirements: [IDLE-01, TRANS-01, TALK-01]
must_haves:
  truths:
    - "On first load, before Connect is ever pressed, the avatar's full skeleton is driven by the idle base clip (a live/held idle pose) — NOT a frozen bind/T-pose with arms out"
    - "Pressing Connect for the first time does not produce a visible downward Y-axis jump of the avatar"
    - "When the talk clip cycles between speaking variants, the torso (spine/chest/hips) does not visibly snap toward bind pose and back (no jiggle at the switch boundary)"
    - "The 11-09 first-mount 'spins weird' bug is NOT reintroduced (bounded-accumulation invariant still holds)"
    - "The 11-11-fixed G2 idle->talking crossfade smoothness is NOT reintroduced as a snap (multi-frame-ramp non-snap invariant still holds)"
  artifacts:
    - path: "packages/react/src/animation/AnimationStateEngine.ts"
      provides: "Recorded runtime-evidenced diagnosis (file-header 11-13 block) for G1/G3/G4 + the targeted fix"
      contains: "11-13 gap closure"
    - path: "packages/react/src/animation/AnimationStateEngine.test.ts"
      provides: "Regression tests: pre-connect base-clip-starts-when-clips-load (G1), no-torso-snap-at-variant-switch (G4), plus preserved 11-09 spin + 11-11 G2 invariants"
      contains: "11-13"
  key_links:
    - from: "AnimationStateEngine.ts crossfade-trigger useEffect"
      to: "switchToClip / beginCrossfade"
      via: "effect deps must re-evaluate once clips/root become resolvable"
      pattern: "useEffect\\("
---

<objective>
Fourth gap-closure pass for Phase 11. Round 3 (11-11) correctly root-caused and fixed G2 (idle->talking snap) via genuine headless runtime evidence, but the 11-12 human re-check found:
- **G1** — pre-connect T-pose persists on first load; resolves only after Connect is pressed. 11-11's `resetToRestPoseIfNotDriven` fix covered a post-connect / mid-crossfade near-zero-weight window, but NOT the actual pre-connect resting state.
- **G3 (new)** — the avatar visibly drops on the Y axis when Connect is first pressed.
- **G4 (new, minor)** — a small jiggle when the talk clip cycles between variants (TALK-01 was previously fully passing).

Purpose: Root-cause G1 (specifically WHY 11-11's fix was not enough), G3, and G4 with the SAME rigor 11-11 applied — actual runtime evidence via a headless replay of the REAL production path (`switchToClip`/`beginCrossfade`/`stepCrossfade`/`resetToRestPoseIfNotDriven`), not assumption — determine whether they share a root cause, then apply a targeted fix that does NOT reintroduce the 11-09 first-mount spin OR the 11-11-fixed G2 snap.

Output: Recorded diagnosis in the `AnimationStateEngine.ts` file header (an additive 11-13 block, mirroring the existing 11-09/11-11 blocks) + a targeted fix + regression tests, all committed atomically per task.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-idle-transition-talking-states/11-11-SUMMARY.md
@.planning/phases/11-idle-transition-talking-states/11-12-SUMMARY.md
@.planning/phases/11-idle-transition-talking-states/11-PATTERNS.md
@packages/react/src/animation/AnimationStateEngine.ts
@packages/react/src/animation/crossfade.ts
@packages/react/src/KhaveeProvider.tsx
@src/app/openai-avatar-test/page.tsx

<interfaces>
<!-- Key production functions the diagnostic must replay against — extracted from the codebase. Use these directly; do NOT stub them. -->

From packages/react/src/animation/crossfade.ts:
- `beginCrossfade(fromAction: AnimationAction | null, toAction: AnimationAction, scene: Object3D, floorSeconds?: number): BlendState` — starts toAction at effectiveWeight 0 via `.setEffectiveWeight(0)` after `.enabled=true`/`.play()`; startTime = performance.now().
- `stepCrossfade(blend: BlendState): void` — ramps effective weight off WALL-CLOCK (`performance.now() - blend.startTime`), NOT the mixer delta. (11-11 discovered this: a synchronous frame loop must deterministically advance `performance.now()` to make the ramp progress — see 11-11-SUMMARY "Issues Encountered".)

From packages/react/src/animation/AnimationStateEngine.ts:
- `shouldRunProceduralBoneWrites(action): boolean` — false when action is null OR its effectiveWeight < MIN_BASE_ACTION_WEIGHT (0.05).
- `resetToRestPoseIfNotDriven(bones, restPose, isBaseActionDriving)` — copies the captured bind-pose anchor onto spine/chest/hips ONLY while the base action is not driving; no-ops otherwise.
- `resolveBaseClip(chatStatus, currentAnimation, availableNames)` — for `stopped` (the pre-connect state) no bundled clip matches `/stop|bye|goodbye|outro/i`, so it falls through to `currentAnimation ?? availableNames[0] ?? null`.
- The crossfade-trigger `useEffect(..., [targetName, chatStatus])` — its dependency array is the load-bearing detail for G1: it does NOT list any "clips/root became available" signal.

From packages/react/src/KhaveeProvider.tsx:
- `chatStatus` defaults to `"stopped"` (line 97) and `currentAnimation` defaults to `"idle"` (line 90) BEFORE Connect is ever pressed.

From src/app/openai-avatar-test/page.tsx:
- `AVATAR_ANIMATIONS = { idle: 'Idle.fbx', talking: 'talking.fbx', talking1: 'talking1.fbx' }` — no clip matches the `stopped` or `starting` patterns; `idle` matches the `ready` pattern `/idle|ready|rest/i`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose G1 (why 11-11's fix was not enough), G3, and G4 at runtime — record evidence, do NOT fix yet</name>
  <read_first>
    - packages/react/src/animation/AnimationStateEngine.ts (full — the 11-09 and 11-11 file-header diagnosis blocks, `shouldRunProceduralBoneWrites`, `resetToRestPoseIfNotDriven`, `switchToClip`, the `[targetName, chatStatus]` effect, and `update()` steps 3-9)
    - packages/react/src/animation/crossfade.ts (full — `beginCrossfade`/`stepCrossfade`, note the wall-clock ramp)
    - packages/react/src/animation/AnimationStateEngine.test.ts (full — the existing 11-09 first-mount compounding repro and the 11-11 G2 non-snap multi-frame-ramp test, so your new evidence extends rather than duplicates them; reuse their `vi.spyOn(performance, "now")` deterministic-clock convention)
    - packages/react/src/KhaveeProvider.tsx lines 85-131 (pre-connect defaults: chatStatus="stopped", currentAnimation="idle")
    - src/app/openai-avatar-test/page.tsx (the exact page verified against — clip set + no stopped/starting clip)
    - .planning/phases/11-idle-transition-talking-states/11-11-SUMMARY.md ("Issues Encountered" — the performance.now() harness technique and the VRMCoreLoaderPlugin `userData.vrmCore` gotcha; reuse both)
  </read_first>
  <action>
Build a headless diagnostic that replays the REAL production path (import and drive the actual `beginCrossfade`/`stepCrossfade` from crossfade.ts, the actual `resolveBaseClip`/`shouldRunProceduralBoneWrites`/`resetToRestPoseIfNotDriven` from AnimationStateEngine.ts, and — where feasible per 11-11's precedent — a real `public/models/male.vrm` + `Idle.fbx`/`talking.fbx`/`talking1.fbx` loaded via GLTFLoader.parse + VRMCoreLoaderPlugin and remapped via the real `remapMixamoAnimationToVrm`). Do NOT reason from code alone and do NOT use stub actions where a real one is testable. Advance `performance.now()` deterministically per simulated frame exactly as 11-11 did. Put the throwaway script at `packages/react/scratch-g1g3g4-diagnostic.ts` and DELETE it before committing (like 11-11's scratch file).

Investigate three named hypotheses. These are LEADS, not conclusions — confirm or DISPROVE each with recorded measurements, and if the evidence points elsewhere, report the actual mechanism transparently (as 11-11 did when all three of its pre-defined leads were disproven):

- **H-G1 (pre-connect effect-ordering — the base clip's crossfade never starts before Connect):** The crossfade-trigger `useEffect` depends only on `[targetName, chatStatus]`. Pre-connect, chatStatus="stopped" and currentAnimation="idle" resolve `targetName="idle"`. If that effect's single run for the pre-connect state happens while clips/root are not yet resolvable (`getAction("idle")` null or `getRoot()` null), `switchToClip` returns early and the effect never re-fires when the VRM finishes loading (targetName/chatStatus are unchanged), so `currentActionRef` stays null, the mixer never plays idle, and only spine/chest/hips get the 11-11 reset-to-bind-anchor (leaving arms/legs frozen in bind = the reported T-pose). Pressing Connect changes chatStatus, re-firing the effect after clips are ready. Replay this exact ordering: (1) run the effect logic with clips absent; (2) make clips/root resolvable WITHOUT changing chatStatus or targetName; (3) run `update(delta)` for N frames and measure whether `currentActionRef` ever becomes non-null and whether the idle action's effectiveWeight ever ramps above 0. Record the measured outcome. This is the specific case 11-11's harness never covered (11-11 replayed a crossfade that had ALREADY begun).

- **H-G3 (Y-drop on connect — G1 symptom vs. independent hip-Y blend):** Measure the idle clip's first-frame hips WORLD/local Y (and whether it animates a hips position track at all) against the captured bind-pose hips Y. Determine whether the on-connect drop is inherent to the idle clip finally driving the hips (i.e. fixing G1 so the clip drives from frame 0 would move the settle to load-time and eliminate the on-connect discontinuity) OR an independent artifact (root motion, or the rest-pose anchor capturing only quaternion while position blends separately). Record which.

- **H-G4 (talk-cycle jiggle — reset-to-rest firing during variant-switch crossfade windows):** 11-11 confirmed `resetToRestPoseIfNotDriven` fires during every crossfade's near-zero-weight window (~3/60 frames). Replay an idle->talking->talking1 sequence and log spine/chest/hips quaternion `angleTo` the rest anchor across each talkCycle-triggered `switchToClip` boundary. Confirm or disprove that the torso snaps toward the bind anchor for those frames then recovers (= the jiggle), which would make G4 a NEW artifact introduced by 11-11's fix mechanism itself.

Then state explicitly whether G1/G3/G4 share a root cause or are independent, backed by the measurements. Record the full diagnosis as an additive `11-13 gap closure` block in the `AnimationStateEngine.ts` file header (mirroring the 11-09 and 11-11 blocks — do not delete or rewrite those). Add off-by-default instrumentation only if it materially helps the 11-14 human checkpoint; otherwise leave the existing GATE_DEBUG as-is. Commit this task with ONLY the header diagnosis (+ any instrumentation) — no behavioral fix yet.
  </action>
  <verify>
    <automated>cd packages/react && node --experimental-strip-types scratch-g1g3g4-diagnostic.ts 2>&1 | grep -Ei "H-G1|H-G3|H-G4" ; test -f src/animation/AnimationStateEngine.ts && grep -c "11-13 gap closure" src/animation/AnimationStateEngine.ts</automated>
  </verify>
  <acceptance_criteria>
    - The headless script imports and drives the REAL `beginCrossfade`/`stepCrossfade`/`resetToRestPoseIfNotDriven`/`resolveBaseClip` (grep the script for these identifiers before deleting it) — not reimplemented stubs.
    - For EACH of H-G1, H-G3, H-G4 the diagnosis records a concrete measured outcome (a number or a definitive CONFIRMED/DISPROVEN), not a code-reading assertion.
    - The diagnosis explicitly answers "why was 11-11's fix not enough for G1" with evidence (i.e. identifies the case 11-11's harness did not cover).
    - The diagnosis explicitly states whether G1/G3/G4 share a cause.
    - `AnimationStateEngine.ts` contains a new `11-13 gap closure` header block; the 11-09 and 11-11 blocks are still present (grep both).
    - The scratch diagnostic file is deleted before commit (`git status --short packages/react` shows no untracked scratch file).
    - No production behavior changed in this task's commit (the `update()` control flow is unchanged; only the header/instrumentation differs).
  </acceptance_criteria>
  <done>Runtime evidence for G1/G3/G4 recorded in the file header, shared-vs-independent determination made, no fix applied yet, committed atomically.</done>
</task>

<task type="auto">
  <name>Task 2: Fix G1/G3/G4 targeting the confirmed cause(s) — without reintroducing the 11-09 spin or the 11-11 G2 snap</name>
  <read_first>
    - packages/react/src/animation/AnimationStateEngine.ts (the 11-13 diagnosis block written in Task 1, plus `switchToClip`, the `[targetName, chatStatus]` effect, `resetToRestPoseIfNotDriven`, and `update()` steps 4a-9)
    - packages/react/src/animation/AnimationStateEngine.test.ts (the existing 11-09 first-mount compounding repro and the 11-11 G2 non-snap multi-frame-ramp test — these two MUST stay green and are your regression baseline)
    - packages/react/src/animation/crossfade.ts (do NOT modify — `beginCrossfade`/`stepCrossfade` are the validated reference per D-02)
  </read_first>
  <action>
Apply the minimal targeted fix that resolves the mechanism(s) confirmed in Task 1. Do NOT improvise against an undiagnosed cause; the fix must map directly to Task 1's recorded evidence.

Likely shape given the leading hypotheses (adjust to the confirmed diagnosis):
- **G1:** ensure the base-clip crossfade actually starts once clips/root become resolvable in the pre-connect state — e.g. make the crossfade-trigger effect re-evaluate when the resolvable target action first becomes available (add a clips/root-readiness signal to its dependency array, or an equivalent one-shot retry), so `switchToClip("idle")` runs as soon as the VRM loads instead of waiting for a `chatStatus` change. Preserve the existing TALK-01 speaking-variant ownership guard and the `getAction`/`getRoot` deps-omission rationale (do not re-add `getAction` to the deps in a way that reintroduces TALK-01's double-trigger).
- **G3:** if Task 1 shows the Y-drop is a G1 symptom, confirm the G1 fix removes the on-connect discontinuity (the settle moves to load time). If Task 1 shows an independent hip-Y artifact, address it specifically (e.g. include hips position in the rest-pose anchor / crossfade handling) — but only if evidenced.
- **G4:** stop the talk-cycle-switch jiggle without reintroducing the 11-09 spin. The jiggle comes from `resetToRestPoseIfNotDriven` snapping spine/chest/hips to the bind anchor during the near-zero-weight window of a talk-variant crossfade, where the OUTGOING variant is still driving the torso. Prefer a fix that does not reset to the bind anchor when a prior base action is still contributing weight during a crossfade (i.e. distinguish "no action has ever driven the skeleton" from "mid-crossfade between two real actions"), so the mixer's own blend handles the torso smoothly while still bounding the true first-mount/null-base case. Keep `resetToRestPoseIfNotDriven`'s pure/exported/unit-testable shape.

Add regression tests to `AnimationStateEngine.test.ts`:
- A G1 test: replay the pre-connect ordering (effect runs with clips absent, clips then become available WITHOUT a chatStatus change) and assert the idle base action starts driving (effectiveWeight ramps above 0) within a bounded number of frames.
- A G4 test: replay idle->talking->talking1 and assert spine/chest/hips do NOT snap toward the bind anchor at the variant-switch boundary while the outgoing action is still contributing (bounded angular deviation across the switch).
- If G3 was independent, a G3 test asserting no anomalous hips-Y discontinuity.

Explicit regression re-verification (both must remain green, add assertions if the fix touches their code paths):
- The 11-09 first-mount "spins weird" bounded-accumulation invariant.
- The 11-11 G2 idle->talking non-snap multi-frame-ramp invariant.

Do not lower `MIN_BASE_ACTION_WEIGHT` toward 0 (11-11 warns this re-enables the exact 11-09 compounding). Record the fix rationale in the `11-13 gap closure` header block. Run `tsc --noEmit` and the full react test suite before committing. Commit this task atomically (fix + tests).
  </action>
  <verify>
    <automated>cd packages/react && pnpm exec tsc --noEmit && pnpm --filter @khaveeai/react test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @khaveeai/react test` passes with the full suite green (>= the prior 80 tests, plus the new G1/G4[/G3] regression tests).
    - The pre-existing 11-09 first-mount compounding repro test is present and green (grep the test file for the describe/it block) — the spin is NOT reintroduced.
    - The pre-existing 11-11 G2 non-snap multi-frame-ramp test is present and green — the G2 snap is NOT reintroduced.
    - A new G1 regression test asserts the idle base action starts driving after clips load without a chatStatus change.
    - A new G4 regression test asserts no torso snap-to-bind at a talk-variant switch boundary.
    - `MIN_BASE_ACTION_WEIGHT` is not lowered below its current value (0.05) (grep).
    - `crossfade.ts` is unmodified (`git diff --stat` shows no change to crossfade.ts).
    - `pnpm exec tsc --noEmit` is clean in packages/react.
    - The `11-13 gap closure` header block records the fix rationale mapping to Task 1's evidence.
  </acceptance_criteria>
  <done>G1/G3/G4 fixed against confirmed causes; new + preserved regression tests green; 11-09 spin and 11-11 G2 snap both explicitly verified not reintroduced; tsc clean; committed atomically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan modifies a browser-local, internal animation module. No new untrusted input crosses any boundary; `currentVolume` is already clamped to [0,1] at its KhaveeProvider ingress (unchanged here). No network, filesystem, or package-manager surface is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | AnimationStateEngine per-frame bone writes | accept | Internal-only module (not exported from index.ts); all inputs are internal refs/clamped scalars; no external attacker surface. Bounded magnitude already enforced by MAX_COMBINED_SPINE_DELTA_RAD. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (fix + tests only; no new dependencies). |
</threat_model>

<verification>
- `pnpm --filter @khaveeai/react test` — full suite green including the new G1/G4[/G3] tests AND the preserved 11-09 spin + 11-11 G2 invariants.
- `pnpm exec tsc --noEmit` clean in packages/react.
- `AnimationStateEngine.ts` carries an additive `11-13 gap closure` header block with recorded runtime evidence for G1/G3/G4; the 11-09 and 11-11 blocks are intact.
- The decisive visual confirmation (no pre-connect T-pose, no Y-drop on connect, no talk-cycle jiggle, plus a full 7-requirement regression pass) is deferred to the 11-14 blocking human-verify checkpoint — headless/unit evidence cannot make that final call.
</verification>

<success_criteria>
- G1's pre-connect mechanism is root-caused with recorded runtime evidence explaining why 11-11's fix was insufficient, and fixed.
- G3 and G4 are each root-caused with evidence (shared-with-G1 or independent, stated explicitly) and fixed.
- No regression of the 11-09 first-mount spin or the 11-11 G2 idle->talking snap (both asserted by preserved green tests).
- IDLE-02, TRANS-02, PERF-01 grep-level invariants remain intact (re-checked by 11-14).
</success_criteria>

<output>
Create `.planning/phases/11-idle-transition-talking-states/11-13-SUMMARY.md` when done.
</output>
