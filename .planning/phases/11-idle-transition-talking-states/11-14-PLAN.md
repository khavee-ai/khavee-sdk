---
phase: 11-idle-transition-talking-states
plan: 14
type: execute
wave: 9
gap_closure: true
depends_on: ["11-13"]
files_modified: []
autonomous: false
requirements: [IDLE-01, IDLE-02, TRANS-01, TRANS-02, TALK-01, TALK-02, PERF-01]
must_haves:
  truths:
    - "The full react test suite is green and 11-13's fix is present at the code level (fix-landed gates pass) BEFORE any human checkpoint is presented"
    - "The 11-09 first-mount spin and 11-11 G2 snap are both confirmed NOT reintroduced at the code level"
    - "A human decisively confirms or rejects G1 (no pre-connect T-pose), G3 (no Y-drop on connect), and G4 (no talk-cycle jiggle) against a live build"
    - "A human re-confirms G2 (idle->talking smooth) still holds and runs a full regression pass across all 7 original requirements"
  artifacts:
    - path: ".planning/phases/11-idle-transition-talking-states/11-14-SUMMARY.md"
      provides: "The decisive round-4 human re-verification record (approved or not, verbatim per item)"
  key_links:
    - from: "objective + fix-landed gates"
      to: "human checkpoint"
      via: "gates run first; checkpoint is only presented if all gates pass"
      pattern: "pnpm --filter @khaveeai/react test"
---

<objective>
Fourth-round re-verification for Phase 11. Run the objective invariant gates + fix-landed gates (proving 11-13's fix is present and the 11-09 spin / 11-11 G2 snap are NOT reintroduced) + the full react test suite, THEN present a single decisive blocking human-verify checkpoint.

This is the 4th verification checkpoint for this phase. Keep it as broad as 11-12's: G1, G3, G4, a quick G2 re-confirm, and a full spot-check of all 7 original requirements — because G2 was only confirmed once (in the same round G3/G4 surfaced), and 6 of the 7 requirements were not explicitly re-confirmed in the 11-12 round.

Purpose: Determine whether Phase 11 gap closure is finally complete.
Output: `11-14-SUMMARY.md` recording the decisive result verbatim per item.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-idle-transition-talking-states/11-13-SUMMARY.md
@.planning/phases/11-idle-transition-talking-states/11-12-SUMMARY.md
@packages/react/src/animation/AnimationStateEngine.ts
@src/app/openai-avatar-test/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Objective invariant + fix-landed gates + full react test suite</name>
  <read_first>
    - .planning/phases/11-idle-transition-talking-states/11-13-SUMMARY.md (what the fix actually changed — so the fix-landed gates target the real change, not a guessed one)
    - packages/react/src/animation/AnimationStateEngine.ts (11-13 header block, the fixed effect/update path)
    - packages/react/src/animation/AnimationStateEngine.test.ts (the new G1/G4[/G3] tests + preserved 11-09/11-11 tests)
    - .planning/phases/11-idle-transition-talking-states/11-12-SUMMARY.md ("Task 1 (objective gates)" — reuse the same 5 invariant gates verbatim so this round is comparable to the last)
  </read_first>
  <action>
If this runs in a fresh worktree with no node_modules, run `pnpm install --frozen-lockfile` at the repo root first (11-12 hit `vitest: command not found` otherwise). Then run these gates and record each PASS/FAIL:

Invariant gates (must still hold after 11-13):
1. Timer-free: no `setInterval`/`setTimeout` anywhere in `packages/react/src/animation/` (grep count 0 across every file, excluding comment lines: `grep -rn --include=*.ts -E "setInterval|setTimeout" packages/react/src/animation/ | grep -v -E "^[^:]+:[0-9]+:\\s*(//|\\*)"`).
2. Additive-not-overwrite: in `breathing.ts` and `sway.ts`, `.quaternion.set(` count is 0 and `.multiply(` count is >= 1 in each.
3. Internal-only: the new procedural systems are not exported from `packages/react/src/index.ts` (grep for `breathing|sway|expressionDrift|talkCycle|audioAmplitude` returns 0).
4. IDLE-02 intact: `expressionDrift.ts` still raises DEFAULT_AMPLITUDE (the 11-09 value) and keeps drift-ownership tracking.
5. PERF-01 intact: `MAX_COMBINED_SPINE_DELTA_RAD` present in `AnimationStateEngine.ts`; breathing/sway steps still called (guarded, not deleted).

Fix-landed gates (prove 11-13 is present and neither prior bug is reintroduced):
6. 11-13 diagnosis present: `AnimationStateEngine.ts` contains a `11-13 gap closure` header block; the 11-09 and 11-11 blocks are also still present.
7. G1 fix present: the crossfade-trigger effect now re-evaluates on clips/root readiness (or the equivalent mechanism 11-13-SUMMARY records) — verify the specific change 11-13-SUMMARY describes actually exists in the file.
8. Spin NOT reintroduced: `MIN_BASE_ACTION_WEIGHT` not lowered below 0.05; the 11-09 first-mount compounding repro test present.
9. G2 snap NOT reintroduced: the 11-11 G2 non-snap multi-frame-ramp test present.
10. Full suite: `pnpm --filter @khaveeai/react test` — all suites green (>= 80 tests plus 11-13's new regression tests).

If ANY gate fails, STOP and report before presenting the Task 2 checkpoint — do not put a human checkpoint on top of a failing invariant (this is the same discipline 11-12 followed).
  </action>
  <verify>
    <automated>cd packages/react && pnpm --filter @khaveeai/react test 2>&1 | tail -5 && grep -c "11-13 gap closure" src/animation/AnimationStateEngine.ts && grep -c "MIN_BASE_ACTION_WEIGHT = 0.05" src/animation/AnimationStateEngine.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 5 invariant gates recorded PASS.
    - All 5 fix-landed gates (incl. the full test suite) recorded PASS, with the actual gate outputs quoted in the summary.
    - Gate 7 verifies the SPECIFIC change 11-13-SUMMARY claims (not a generic grep) — the fix is really present.
    - If any gate fails, the run stops and reports without presenting the human checkpoint.
  </acceptance_criteria>
  <done>All 10 gates pass and are recorded; the checkpoint is cleared to present. (Or: a failing gate is reported and the checkpoint is withheld.)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Decisive human re-verification of G1/G3/G4 + G2 re-confirm + full 7-requirement regression pass</name>
  <what-built>
11-13 applied a runtime-evidenced fix for G1 (pre-connect T-pose), G3 (Y-drop on connect), and G4 (talk-cycle jiggle), with regression tests confirming the 11-09 spin and 11-11 G2 snap are not reintroduced. Task 1's objective + fix-landed gates and the full test suite all pass at the code level. This checkpoint is the decisive visual judgment the automated gates cannot make.
  </what-built>
  <how-to-verify>
Before presenting, start the dev server and confirm the page returns 200 (automate this — do not ask the human to start it): `pnpm dev` (or the repo's dev script) and confirm `http://localhost:<port>/openai-avatar-test` (and `/glb-avatar-test` if present) load. Then ask the human to check, in order:

1. **G1 (pre-connect, PRIMARY):** Load `/openai-avatar-test` and DO NOT press Connect. Within the first ~2 seconds, is the avatar in a live/held idle pose (arms down, subtle breathing/sway) rather than a frozen T-pose with arms out? [This is the exact failure 11-12 caught — the T-pose must be gone in the pre-connect resting state.]
2. **G3 (Y-drop, NEW regression):** Now press Connect for the first time. Does the avatar stay at the same vertical position (no visible downward drop/jump on the Y axis)?
3. **G4 (talk-cycle jiggle, NEW minor):** Start a conversation so the avatar enters `speaking` for 60+ seconds. When the talk clip cycles between variants, is the torso smooth (no small snap/jiggle at the switch)?
4. **G2 (quick re-confirm):** Does the idle->talking transition still crossfade smoothly (no snap)? [Confirmed once in 11-12; re-confirm it survived 11-13's changes.]
5. **Full 7-requirement regression spot-check:**
   - IDLE-01: left idle 30s+, continuous randomized breathing (chest/spine) + independent weight-shift sway (hip/spine)?
   - IDLE-02: on the VRM, subtle randomized expression drift visible in idle? (GLB shows none — expected.)
   - TRANS-01: session start plays a greeting moment with a ~1-1.5s minimum floor (not an instant snap)?
   - TRANS-02: session end (`stopped`) settles distinctly from the live idle base (damped-toward-still), not an instant cut?
   - TALK-01: during speaking, talk clips cycle at natural loop boundaries (not a fixed interval), each dwelling ~2s+?
   - TALK-02: speaking louder/quieter visibly changes procedural amplitude, WITHOUT changing which clip plays or when it switches?
   - PERF-01: no visible over-bending/spasming of the spine when breathing+sway peak together (bounded)?

Report each item as PASS / FAIL / (not observed), verbatim — do not infer a pass for anything not explicitly checked.
  </how-to-verify>
  <read_first>
    - .planning/phases/11-idle-transition-talking-states/11-13-SUMMARY.md
    - .planning/phases/11-idle-transition-talking-states/11-12-SUMMARY.md (the prior round's verbatim-recording discipline)
    - src/app/openai-avatar-test/page.tsx (the page under test)
  </read_first>
  <action>
Present the checkpoint above only after Task 1's gates all pass. Record the human's result verbatim, per item, in `11-14-SUMMARY.md` — without inferring pass/fail on any item not explicitly addressed (exactly as 11-12 recorded its result). If approved, note that Phase 11 gap closure is complete and requirements IDLE-01/02, TRANS-01/02, TALK-01/02, PERF-01 can be finalized. If not approved, record the specific failing items with enough detail (which behavior, when observed) to seed a fifth gap-closure round.
  </action>
  <resume-signal>Type "approved" (all items pass) or describe the specific failing item(s) with when/where observed.</resume-signal>
  <acceptance_criteria>
    - The dev server was started and the target page(s) confirmed 200 before the checkpoint was presented (automation-first).
    - Every one of the 5 checkpoint groups (G1, G3, G4, G2, and each of the 7 requirements) has an explicit human PASS/FAIL/(not observed) verdict recorded verbatim.
    - No item is marked pass by inference; unaddressed items are recorded as "not observed", not "pass".
    - The summary states clearly whether Phase 11 gap closure is APPROVED or a further round is required.
  </acceptance_criteria>
  <done>The decisive human verdict for G1/G3/G4/G2 + all 7 requirements is recorded verbatim, and Phase 11 gap-closure status (complete vs. further round) is stated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification-only plan (no source changes; `files_modified: []`). No new trust boundary is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | (n/a) | verification-only plan | accept | No code changes, no dependencies, no external input surface — nothing to mitigate beyond what 11-13 already covers. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Only `pnpm install --frozen-lockfile` (lockfile-pinned, no new packages) may run for environment setup. |
</threat_model>

<verification>
- Task 1: all 10 gates pass (5 invariant + 5 fix-landed incl. full test suite), recorded with quoted outputs.
- Task 2: decisive human verdict recorded verbatim per item; Phase 11 gap-closure status stated.
</verification>

<success_criteria>
- Objective + fix-landed gates confirm 11-13's fix is present and the 11-09 spin / 11-11 G2 snap are not reintroduced, at the code level, before the checkpoint.
- The human checkpoint delivers a decisive verdict on G1, G3, G4, G2, and all 7 requirements.
- The outcome (approved / further round needed) is unambiguous and recorded.
</success_criteria>

<output>
Create `.planning/phases/11-idle-transition-talking-states/11-14-SUMMARY.md` when done.
</output>
