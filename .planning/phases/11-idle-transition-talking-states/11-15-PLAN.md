---
phase: 11-idle-transition-talking-states
plan: 15
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/react/src/animation/AnimationStateEngine.ts
  - packages/react/src/animation/AnimationStateEngine.test.ts
autonomous: true
gap_closure: true
requirements: [IDLE-01, TRANS-01, TRANS-02]

must_haves:
  truths:
    - "G5's mechanism is root-caused with RECORDED headless runtime evidence (frame-by-frame hips.position.y across the real page-load settle), NOT assumed from code reading"
    - "On first page load, before Connect is pressed, the avatar settles from T-pose to idle WITHOUT a visible ~1-unit Y-axis (hips-height) drop"
    - "The idle base clip's hips height is anchored at the VRM bind-pose local hips Y, not driven down to the remap-normalized 0"
    - "G1 (pre-connect base clip drives), G2 (idle->talk crossfade smooth), G3 (no connect-time drop), G4 (no talk-cycle jiggle) remain fixed — no regression"
    - "IDLE-01, IDLE-02, TRANS-01, TRANS-02, TALK-01, TALK-02, PERF-01 are not regressed"
  artifacts:
    - path: "packages/react/src/animation/AnimationStateEngine.ts"
      provides: "New '11-15 gap closure' file-header block with measured hips-Y evidence + the FIX (bind-pose-aware hips-Y anchor)"
      contains: "11-15 gap closure"
    - path: "packages/react/src/animation/AnimationStateEngine.test.ts"
      provides: "New G5 regression describe block asserting hips.position.y holds at bind-pose height across the real crossfade settle"
      contains: "G5"
  key_links:
    - from: "update() hips-Y correction step"
      to: "captured bind-pose hips.position.y anchor"
      via: "per-frame additive Y correction gated on base-action-driving"
      pattern: "position\\.y"
    - from: "AnimationStateEngine.test.ts G5 test"
      to: "real beginCrossfade(null, idleAction, root) + stepCrossfade settle"
      via: "headless production-path replay assertion on hips.position.y"
      pattern: "beginCrossfade"
---

<objective>
Root-cause and fix G5: the visible Y-axis (hips-height) drop during the initial T-pose-to-idle settle on first page load, before Connect is pressed.

This is the fifth gap-closure round for Phase 11. Rounds 3-4 (11-11, 11-13) proved that only headless-runtime-evidence-based diagnosis succeeds here; four prior assumption-based rounds (11-06 through 11-10) failed live re-verification. 11-13's G1 fix (the base clip's first crossfade now fires at page-load time via `shouldTriggerClipSwitch`) had the side effect of relocating 11-13's already-measured hips-Y settle motion from connect-time (G3, fixed) to page-load-time (G5). The human questioned this ("shouldn't it not drop?"), signaling it reads as unwanted, not an accepted cost.

Purpose: Eliminate the visible page-load drop while preserving G1-G4 and all 7 approved requirements, using the same runtime-evidence discipline 11-11/11-13 established — measure the exact mechanism against the REAL production path before changing any behavior.
Output: A recorded diagnosis (new "11-15 gap closure" header block) plus a targeted, regression-tested fix in AnimationStateEngine.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-idle-transition-talking-states/11-13-SUMMARY.md
@.planning/phases/11-idle-transition-talking-states/11-14-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs — extracted from the shipped code. Use these directly, no exploration needed. -->

From packages/react/src/animation/AnimationStateEngine.ts (current, post-11-13):
- `export function shouldTriggerClipSwitch(params: { targetName; chatStatus; currentClipName; canResolveAction; canResolveRoot }): boolean` — the G1/G3 fix; called by both the crossfade-trigger useEffect and update()'s per-frame retry (step 0).
- `export function isBaseActionMeaningfullyDriving(currentAction, blendFromAction): boolean` — G4 fix; true when the incoming action's weight cleared MIN_BASE_ACTION_WEIGHT OR a non-null outgoing (from) action exists.
- `export interface RestPoseAnchor { spine: THREE.Quaternion; chest: THREE.Quaternion; hips: THREE.Quaternion }` — captured once in update() step 4a, the first frame all three bones resolve. NOTE: captures ONLY quaternions — NOT hips.position.
- `export function resetToRestPoseIfNotDriven(bones, restPose, isBaseActionDriving)` — copies restPose.spine/chest/hips QUATERNIONS onto the bones while not driving; lines 408-410 do `bones.hips?.quaternion.copy(restPose.hips)` — it never touches hips.position.y.
- `update(delta)` frame-ordering: mixer.update (in VRMAvatar useFrame) -> controller.update -> vrm.update. Inside controller.update, documented step order: 0 retry-switch -> 1 stepCrossfade -> 2 blink -> 3 amplitude/settle -> 4a capture rest anchor -> 4b resetToRestPoseIfNotDriven -> 4c capture spine base -> 5 breathing -> 6 sway -> 7 spine clamp -> 8 drift -> 9 talk-cycle.

From packages/react/src/utils/remapMixamoAnimationToVrm.ts (the retargeter — SHARED, also consumed by VRMAvatar.tsx and src/app/components/VRMAvatarRef.tsx):
- Lines 82-87: for the hips VectorKeyframeTrack it computes `const firstY = scaled[1] ?? 0;` then `value = scaled.map((v, i) => (i % 3 === 1 ? v - firstY : v))` — this normalizes the FIRST keyframe's Y to exactly 0. This is the measured source of the ~1.008-unit drop (VRM bind-pose local hips Y ~1.008 vs. animation start 0).

From packages/react/src/animation/crossfade.ts:
- `beginCrossfade(fromAction, toAction, root, floorSeconds?): BlendState` and `stepCrossfade(blend)` — `stepCrossfade` times off `performance.now()`, NOT the mixer delta (so the headless harness must advance performance.now() per simulated frame — 11-11/11-13 precedent).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose G5 at runtime via headless production-path replay — record evidence, do NOT fix yet</name>
  <read_first>
    - packages/react/src/animation/AnimationStateEngine.ts (full — the 11-09/11-11/11-13 header diagnosis blocks AND the update() hips/restPose handling at steps 4a-4b)
    - packages/react/src/utils/remapMixamoAnimationToVrm.ts (full — the hips VectorKeyframeTrack normalization at lines 75-96)
    - packages/react/src/animation/crossfade.ts (beginCrossfade/stepCrossfade signatures and the performance.now() timing)
    - .planning/phases/11-idle-transition-talking-states/11-13-SUMMARY.md (H-G3 finding + the headless harness build notes: Node --experimental-strip-types + a custom ESM resolve hook appending .ts to extensionless relative imports; the scratch harness was deleted before commit and must be reconstructed)
    - .planning/phases/11-idle-transition-talking-states/11-14-SUMMARY.md (the verbatim G5 human finding and the two hypotheses to decide between)
  </read_first>
  <action>
    Reconstruct the headless diagnostic harness (11-11/11-13 precedent — the prior scratch scripts were deleted after commit; rebuild them as temporary files under the scratchpad, NOT committed). Use Node with `--experimental-strip-types` plus a small custom ESM resolve hook that appends `.ts` to extensionless relative specifiers when the `.ts` file exists on disk (Node 23 removed `--experimental-specifier-resolution=node`; this is the exact workaround 11-13-SUMMARY.md documents). Load the REAL `public/models/male.vrm` via `GLTFLoader.parse()` + `VRMCoreLoaderPlugin`, and `public/animations/Idle.fbx` (or the actual bundled idle clip path used by VRMAvatar.tsx) via the REAL `remapMixamoAnimationToVrm`.

    Replay the EXACT page-load sequence 11-13's G1 fix now produces: (1) capture the VRM bind-pose LOCAL `hips.position.y` (via `vrm.humanoid.getNormalizedBoneNode("hips")`) BEFORE any action has ever played (expect ~1.008 per 11-13's H-G3 measurement); (2) trigger the first successful base-clip switch at load-time — i.e. `beginCrossfade(null, idleAction, root, floor)` with `floor` set to the TRANS-01/02 starting/stopped floor path that switchToClip uses (idle/ready resolves through resolveBaseClip); (3) step `stepCrossfade` + `mixer.update` + `vrm.update` across the FULL crossfade floor duration, advancing `performance.now()` deterministically per simulated frame.

    Record FRAME-BY-FRAME: `hips.position.y` for every frame of the settle, plus the spine/chest quaternion angle-from-bind for context. Compute and record: starting hips-Y, ending hips-Y, `totalDelta`, and `maxSingleFrameDelta`. Then DECIDE, from the recorded numbers, which character G5 is: (a) the IDENTICAL smooth eased ~1.008-unit ramp 11-13 already measured for G3 (totalDelta ~1.008, maxSingleFrameDelta ~0.094, no single-frame discontinuity), now simply at load-time; OR (b) a different-in-magnitude or discontinuous artifact specific to the T-pose bind-pose starting point. Also confirm, by inspection AND by the measurement, that the root cause is the `remapMixamoAnimationToVrm` first-keyframe-Y normalization (line 86-87: `firstY` subtracted → animation hips-Y starts at 0) conflicting with the VRM's ~1.008 bind-pose local hips Y, AND confirm that `resetToRestPoseIfNotDriven` (lines 408-410) copies ONLY quaternions and therefore does NOT currently anchor `hips.position.y` — so nothing holds the bind-pose height once the mixer drives the normalized track.

    Record ALL findings as a NEW "11-15 gap closure" file-header block in AnimationStateEngine.ts, additive to (never replacing) the existing 11-09/11-11/11-13 blocks. Do NOT change any production logic in this task — instrumentation and header diagnosis only. Delete all scratch harness files before committing (verify `git status --short` is clean of scratch artifacts, per 11-13 precedent).
  </action>
  <verify>
    <automated>grep -c "11-15 gap closure" packages/react/src/animation/AnimationStateEngine.ts</automated>
    <automated>cd packages/react && npx tsc --noEmit && pnpm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "11-15 gap closure" packages/react/src/animation/AnimationStateEngine.ts` returns a match (new header block present).
    - The new header block records ACTUAL measured numbers from the headless replay — starting hips-Y (~1.008 bind pose), ending hips-Y (~0), totalDelta, and maxSingleFrameDelta — NOT a code-reading rationale.
    - The block explicitly states whether G5 is character-(a) [same eased ramp, relocated to load-time] or character-(b) [distinct artifact], and cites the specific measurement that decides it.
    - The block confirms the root cause: `remapMixamoAnimationToVrm` first-keyframe-Y normalization vs. the VRM ~1.008 bind-pose local hips Y, AND that `resetToRestPoseIfNotDriven` anchors only quaternions (never hips.position.y).
    - `git diff` for this task touches ONLY comment/header lines in AnimationStateEngine.ts (no executable logic line changes); `git status --short` shows no leftover scratch harness files.
    - `cd packages/react && pnpm test` still reports the pre-existing count (93/93 or higher) — instrumentation caused no test regression.
  </acceptance_criteria>
  <done>A committed "11-15 gap closure" diagnosis header block in AnimationStateEngine.ts containing recorded frame-by-frame hips-Y evidence and the (a)-vs-(b) character determination, with zero production-logic change and the full react suite still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix G5 — anchor the idle clip's hips height at bind pose so the load-time settle no longer drops</name>
  <read_first>
    - packages/react/src/animation/AnimationStateEngine.ts (the new 11-15 header block from Task 1, RestPoseAnchor at lines 365-370, resetToRestPoseIfNotDriven at 398-411, update() steps 4a-4c at ~782-810)
    - packages/react/src/utils/remapMixamoAnimationToVrm.ts (the hips normalization at lines 82-96 — the shared retargeter)
    - packages/react/src/animation/AnimationStateEngine.test.ts (existing describe blocks: the 11-09 first-mount spin repro, the 11-11 G1/G2 tests, the 11-13 G4 non-snap test — the new G5 test must match their structure and MUST NOT break them)
    - packages/react/src/animation/crossfade.ts (beginCrossfade/stepCrossfade for the real-path regression test)
  </read_first>
  <behavior>
    - Test: driving a real `beginCrossfade(null, idleAction, root)` + `stepCrossfade` settle from the VRM bind pose, `hips.position.y` stays within a small tolerance of the captured bind-pose hips Y across ALL frames (does NOT drop by ~1 unit toward 0).
    - Test (contrasting guard): the pre-fix code path in the same simulated window would have dropped ~1.008 units — so the test genuinely guards the fix, not a tautology.
    - Test: the hips-Y correction is a NO-OP during the pre-drive window (before the base action meaningfully drives), so it never double-offsets the bind-pose height when the mixer is not yet writing the normalized track.
    - Existing behavior preserved: 11-09 first-mount spin repro, 11-11 G2 non-snap multi-frame ramp, and 11-13 G4 non-snap tests all still pass.
  </behavior>
  <action>
    Apply the targeted fix for the confirmed G5 cause. Preferred site is CONTROLLER-LOCAL in AnimationStateEngine.ts (lowest blast radius) — do NOT modify `remapMixamoAnimationToVrm.ts` unless Task 1's recorded evidence explicitly concludes the retargeter is the only correct site, because it is a shared retargeter also consumed by `VRMAvatar.tsx` (lines 337, 353) and `src/app/components/VRMAvatarRef.tsx`.

    Extend the rest-pose anchor to also capture the bind-pose `hips.position.y` in update() step 4a (alongside the existing spine/chest/hips quaternion capture), taken on the same first-all-bones-resolvable frame before any action drives the skeleton. Add a new, numbered composition step in update() (do NOT reorder existing steps 0-9 — insert as e.g. step 4d with a documented comment) that adds the captured bind-pose hips-Y offset back onto the animated `hips.position.y`, so the idle clip is anchored at bind-pose height instead of sinking to the remap-normalized 0. Because the offset is a constant added uniformly to every frame of every clip, this preserves the remap normalization's original purpose (preventing inter-clip height JUMPS — the reason line 82-87 exists) while removing the absolute-height mismatch.

    CRITICAL correctness constraint (name it in an inline comment): the correction MUST be gated so it only applies while the mixer is actually driving the normalized hips track (i.e. `isBaseActionMeaningfullyDriving(currentActionRef.current, blendRef.current.from)` is true) — otherwise, during the pre-drive window where `hips.position.y` is still sitting at the true bind pose (~1.008), adding the offset again would DOUBLE the height. Extend the `RestPoseAnchor` interface (or add a sibling ref) to hold the numeric bind-pose hips Y.

    If — and only if — Task 1's evidence concluded the motion is genuinely inherent and cannot be anchored without breaking clip-height consistency, instead implement the least-invasive visible mitigation: hold the avatar at bind-pose hips-Y for the initial load-time settle only. Either way, given the human's "shouldn't it not drop?", the visible page-load drop MUST be eliminated.

    Document the fix as an additive "11-15 gap closure — FIX" section in the header block and inline at the correction site. Add a new describe block "G5 fix — hips height holds at bind pose during the load-time idle settle" to AnimationStateEngine.test.ts implementing the <behavior> cases above via the real beginCrossfade/stepCrossfade path (not a stub). Keep the 11-09 spin repro, 11-11 G2 non-snap, and 11-13 G4 non-snap describe blocks present and green.
  </action>
  <verify>
    <automated>cd packages/react && pnpm test 2>&1 | tail -8</automated>
    <automated>cd packages/react && npx tsc --noEmit</automated>
    <automated>grep -c "G5" packages/react/src/animation/AnimationStateEngine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - A new describe block whose name contains "G5" exists in AnimationStateEngine.test.ts (`grep "G5" ...` matches).
    - The new test drives the real `beginCrossfade(null, idleAction, root)` + `stepCrossfade` settle and asserts `hips.position.y` stays within tolerance of the captured bind-pose Y across the full settle (does NOT drop ~1 unit) — a behavior assertion on the production path, not a stub.
    - A contrasting assertion demonstrates the pre-fix path would have dropped ~1.008 units in the identical window (the test guards the fix).
    - A test proves the correction is a no-op during the pre-drive window (no double-offset).
    - `git diff --stat` shows `remapMixamoAnimationToVrm.ts` UNCHANGED, unless the 11-15 header block records an explicit Task-1 evidence-based reason it must change.
    - `cd packages/react && pnpm test` → all prior tests still pass (>=93 pre-existing + the new G5 tests); the "first-mount procedural-write accumulation repro (11-09)", "G2 fix ... (11-11)", and "G4 fix ... (11-13)" describe blocks are all still present and green.
    - `cd packages/react && npx tsc --noEmit` produces no output (clean).
    - `grep "11-15 gap closure" packages/react/src/animation/AnimationStateEngine.ts` shows the FIX documentation section is present.
  </acceptance_criteria>
  <done>The page-load T-pose-to-idle hips-Y drop is eliminated by a bind-pose-aware hips-Y anchor (or, only if Task 1 proved anchoring impossible, an initial-settle hold), guarded by a real-path G5 regression test, with the full react suite green, tsc clean, and no 11-09/11-11/11-13 regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new this round) | Pure in-process animation math; no network, no new external input. The only externally-sourced scalar (`currentVolume`) was already clamped to [0,1] in KhaveeProvider in an earlier plan — untouched here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | hips-Y correction gating logic | mitigate | Gate correction on `isBaseActionMeaningfullyDriving` so it never double-offsets the bind-pose height; proven by the "no-op during pre-drive window" regression test. |
| T-11-02 | Denial of Service | per-frame update() path (PERF-01) | accept | Correction is one scalar add per frame on an already-resolved bone; well within the ~1-2ms synthesized budget (CONTEXT D-03). No new allocation in the per-frame path. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs this round — pure source edit + tests against existing deps. Legitimacy gate N/A. |
</threat_model>

<verification>
- Diagnosis is evidence-based: the "11-15 gap closure" block contains recorded frame-by-frame hips-Y numbers, not a code-reading rationale.
- The visible page-load drop is eliminated (proven at the code level by the G5 real-path regression test; decisive live confirmation is deferred to 11-16's human checkpoint).
- No regression: 11-09 spin repro, 11-11 G1/G2 tests, 11-13 G4 test, and the full react suite (>=93 tests) all green; tsc --noEmit clean.
- `remapMixamoAnimationToVrm.ts` (shared retargeter) is unchanged unless Task 1 evidence mandates otherwise.
</verification>

<success_criteria>
- G5 root-caused with recorded headless runtime evidence (frame-by-frame hips.position.y), following the 11-11/11-13 discipline.
- Bind-pose-aware hips-Y anchor (or evidence-justified alternative) eliminates the load-time drop, gated so it never double-offsets.
- New G5 real-path regression test green; 11-09/11-11/11-13 regression tests and full suite green; tsc clean.
- IDLE-01/02, TRANS-01/02, TALK-01/02, PERF-01 not regressed at the code level (decisive live re-check is 11-16).
</success_criteria>

<output>
Create `.planning/phases/11-idle-transition-talking-states/11-15-SUMMARY.md` when done.
</output>
