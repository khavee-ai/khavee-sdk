---
phase: 11-idle-transition-talking-states
plan: 16
type: execute
wave: 2
depends_on: [11-15]
files_modified: []
autonomous: false
gap_closure: true
requirements: [IDLE-01, IDLE-02, TRANS-01, TRANS-02, TALK-01, TALK-02, PERF-01]

must_haves:
  truths:
    - "All invariant gates (timer-free, additive-not-overwrite, internal-only, IDLE-02 intact, PERF-01 intact) still hold after 11-15"
    - "All fix-landed gates prove 11-15's G5 fix (the retarget-time bind-pose hips-Y anchor in remapMixamoAnimationToVrm.ts) is present and neither the 11-09 spin, 11-11 G2 snap, nor 11-13 G4 snap is reintroduced"
    - "The GLB height path is confirmed untouched — GLBAvatar.tsx never calls remapMixamoAnimationToVrm, so 11-15's fix cannot have regressed the GLB settle"
    - "The full @khaveeai/react test suite passes including 11-15's new G5 regression + remap unit tests; tsc --noEmit clean"
    - "A human confirms LIVE that the page-load T-pose-to-idle settle no longer drops on the Y axis (G5 fixed), on BOTH the VRM and GLB pages"
    - "A human confirms LIVE that G1, G2, G3, G4 and all 7 original requirements (IDLE-01/02, TRANS-01/02, TALK-01/02, PERF-01) still pass — no regression from 11-15"
  artifacts:
    - path: ".planning/phases/11-idle-transition-talking-states/11-16-SUMMARY.md"
      provides: "Recorded gate results (per gate PASS/FAIL) + the decisive human verdict per item, verbatim"
      contains: "G5"
  key_links:
    - from: "11-16 gate checks"
      to: "11-15's committed fix (retargeter anchor + diagnosis header)"
      via: "grep of the 11-15 diagnosis + the remapMixamoAnimationToVrm bind-pose anchor + G5 test + full suite run"
      pattern: "11-15 gap closure"
    - from: "Task 2 human checkpoint"
      to: "live /openai-avatar-test and /glb-avatar-test pages"
      via: "blocking human-verify of the page-load settle + regression sweep"
      pattern: "human"
---

<objective>
Re-verify Phase 11 gap-closure round 5: confirm 11-15's G5 fix is landed at the code level and holds under decisive live human verification, without regressing G1-G4 or any of the 7 approved requirements.

This is the FIFTH verification checkpoint for Phase 11. The prior four live re-checks (11-08, 11-10, 11-12, 11-14) each surfaced either a still-failing requirement or a new finding — so the objective gates alone are NOT sufficient; the blocking human re-check is the decisive gate. Phase 11 gap closure can only be declared complete if BOTH the code-level gates pass AND the human approves G5 plus the full G1-G4 + 7-requirement regression sweep.

Purpose: Prove the load-time drop is actually gone in the running app (not just in tests) and that nothing regressed — including the GLB path, even though 11-15's fix is VRM-scoped by construction.
Output: 11-16-SUMMARY.md with per-gate results and the verbatim human verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-idle-transition-talking-states/11-14-SUMMARY.md
@.planning/phases/11-idle-transition-talking-states/11-15-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Objective invariant + fix-landed gates + full react test suite</name>
  <read_first>
    - .planning/phases/11-idle-transition-talking-states/11-14-SUMMARY.md (the 10 gates 11-14 ran — reuse the 5 invariant gates VERBATIM for round-to-round comparability)
    - .planning/phases/11-idle-transition-talking-states/11-15-PLAN.md (the specific G5 fix 11-15 landed — Task 1 must verify the SPECIFIC mechanism recorded in 11-15-SUMMARY.md, not a generic grep)
    - .planning/phases/11-idle-transition-talking-states/11-15-SUMMARY.md (the actual fix site 11-15 committed — retargeter anchor by default, or the controller-local fallback if its evidence forced it; gate 7 must match whichever was recorded)
    - packages/react/src/utils/remapMixamoAnimationToVrm.ts (confirm the bind-pose hips-Y anchor is present)
    - packages/react/src/animation/AnimationStateEngine.ts (confirm the 11-15 diagnosis + all prior header blocks present)
    - packages/react/src/animation/AnimationStateEngine.test.ts and packages/react/src/utils/remapMixamoAnimationToVrm.test.ts (confirm the G5 describe block + remap unit tests + all prior regression describe blocks present)
    - packages/react/src/GLBAvatar.tsx (confirm it never calls remapMixamoAnimationToVrm — the fix is VRM-scoped)
  </read_first>
  <action>
    Run all gates in order, recording each PASS/FAIL before considering the Task 2 checkpoint. If ANY gate fails, STOP and report before presenting the human checkpoint.

    In a fresh worktree run `pnpm install --frozen-lockfile` first (11-12/11-14 hit `vitest: command not found` otherwise — lockfile-pinned, no new packages).

    INVARIANT gates (reuse verbatim from 11-14 — same grep patterns, same thresholds):
    1. Timer-free: `grep -rn --include='*.ts' -E "setInterval|setTimeout" packages/react/src/animation/` excluding comment lines → 0 matches.
    2. Additive-not-overwrite: breathing.ts and sway.ts each have `.quaternion.set(` count 0 and `.multiply(` count >=1.
    3. Internal-only: `grep -E "breathing|sway|expressionDrift|talkCycle|audioAmplitude" packages/react/src/index.ts` → 0 matches.
    4. IDLE-02 intact: `expressionDrift.ts` still exports `DEFAULT_AMPLITUDE = 0.35` with drift-ownership tracking present.
    5. PERF-01 intact: `MAX_COMBINED_SPINE_DELTA_RAD = 0.12` present and used in the spine clamp; breathing.step and sway.step still called in update().

    FIX-LANDED gates (prove 11-15 present, no prior bug reintroduced):
    6. 11-15 diagnosis present: AnimationStateEngine.ts contains a "11-15 gap closure" header block AND the 11-09/11-11/11-13 blocks still present (additive, not replaced).
    7. G5 fix present (SPECIFIC, not generic) — verify the site 11-15-SUMMARY.md records:
       - Default (retargeter anchor): `remapMixamoAnimationToVrm.ts`'s hips VectorKeyframeTrack branch anchors to the VRM bind-pose local hips Y instead of subtracting to 0 — grep the actual anchor (`getNormalizedBoneNode("hips")` used for the local bind-pose Y in the Y-offset computation, and the `(11-15 gap closure — FIX)` comment tag) rather than "does the file mention G5"; AND the anchor is NOT a flat additive offset gated on `isBaseActionMeaningfullyDriving` (that overshoot path must NOT have been used).
       - Fallback (only if 11-15-SUMMARY.md records the retargeter anchor could not work): the controller-local correction is weight-proportional (`offset * getEffectiveWeight()`, NOT a flat offset), format-scoped so it never runs for GLB, backed by a GLB-path regression test, and any bind-pose Y capture lives in a sibling `hipsYRef` (not a heterogeneous `RestPoseAnchor` field).
    8. G5 regression tests present: the "G5" describe block exists in AnimationStateEngine.test.ts asserting hips.position.y holds at bind pose on the real crossfade path, AND `remapMixamoAnimationToVrm.test.ts` asserts the remapped first-keyframe Y equals the bind-pose Y (not 0) with the PropertyMixer flatness simulation.
    9. GLB path untouched: `grep -c "remapMixamoAnimationToVrm" packages/react/src/GLBAvatar.tsx` → 0 (the VRM-only fix cannot reach the GLB height path). AND no prior regression reintroduced: `MIN_BASE_ACTION_WEIGHT = 0.05` unchanged; the 11-09 first-mount spin repro, 11-11 G2 non-snap, and 11-13 G4 non-snap describe blocks all still present.
    10. Full suite: `pnpm --filter @khaveeai/react test` passes (>=93 tests + 11-15's new G5 + remap unit tests), and `npx tsc --noEmit` in packages/react is clean.
  </action>
  <verify>
    <automated>cd packages/react && pnpm install --frozen-lockfile && pnpm test 2>&1 | tail -8 && npx tsc --noEmit</automated>
    <automated>grep -c "11-15 gap closure" packages/react/src/animation/AnimationStateEngine.ts && grep -c "G5" packages/react/src/animation/AnimationStateEngine.test.ts && grep -c "remapMixamoAnimationToVrm" packages/react/src/GLBAvatar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All 5 invariant gates PASS with the exact grep patterns/thresholds reused verbatim from 11-14.
    - Gate 6: both `grep "11-15 gap closure"` and `grep "11-13 gap closure"` (and 11-09/11-11) return matches — the new block is additive.
    - Gate 7 verifies the SPECIFIC G5 mechanism recorded in 11-15-SUMMARY.md: the retargeter bind-pose anchor in remapMixamoAnimationToVrm.ts (default) — NOT a flat `isBaseActionMeaningfullyDriving`-gated offset — or, if the fallback was recorded, the weight-proportional + format-scoped + GLB-tested controller-local correction.
    - Gate 8: the "G5" describe block is present in AnimationStateEngine.test.ts AND remapMixamoAnimationToVrm.test.ts exists with the first-keyframe-Y-at-bind-pose + flatness assertions.
    - Gate 9: `grep -c "remapMixamoAnimationToVrm" GLBAvatar.tsx` returns 0; `MIN_BASE_ACTION_WEIGHT = 0.05` unchanged; the 11-09 / 11-11 G2 / 11-13 G4 describe blocks all still present.
    - Gate 10: `pnpm --filter @khaveeai/react test` reports all tests passing (>=93 + new G5/remap tests); `npx tsc --noEmit` produces no output.
    - Every gate result is recorded (PASS/FAIL) in 11-16-SUMMARY.md before Task 2 is presented; if any gate FAILED, the checkpoint was NOT presented.
  </acceptance_criteria>
  <done>All 10 gates recorded PASS with the full react suite green and tsc clean, clearing the plan to present the Task 2 decisive human checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Decisive human re-verification — G5 fixed + full G1-G4 and 7-requirement regression sweep</name>
  <read_first>
    - .planning/phases/11-idle-transition-talking-states/11-14-SUMMARY.md (the exact G5 finding the human reported: "T-pose -> drop -> idle" on first page load before Connect)
  </read_first>
  <what-built>
    11-15 root-caused G5 (the page-load Y-axis hips-height drop) via headless runtime evidence and fixed it at the retarget source: `remapMixamoAnimationToVrm` now anchors each Mixamo idle clip's hips-Y track at the VRM bind-pose local hips Y instead of normalizing it to 0, so the idle base clip holds bind-pose height instead of sinking. Because both crossfade endpoints now sit at the same height, the mixer's blend is flat at every weight (no drop, no overshoot). The fix is VRM-scoped by construction — GLBAvatar.tsx never calls the retargeter — so the GLB height path is unaffected. Task 1 confirmed all 10 code-level gates PASS and the full react suite is green.

    Before presenting this checkpoint, prepare and verify the dev environment automatically (do NOT ask the human to run CLI): copy `.env` from the repo root into this worktree, start the dev server fresh from this worktree's source, and confirm both target pages return HTTP 200 — `http://localhost:3000/openai-avatar-test` and `http://localhost:3000/glb-avatar-test` — before presenting anything.
  </what-built>
  <how-to-verify>
    1. Open `http://localhost:3000/openai-avatar-test` and do a HARD reload (avoid cache). Watch the VERY FIRST render, BEFORE pressing Connect. Expected: the avatar settles from its initial pose into the live idle WITHOUT a visible drop on the Y (vertical) axis — no "T-pose -> drop -> idle" sink. This is the G5 fix (VRM path).
    2. Repeat step 1 on `http://localhost:3000/glb-avatar-test` (hard reload, watch the initial settle). Expected: the GLB avatar's initial settle is UNCHANGED from before (11-15's fix does not touch the GLB path) — confirm no NEW drop or height pop was introduced.
    3. Press Connect. Confirm G3 remains fixed: no Y-axis drop at connect-time.
    4. Confirm G1: live idle motion is playing pre-connect (breathing/sway), no stuck T-pose.
    5. Trigger a talking state. Confirm G2 (idle->talking crossfade is smooth, no snap) and G4 (no jiggle during talk-clip cycling).
    6. Full regression sweep of the 7 original requirements against the wayfinder checklist: IDLE-01 (breathing + sway), IDLE-02 (VRM expression drift; GLB none), TRANS-01 (starting greeting min-duration floor), TRANS-02 (stopped settle), TALK-01 (loop-boundary variant cycling, ~2s min dwell), TALK-02 (volume changes procedural amplitude, not clip selection), PERF-01 (bounded combined spine motion, no over-bend).

    Record each verdict verbatim, per item — do NOT infer or upgrade any item to a pass. If the human surfaces a NEW finding (as happened in every prior round), record it as an OPEN issue, not a pass-by-omission.
  </how-to-verify>
  <acceptance_criteria>
    - The dev server was started and both pages confirmed HTTP 200 BEFORE the checkpoint was presented (automation-first — the human was never asked to run a CLI command).
    - The human's verdict is recorded verbatim in 11-16-SUMMARY.md, per item: G5 on the VRM page (page-load settle), G5-no-regression on the GLB page, G1, G2, G3, G4, and each of the 7 requirements.
    - No item is marked pass by inference; any new finding is recorded as an open, unresolved issue.
    - If G5 is APPROVED (VRM fixed, GLB not regressed) and no new finding is surfaced: 11-16-SUMMARY.md states Phase 11 gap closure is COMPLETE and requirements-completed lists IDLE-01, IDLE-02, TRANS-01, TRANS-02, TALK-01, TALK-02, PERF-01.
    - If G5 is NOT approved or a new finding surfaces: 11-16-SUMMARY.md records it as open and states a sixth round is needed (Phase 11 NOT complete).
  </acceptance_criteria>
  <resume-signal>Type "approved" (G5 fixed on VRM, GLB not regressed, G1-G4 + 7 requirements confirmed) or describe the remaining issue(s).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification-only plan; no source behavior change. Dev-server startup uses the existing `.env` (no new secrets introduced). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-16-01 | Information Disclosure | `.env` copied into worktree for dev server | accept | Standard local dev pattern (11-14 precedent); `.env` is gitignored and never committed. |
| T-11-16-02 | Tampering | GLB height path regression risk | mitigate | Gate 9 proves GLBAvatar.tsx never calls the retargeter (grep == 0), and step 2 of the live sweep confirms the GLB settle is visually unchanged — the VRM-scoped fix cannot regress GLB. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | `pnpm install --frozen-lockfile` only — lockfile-pinned, no new packages. Legitimacy gate N/A. |
</threat_model>

<verification>
- All 10 gates recorded PASS before the checkpoint (fail-fast if any gate fails).
- Gate 9 proves the GLB path is untouched (grep == 0) — the VRM-scoped retargeter fix cannot regress GLB.
- Live human confirmation that the page-load T-pose-to-idle settle no longer drops on Y (G5, VRM), that the GLB settle is unchanged, and that G1-G4 + all 7 requirements still pass.
- Any new finding recorded as an open issue, never smoothed into a pass.
</verification>

<success_criteria>
- 10/10 code-level gates PASS; full react suite green; tsc clean; GLB path grep == 0.
- Human APPROVES G5 (VRM fixed, GLB not regressed) plus the full G1-G4 + 7-requirement regression sweep with no new finding → Phase 11 gap closure COMPLETE.
- 11-16-SUMMARY.md records per-gate results and the verbatim per-item human verdict.
</success_criteria>

<output>
Create `.planning/phases/11-idle-transition-talking-states/11-16-SUMMARY.md` when done.
</output>
</content>
