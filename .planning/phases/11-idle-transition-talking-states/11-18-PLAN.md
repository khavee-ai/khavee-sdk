---
phase: 11-idle-transition-talking-states
plan: 18
type: execute
wave: 2
depends_on: [11-17]
files_modified: []
autonomous: false
requirements: [IDLE-01, IDLE-02, TRANS-01, TRANS-02, TALK-01, TALK-02, PERF-01]

must_haves:
  truths:
    - "All code-level invariant gates from prior rounds still PASS (timer-free, additive-not-overwrite, internal-only, IDLE-02 intact, PERF-01 clamp intact, no prior regression describe-block removed)."
    - "The two 11-17 fixes are landed and gated at the code level: sway writes no hips bone; GLB opts into the manual-clip procedural gate; VRM does not."
    - "A human, on a running build, confirms the two new findings are resolved: VRM legs no longer sway/breathe, and GLB procedural sway is no longer too strong after changing the animation."
    - "The same human re-walks and confirms G1-G5 AND all 7 phase requirements (IDLE-01, IDLE-02, TRANS-01, TRANS-02, TALK-01, TALK-02, PERF-01) still hold on BOTH the VRM and GLB pages — none assumed to hold by default."
  artifacts:
    - path: ".planning/phases/11-idle-transition-talking-states/11-18-SUMMARY.md"
      provides: "Recorded per-item human verdict for both new findings + the full G1-G5 + 7-requirement re-sweep"
  key_links:
    - from: "human live verification"
      to: "Phase 11 completion decision"
      via: "per-item verdict recorded verbatim, no item upgraded to pass without explicit confirmation"
      pattern: "verdict"
---

<objective>
Decisive re-verification for Phase 11 gap-closure round 6. First re-confirm at the code level that 11-17's two fixes are landed and no prior regression was reintroduced, then present a single blocking human-verify checkpoint that covers BOTH the two new findings from 11-16 AND a full re-sweep of G1-G5 and all 7 phase requirements.

This is mandated by 11-16-SUMMARY.md's own recommendation: G5 was confirmed only on the VRM path last round, and G1-G4 plus the 7-requirement checklist were NOT re-walked item-by-item. They "should be re-swept alongside the two new fixes in the next round, not assumed to still hold by default." This plan does not modify source — it verifies 11-17's fixes and gates Phase 11 completion on an explicit, per-item human verdict.

Purpose: Establish whether Phase 11 can finally be marked complete (both new findings resolved AND the full checklist re-confirmed) or whether a seventh round is required.
Output: 11-18-SUMMARY.md recording every code-level gate result and the human's per-item verdict, following the strict "do not upgrade any unaddressed item to a pass" discipline of every prior checkpoint (11-08/11-10/11-12/11-14/11-16).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-idle-transition-talking-states/11-16-SUMMARY.md
@.planning/phases/11-idle-transition-talking-states/11-17-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run all code-level gates (invariants + 11-17 fix-landed) — no source changes</name>
  <files>(none — verification only)</files>
  <read_first>
    - packages/react/src/animation/sway.ts (confirm no executable hips write)
    - packages/react/src/animation/AnimationStateEngine.ts (confirm shouldDisableProceduralForManualClip present + applied; all prior 11-09/11-11/11-13/11-15 header blocks intact)
    - packages/react/src/GLBAvatar.tsx and packages/react/src/VRMAvatar.tsx (GLB opts in, VRM does not)
    - .planning/phases/11-idle-transition-talking-states/11-16-SUMMARY.md (the 10-gate table format to mirror and extend)
  </read_first>
  <action>
    Run and record each gate as PASS/FAIL in a table (mirroring 11-16's 10-gate table, extended with the two 11-17 fix gates). This task makes NO source changes.

    Invariant gates (must all still PASS):
    1. Timer-free: no `setInterval`/`setTimeout` under `packages/react/src/animation/` (grep, filtering comments).
    2. Additive-not-overwrite: `breathing.ts` and `sway.ts` use `.multiply(`, not `.quaternion.set(`.
    3. Internal-only: breathing/sway/expressionDrift/talkCycle/audioAmplitude not re-exported from `packages/react/src/index.ts`.
    4. IDLE-02 intact: `expressionDrift.ts` DEFAULT_AMPLITUDE and drift-ownership tracking unchanged.
    5. PERF-01 intact: `MAX_COMBINED_SPINE_DELTA_RAD = 0.12` present and used in the spine clamp; breathing/sway `.step()` still called in `update()`.
    6. No prior regression removed: the 11-09 first-mount spin, 11-11 G1/G2, 11-13 G4, and 11-15 G5 describe blocks are all still present in the test files; `MIN_BASE_ACTION_WEIGHT = 0.05` unchanged.

    11-17 fix-landed gates (new this round):
    7. OPEN ISSUE 1: `grep 'getHumanoidBoneNode("hips")' packages/react/src/animation/sway.ts` returns 0 executable matches; `getHumanoidBoneNode("spine")` and `getHumanoidBoneNode("chest")` both present; sway.test.ts asserts a hips stub is untouched.
    8. OPEN ISSUE 2: `shouldDisableProceduralForManualClip` exported and applied in `update()`; `dampProceduralOnManualClip: true` in GLBAvatar.tsx; 0 matches in VRMAvatar.tsx; new manual-clip-gate describe block present in AnimationStateEngine.test.ts.
    9. Full suite + tsc: `pnpm --filter @khaveeai/react test` all suites green (>= the prior 98 tests plus 11-17's additions); `npx tsc --noEmit` clean in packages/react.

    If any gate FAILS, stop and record it — do not proceed to the human checkpoint with a failing gate; return so 11-17 can be corrected first.
  </action>
  <verify>
    <automated>cd packages/react && grep -rn "setInterval\|setTimeout" src/animation/ | grep -vc '^\s*\*\|//' ; grep -n 'getHumanoidBoneNode("hips")' src/animation/sway.ts | grep -vc '^\s*\*' ; grep -c "shouldDisableProceduralForManualClip" src/animation/AnimationStateEngine.ts ; grep -c "dampProceduralOnManualClip: true" src/GLBAvatar.tsx ; pnpm --filter @khaveeai/react test 2>&1 | tail -8 ; npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - All 6 invariant gates recorded PASS.
    - Gate 7: 0 executable `getHumanoidBoneNode("hips")` matches in sway.ts; spine+chest present.
    - Gate 8: `shouldDisableProceduralForManualClip` present + applied; GLBAvatar opts in; VRMAvatar has 0 matches.
    - Gate 9: full @khaveeai/react suite green; tsc clean.
    - Any FAIL halts before the checkpoint and is reported.
  </acceptance_criteria>
  <done>All 9 gates recorded; all PASS (or the failing gate reported and the checkpoint not presented).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Decisive human re-sweep — both new findings + full G1-G5 + 7-requirement checklist</name>
  <what-built>
    11-17 removed the hips write from procedural sway (so leg bones no longer rotate) and gated procedural breathing/sway off on GLB when a manually-selected non-idle clip is the active base. All code-level gates from Task 1 pass. This checkpoint is the decisive live confirmation on a running build.
  </what-built>
  <how-to-verify>
    AUTOMATION-FIRST (executor does this before presenting): start the dev server, and confirm both `/vrm-avatar-test` and `/glb-avatar-test` return HTTP 200 before asking the human to look. Report the URLs.

    Then ask the human to verify, and record each answer verbatim. Present the checklist in these three groups. Do NOT infer or upgrade any item to a pass unless the human explicitly addresses it (per every prior round's discipline).

    GROUP A — the two new findings (must both be resolved to complete the phase):
    1. VRM (/vrm-avatar-test): do the legs stay still while the avatar breathes/sways? (OPEN ISSUE 1 — legs must NOT sway/breathe anymore.)
    2. GLB (/glb-avatar-test): after clicking a non-idle animation button (e.g. "State 3 Welcome (loop)", "State 5 listening (loop)"), is the procedural sway/breathing intensity no longer too strong? (OPEN ISSUE 2 — procedural motion should be off/negligible on the selected clip.)
    3. GLB: on the default idle (before clicking any button, or on "State 1 Idle (loop)"), does breathing/sway still look alive as before? (Must be unchanged — the fix must not kill the approved default idle motion.)

    GROUP B — G1-G5 re-sweep (both pages where applicable):
    - G1: idle motion is present pre-connect; no stuck T-pose on load.
    - G2: idle→talking transition crossfades smoothly, no snap.
    - G3: no Y-axis drop when Connect is first pressed.
    - G4: no jiggle during talk-clip cycling while speaking.
    - G5: no visible page-load Y-axis drop on settle — confirm on BOTH the VRM page AND the GLB page (GLB was not confirmed last round).

    GROUP C — the 7 phase requirements:
    - IDLE-01 (breathing + weight-shift sway, independent cycles), IDLE-02 (VRM expression drift; GLB none), TRANS-01 (starting greeting min-duration floor), TRANS-02 (stopped settle), TALK-01 (talk-variant cycling ~2s dwell), TALK-02 (volume scales procedural amplitude, not clip selection), PERF-01 (bounded combined spine motion, no over-bend).
  </how-to-verify>
  <resume-signal>
    Type per-item results, or a free-form verdict. Record verbatim. If ALL of Group A resolved AND Group B (G1-G5, incl. GLB G5) AND Group C (all 7 requirements) are explicitly confirmed → Phase 11 gap closure is COMPLETE; populate requirements-completed with all 7 IDs in the SUMMARY. If any Group A finding persists, OR any G1-G5/requirement item is reported broken OR a NEW finding surfaces → record it as an OPEN issue (do NOT fold into a pass), leave requirements-completed empty, and note a seventh round is required.
  </resume-signal>
</task>

</tasks>

<verification>
- Task 1's 9 gates all PASS before the checkpoint is presented.
- Both dev pages confirmed HTTP 200 before the human is asked.
- Human verdict recorded verbatim, per-item, with no unaddressed item upgraded to a pass.
</verification>

<success_criteria>
- Both new findings (VRM leg sway, GLB manual-clip sway intensity) explicitly confirmed resolved by the human.
- G1-G5 (including GLB-side G5) and all 7 phase requirements explicitly re-confirmed on a running build.
- 11-18-SUMMARY.md records the full per-item verdict and the correct completion decision.
- Only when ALL of the above hold is Phase 11 gap closure declared complete.
</success_criteria>

<output>
Create `.planning/phases/11-idle-transition-talking-states/11-18-SUMMARY.md` when done, recording: the 9 code-level gate results (table); the human's verbatim verdict; a per-item breakdown for Group A / B / C (marking any unaddressed item as NOT ADDRESSED, never as a pass); and the completion decision — either "Phase 11 gap closure COMPLETE" with all 7 requirement IDs in requirements-completed, or "seventh round required" with requirements-completed empty and each open item recorded.
</output>
