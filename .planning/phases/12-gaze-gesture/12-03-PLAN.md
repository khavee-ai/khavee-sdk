---
phase: 12-gaze-gesture
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/react/src/animation/talkCycle.ts
  - packages/react/src/animation/talkCycle.test.ts
  - packages/react/src/animation/gesture.ts
  - packages/react/src/animation/gesture.test.ts
autonomous: true
requirements: [GEST-02]
user_setup: []

must_haves:
  truths:
    - "detectLoopBoundary is a pure exported function reused by BOTH stepTalkCycle and gesture queuing"
    - "stepTalkCycle behavior is unchanged after the extraction (all existing tests still pass)"
    - "A gesture applies immediately when chatStatus is not 'speaking' (D-06)"
    - "A gesture during 'speaking' waits for the next loop boundary before starting — never interrupts mid-clip (GEST-02)"
    - "A gesture is a bounded one-shot bone-delta pulse (head-pitch for nod, head-yaw for shake) with no new animation clip"
  artifacts:
    - path: "packages/react/src/animation/talkCycle.ts"
      provides: "Extracted detectLoopBoundary pure function + unchanged stepTalkCycle"
      exports: ["detectLoopBoundary"]
      contains: "export function detectLoopBoundary"
    - path: "packages/react/src/animation/gesture.ts"
      provides: "useGesture() hook + stepGesture triggered/consumed one-shot bone pulse"
      exports: ["useGesture", "stepGesture", "createGestureState"]
      min_lines: 60
    - path: "packages/react/src/animation/gesture.test.ts"
      provides: "Immediate-vs-queued + pulse-envelope + additive-write + consume-once assertions"
  key_links:
    - from: "packages/react/src/animation/gesture.ts"
      to: "packages/react/src/animation/talkCycle.ts"
      via: "import { detectLoopBoundary }"
      pattern: "detectLoopBoundary"
    - from: "packages/react/src/animation/talkCycle.ts"
      to: "detectLoopBoundary"
      via: "stepTalkCycle calls the extracted function (no duplicate wrap-check)"
      pattern: "detectLoopBoundary\\("
---

<objective>
Extract `talkCycle.ts`'s inline loop-boundary wrap-detection (lines ~96-109) into a small reusable pure function `detectLoopBoundary`, then build `gesture.ts` — the triggered, consumed-and-cleared procedural bone-delta pulse (GEST-02) — reusing that extracted primitive so gesture queuing and talk-cycle variant-switching can never silently disagree on where a loop boundary is.

Purpose: GEST-02 requires a triggered gesture to play as a procedural bone delta (no new clip), applying immediately outside `speaking` (D-06) but queued to the ambient talk-cycle's next natural loop boundary during `speaking` — never interrupting a clip mid-play. RESEARCH mandates extracting the existing wrap-check rather than reimplementing it (two independent detectors would drift on edge cases like a non-looping clip clamped at its end).

Output: `talkCycle.ts` (modified — extract `detectLoopBoundary`, `stepTalkCycle` behavior unchanged), `gesture.ts` (new, not exported from index.ts), and tests for both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-gaze-gesture/12-CONTEXT.md
@.planning/phases/12-gaze-gesture/12-RESEARCH.md
@.planning/phases/12-gaze-gesture/12-PATTERNS.md

<interfaces>
<!-- Executor MUST use these directly. -->

The exact inline block in talkCycle.ts (lines 96-109) to extract into detectLoopBoundary:
```typescript
let loopBoundary = false;
if (currentTime !== null && duration !== null && duration > 0) {
  if (state.prevActionTime !== null) {
    loopBoundary =
      currentTime < state.prevActionTime ||
      (currentTime >= duration && state.prevActionTime < duration);
  }
  state.prevActionTime = currentTime;
}
```
Extract the BOOLEAN computation (not the state mutation) into:
`export function detectLoopBoundary(currentTime: number | null, prevTime: number | null, duration: number | null): boolean`
Each caller keeps owning its own prevActionTime tracking (talk-cycle and gesture are independent consumers).

talkCycle.ts's existing pure-helper convention to follow (nextVariantIndex, lines 62-66) and its
useRef hook wrapper (useTalkCycle, lines 126-134).

breathing.ts / blink.ts shape for gesture (useRef hook + module-scoped scratch + additive multiply).
blink.ts envelope shape (a sine pulse over an elapsed accumulator): blinkState = Math.sin(anim * PI),
anim advances by a per-step increment — the closest precedent for a triggered, bounded, non-oscillating pulse.

ChatStatus (from @khaveeai/core): 'ready'|'speaking'|'listening'|'thinking'|'stopped'|'starting'.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract detectLoopBoundary from talkCycle.ts (behavior-preserving)</name>
  <files>packages/react/src/animation/talkCycle.ts, packages/react/src/animation/talkCycle.test.ts</files>
  <read_first>
    - packages/react/src/animation/talkCycle.ts (whole file — the inline wrap-check at lines 96-109 to extract, and nextVariantIndex at lines 62-66 as the pure-helper convention to match)
    - packages/react/src/animation/talkCycle.test.ts (existing suite that MUST keep passing to prove stepTalkCycle behavior is unchanged)
  </read_first>
  <behavior>
    - detectLoopBoundary(currentTime, prevTime, duration) returns false when currentTime/duration is null or duration <= 0
    - returns false when prevTime is null (first frame — no prior sample to compare)
    - returns true when currentTime < prevTime (wrap)
    - returns true when currentTime >= duration && prevTime < duration (crossing without wrap, e.g. non-looping clip clamped at end)
    - returns false otherwise (mid-clip, no boundary)
    - stepTalkCycle produces byte-identical decisions after refactor: all existing talkCycle.test.ts cases still pass
  </behavior>
  <action>
    In `packages/react/src/animation/talkCycle.ts`, add an exported pure function `detectLoopBoundary(currentTime: number | null, prevTime: number | null, duration: number | null): boolean` capturing ONLY the boolean logic from the current inline block (lines 96-109) — the `currentTime < prevTime || (currentTime >= duration && prevTime < duration)` decision, guarded by the null/duration checks. Do NOT move the `state.prevActionTime = currentTime` mutation into the pure function; that stays in the caller.

    Refactor `stepTalkCycle` to call `detectLoopBoundary(currentTime, state.prevActionTime, duration)` for its boundary decision, then update `state.prevActionTime = currentTime` exactly as before — preserving current behavior. Follow the existing `nextVariantIndex` export/doc-comment convention.

    Add talkCycle.test.ts cases covering detectLoopBoundary's five behaviors above. Do not modify existing stepTalkCycle test cases — they are the regression guard that the extraction changed nothing.
  </action>
  <verify>
    <automated>cd packages/react && pnpm test -- talkCycle 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function detectLoopBoundary" packages/react/src/animation/talkCycle.ts` matches
    - `grep -c "currentTime < state.prevActionTime" packages/react/src/animation/talkCycle.ts` returns 0 (inline wrap-check removed from stepTalkCycle; logic now lives only in the extracted function)
    - `grep -c "detectLoopBoundary(" packages/react/src/animation/talkCycle.ts` returns >= 2 (defined once + called by stepTalkCycle)
    - `cd packages/react && pnpm test -- talkCycle` exits 0 with ALL pre-existing stepTalkCycle cases passing plus new detectLoopBoundary cases
    - `cd packages/react && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>detectLoopBoundary is exported and unit-tested; stepTalkCycle uses it with unchanged behavior (existing tests green).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement gesture.ts (triggered one-shot pulse, immediate-vs-queued per D-06)</name>
  <files>packages/react/src/animation/gesture.ts, packages/react/src/animation/gesture.test.ts</files>
  <read_first>
    - packages/react/src/animation/breathing.ts (hook/state/step split + module-scoped scratch + additive multiply template)
    - packages/react/src/animation/blink.ts (triggered sine-pulse envelope over an elapsed accumulator — the closest precedent for a bounded one-shot, non-oscillating pulse)
    - packages/react/src/animation/talkCycle.ts (detectLoopBoundary from Task 1 + the TalkCycleStepParams params-object step() convention to mirror for GestureStepParams)
    - packages/react/src/animation/types.ts (getHumanoidBoneNode('head'/'neck') seam)
  </read_first>
  <behavior>
    - createGestureState() returns { activeGesture: null, elapsed: 0, prevActionTime: null }
    - When gestureHint is 'nod'/'shake' and chatStatus !== 'speaking': the pulse begins THIS step (D-06 immediate), onConsume() is called once, activeGesture is latched
    - When gestureHint is 'nod'/'shake' and chatStatus === 'speaking': the pulse does NOT begin until detectLoopBoundary(...) returns true for the current action; onConsume() fires only when it begins (GEST-02 — no mid-clip interrupt)
    - Once latched, the pulse animates over a bounded duration (envelope eases up then down, like blink's sin(anim*PI)) and clears activeGesture when elapsed exceeds the duration — independent of gestureHint afterward
    - nod applies a head-PITCH delta; shake applies a head-YAW delta; both additive via bone.quaternion.multiply(scratch), magnitude bounded (extrapolate from breathing/sway amplitudes + blink's ~150ms envelope per A1)
    - gestureHint of 'none' or null or an unrecognized value never starts a pulse (no-op)
    - defensive early-return if the head bone cannot be resolved
  </behavior>
  <action>
    Create `packages/react/src/animation/gesture.ts` following breathing.ts's header/hook/step shape (NOT exported from index.ts). Model gesture state as `{ activeGesture: "nod" | "shake" | null, elapsed: number, prevActionTime: number | null }` in a useRef inside `useGesture()`. Export `createGestureState()`, `stepGesture(state, params)` and `useGesture()` returning `{ step(params) }`, where params follow talkCycle's params-object convention: `{ adapter, chatStatus, gestureHint, currentAction, delta, onConsume }` (gestureHint: `"nod" | "shake" | "none" | null`; currentAction: `THREE.AnimationAction | null` for loop-boundary detection while speaking; onConsume: `() => void` the caller wires to clear the context hint).

    Trigger logic (D-06 / GEST-02): if there is no `activeGesture` yet and `gestureHint` is `"nod"` or `"shake"`, decide whether to start the pulse this step: start immediately when `chatStatus !== "speaking"`; when `chatStatus === "speaking"`, only start once `detectLoopBoundary(currentAction?.time ?? null, state.prevActionTime, currentAction?.getClip().duration ?? null)` returns true (track `state.prevActionTime` each step exactly as talk-cycle does). On start, latch `state.activeGesture`, reset `state.elapsed = 0`, and call `onConsume()` once.

    Playback: while `activeGesture` is set, advance `state.elapsed` by `delta`, compute an eased one-shot envelope (rise-then-fall, e.g. `Math.sin((elapsed/duration) * Math.PI)` clamped to [0,1] window like blink), map to an axis-angle on the head bone — PITCH axis for `nod`, YAW axis for `shake` — into a module-scoped scratch quaternion, and apply additively via `head.quaternion.multiply(_scratch)`. Never `.set()`/overwrite. When `elapsed >= duration`, clear `activeGesture` and reset `elapsed`. Resolve the head bone via `adapter.getHumanoidBoneNode("head")` (optionally compose a smaller neck contribution) with a defensive early-return if unresolved. Treat `"none"`/null/unrecognized `gestureHint` as never starting a pulse.

    Write `gesture.test.ts` (vitest, node env, stub adapter with a fake head bone + a stub AnimationAction) covering every behavior above, including: immediate start when not speaking; deferred start until a simulated loop boundary when speaking; onConsume called exactly once per triggered gesture; additive (non-overwriting) write; and pulse self-clears after its duration.
  </action>
  <verify>
    <automated>cd packages/react && pnpm test -- gesture 2>&1 | tail -25 && grep -c "\.set(" src/animation/gesture.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages/react && pnpm test -- gesture` exits 0 with all gesture.test.ts cases passing
    - `grep -c "detectLoopBoundary" packages/react/src/animation/gesture.ts` returns >= 1 (reuses extracted primitive, no reimplemented wrap-check)
    - `grep -c "currentTime < state.prevActionTime\|prevActionTime <" packages/react/src/animation/gesture.ts` returns 0 (no duplicated inline wrap math)
    - `grep -c "\.multiply(" packages/react/src/animation/gesture.ts` returns >= 1 (additive write)
    - `grep -c "new THREE.Quaternion()" packages/react/src/animation/gesture.ts` scratch declared at module scope only (none inside a function body)
    - `cd packages/react && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>gesture.ts plays a bounded, additive one-shot head pulse — immediately outside speaking, queued to the next loop boundary during speaking — consuming the hint exactly once via onConsume; tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| context hint → gesture step | `gestureHint` originates from an LLM tool call (untrusted); an out-of-enum value must not start an unexpected pulse or crash the render loop. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Tampering | `stepGesture` gestureHint branch | mitigate | Only `"nod"`/`"shake"` start a pulse; `"none"`/null/unrecognized are treated as no-op (never thrown). Enum narrowing also enforced upstream at `setGestureHint` (plan 12-04). |
| T-12-05 | Tampering | gesture bone write | mitigate | Bounded one-shot magnitude, additive `multiply()` only — never interrupts a playing clip (queued to loop boundary during speaking, GEST-02). |

No package installs this phase — no supply-chain checkpoint applies.
</threat_model>

<verification>
- `cd packages/react && pnpm test -- talkCycle gesture` exits 0
- Existing stepTalkCycle behavior preserved (regression guard)
- gesture.ts reuses detectLoopBoundary (no duplicated wrap math)
</verification>

<success_criteria>
- detectLoopBoundary extracted, exported, tested; stepTalkCycle unchanged in behavior
- gesture.ts plays a bounded additive one-shot pulse, immediate outside speaking, queued to loop boundary during speaking, consumed once
- Both files tsc-clean and NOT exported from any package barrel
</success_criteria>

<output>
Create `.planning/phases/12-gaze-gesture/12-03-SUMMARY.md` when done
</output>
