---
phase: 12-gaze-gesture
plan: 05
type: execute
wave: 3
depends_on: ["12-01", "12-04"]
files_modified:
  - packages/react/src/VRMAvatar.tsx
  - packages/react/src/GLBAvatar.tsx
  - src/app/openai-avatar-test/page.tsx
autonomous: true
requirements: [GAZE-01, GAZE-02, GEST-01, GEST-02]
user_setup: []

must_haves:
  truths:
    - "VRMAvatar and GLBAvatar both read the R3F active scene camera via useThree() (not VRMAvatar's separate cameras prop) and pass it into the controller (D-04)"
    - "Both avatars read gestureHint + setGestureHint from useKhavee and pass gestureHint + an onGestureConsumed clear callback into the controller"
    - "The demo page registers toolGesture with an app-supplied execute that calls setGestureHint, via registerFunction inside a useEffect"
    - "The demo wiring lives inside the React tree (useEffect after provider is available), not at module scope"
  artifacts:
    - path: "packages/react/src/VRMAvatar.tsx"
      provides: "useThree camera + gestureHint/onGestureConsumed threaded into useAnimationController"
      contains: "useThree"
    - path: "packages/react/src/GLBAvatar.tsx"
      provides: "useThree camera + gestureHint/onGestureConsumed threaded into useAnimationController"
      contains: "useThree"
    - path: "src/app/openai-avatar-test/page.tsx"
      provides: "End-to-end set_gesture registration exercising GEST-01/02 for human verify"
      contains: "toolGesture"
  key_links:
    - from: "packages/react/src/VRMAvatar.tsx"
      to: "useAnimationController"
      via: "camera + gestureHint + onGestureConsumed params"
      pattern: "useThree|gestureHint"
    - from: "src/app/openai-avatar-test/page.tsx"
      to: "setGestureHint"
      via: "registerFunction({ ...toolGesture, execute }) in useEffect"
      pattern: "registerFunction|setGestureHint"
---

<objective>
Wire the R3F active scene camera and the gesture-hint signal through `VRMAvatar.tsx` and `GLBAvatar.tsx` into the shared controller, and wire an end-to-end `set_gesture` registration into the existing `openai-avatar-test` demo page so the LLM can trigger gestures live — the surface the human-verify checkpoint (plan 12-06) exercises.

Purpose: The controller (plan 12-04) now accepts `camera`, `gestureHint`, and `onGestureConsumed`, but nothing supplies them yet. Per D-04, each avatar reads `useThree().camera`; per RESEARCH Pitfall 3, the app-supplied tool `execute` must call the public `setGestureHint` from inside the React tree, most naturally via `provider.registerFunction({ ...toolGesture, execute })` in a `useEffect` after the provider is available in context.

Output: both avatars threading camera + gesture wiring, and the demo page registering `set_gesture` end-to-end (GEST-01/02) — gaze needs no per-app wiring beyond the camera (it is automatic).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-gaze-gesture/12-CONTEXT.md
@.planning/phases/12-gaze-gesture/12-RESEARCH.md
@.planning/phases/12-gaze-gesture/12-PATTERNS.md
@.planning/phases/12-gaze-gesture/12-04-SUMMARY.md

<interfaces>
<!-- Executor MUST use these directly. -->

VRMAvatar.tsx current controller call (lines 468-477) and useFrame (494-512):
```typescript
import { useFrame } from "@react-three/fiber";   // add useThree here
const { setVrm, expressions, currentAnimation, animate, chatStatus, currentVolume } = useKhavee();
const controller = useAnimationController({ adapter: vrmAdapter, chatStatus, currentAnimation,
  availableNames, getAction, getRoot, enableBlinking, currentVolume });
useFrame((_, delta) => { ...; controller.update(delta); ... });
```

GLBAvatar.tsx current controller call (lines 159-180):
```typescript
import { useFrame } from "@react-three/fiber";   // add useThree here
const { currentAnimation, chatStatus, setAvailableAnimations, currentVolume } = useKhavee();
const controller = useAnimationController({ adapter: glbAdapter, chatStatus, currentAnimation,
  availableNames: names, getAction, getRoot, enableBlinking: true, currentVolume,
  dampProceduralOnManualClip: true });
useFrame((_, delta) => { controller.update(delta); });
```

New controller params (from plan 12-04): camera?: THREE.Camera | null; gestureHint?: "nod"|"shake"|"none"|null; onGestureConsumed?: () => void;
useKhavee() now also returns: gestureHint: "nod"|"shake"|null; setGestureHint: (g: string|null) => void;
D-04: use `const camera = useThree((state) => state.camera);` (once per render, not per-frame).

openai-avatar-test/page.tsx: module-scope `const openaiProvider = new OpenAIRealtimeProvider({...})`;
uses `useRealtime()` in the component. registerFunction exists on both providers:
`provider.registerFunction(tool: RealtimeTool): void`.
toolGesture importable from '@khaveeai/core' (plan 12-01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread camera + gesture wiring through VRMAvatar and GLBAvatar</name>
  <files>packages/react/src/VRMAvatar.tsx, packages/react/src/GLBAvatar.tsx</files>
  <read_first>
    - packages/react/src/VRMAvatar.tsx (import line 3; useKhavee destructure line 307; controller call 468-477; useFrame 494-512)
    - packages/react/src/GLBAvatar.tsx (import line 3; useKhavee destructure line 97; controller call 159-173; useFrame 178-180)
    - packages/react/src/KhaveeProvider.tsx (confirm useKhavee returns gestureHint + setGestureHint from plan 12-04)
    - .planning/phases/12-gaze-gesture/12-RESEARCH.md (D-04 — use useThree().camera; Don't-Hand-Roll note that state.camera === useThree().camera)
  </read_first>
  <action>
    In BOTH `VRMAvatar.tsx` and `GLBAvatar.tsx`: extend the `@react-three/fiber` import to include `useThree`, and add `const camera = useThree((state) => state.camera);` at component scope (called once per render, per D-04 — not inside useFrame). Extend the `useKhavee()` destructure to also pull `gestureHint` and `setGestureHint`.

    Pass three new params into the existing `useAnimationController({...})` call: `camera`, `gestureHint`, and `onGestureConsumed: () => setGestureHint(null)`. Do NOT remove or reorder any existing params (`currentVolume`, and GLB's `dampProceduralOnManualClip: true`, must stay). No changes to the `useFrame` bodies are needed — the controller already reads the params it closed over.

    Keep the two avatars symmetric so gaze/gesture behave identically on VRM and GLB (GAZE-02) — the only intentional difference remains GLB's pre-existing `dampProceduralOnManualClip: true`.
  </action>
  <verify>
    <automated>cd packages/react && npx tsc --noEmit 2>&1 | tail -10 && grep -c "useThree\|onGestureConsumed" src/VRMAvatar.tsx src/GLBAvatar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useThree" packages/react/src/VRMAvatar.tsx` returns >= 1 and same for GLBAvatar.tsx
    - `grep -c "camera" packages/react/src/VRMAvatar.tsx` and GLBAvatar.tsx each show camera passed into the controller call
    - `grep -c "onGestureConsumed: () => setGestureHint(null)" packages/react/src/VRMAvatar.tsx` returns 1 and same for GLBAvatar.tsx
    - `grep -c "gestureHint" packages/react/src/VRMAvatar.tsx` and GLBAvatar.tsx each return >= 2 (destructure + controller param)
    - GLB's existing `dampProceduralOnManualClip: true` still present: `grep -c "dampProceduralOnManualClip: true" packages/react/src/GLBAvatar.tsx` returns 1
    - `cd packages/react && npx tsc --noEmit` exits 0
    - `cd packages/react && pnpm test` exits 0 (no regressions)
  </acceptance_criteria>
  <done>Both avatars supply the R3F camera + gestureHint + an onGestureConsumed clear callback to the controller, symmetrically; build and tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Register set_gesture end-to-end in the openai-avatar-test demo page</name>
  <files>src/app/openai-avatar-test/page.tsx</files>
  <read_first>
    - src/app/openai-avatar-test/page.tsx (whole file — module-scope openaiProvider construction, the component using useRealtime, the Scene wiring VRMAvatar inside KhaveeProvider)
    - .planning/phases/12-gaze-gesture/12-RESEARCH.md (Open Question 3 — use registerFunction() inside a useEffect after the provider is available, rather than restructuring the module-scope provider construction)
    - packages/react/src/KhaveeProvider.tsx (setGestureHint is returned by useKhavee)
  </read_first>
  <action>
    Update `src/app/openai-avatar-test/page.tsx` to exercise `set_gesture` end-to-end for the plan 12-06 human-verify checkpoint. Import `toolGesture` from `@khaveeai/core`. Inside a component that is rendered within `KhaveeProvider` (so it can call `useKhavee()` to get `setGestureHint`), add a `useEffect` that calls `openaiProvider.registerFunction({ ...toolGesture, execute: async (args) => { setGestureHint(args?.gesture ?? null); return { success: true, message: \`gesture: \${args?.gesture}\` }; } })`. This is the RESEARCH-recommended bridge (Open Question 3): the app supplies `execute`, which writes into React context via the public setter — do NOT move provider construction out of module scope.

    Add a short on-page note instructing the human verifier that the assistant will nod/shake as it agrees/disagrees during conversation, and (optionally) add manual "Nod"/"Shake" buttons that call `setGestureHint('nod')`/`setGestureHint('shake')` so gesture playback can be verified deterministically without depending on the LLM emitting a tool call. Keep gaze verification note-only (gaze is automatic once the camera is threaded — no per-page wiring needed).
  </action>
  <verify>
    <automated>cd /Users/whitemalt/Documents/khavee-sdk && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "openai-avatar-test" | head; grep -c "toolGesture\|registerFunction\|setGestureHint" src/app/openai-avatar-test/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "toolGesture" src/app/openai-avatar-test/page.tsx` returns >= 1
    - `grep -c "registerFunction" src/app/openai-avatar-test/page.tsx` returns >= 1
    - `grep -c "setGestureHint" src/app/openai-avatar-test/page.tsx` returns >= 1
    - `grep -c "useEffect" src/app/openai-avatar-test/page.tsx` returns >= 1 (registration inside the React tree, not module scope)
    - `npx tsc --noEmit` reports no errors originating in `src/app/openai-avatar-test/page.tsx`
  </acceptance_criteria>
  <done>The demo page registers set_gesture via registerFunction inside a useEffect (app-supplied execute → setGestureHint), plus manual nod/shake buttons, giving plan 12-06 a live end-to-end surface for GEST-01/02 and gaze.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM tool call → app execute → context | The demo's `execute` forwards the LLM `gesture` arg into `setGestureHint`, which narrows it to the enum (plan 12-04). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Tampering | demo `execute` → setGestureHint | mitigate | Value narrowed by `setGestureHint`'s allow-list (plan 12-04); demo `execute` never trusts the raw arg for branching. |

No package installs this phase — no supply-chain checkpoint applies. The edited demo page is dev/test-only surface (not shipped SDK).
</threat_model>

<verification>
- `cd packages/react && pnpm test` and `npx tsc --noEmit` exit 0
- Root `npx tsc --noEmit` reports no new errors in the demo page
- Both avatars symmetric; demo registers set_gesture in-tree
</verification>

<success_criteria>
- VRMAvatar + GLBAvatar thread useThree() camera + gestureHint + onGestureConsumed into the controller symmetrically
- openai-avatar-test registers set_gesture end-to-end via registerFunction in a useEffect and offers manual nod/shake triggers
- Build + tests green
</success_criteria>

<output>
Create `.planning/phases/12-gaze-gesture/12-05-SUMMARY.md` when done
</output>
