---
phase: 12-gaze-gesture
plan: 06
type: execute
wave: 4
depends_on: ["12-05"]
files_modified:
  - .planning/phases/12-gaze-gesture/12-06-VERIFICATION.md
autonomous: false
requirements: [GAZE-01, GAZE-02, GEST-01, GEST-02]
user_setup: []

must_haves:
  truths:
    - "Objective code-level gates confirm: no live-bone lookAt, additive multiply writes, starting/stopped gaze no-op, detectLoopBoundary reused, toolGesture exported, gaze/gesture not in any package barrel"
    - "A human confirms per-state gaze reads correctly on BOTH a VRM and a GLB avatar (GAZE-02 symmetry)"
    - "A human confirms nod/shake gestures play as bone deltas and never interrupt the talk cycle mid-clip"
    - "All four phase requirements (GAZE-01, GAZE-02, GEST-01, GEST-02) are explicitly confirmed or gaps recorded"
  artifacts:
    - path: ".planning/phases/12-gaze-gesture/12-06-VERIFICATION.md"
      provides: "Objective gate results + human per-state verdicts + requirement sign-off"
  key_links:
    - from: ".planning/phases/12-gaze-gesture/12-06-VERIFICATION.md"
      to: "phase requirements"
      via: "explicit pass/fail per GAZE-01/02, GEST-01/02"
      pattern: "GAZE-01|GEST-02"
---

<objective>
Verify Phase 12 against its four requirements: run the objective code-level gates, then a blocking human-verify checkpoint of per-state gaze (on both VRM and GLB) and nod/shake gesture behavior on a live build.

Purpose: Gaze's numeric amplitude and per-state read, and gesture's magnitude/timing, are Claude's-Discretion numbers with no external benchmark (Assumption A1) — Phase 11's own amplitude work needed multiple human-verify rounds. This checkpoint is where GAZE-01's per-state read, GAZE-02's VRM/GLB symmetry, and GEST-02's never-interrupt-mid-clip behavior are confirmed by a human, and where any tuning gap is recorded for a gap-closure round.

Output: `12-06-VERIFICATION.md` capturing objective gate results, human verdicts, and explicit per-requirement sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-gaze-gesture/12-CONTEXT.md
@.planning/phases/12-gaze-gesture/12-RESEARCH.md
@.planning/phases/wayfinder-map-1-animation-architecture/VERIFICATION-CHECKLIST.md
@.planning/phases/12-gaze-gesture/12-02-SUMMARY.md
@.planning/phases/12-gaze-gesture/12-03-SUMMARY.md
@.planning/phases/12-gaze-gesture/12-04-SUMMARY.md
@.planning/phases/12-gaze-gesture/12-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run objective code-level gates and record results</name>
  <files>.planning/phases/12-gaze-gesture/12-06-VERIFICATION.md</files>
  <read_first>
    - packages/react/src/animation/gaze.ts (confirm no live-bone lookAt, additive multiply, starting/stopped no-op)
    - packages/react/src/animation/gesture.ts (confirm detectLoopBoundary reuse, additive multiply, no duplicated wrap math)
    - packages/react/src/animation/AnimationStateEngine.ts (confirm steps 1-9 unreordered, gaze=10 / gesture=11 appended)
    - packages/core/src/index.ts (confirm toolGesture exported)
    - .planning/phases/wayfinder-map-1-animation-architecture/VERIFICATION-CHECKLIST.md (the objective "no live-clock interrupts in speaking" + frame-budget items naming gaze/gesture)
  </read_first>
  <action>
    Create `.planning/phases/12-gaze-gesture/12-06-VERIFICATION.md`. Run and record PASS/FAIL for each objective gate (capture the exact command + output):
    - G-1 (no absolute overwrite): `grep -rc "\.lookAt(" packages/react/src/animation/gaze.ts packages/react/src/animation/gesture.ts` returns 0 for both (Pitfall 1).
    - G-2 (additive composition, PERF-01): both gaze.ts and gesture.ts contain `.multiply(` and neither uses `.set(` / direct-assignment overwrite on the live bone quaternion.
    - G-3 (starting/stopped no-op, Pitfall 5): gaze.ts has an explicit early-return branch on `starting`/`stopped` (not a proceduralScale/settleScale damp).
    - G-4 (no duplicated loop-boundary math): `detectLoopBoundary` is defined once in talkCycle.ts and imported by gesture.ts; no inline `currentTime < ...prevActionTime` reimplementation in gesture.ts.
    - G-5 (composition order preserved): steps 1-9 unchanged in AnimationStateEngine.ts; gaze appended as step 10, gesture as step 11.
    - G-6 (GEST-01 packaging): `toolGesture` is re-exported from `packages/core/src/index.ts`.
    - G-7 (internal-only): gaze.ts and gesture.ts are NOT exported from `packages/react/src/index.ts` (procedural systems stay internal, per Phase 10/11 precedent).
    - G-8 (frame-budget hygiene): no `new THREE.Quaternion()`/`new THREE.Vector3()` inside any per-frame `step()` body in gaze.ts/gesture.ts (module-scoped scratch only).
    - G-9 (suites green): `cd packages/core && pnpm test` and `cd packages/react && pnpm test` both exit 0; `npx tsc --noEmit` clean in both packages.

    Record each gate's command, output, and PASS/FAIL verdict in the file. If any gate FAILS, mark it clearly — a failed objective gate blocks the human checkpoint.
  </action>
  <verify>
    <automated>cd /Users/whitemalt/Documents/khavee-sdk && grep -rc "\.lookAt(" packages/react/src/animation/gaze.ts packages/react/src/animation/gesture.ts; grep -c "toolGesture" packages/core/src/index.ts; (cd packages/react && pnpm test 2>&1 | tail -5); (cd packages/core && pnpm test 2>&1 | tail -5)</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/12-gaze-gesture/12-06-VERIFICATION.md` exists with a PASS/FAIL row for every gate G-1..G-9 including command + output evidence
    - G-1 shows 0 lookAt occurrences in gaze.ts and gesture.ts
    - G-6 shows toolGesture exported from core barrel
    - G-9 shows both test suites exit 0 and both packages tsc-clean
    - Any FAIL is explicitly flagged as blocking the human checkpoint
  </acceptance_criteria>
  <done>All objective gates are executed and recorded with evidence in 12-06-VERIFICATION.md; failures (if any) are flagged before the human checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify per-state gaze (VRM + GLB) and nod/shake gestures</name>
  <what-built>
    A camera-relative soft-gaze system (continuous head/neck offset toward the R3F camera in ready/listening/speaking, a brief aversion in thinking, no gaze in starting/stopped) plus an LLM-tool-triggered nod/shake gesture that plays as a bounded bone-delta pulse — applying immediately outside speaking and queued to the next talk-clip loop boundary during speaking. Exercised on the openai-avatar-test (VRM) demo page, plus a GLB avatar page for GAZE-02 symmetry.
  </what-built>
  <how-to-verify>
    1. Run the dev app (`pnpm dev`) and open `/openai-avatar-test` (VRM, male.vrm).
    2. GAZE-01 per-state: before connecting (ready) confirm the head shows a soft, steady offset toward the camera (orbit the camera with the mouse — the head should keep gently attending toward it, a subtle offset, not a full head-turn, not a snap). Connect and speak: during listening and speaking the soft gaze should persist; during thinking the avatar should briefly look away (aversion); starting/stopped should show NO separate gaze treatment (the greeting/goodbye clips play unaffected).
    3. GEST-01/02: use the manual "Nod"/"Shake" buttons AND converse so the LLM emits set_gesture. Confirm: outside speaking a nod/shake plays immediately; while the avatar is speaking, a triggered gesture waits and begins only at a natural talk-clip loop boundary — it must NOT cut/interrupt the talk clip mid-play. The gesture is a head pitch (nod) / yaw (shake) pulse, not a new clip.
    4. GAZE-02 symmetry: open the GLB avatar page (`/glb-avatar-test` or `/glb`) and confirm gaze behaves the same bone-level way on the GLB avatar — soft attend toward camera in ready/listening/speaking, aversion in thinking.
    5. Regression: confirm Phase 11 idle behavior (breathing/sway/blink) still reads normally and gaze/gesture do not fight or overwrite it (no head jitter, no T-pose, no snap).
  </how-to-verify>
  <resume-signal>Type "approved" if all four requirements (GAZE-01, GAZE-02, GEST-01, GEST-02) read correctly on both formats, or describe each issue (state, format, symptom) so a gap-closure round can be planned. Record the verdict and per-requirement sign-off in 12-06-VERIFICATION.md.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM tool call → live avatar | Verification confirms the untrusted `gesture` enum drives only a bounded, non-interrupting bone delta. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Tampering | set_gesture enum | mitigate | Verified live: out-of-enum/none values produce no gesture (no-op), never a crash. |
| T-12-05 | Tampering | gesture vs talk cycle | mitigate | Verified live: gesture during speaking never interrupts a clip mid-play (GEST-02). |

No package installs this phase — no supply-chain checkpoint applies.
</threat_model>

<verification>
- All objective gates G-1..G-9 recorded with evidence
- Human confirms GAZE-01/02, GEST-01/02 on both VRM and GLB, or gaps recorded
</verification>

<success_criteria>
- 12-06-VERIFICATION.md contains objective gate evidence + human per-state verdicts + explicit per-requirement sign-off
- Phase 12 either fully confirmed (all four requirements) or gaps documented for a gap-closure round
</success_criteria>

<output>
Create `.planning/phases/12-gaze-gesture/12-06-SUMMARY.md` when done
</output>
