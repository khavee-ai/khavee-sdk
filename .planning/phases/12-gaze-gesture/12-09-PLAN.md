---
phase: 12-gaze-gesture
plan: 09
type: execute
wave: 7
depends_on: ["12-07", "12-08"]
files_modified:
  - .planning/phases/12-gaze-gesture/12-09-VERIFICATION.md
autonomous: false
gap_closure: true
requirements: [GAZE-01, GAZE-02]
user_setup: []

must_haves:
  truths:
    - "A human confirms camera-relative gaze eases smoothly (no snap) on the VRM avatar across ready/listening/speaking/thinking"
    - "A human confirms gaze is smooth AND the idle-animation spin is gone on the GLB avatar (GAZE-02 symmetry)"
    - "GAZE-01 and GAZE-02 are explicitly signed off as PASS, or any remaining gap is recorded for a further round"
  artifacts:
    - path: ".planning/phases/12-gaze-gesture/12-09-VERIFICATION.md"
      provides: "Human per-state re-verification of the two gap-closure fixes + explicit GAZE-01/GAZE-02 sign-off"
  key_links:
    - from: ".planning/phases/12-gaze-gesture/12-09-VERIFICATION.md"
      to: "GAZE-01, GAZE-02 requirements"
      via: "explicit pass/fail sign-off after Gap 1 + Gap 2 fixes"
      pattern: "GAZE-01|GAZE-02"
---

<objective>
Re-verify the two open Phase 12 requirements (GAZE-01, GAZE-02) after the Gap 1 (smoothing) and Gap 2 (GLB group-rotation-agnostic) fixes land. GEST-01 and GEST-02 are already confirmed PASS in 12-06 and are NOT re-tested here. This is a blocking human-verify checkpoint on a live build.

Purpose: Gap 1 and Gap 2 are perceptual (smooth-not-snap; no idle spin) — they cannot be fully confirmed by unit tests alone. A human must confirm both fixes read correctly on both avatar formats before Phase 12 can close.
Output: `12-09-VERIFICATION.md` with per-state human verdicts and explicit GAZE-01/GAZE-02 sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-gaze-gesture/12-06-VERIFICATION.md
@.planning/phases/12-gaze-gesture/12-07-SUMMARY.md
@.planning/phases/12-gaze-gesture/12-08-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human re-verify smooth gaze (VRM) + smooth gaze & no idle-spin (GLB)</name>
  <files>.planning/phases/12-gaze-gesture/12-09-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/12-gaze-gesture/12-06-VERIFICATION.md (the original how-to-verify checklist and the exact two gaps being re-checked)
    - .planning/phases/12-gaze-gesture/12-07-SUMMARY.md (what the smoothing fix changed)
    - .planning/phases/12-gaze-gesture/12-08-SUMMARY.md (the confirmed GLB spin root cause + what the fix changed)
  </read_first>
  <action>
    Automate everything up to the point of human judgment, then present the checklist and record the verdict:
    1. Confirm the fixes are in the working tree and green: `cd packages/react && pnpm test` exits 0 and `npx tsc --noEmit` is clean (both should already be true from 12-07/12-08 — re-run as a pre-checkpoint gate; a failure here blocks the human check).
    2. Start the dev app (`pnpm dev`) so both demo pages are reachable.
    3. Present the `<how-to-verify>` checklist below to the human and wait for the `<resume-signal>`.
    4. Create `.planning/phases/12-gaze-gesture/12-09-VERIFICATION.md` capturing the human's per-state verdicts (VRM ready/listening/speaking/thinking; GLB smooth + no idle spin), the regression note, and an explicit PASS-or-remaining-gap sign-off for GAZE-01 and GAZE-02. If both PASS, state that Phase 12 is fully confirmed (GEST-01/GEST-02 already PASS in 12-06). If any gap remains, record (state, format, symptom) so a further gap-closure round can be planned.
  </action>
  <what-built>
    Two gap-closure fixes to the camera-relative gaze system: (1) persistent frame-rate-independent smoothing in gaze.ts so the head eases toward its target instead of snapping, and mode transitions no longer route through the neutral base; (2) a group-rotation-agnostic gaze target so the GLB avatar (group rotation [0,0,0]) gets a small correctly-directed attentive offset instead of a persistent maximum off-axis twist that read as an idle spin. GEST-01/GEST-02 are unchanged and already confirmed.
  </what-built>
  <how-to-verify>
    1. Run the dev app (`pnpm dev`).
    2. GAZE-01 (VRM) — open `/openai-avatar-test` (male.vrm). Before connecting (ready), orbit the camera with the mouse: the head should EASE toward the camera as a soft, steady, subtle offset — it must NOT snap/jump to the target. Connect and converse and confirm across all four live states: `ready`, `listening`, `speaking` show smooth soft gaze toward camera; `thinking` shows a smooth brief look-away (aversion) that transitions in/out without snapping back through a neutral pose; `starting`/`stopped` show NO separate gaze treatment (greeting/goodbye clips unaffected).
    3. GAZE-02 (GLB) — open `/glb-avatar-test` (happy.glb). Confirm BOTH: (a) gaze eases smoothly toward the camera (same smooth soft-attend as VRM, no snap), and (b) during idle animation the model does NOT spin/twist weirdly — the earlier GLB-only idle spin must be gone. Orbit the camera and confirm the head attends smoothly without the body spinning.
    4. Regression — confirm Phase 11 idle (breathing/sway/blink) still reads normally on both avatars, and gaze does not fight, jitter, T-pose, or overwrite it. (GEST-01/GEST-02 are already PASS and need not be re-tested, but note if anything about nod/shake visibly regressed.)
  </how-to-verify>
  <resume-signal>Type "approved" if GAZE-01 (smooth on VRM, all states) AND GAZE-02 (smooth + no idle spin on GLB) both read correctly, or describe each remaining issue (state, format, symptom) so a further gap-closure round can be planned. Record the verdict and explicit GAZE-01/GAZE-02 sign-off in 12-09-VERIFICATION.md.</resume-signal>
  <acceptance_criteria>
    - `.planning/phases/12-gaze-gesture/12-09-VERIFICATION.md` exists and records the human's per-state verdicts for VRM (ready/listening/speaking/thinking) and GLB (smooth + no idle spin)
    - GAZE-01 and GAZE-02 each have an explicit PASS or a recorded remaining-gap verdict with (state, format, symptom)
    - If both PASS, the file states Phase 12 is fully confirmed (all four requirements: GAZE-01, GAZE-02 now PASS; GEST-01, GEST-02 already PASS in 12-06)
  </acceptance_criteria>
  <done>A human has re-verified both gap fixes on a live build; GAZE-01 and GAZE-02 are explicitly signed off (PASS or remaining gap recorded) in 12-09-VERIFICATION.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM tool call → live avatar | Unchanged from 12-06; this checkpoint re-verifies gaze only. The set_gesture enum still drives only a bounded, non-interrupting bone delta (already confirmed). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-10 | Tampering (visual state) | gaze angle bound | accept | Re-confirmed live: gaze stays a subtle bounded offset (MAX_GAZE_ANGLE_RAD), never a full head-turn or spin, on both formats. |

No package installs this plan — no supply-chain checkpoint applies.
</threat_model>

<verification>
- Human confirms smooth gaze on VRM (all live states) and smooth gaze + no idle spin on GLB
- GAZE-01 and GAZE-02 explicitly signed off in 12-09-VERIFICATION.md
</verification>

<success_criteria>
- Phase 12 is either fully confirmed (GAZE-01, GAZE-02 now PASS; GEST-01/02 already PASS) or a remaining gap is documented for a further round
</success_criteria>

<output>
Create `.planning/phases/12-gaze-gesture/12-09-SUMMARY.md` when done
</output>
