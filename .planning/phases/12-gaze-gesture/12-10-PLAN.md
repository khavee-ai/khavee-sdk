---
phase: 12-gaze-gesture
plan: 10
type: execute
wave: 8
depends_on: ["12-09"]
files_modified:
  - packages/react/src/animation/gaze.ts
  - packages/react/src/animation/gaze.test.ts
autonomous: false
gap_closure: true
requirements: [GAZE-02]

must_haves:
  truths:
    - "The applied gaze head-bone delta never exceeds MAX_GAZE_ANGLE_RAD, even when the head bone's mixer-driven base pose (head.quaternion) changes DISCONTINUOUSLY between two consecutive stepGaze calls (idle-clip loop seam, clip switch, or crossfade boundary) — the exact invariant CR-01 proved is currently violated."
    - "On GLB (happy.glb) during idle animation on /glb-avatar-test, the avatar's head/body no longer spins or twists (the open GAZE-02 gap 12-08 failed to close)."
    - "Gaze still eases smoothly toward the camera on GLB — the 12-07 smoothing behaviour confirmed PASS in 12-09 is NOT regressed."
    - "VRM per-state gaze (GAZE-01, confirmed PASS in 12-09) and GEST-01/GEST-02 (confirmed PASS in 12-06) are untouched — no code path outside gaze.ts/gaze.test.ts changes."
  artifacts:
    - path: "packages/react/src/animation/gaze.ts"
      provides: "Final-delta re-clamp so the applied head delta is bounded by MAX_GAZE_ANGLE_RAD regardless of how far state.smoothedTarget has drifted from this frame's base"
      contains: "angleTo"
    - path: "packages/react/src/animation/gaze.test.ts"
      provides: "RED-first regression test that jumps the head base pose discontinuously between frames and asserts the applied delta stays within MAX_GAZE_ANGLE_RAD"
      contains: "MAX_GAZE_ANGLE_RAD"
  key_links:
    - from: "packages/react/src/animation/gaze.ts stepGaze final delta application (head.quaternion.multiply(_scratchDelta))"
      to: "MAX_GAZE_ANGLE_RAD"
      via: "re-clamp of the current->easedTarget angle before deriving _scratchDelta"
      pattern: "MAX_GAZE_ANGLE_RAD"
---

<objective>
Close the LAST open Phase 12 requirement, GAZE-02, on its remaining half: the GLB-only idle-animation spin/twist that persists on `happy.glb` after three prior gap-closure rounds (12-07 gaze smoothing, 12-08 world-target math, 12-09 verification-only). GAZE-01, GEST-01, GEST-02 are already signed off PASS (12-06/12-09) and are OUT OF SCOPE — do not touch them.

This is gap-closure round 4 (Wave 8), blocked on 12-09's verification. The authoritative gap report is `12-09-VERIFICATION.md` (GAZE-02 = REMAINING GAP: gaze easing PASS, idle spin FAIL). The concrete, empirically-verified root-cause candidate is CR-01 in `12-REVIEW-wave7.md`.

Primary fix candidate (CR-01): `GazeState.smoothedTarget` is a persisted ABSOLUTE quaternion re-seeded from the head's base pose ONLY on a mode transition out of `"none"`. Every other frame it slerps toward that frame's clamped target at ~8.6%/frame. If the head bone's mixer-driven base pose (`head.quaternion`, read as `_scratchCurrent`) changes DISCONTINUOUSLY between two frames — an idle-clip loop-seam discontinuity or a `switchToClip`/crossfade boundary, both of which run earlier in `AnimationStateEngine.ts`'s per-frame `update()` (steps 0-1, 5-6) BEFORE gaze (step 10) — then `smoothedTarget` is no longer within `MAX_GAZE_ANGLE_RAD` of the NEW base, and the final delta (`_scratchCurrent.invert().multiply(_scratchEasedTarget)`) is applied WITHOUT a post-smoothing re-clamp. The reviewer reproduced 0.177 rad (~10.1 deg, 3.5x the 0.05 rad bound) for an 8-deg base jump and 28x for a 90-deg jump. This reproduces even in `thinking`/aversion mode (no camera dependency), so it is INDEPENDENT of 12-08's world-target fix.

CRITICAL HONESTY: CR-01 is a strong, reproducible hypothesis, NOT a confirmed cause of the live symptom. It requires two untested assumptions: (a) `happy.glb`'s idle clip actually has a head-bone frame-to-frame discontinuity that `male.vrm`'s does not, and (b) gaze is actually ACTIVE (mode `camera`/`aversion`, i.e. `chatStatus` in ready/listening/speaking/thinking) in the exact state the human observed the spin — gaze is a FULL no-op in `starting`/`stopped`, so if the observed idle plays under `stopped`, CR-01 cannot be the cause at all. This plan therefore: (1) lands the CR-01 re-clamp (a correct invariant fix regardless of whether it is THE cause), (2) empirically determines whether CR-01 is even applicable to the live symptom AND pre-scopes the IN-02 fallback (breathing.ts/sway.ts/idle-clip loop continuity), and (3) ends in a BLOCKING human-verify against the real `happy.glb` idle animation — objective test-green is explicitly NOT sufficient (it passed 150/150 twice while this bug shipped).

Purpose: Sign off GAZE-02 as PASS so Phase 12 can close, or produce a decisively-scoped 12-11 fallback if the head-bone re-clamp alone does not resolve the live spin.
Output: Bounded-delta invariant fix in gaze.ts, a RED-first regression test proving it, corrected file-header docs, an empirical applicability/fallback diagnosis, and a human GAZE-02 sign-off (or a scoped fallback gap).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/12-gaze-gesture/12-09-VERIFICATION.md
@.planning/phases/12-gaze-gesture/12-REVIEW-wave7.md
@.planning/phases/12-gaze-gesture/12-08-SUMMARY.md
@.planning/phases/12-gaze-gesture/12-07-SUMMARY.md
@.planning/phases/12-gaze-gesture/12-CONTEXT.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase; use directly, no exploration required. -->

From packages/react/src/animation/gaze.ts (the file being modified):
```typescript
export const MAX_GAZE_ANGLE_RAD = 0.05;          // the invariant bound CR-01 violates
export const GAZE_SMOOTH_TIME_CONSTANT = 0.18;   // smoothing time constant (~8.6%/frame @16ms)
export type GazeMode = "camera" | "aversion" | "none";
export interface GazeState {
  activeMode: GazeMode;
  smoothedTarget: THREE.Quaternion;   // PERSISTED ABSOLUTE quaternion — the CR-01 drift vector
  smoothedInitialized: boolean;
}
export function createGazeState(): GazeState;
export function stepGaze(state, adapter, camera, chatStatus, delta): void;  // fix site
```
Key CR-01 lines in stepGaze (verify current line numbers on read — they shift with edits):
- Re-seed guard (only on enteringFromNone / first use): `if (!state.smoothedInitialized || enteringFromNone) { state.smoothedTarget.copy(_scratchCurrent); ... }`
- Per-frame smoothing advance: `state.smoothedTarget.slerp(_scratchClampedTarget, smoothT);`
- UN-clamped final delta (the bug): `_scratchDelta.copy(_scratchCurrent).invert().multiply(_scratchEasedTarget); head.quaternion.multiply(_scratchDelta);`
- Existing clamp idiom to reuse for the fix: `const targetAngle = _scratchCurrent.angleTo(_scratchLocalTarget); if (targetAngle > MAX_GAZE_ANGLE_RAD && targetAngle > 0) { const t = MAX_GAZE_ANGLE_RAD / targetAngle; _scratchClampedTarget.copy(_scratchCurrent).slerp(_scratchLocalTarget, t); }`

From packages/react/src/animation/AnimationStateEngine.ts update() per-frame order (confirms CR-01 premise):
```
step 0: switchToClip(...)         // clip switch — DISCONTINUOUS head base-pose jump possible
step 1: stepCrossfade(...)        // crossfade boundary — DISCONTINUOUS head base-pose jump possible
steps 5-6: breathing.step / sway.step   // write spine/chest ONLY (never head) — see IN-02
step 10: if (camera) gaze.step(adapter, camera, chatStatus, delta)  // gaze runs LAST, on top of a base that just moved
```
Frame-ordering contract: mixer.update(delta) -> controller.update(delta) -> vrm.update(delta). The mixer (idle clip) rewrites head.quaternion every frame BEFORE gaze reads it.

From packages/react/src/animation/gaze.test.ts (the test harness to extend):
```typescript
function makeStubAdapter(head): AvatarFormatAdapter;      // getHumanoidBoneNode("head") -> head
function makeHeadWithParent(): { parent, head };          // neck->head hierarchy
function makeStubCamera(x, y, z): THREE.Camera;
const FRAME_DT = 0.016;
function runUntilConverged(state, head, baseQuat, adapter, camera, chatStatus, frames=90): void;
// NOTE (WR-01): runUntilConverged resets head.quaternion to the SAME baseQuat every frame — no
// existing test varies the base pose between frames. That is the exact untested gap CR-01 shipped through.
```
</interfaces>

<!-- OUT OF SCOPE (do not read for editing, do not modify): GAZE-01 VRM behaviour, gesture.ts, toolGesture, GEST-01/02. gaze.ts only ever writes the HEAD bone; breathing.ts/sway.ts own spine/chest and are the IN-02 fallback surface only. -->
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED-first base-pose-discontinuity regression test + CR-01 final-delta re-clamp + WR-02 doc correction</name>
  <files>packages/react/src/animation/gaze.test.ts, packages/react/src/animation/gaze.ts</files>
  <read_first>
    - packages/react/src/animation/gaze.ts (the full file — you are modifying stepGaze's final-delta application and the file header)
    - packages/react/src/animation/gaze.test.ts (the full file — you are adding a describe block and must match runUntilConverged's harness conventions)
    - .planning/phases/12-gaze-gesture/12-REVIEW-wave7.md (CR-01 for the exact mechanism + reproduction magnitudes and the reviewer's suggested fix idiom; WR-01 for the test shape; WR-02 for the doc overclaim to correct)
  </read_first>
  <behavior>
    Write these tests FIRST and confirm they FAIL (RED) against current gaze.ts, then land the fix to turn them GREEN:
    - Test A (camera mode, the CR-01 repro): create a fresh GazeState, converge gaze fully in "ready" camera mode from a FIXED base pose via runUntilConverged (using an in-front camera such as makeStubCamera(1, 0.3, -3) with parent rotation [0,0,0], matching the existing 12-08 "front" convention). THEN, on the NEXT single stepGaze call, set head.quaternion to a base pose jumped ~8 degrees away from the converged base (a discontinuous jump, e.g. setFromAxisAngle around (0,1,0) by 0.14 rad) BEFORE calling stepGaze once more. Assert: the applied delta from THAT frame's NEW base — measured as newBase.angleTo(head.quaternion after the call) — is <= MAX_GAZE_ANGLE_RAD + 1e-6. Against current code this asserts ~0.177 rad <= 0.05 and MUST fail RED.
    - Test B (aversion mode, proves CR-01 is camera-independent): identical structure but chatStatus "thinking" and camera=null. Converge, then jump the base ~8 deg, step once, assert applied delta from the new base <= MAX_GAZE_ANGLE_RAD + 1e-6. MUST fail RED against current code (aversion has no camera dependency, isolating the smoothedTarget-drift mechanism).
    - Regression guard: the existing "converges monotonically... never exceeding the max" and all 20 existing gaze tests still pass unchanged (the fix must not alter the fixed-base convergence behaviour those assert).
  </behavior>
  <action>
    STEP 1 (RED): Add a new describe block to gaze.test.ts, e.g. "stepGaze — bounded delta under a discontinuous base-pose jump between frames (CR-01/WR-01 regression)", implementing Test A and Test B from <behavior>. To measure "the applied delta from the NEW base", capture the jumped base quaternion into a local `newBase` clone immediately after setting head.quaternion and before the stepGaze call, then assert `newBase.angleTo(head.quaternion)` <= MAX_GAZE_ANGLE_RAD + 1e-6. Run `cd packages/react && pnpm exec vitest run src/animation/gaze.test.ts` and CONFIRM the two new tests FAIL with an over-bound angle (record the observed rad value in the SUMMARY as RED evidence). Do NOT proceed to STEP 2 until you have observed the RED failure.

    STEP 2 (GREEN — CR-01 fix in gaze.ts): In stepGaze, immediately before the final delta application (the current `_scratchDelta.copy(_scratchCurrent).invert().multiply(_scratchEasedTarget); head.quaternion.multiply(_scratchDelta);`), re-clamp the applied delta so it can never exceed MAX_GAZE_ANGLE_RAD from THIS frame's base regardless of how far state.smoothedTarget has drifted. Reuse the EXACT clamp idiom already present in this file (angleTo + copy().slerp() into a scratch): compute `deltaAngle = _scratchCurrent.angleTo(_scratchEasedTarget)`; if `deltaAngle > MAX_GAZE_ANGLE_RAD && deltaAngle > 0`, slerp a scratch FROM `_scratchCurrent` TOWARD `_scratchEasedTarget` by `t = MAX_GAZE_ANGLE_RAD / deltaAngle` to get the bounded eased target, then derive `_scratchDelta` from that bounded target (`_scratchCurrent.invert().multiply(boundedEased)`); else derive `_scratchDelta` as today. Use an existing module-scoped scratch or add ONE new module-scoped scratch quaternion (never `new` inside stepGaze — PERF-01/T-12-03, same precedent as the other _scratch* objects). Do NOT mutate state.smoothedTarget in this clamp (it must keep drifting/re-converging naturally next frame). RECOMMENDED secondary robustness (optional, apply if it does not break existing tests): additionally re-seed state.smoothedTarget from _scratchCurrent when `_scratchCurrent.angleTo(state.smoothedTarget)` exceeds a clear-discontinuity threshold (e.g. several x MAX_GAZE_ANGLE_RAD), treating a large single-frame base jump like an enteringFromNone re-seed to eliminate visible lag — but the final re-clamp above is the MANDATORY guarantee.

    STEP 3 (WR-02 doc correction): The gaze.ts file header currently asserts the MAX_GAZE_ANGLE_RAD bound holds unconditionally (steps 3-5 narrative). Update the header so the bound is described as GUARANTEED by the final re-clamp even under a discontinuous base-pose change (cite 12-10/CR-01), replacing any language implying the pre-fix code already bounded it in all cases. Preserve the existing RESEARCH/pitfall traceability-tag comment style.
  </action>
  <verify>
    <automated>cd packages/react && pnpm exec vitest run src/animation/gaze.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages/react && pnpm exec vitest run src/animation/gaze.test.ts` passes ALL tests including the two new CR-01/WR-01 regression tests (Test A camera + Test B aversion), with a test count of at least 22 (20 existing + 2 new).
    - `cd packages/react && pnpm test` reports the full suite green (>= 150 passing, no regressions).
    - `cd packages/react && npx tsc --noEmit` exits 0 (clean).
    - `grep -n "angleTo" packages/react/src/animation/gaze.ts` shows the NEW final-delta clamp usage (a second/third angleTo call beyond the existing target-clamp), confirming the re-clamp is present at the final-delta site.
    - `grep -vn '^\s*\*' packages/react/src/animation/gaze.ts | grep -c "new THREE\." within stepGaze's body is 0 — no per-frame allocation added (any new scratch is module-scoped).
    - SUMMARY records the observed RED angle (rad) for Test A and Test B against pre-fix code as evidence the test was genuinely RED-first.
    - `git diff --name-only` shows ONLY packages/react/src/animation/gaze.ts and packages/react/src/animation/gaze.test.ts changed — no other file touched (GAZE-01/GEST scope untouched).
  </acceptance_criteria>
  <done>The MAX_GAZE_ANGLE_RAD bound holds for the applied head delta even under a discontinuous frame-to-frame base-pose jump, proven by two RED-first regression tests now GREEN; the file-header overclaim is corrected; full suite and tsc are clean; only gaze.ts/gaze.test.ts changed.</done>
</task>

<task type="auto">
  <name>Task 2: Empirical applicability diagnosis (is CR-01 even the live cause?) + IN-02 fallback scoping against real happy.glb</name>
  <files>packages/react/scripts/tmp-diagnose-glb-idle-headpose.mjs (throwaway — created, run, and DELETED before this task completes; no committed source change)</files>
  <read_first>
    - packages/react/src/animation/gaze.ts (confirm gaze is a FULL no-op for chatStatus starting/stopped, active only for ready/listening/speaking/thinking)
    - packages/react/src/animation/AnimationStateEngine.ts (resolveBaseClip + STATUS_CLIP_PATTERNS + update() step order; determine which chatStatus/base clip the GLB idle plays under)
    - packages/react/src/animation/breathing.ts and packages/react/src/animation/sway.ts (IN-02 fallback surface: they write spine/chest ONLY, never head — the only systems that could produce a spine/chest "body twist" during idle)
    - src/app/glb-avatar-test/page.tsx (the live verification surface: happy.glb, camera [0,1,3], clip "State 1 Idle (loop)", KhaveeProvider default chatStatus)
    - .planning/phases/12-08-SUMMARY.md (precedent: throwaway real-asset headless diagnostic loading public/models/happy.glb via GLTFLoader, mirroring packages/react/scripts/verify-head-axis.mjs; DELETE all throwaway scripts before finishing)
  </read_first>
  <action>
    Write ONE throwaway headless Node diagnostic (mirror 12-08's deleted tmp-diagnose-gaze*.mjs and 12-02's packages/react/scripts/verify-head-axis.mjs methodology) that loads the REAL public/models/happy.glb via GLTFLoader and measures, for the "State 1 Idle (loop)" clip driven through a real THREE.AnimationMixer:
      1. The head bone's frame-to-frame LOCAL quaternion delta (head.quaternion.angleTo(previousFrameHeadQuaternion)) across a full clip cycle INCLUDING the loop seam (wrap playback time past the clip duration). Report max single-frame head-bone base-pose delta and whether a loop-seam discontinuity exists. This directly tests CR-01's untested assumption (a): does happy.glb's idle clip actually jump the head base pose between frames?
      2. Whether gaze is even ACTIVE in the state the /glb-avatar-test idle is observed under: determine from resolveBaseClip + KhaveeProvider's default chatStatus which chatStatus the GLB idle plays under on that page, and whether resolveMode maps it to camera/aversion (gaze active) or none (gaze no-op). This tests CR-01's untested assumption (b): if the observed idle is under starting/stopped, gaze is a no-op and CR-01 CANNOT be the live cause — the fallback is then primary.
      3. IN-02 fallback scope: whether breathing.step/sway.step produce a non-trivial spine/chest delta during that same idle state (they are the ONLY systems writing spine/chest — a "body twist" the human sees during idle, if gaze is inactive, must originate here or in the idle clip itself). Report the measured spine/chest per-frame delta magnitude.
    Record all measured numbers in the SUMMARY. Then DELETE the throwaway script and confirm `git status` shows no stray files. Make NO change to gaze.ts, gaze.test.ts, breathing.ts, sway.ts, or any committed source in this task — it is measurement-only.
    Conclude with an explicit written verdict in the SUMMARY, one of:
      (A) "CR-01 APPLICABLE" — gaze is active in the observed state AND happy.glb's idle head-bone has a measurable frame-to-frame/loop-seam discontinuity, so Task 1's re-clamp is expected to resolve the live spin (human-verify in Task 3 confirms).
      (B) "CR-01 NOT SUFFICIENT / FALLBACK PRIMARY" — gaze is a no-op in the observed state OR the head-bone shows no discontinuity, so the live spin most likely originates in breathing.ts/sway.ts or the idle clip's own loop continuity (IN-02). Record the specific measured fallback signal for a 12-11 round.
  </action>
  <verify>
    <automated>test ! -e packages/react/scripts/tmp-diagnose-glb-idle-headpose.mjs && echo "THROWAWAY_DELETED" && git status --porcelain packages/react/scripts/ | grep -c tmp-diagnose || true</automated>
  </verify>
  <acceptance_criteria>
    - The diagnostic ran against the REAL public/models/happy.glb (not a synthetic proxy) and reported: (a) max single-frame head-bone base-pose delta during "State 1 Idle (loop)" including the loop seam, (b) which chatStatus the /glb-avatar-test GLB idle plays under and whether gaze's resolveMode is active (camera/aversion) or no-op (none) in it, (c) the measured breathing/sway spine/chest per-frame delta in that state.
    - SUMMARY contains an explicit verdict labelled exactly "CR-01 APPLICABLE" or "CR-01 NOT SUFFICIENT / FALLBACK PRIMARY", with the numbers supporting it.
    - `test ! -e packages/react/scripts/tmp-diagnose-glb-idle-headpose.mjs` succeeds (throwaway deleted) and `git status --porcelain` shows no stray tmp-diagnose files.
    - `git diff --name-only` shows NO committed source change from this task (gaze.ts/gaze.test.ts diffs are Task 1's only; breathing.ts/sway.ts unchanged).
  </acceptance_criteria>
  <done>An empirical, real-asset verdict exists on whether CR-01 is the live cause of the GLB idle spin, with the IN-02 fallback (breathing/sway/idle-clip loop continuity) pre-scoped with concrete measured signals; no committed source touched; throwaway script deleted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: BLOCKING human verification — GAZE-02 sign-off on the real happy.glb idle animation</name>
  <read_first>
    - .planning/phases/12-gaze-gesture/12-09-VERIFICATION.md (the exact gap: GLB idle spin FAIL; reproduce the same setup the human used)
    - Task 2's SUMMARY verdict (CR-01 APPLICABLE vs FALLBACK PRIMARY) — tells the human what outcome to expect
  </read_first>
  <what-built>
    Task 1 landed a final-delta re-clamp in gaze.ts (CR-01) guaranteeing the applied head-bone gaze delta never exceeds MAX_GAZE_ANGLE_RAD even when the mixer-driven head base pose jumps discontinuously between frames (idle loop seam / clip switch), with two RED-first regression tests now GREEN and full suite + tsc clean. Task 2 empirically determined against the real happy.glb whether CR-01 is the live cause and pre-scoped the breathing/sway/idle-clip fallback (IN-02). This checkpoint is the decisive live confirmation — objective test-green is NOT sufficient (it passed 150/150 twice while this exact bug shipped in 12-07 and 12-08).
  </what-built>
  <how-to-verify>
    Run the dev server: `pnpm dev`. Open `/glb-avatar-test` (the SAME page 12-09 used — happy.glb, camera [0,1,3], "State 1 Idle (loop)").
    1. IDLE SPIN (the open GAZE-02 gap): Let the avatar sit in its idle animation and watch for at least 20-30 seconds, through several idle-clip loop cycles. Confirm whether the body/head STILL spins or twists during idle (the 12-09 FAIL symptom), or whether it is now steady. This is the pass/fail gate.
    2. GAZE EASING NON-REGRESSION (12-07/12-09 PASS — must not break): Confirm the head still attends smoothly toward the camera on GLB (orbit the camera with the mouse if a gaze-active state is reachable on this page) — smooth ease, no snap. If the page has no gaze-active control, confirm at minimum the idle head motion reads as calm/attentive, not snapping.
    3. Cross-check against Task 2's verdict: if Task 2 said "CR-01 APPLICABLE", the spin should now be gone; if it said "FALLBACK PRIMARY", the spin may persist and that is expected — report exactly what you see.
  </how-to-verify>
  <resume-signal>
    Reply "approved" if the GLB idle spin/twist is GONE and gaze easing is not regressed — this signs off GAZE-02 as PASS and closes Phase 12 (update ROADMAP + STATE accordingly).
    If the spin PERSISTS: describe exactly what still spins (head only? whole torso? during which idle phase?) and reply "gap remains". Per IN-02, a persisting spin after the head-bone re-clamp indicates the cause is NOT gaze.ts (which only writes the head bone) — it points to breathing.ts/sway.ts (spine/chest) or the idle clip's own loop continuity. A 12-11 gap-closure round must then investigate that fallback surface using Task 2's pre-scoped measurements; do NOT re-diagnose gaze.ts.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This change is pure browser-side quaternion math on an internal, non-exported animation helper. No new external input, network call, package install, or persisted data crosses any boundary. gaze.ts consumes only the R3F scene camera and the avatar's own bones. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-10-01 | Denial of Service | stepGaze per-frame path | mitigate | No per-frame allocation added — the re-clamp reuses module-scoped scratch quaternions (PERF-01/T-12-03), asserted by the acceptance-criteria grep gate for `new THREE.` inside stepGaze's body. |
| T-12-10-02 | Tampering | correctness invariant (MAX_GAZE_ANGLE_RAD bound) | mitigate | The final-delta re-clamp enforces the documented angular bound unconditionally, closing the CR-01 invariant violation; two RED-first regression tests lock it in against regression. |
</threat_model>

<verification>
- `cd packages/react && pnpm exec vitest run src/animation/gaze.test.ts` — all gaze tests pass incl. 2 new CR-01/WR-01 regression tests (>= 22 total).
- `cd packages/react && pnpm test` — full react suite green (>= 150 passing).
- `cd packages/react && npx tsc --noEmit` — exit 0.
- `grep -n "angleTo" packages/react/src/animation/gaze.ts` — shows the added final-delta re-clamp.
- `git diff --name-only` — only gaze.ts + gaze.test.ts changed (no GAZE-01/GEST scope touched); no stray tmp-diagnose script.
- BLOCKING human-verify: GLB idle spin gone on /glb-avatar-test AND gaze easing not regressed → GAZE-02 PASS.
</verification>

<success_criteria>
- The applied gaze head-bone delta is provably bounded by MAX_GAZE_ANGLE_RAD even under a discontinuous frame-to-frame base-pose jump (RED-first regression tests GREEN).
- File-header docs no longer overclaim the bound (WR-02 corrected).
- An empirical verdict exists on whether CR-01 is the live cause, with the IN-02 fallback pre-scoped.
- Human confirms live on the real happy.glb idle: spin GONE + gaze easing not regressed → GAZE-02 signed off PASS and Phase 12 closes; OR a decisively-scoped 12-11 fallback gap is recorded (breathing/sway/idle-clip, not gaze.ts).
- GAZE-01, GEST-01, GEST-02 untouched and un-regressed.
</success_criteria>

<output>
Create `.planning/phases/12-gaze-gesture/12-10-SUMMARY.md` when done. Record: the observed RED angles (Test A/B), the CR-01 fix applied, Task 2's real-asset measurements and CR-01-APPLICABLE-vs-FALLBACK verdict, and the human GAZE-02 sign-off outcome (PASS → close Phase 12; or gap-remains → scope 12-11 on the breathing/sway/idle-clip fallback surface).
</output>
