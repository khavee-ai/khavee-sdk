---
phase: quick-260704-77n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/wp-bundle/src/config.ts
  - packages/wp-bundle/src/mount.tsx
  - packages/wp-bundle/src/floating/FloatingWidget.tsx
  - packages/wp-bundle/styles.css
  - wordpress-plugin/includes/Admin/SettingsPage.php
  - wordpress-plugin/includes/Render/AvatarRenderer.php
  - wordpress-plugin/includes/Plugin.php
autonomous: false
requirements: [FLOAT-01]
must_haves:
  truths:
    - "An admin can toggle 'Enable floating widget' on in the Settings page and it persists"
    - "When enabled, every front-end page shows a fixed bottom-right 60px purple launcher button with a red unread dot"
    - "Clicking the launcher expands a 360x520 white panel (purple header + purple avatar area + ChatBox) with a scale/opacity transition"
    - "Clicking the close X (or launcher again) collapses the panel back to the launcher"
    - "The panel renders the real AvatarScene + ClickToTalkOverlay + ChatBox + ControlBar wired to the site's GLOBAL config (no block needed)"
    - "The floating widget never appears in wp-admin"
    - "The floating widget config JSON never contains the OpenAI or Platform secret"
  artifacts:
    - path: "packages/wp-bundle/src/floating/FloatingWidget.tsx"
      provides: "Collapsed launcher / expanded panel wrapper around existing avatar+chat pieces"
    - path: "wordpress-plugin/includes/Admin/SettingsPage.php"
      provides: "Floating Widget section with Enable toggle + sanitize handling"
    - path: "wordpress-plugin/includes/Render/AvatarRenderer.php"
      provides: "Public floating-render method reusing public_safe() + floating:true"
    - path: "wordpress-plugin/includes/Plugin.php"
      provides: "wp_footer front-end-only hook that outputs the single floating mount point"
  key_links:
    - from: "AppRoot (mount.tsx)"
      to: "FloatingWidget"
      via: "config.floating === true branch"
      pattern: "config\\.floating"
    - from: "Plugin.php wp_footer"
      to: "AvatarRenderer floating render"
      via: "data-khaveeai-config with floating:true"
      pattern: "floating"
    - from: "index.ts scanner"
      to: "FloatingWidget"
      via: "existing [data-khaveeai-config] mount scan"
      pattern: "data-khaveeai-config"
---

<objective>
Add a site-wide floating chat launcher to the Khavee WordPress plugin, per the approved mockup at
`/private/tmp/claude-501/-Users-whitemalt-Documents-khavee-sdk/8cd40e85-62bc-4a26-aa71-082bc62714d3/scratchpad/floating-widget-mockup.html`.

A new admin toggle ("Enable floating widget") makes every front-end page emit one fixed bottom-right
launcher that expands into a 360x520 panel containing the existing AvatarScene + ChatBox + overlays,
wired to the site's GLOBAL config only. This is a new LAYOUT/CONTAINER around existing working pieces —
NOT a rebuild of the avatar/chat/voice pipeline.

Purpose: give site owners a one-toggle, block-independent way to put the assistant on every page.
Output: `floating?: boolean` config field, `FloatingWidget` component + CSS, a Settings toggle,
an `AvatarRenderer` floating-render method, and a `wp_footer` front-end-only injection hook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

DESIGN SPEC (read directly for exact hex/dimensions — solid #6929ff, #dde1ea border, 20px radius, NO shadow, NO gradient):
@/private/tmp/claude-501/-Users-whitemalt-Documents-khavee-sdk/8cd40e85-62bc-4a26-aa71-082bc62714d3/scratchpad/floating-widget-mockup.html

Existing pieces to reuse (do NOT rebuild):
@packages/wp-bundle/src/mount.tsx
@packages/wp-bundle/src/config.ts
@packages/wp-bundle/src/index.ts
@wordpress-plugin/includes/Render/AvatarRenderer.php
@wordpress-plugin/includes/Assets/AssetManager.php
@wordpress-plugin/includes/Admin/SettingsPage.php
@wordpress-plugin/includes/Plugin.php
@wordpress-plugin/includes/ConfigSource/WpOptionsConfigSource.php

<interfaces>
<!-- Contracts the executor needs — no codebase exploration required. -->

mount.tsx exports `mountAvatarInstance(root, config)`; `AppRoot({config, containerStyle})` renders the
current inline `.khaveeai-root` layout. `AvatarScene({config})` is currently a LOCAL (non-exported)
function in mount.tsx — it must be exported so FloatingWidget can import it.

UI components already wired to `useRealtime()` / `useKhavee()` via KhaveeProvider context (all reusable as-is):
- `ChatBox({ placement: "beside" | "below" })`
- `ClickToTalkOverlay()` — renders the "Click to talk" CTA button (`.khaveeai-cta-button`), only visible when chatStatus==="stopped"; calls `connect()` on click
- `ControlBar({ chatEnabled, isChatOpen, onToggleChat })`
- `ErrorOverlay()`

config.ts `KhaveeAvatarConfig` — add `floating?: boolean`.

AvatarRenderer (PHP): `render(array $atts): string` merges instance atts over global defaults, gates on
`is_configured()`, enqueues assets, emits `<div class="khaveeai-root" data-khaveeai-config="{esc_attr json}">`.
`private public_safe(array $merged): array` whitelists secret-free keys (voice/instructions/avatarUrl/model/
restUrl + Phase-9 visual keys). It reads `get_runtime_config()` via the injected `ConfigSourceInterface`.

Plugin::boot() is the composition root; `$renderer = new AvatarRenderer(...)` already exists there.
AssetManager::enqueue() is idempotent (`wp_script_is` guard) and enqueues `khaveeai-bundle.js`/`.css`.

index.ts scans `[data-khaveeai-config]` on DOMContentLoaded and calls mountAvatarInstance per mount point,
with a `data-khaveeai-mounted` idempotency guard — the footer mount point is picked up by this SAME path.

SettingsPage.php stores everything in the `khaveeai_settings` option. `sanitize_settings(array $input)`
orchestrates per-field sanitize and returns the merged array. `render_page()` uses manual sectioned layout:
`render_section_heading($label)` + `render_form_table_row($label, $callback)`. Existing boolean-checkbox
renderer to mirror: `render_remove_key_field()`, consumed in sanitize as
`isset($input['x']) && '1' === (string)$input['x']`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FloatingWidget component + config field + AppRoot branch + CSS</name>
  <files>packages/wp-bundle/src/config.ts, packages/wp-bundle/src/mount.tsx, packages/wp-bundle/src/floating/FloatingWidget.tsx, packages/wp-bundle/styles.css</files>
  <action>
1. In `config.ts`, add `floating?: boolean;` to the `KhaveeAvatarConfig` interface (JSDoc: "When true, render the site-wide floating launcher/panel layout instead of the inline embed layout. containerWidth/Height/fullWidth are ignored in this mode — the panel has fixed 360x520 sizing.").

2. In `mount.tsx`: (a) add `export` to the existing `AvatarScene` function so it can be imported by FloatingWidget. (b) In `AppRoot`, before the current inline-layout return, add a branch: when `config.floating === true`, return the FloatingWidget element (import it from `./floating/FloatingWidget`, pass config). Do NOT apply `containerStyle` in floating mode. Leave the existing non-floating inline layout untouched. In `mountAvatarInstance`, the OpenAIRealtimeProvider construction and KhaveeProvider wrapping are unchanged (FloatingWidget renders inside the same KhaveeProvider, so its children reach useRealtime/useKhavee normally).

3. Create `floating/FloatingWidget.tsx` — a component `FloatingWidget({ config })` using local `useState(false)` for isOpen. Structure mirrors the mockup DOM but with the `khaveeai-floating-` prefix on EVERY class and adapted to render live pieces:
   - Root div `className="khaveeai-floating-widget"` with `data-open={isOpen ? "true" : "false"}`.
   - Panel div `khaveeai-floating-panel` containing:
     - Header (`khaveeai-floating-header`): title "AI Assistant" + sub "Usually replies instantly" + a close button (`khaveeai-floating-close`, aria-label "Minimize", onClick sets isOpen false) with the mockup's X svg.
     - Avatar area (`khaveeai-floating-avatar-area`, position:relative): render AvatarScene (imported from `../mount`) when `config.avatarUrl` is set, filling the area, with ClickToTalkOverlay overlaid on top (it supplies the real "Click to talk" CTA — do NOT hardcode the mockup's static `.widget-cta`). Also render ErrorOverlay here so errors overlay the panel.
     - Chat (`khaveeai-floating-chat`): render ChatBox with placement "below".
     - ControlBar at the bottom (`chatEnabled` true, `isChatOpen` true, `onToggleChat` a no-op) — keep it, it is already-working mic UI.
   - Launcher button (`khaveeai-floating-launcher`, aria-label "Open chat with AI Assistant", aria-expanded={isOpen}, onClick toggles isOpen) containing the mockup's chat-icon svg (`khaveeai-floating-icon-chat`), close-icon svg (`khaveeai-floating-icon-close`), and an unread-dot span (`khaveeai-floating-unread-dot`).
   Import AvatarScene/ChatBox/ClickToTalkOverlay/ErrorOverlay/ControlBar from their existing modules (`../mount`, `../ui/ChatBox`, `../ui/ClickToTalkOverlay`, `../ui/ErrorOverlay`, `../ui/ControlBar`).

4. In `styles.css`, append a new section porting the mockup's floating-widget rules VERBATIM for colors/dimensions/transitions, renaming every selector to the `khaveeai-floating-` prefix (`.khaveeai-floating-widget`, `.khaveeai-floating-panel`, `.khaveeai-floating-header`, `.khaveeai-floating-close`, `.khaveeai-floating-avatar-area`, `.khaveeai-floating-chat`, `.khaveeai-floating-launcher`, `.khaveeai-floating-icon-chat`, `.khaveeai-floating-icon-close`, `.khaveeai-floating-unread-dot`, plus the `[data-open="true"]`/`[data-open="false"]` state rules, the `prefers-reduced-motion` and `max-width:480px` media queries). CRITICAL: solid `#6929ff` purple, `#dde1ea` border, `20px` panel radius, `220ms cubic-bezier(0.2,0.8,0.2,1)` transform transition — NO box-shadow and NO gradient anywhere. The avatar-area lets AvatarScene's canvas fill it (height ~200px, canvas fills 100%). Do NOT touch the existing `.khaveeai-*` inline-embed classes.
  </action>
  <verify>
    <automated>cd /Users/whitemalt/Documents/khavee-sdk && pnpm --filter @khaveeai/wp-bundle build 2>&1 | tail -20 && (grep -Eiq "box-shadow|gradient" packages/wp-bundle/styles.css && echo "FAIL: shadow/gradient present" && exit 1 || echo "OK: no shadow/gradient")</automated>
  </verify>
  <done>Bundle builds with no errors; `floating?: boolean` exists on KhaveeAvatarConfig; AppRoot renders FloatingWidget when config.floating is true; FloatingWidget.tsx exists rendering real AvatarScene+ChatBox+ClickToTalkOverlay; styles.css has khaveeai-floating- rules with zero box-shadow/gradient.</done>
</task>

<task type="auto">
  <name>Task 2: Settings toggle + AvatarRenderer floating method + wp_footer hook</name>
  <files>wordpress-plugin/includes/Admin/SettingsPage.php, wordpress-plugin/includes/Render/AvatarRenderer.php, wordpress-plugin/includes/Plugin.php</files>
  <action>
1. `SettingsPage.php`:
   - Add a `render_floating_widget_field()` public method mirroring `render_remove_key_field()`'s checkbox pattern: a label wrapping `input type="checkbox" name="khaveeai_settings[floating_widget_enabled]" value="1"` with `checked()` applied from the stored value, text "Enable the site-wide floating chat widget", plus a `<p class="description">` explaining it shows on every front-end page using the global config. Read the current value via `get_option( self::OPTION_NAME, array() )['floating_widget_enabled']`.
   - In `render_page()`, add a new section AFTER the "Avatar" section: call `render_section_heading( __( 'Floating Widget', 'khaveeai' ) )`, then a form-table wrapping `render_form_table_row( __( 'Enable floating widget', 'khaveeai' ), array( $this, 'render_floating_widget_field' ) )`.
   - In `sanitize_settings()`, persist the boolean: `$sanitized['floating_widget_enabled'] = isset( $input['floating_widget_enabled'] ) && '1' === (string) $input['floating_widget_enabled'];`. (Follows the existing remove-checkbox read shape but is PERSISTED, unlike the transient remove_* flags.)
   - Optionally also register it via `add_settings_field` in `register_settings()` for consistency; the manual `render_page()` layout is what actually renders it.

2. `AvatarRenderer.php`: add a public method `render_floating(): string` that:
   - Returns empty string immediately if `! $this->config_source->is_configured()` (no admin notice, no placeholder — footer output stays silent for unconfigured sites; the visitor never sees markup).
   - Calls `$this->assets->enqueue();` (same idempotent bundle enqueue as `render()`).
   - Builds `$merged = $this->config_source->get_runtime_config();` then runs the SAME defensive per-field re-application `render()` uses (refactor that block into a private helper both call if convenient — but do not change `render()`'s existing block output/behavior).
   - Emits ONE mount div: id `khaveeai-floating`, class `khaveeai-root`, `data-khaveeai-config="{esc_attr( wp_json_encode( $config ) )}"` where `$config = $this->public_safe( $merged )` with `$config['floating'] = true;` added. The secret-exclusion guarantee is inherited verbatim from `public_safe()` — do NOT read or add any api-key/platform-key field.

3. `Plugin.php` (`boot()` + new static callback):
   - Add a static property to hold the renderer, e.g. `private static ?AvatarRenderer $floating_renderer = null;`, and in `boot()` after `$renderer` is constructed set `self::$floating_renderer = $renderer;`.
   - Register the front-end footer hook in `boot()`: `add_action( 'wp_footer', array( __CLASS__, 'render_floating_widget' ) );`.
   - Add `public static function render_floating_widget(): void` that: (a) returns early if `is_admin()` (defense-in-depth even though wp_footer is front-end only); (b) uses a static once-guard `bool` so it outputs at most once per page load; (c) reads the setting via `get_option( 'khaveeai_settings', array() )` and returns if `floating_widget_enabled` is falsy; (d) returns if `self::$floating_renderer` is null; (e) `echo self::$floating_renderer->render_floating();` (already escaped inside render_floating). Renders regardless of whether any block exists on the page.
  </action>
  <verify>
    <automated>cd /Users/whitemalt/Documents/khavee-sdk/wordpress-plugin && php -l includes/Admin/SettingsPage.php && php -l includes/Render/AvatarRenderer.php && php -l includes/Plugin.php && php tests/render-logic-harness.php && php tests/settings-page-harness.php</automated>
  </verify>
  <done>All three PHP files pass php -l; render-logic-harness.php and settings-page-harness.php still pass; AvatarRenderer has render_floating() that adds floating:true to public_safe output; Plugin.php registers a wp_footer hook gated by is_admin() + the floating_widget_enabled setting + a once-guard; Settings page shows a Floating Widget section with a persisted Enable toggle. (platform-config-harness.php's single pre-existing documented stale-shape failure is unrelated — do NOT fix it.)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
The full floating widget: a Settings toggle, a wp_footer front-end injection that emits one
`data-khaveeai-config` mount point with `floating:true`, and a FloatingWidget layout (launcher +
expandable panel) wrapping the existing AvatarScene/ChatBox/ClickToTalkOverlay/ControlBar. The bundle
was rebuilt and PHP harnesses pass. Assets: rebuilt `packages/wp-bundle` output must be present in
`wordpress-plugin/build/` for wp-env to serve it (rebuild/copy per the repo's existing bundle-deploy step if needed).
  </what-built>
  <how-to-verify>
1. Ensure the freshly built bundle is deployed to `wordpress-plugin/build/` and wp-env is running (http://localhost:8888).
2. In wp-admin -> Khavee AI Avatar Settings, confirm a new "Floating Widget" section with an "Enable floating widget" checkbox. Turn it ON and Save; reload and confirm it stays checked (persisted).
3. Confirm the widget does NOT appear anywhere in wp-admin.
4. Load any front-end page. Confirm a solid-purple 60px round launcher button with a small red unread dot appears fixed at bottom-right.
5. Click it: the 360x520 white panel expands (scale+opacity, ~220ms) with a solid-purple header ("AI Assistant" + close X), a solid-purple avatar area rendering the VRM + a "Click to talk" CTA, and the ChatBox transcript+input below. Confirm NO drop shadow and NO gradient anywhere.
6. Click the close X (and the launcher again): panel collapses back to the launcher.
7. Toggle the setting OFF and reload a front-end page: the widget is gone.
  </how-to-verify>
  <resume-signal>Type "approved" once the widget appears/expands/collapses correctly on the front end, uses the global config avatar/chat, and is absent in wp-admin — or describe any visual/behavioral issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PHP footer render → browser | Config JSON crosses into the page DOM; must never carry secrets |
| Admin form POST → wp_options | The floating_widget_enabled toggle is attacker-influenceable input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77n-01 | Information Disclosure | AvatarRenderer::render_floating() config JSON | mitigate | Reuse public_safe() verbatim (proven secret-free by render-logic-harness); never read get_api_key(); floating:true is the only added key |
| T-77n-02 | Tampering | floating_widget_enabled setting | mitigate | Sanitized to a strict boolean in sanitize_settings() (isset + '1'===); non-'1' input coerces to false |
| T-77n-03 | Elevation/Exposure | wp_footer hook firing in wp-admin | mitigate | wp_footer is front-end only AND an explicit is_admin() early-return guard |
| T-77n-04 | DoS | Duplicate footer output | accept/mitigate | Static once-guard ensures at most one mount point per page load |
| T-77n-05 | Tampering | config.floating branch in bundle | mitigate | floating is a plain boolean; no innerHTML/eval path; React stringifies all rendered values |
</threat_model>

<verification>
- `pnpm --filter @khaveeai/wp-bundle build` completes with no errors.
- `grep -Ei "box-shadow|gradient" packages/wp-bundle/styles.css` returns nothing in the new floating rules.
- `php -l` clean on SettingsPage.php, AvatarRenderer.php, Plugin.php.
- render-logic-harness.php and settings-page-harness.php pass (platform-config-harness.php's one documented stale-shape failure is pre-existing and unrelated).
- Human confirms live behavior on http://localhost:8888 (Task 3 checkpoint).
</verification>

<success_criteria>
- A persisted "Enable floating widget" toggle exists in a new Settings "Floating Widget" section.
- When enabled, every front-end page (never wp-admin) shows the bottom-right launcher that expands into the 360x520 panel with the real avatar + chat, using the site GLOBAL config.
- The panel matches the approved mockup exactly: solid #6929ff, #dde1ea border, 20px radius, no shadow, no gradient.
- Existing block/shortcode embeds and the openai-stt-tts provider are untouched.
- The floating config JSON contains no OpenAI or Platform secret.
</success_criteria>

<output>
Create `.planning/quick/260704-77n-build-a-site-wide-floating-chat-launcher/260704-77n-SUMMARY.md` when done.
</output>
