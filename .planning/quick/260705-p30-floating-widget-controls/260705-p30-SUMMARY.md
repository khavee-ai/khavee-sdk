---
status: complete
---

# Quick Task 260705-p30: Floating Widget Controls — Summary

## What changed

Floating-widget-only changes (inline embed untouched):

1. **Removed red unread-dot** from the floating launcher button (`FloatingWidget.tsx` + CSS).
2. **Removed chat show/hide toggle** in the floating panel — `ControlBar` gained an optional `showChatToggle?: boolean` prop (default true, so the inline-embed call site in `mount.tsx` is unaffected); `FloatingWidget.tsx` passes `showChatToggle={false}`.
3. **Repositioned the mic button** — `ControlBar` gained an optional `className?: string` prop appended to the base `khaveeai-controls` class. `FloatingWidget.tsx` moved its `ControlBar` call inside `.khaveeai-floating-avatar-area` and passes `className="khaveeai-floating-controls"`; new scoped CSS rule anchors it bottom-right, clear of the centered "Click to talk" CTA. Base `.khaveeai-controls` (bottom-center) is untouched for the inline embed.
4. **Floating-specific visual customization** — new `KhaveeAvatarConfig` fields (`floatingBgColor`, `floatingBgTransparent`, `floatingAvatarOffsetX/Y`, `floatingAvatarScale`) used only by `FloatingWidget.tsx` via a derived `floatingSceneConfig`, independent of the inline embed's global `bgColor`/`avatarOffsetX/Y`/`avatarScale`. Wired end-to-end: new `floating_*` option keys in `WpOptionsConfigSource.php`, new admin fields in `SettingsPage.php` (Floating Widget section), new keys added to `AvatarRenderer::render_floating()`'s output (never touching `public_safe()`/`render()`'s inline-embed output).

## Commits

- `19eefca` — frontend TS/CSS (Task 1)
- `d79f400` — PHP settings persistence + render wiring (Task 2)
- `a77913b` — worktree merge

## Verification

**Automated (all passed):**
- `pnpm --filter @khaveeai/wp-bundle build` — clean
- `php -l` clean on all 3 edited PHP files
- `platform-config-harness.php`, `render-logic-harness.php` — all cases passed
- `settings-page-harness.php` — 1 pre-existing failure, unrelated to this task (logged in `deferred-items.md`)

**Live human verification (performed against wp-env at http://localhost:8888, after restarting Docker + `wp-env start` — the instance had stopped since the last session):**
- Red dot confirmed gone from the floating launcher button.
- Floating panel structurally confirmed: `ControlBar` nested inside `.khaveeai-floating-avatar-area`, `showChatToggle={false}`, `className="khaveeai-floating-controls"` — CSS rule `.khaveeai-floating-avatar-area .khaveeai-controls.khaveeai-floating-controls` correctly scoped (bottom-right, `left:auto`, `transform:none`), base `.khaveeai-controls` rule untouched. Mic-only rendering not visually confirmed live (ControlBar only mounts once `isConnected` is true, which requires a live OpenAI Realtime connection not exercised this session) — confirmed structurally instead via source read.
- New floating-specific settings fields (`Floating background color`, `Transparent floating background`, `Floating avatar offset X/Y`, `Floating avatar scale`) present in wp-admin's Floating Widget section. Set `floating_bg_color=#ff2d55`, `floating_avatar_offset_y=0.3`, `floating_avatar_scale=1.4`, saved, confirmed persisted via `wp option get khaveeai_settings` (WP-CLI), and confirmed the live floating widget's avatar-area background rendered red with the avatar visibly offset/scaled — while the inline embed elsewhere on the same page remained the default purple `#6929ff`, unaffected.
- Inline embed confirmed unaffected throughout (mount.tsx's `ControlBar` call site verified unchanged: no `showChatToggle`/`className` passed).

**Note on save UX:** the first two save attempts silently failed to persist (Settings page's "Save Changes" click didn't register the typed values — field values reverted to defaults/placeholders on reload). Root cause not chased (not a code defect introduced by this task — likely a stale click coordinate after scrolling). Third attempt, with direct field clicks before typing, saved correctly.
