---
task: 260707-0u6-settings-page-fixes-batch
type: quick
autonomous: false
files_modified:
  - wordpress-plugin/includes/Admin/SettingsPage.php
  - wordpress-plugin/tests/settings-page-harness.php
  - packages/wp-bundle/src/preview/PreviewScene.tsx
  - packages/wp-bundle/src/mount.tsx
  - wordpress-plugin/build/khaveeai-preview.js
  - wordpress-plugin/build/khaveeai-preview.css
  - wordpress-plugin/build/khaveeai-bundle.js
  - wordpress-plugin/build/khaveeai-bundle.css
---

<objective>
Six UX/bug fixes to the Khavee WordPress admin Settings page and its live
preview, based on direct user feedback after using the redesigned page (quick
tasks 260706-vf4 / 260706-wop / 260706-x6b):

1. Add a clarifying caption near the Floating Widget live preview explaining
   that drag/zoom orbits the CAMERA (not the avatar), and the sliders reposition
   the avatar. (Interaction behavior UNCHANGED.)
2. Remove the "Remove Key" / "Remove Platform Key" checkboxes; make
   blank-field-and-save the removal signal (normal web-form expectation), while
   preserving every existing masked-placeholder and format-rejection guarantee.
3. Add a small live avatar preview to the Avatar section (reusing the SAME
   khaveeai-preview bundle mount mechanism — no second preview mechanism).
4. Fix the real bug where unchecking "Transparent floating background" does not
   restore the background color until the preview is dragged/zoomed. Investigate
   the ACTUAL root cause first, then apply the minimal correct fix; check whether
   the front-end AvatarScene (mount.tsx) has the same defect.
5. Style all `<input type="range">` and `<input type="checkbox">` in the settings
   markup with the plugin's purple `#6929ff` via `accent-color`.
6. Add a static mock chat transcript below the avatar canvas inside the Floating
   Widget live preview panel, matching the real floating-widget chat styling.

Purpose: Resolve confusing/broken UX on the self-configuration Settings page so
a site owner can confidently configure the avatar and floating widget.

Output: Updated SettingsPage.php (all six items), updated settings-page-harness.php
(item 2 coverage), and — only if item 4 requires it — updated PreviewScene.tsx /
mount.tsx plus rebuilt bundle artifacts in wordpress-plugin/build/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Key facts the executor needs. Extracted from the codebase — no exploration needed. -->

SettingsPage.php (wordpress-plugin/includes/Admin/SettingsPage.php) key call sites:
- register_settings() registers fields via add_settings_field():
    'remove_key'          -> render_remove_key_field       (lines ~756-762)
    'remove_platform_key' -> render_remove_platform_key_field (lines ~775-781)
- render_page() renders rows manually via render_form_table_row():
    line ~1281  render_form_table_row('Remove Key', render_remove_key_field)
    line ~1283  render_form_table_row('Remove Platform Key', render_remove_platform_key_field)
- render_page() has a "DO NOT CHANGE THESE IDS" comment block (lines ~1240-1264).
    It lists #khaveeai_remove_key / #khaveeai_remove_platform_key as
    "Not JS-read, but tied to name=..." — those two lines can be dropped when
    the fields are removed (nothing reads them in JS).
- render_settings_page_styles() (lines ~1357-1420): the page-scoped <style> block
    scoped under `.khaveeai-settings`. This is where item 5's accent-color goes.
- render_floating_preview_mount() (lines ~2095-2129): emits
    <p><strong>Live preview</strong></p> then the mount div
    #khaveeai-floating-preview (360x520). This is where items 1 + 6 attach.
- Avatar section rendered in render_page() lines ~1295-1300
    (render_form_table_row('Avatar (VRM/GLB)', render_avatar_field)). Item 3
    adds a preview mount inside this card.

sanitize_api_key($submitted, $existing, $remove_requested=false) — current order:
    1. if $remove_requested -> return ''
    2. trim; $masked = mask_api_key($existing)
    3. if $submitted === $masked -> return $existing   (untouched masked field)
    4. if '' === $submitted OR not 'sk-' prefixed -> add_settings_error, return $existing
    5. return $submitted
sanitize_platform_api_key(...) is identical but gated on 'khavee_' prefix.
sanitize_settings() reads:
    $remove_requested          = isset($input['remove_key']) && '1'===...      (line ~904)
    $remove_platform_requested = isset($input['remove_platform_key']) && '1'=== (line ~915)
    and passes them into the two sanitize_*_api_key() calls (lines ~923-924).

The floating preview bundle auto-mounts: preview.ts's observeDocument(document)
scans the TOP admin document for [data-khaveeai-preview-config] and mounts each
match. A SECOND mount div with that attribute auto-mounts the same way — this is
the established item-3 pattern (each mount is its own WebGL context; two on one
admin page is fine).

The `khaveeai-preview-style` stylesheet (Plugin.php ~217, build/khaveeai-preview.css)
is the compiled output of preview.ts's `import "../styles.css"` and IS enqueued on
this settings page by enqueue_settings_assets(). So packages/wp-bundle/styles.css's
chat classes (.khaveeai-chat, .khaveeai-chat__transcript, .khaveeai-chat__bubble,
.khaveeai-chat__bubble--user, .khaveeai-chat__bubble--assistant) ARE available in
the settings page DOM — item 6's mock chat can reuse them directly.

PreviewScene.tsx (packages/wp-bundle/src/preview/PreviewScene.tsx) — item 4 site:
    line 241: const canvasGl = config.bgTransparent ? { alpha: true } : undefined;
    line 242-244: const canvasStyle = config.bgTransparent ? { background: "transparent" } : undefined;
    line 269-273: <Canvas camera={...} gl={canvasGl} style={canvasStyle}>
    line 229-237: container background: bgTransparent -> "transparent", else bgColor.
    In-file doc (line 60-62 / Pitfall 7 comment ~99-116) documents that R3F's
    camera/gl props are INITIALIZATION-ONLY (not reactive on re-render).
mount.tsx (packages/wp-bundle/src/mount.tsx) — item 4 second site:
    line 98-101: <Canvas camera={...} gl={config.bgTransparent === true ? { alpha: true } : undefined}>
    line 211-219: same container-background switch as PreviewScene.

Build: `pnpm --filter @khaveeai/wp-bundle build` (runs build.mjs) writes to
wordpress-plugin/build/khaveeai-preview.{js,css} and khaveeai-bundle.{js,css}.
These build artifacts ARE git-tracked and must be committed with any TSX change.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preview caption (item 1) + purple accent-color CSS (item 5)</name>
  <files>wordpress-plugin/includes/Admin/SettingsPage.php</files>
  <action>
Two small, purely-presentational edits.

Item 1 (caption): In render_floating_preview_mount(), add a short caption near
the "Live preview" heading (directly under it, before or after the mount div)
reading approximately: "Drag to rotate the camera view. Use the sliders on the
left to reposition and resize the avatar itself." Emit it as
`<p class="description" style="...">` or a scoped class; escape with esc_html__().
Do NOT change any interaction behavior, element id, name, data-attribute, or the
mount div itself — caption is additive markup only.

Item 5 (purple accents): In render_settings_page_styles()'s <style> block, add a
rule scoping `accent-color: #6929ff;` to range and checkbox inputs WITHIN the
settings page wrapper, e.g.:
  .khaveeai-settings input[type="range"],
  .khaveeai-settings input[type="checkbox"] { accent-color: #6929ff; }
Keep it scoped under `.khaveeai-settings` so nothing leaks into other wp-admin
chrome (matches the existing scoping discipline in that method). Do not touch any
field renderer, id, or name attribute.
  </action>
  <verify>
    <automated>php -l wordpress-plugin/includes/Admin/SettingsPage.php</automated>
  </verify>
  <done>php -l is clean. render_settings_page_styles() contains an accent-color:#6929ff rule scoped to input[type=range]/[type=checkbox] under .khaveeai-settings; render_floating_preview_mount() emits an escaped caption clarifying camera-orbit vs avatar-sliders. No element id/name/data-attribute changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove-key checkboxes -> blank-and-save removal (item 2, security-sensitive)</name>
  <files>wordpress-plugin/includes/Admin/SettingsPage.php, wordpress-plugin/tests/settings-page-harness.php</files>
  <behavior>
New sanitize_api_key / sanitize_platform_api_key contract (blank = deliberate removal):
  - Test: submitted === mask(existing), existing non-empty  -> returns existing unchanged (untouched masked field still preserves the key).
  - Test: submitted is '' / whitespace-only, existing non-empty -> returns '' (blank-and-save removes the key). THIS INVERTS the old D-06 "emptied field is NOT deletion" behavior.
  - Test: submitted is '' , existing already '' -> returns '' (stays removed; mask('')='' so it also naturally hits the unchanged branch — either path yields '').
  - Test: submitted is a genuinely new value not prefixed sk-/khavee_ (and non-blank) -> add_settings_error fires, returns existing (format rejection preserved, no overwrite).
  - Test: submitted is a fresh valid sk-/khavee_ value with surrounding whitespace -> returned trimmed.
  </behavior>
  <action>
Read sanitize_api_key() (~1101) and sanitize_platform_api_key() (~1147) FULLY
before editing — this is security-sensitive (must not leak a key, silently no-op
a removal, or corrupt an existing key on an unrelated field edit).

Change removal semantics so blank-field-and-save removes the stored key. New
decision order for BOTH methods (drop the checkbox-driven removal branch):
  1. trim submitted; $masked = mask_*(existing).
  2. if submitted === $masked -> return $existing   (untouched masked field preserved).
  3. if '' === submitted -> return ''                (blank = deliberate removal — NEW; replaces the old $remove_requested branch and the old "'' returns existing" behavior).
  4. if not prefixed sk-/khavee_ -> add_settings_error(...), return $existing.
  5. return submitted.
Remove the now-unused $remove_requested parameter (or leave it defaulting false
and unread — prefer removing it for clarity and update all callers). Update the
methods' JSDoc to describe blank-removes-key.

In sanitize_settings(): delete the $remove_requested / $remove_platform_requested
reads (lines ~904, ~915) and update the sanitize_*_api_key() calls (lines ~923-924)
to the new signature.

Remove the two checkbox fields entirely:
  - Delete add_settings_field('remove_key', ...) (~756-762) and
    add_settings_field('remove_platform_key', ...) (~775-781).
  - Delete the two render_form_table_row('Remove Key'/'Remove Platform Key', ...)
    calls in render_page() (~1281, ~1283).
  - Delete render_remove_key_field() (~1509) and render_remove_platform_key_field()
    (~1555) method definitions.
  - In render_page()'s "DO NOT CHANGE THESE IDS" comment block, drop the
    #khaveeai_remove_key / #khaveeai_remove_platform_key references.

Update the two key-field descriptions (render_api_key_field ~1497,
render_platform_api_key_field ~1542) to say the saved key is masked and
"Leave the field blank and save to remove the stored key." (drop any "an emptied
field alone does NOT clear the key" wording).

Update settings-page-harness.php (do NOT delete coverage — update it):
  - Case 12 "an emptied field is NOT a deletion signal": INVERT to assert
    sanitize_api_key('', $existing) === '' (blank now removes).
  - Case 13 "returns empty string only when remove_requested is true": rewrite to
    the new no-remove-param signature — blank submission returns ''; a masked
    submission still preserves existing. Keep asserting mask-preservation and
    format-rejection (Cases 9, 10, 11) unchanged in spirit (adjust call signatures
    if the third arg was removed).
  - Platform Cases 36 ("remove flag true -> ''") and any that pass a third
    remove-arg: rewrite to the new signature (blank -> '' ; masked -> preserved;
    non-khavee_ -> rejected). Keep Cases 37, 38, 39 semantics.
  - Add explicit new cases: "blank submission with non-empty existing removes the
    key" for BOTH api_key and platform_api_key.
Adjust any harness call that passed the old third boolean argument.
  </action>
  <verify>
    <automated>php -l wordpress-plugin/includes/Admin/SettingsPage.php && php wordpress-plugin/tests/settings-page-harness.php</automated>
  </verify>
  <done>php -l clean. settings-page-harness.php exits 0 ("All cases PASSED") WITH updated + new cases proving: blank-and-save removes the key (both keys), masked-placeholder submission preserves the key, non-prefixed non-blank value is rejected with a settings error and no overwrite. The two Remove-Key checkboxes, their render methods, add_settings_field + render_form_table_row registrations, and the sanitize_settings remove-flag reads are all gone. Field descriptions say "Leave blank and save to remove."</done>
</task>

<task type="auto">
  <name>Task 3: Live avatar preview in the Avatar section (item 3)</name>
  <files>wordpress-plugin/includes/Admin/SettingsPage.php</files>
  <action>
Add a small live avatar preview inside the Avatar card in render_page()
(the card at ~1295-1300), reusing the EXISTING khaveeai-preview bundle mount
mechanism — do NOT build a second preview bundle/mechanism.

Add a private render method (e.g. render_avatar_section_preview_mount()) modeled
on render_floating_preview_mount() (~2095), but driven by the GLOBAL avatar with
neutral default framing (this section has no floating/global visual sliders — the
Avatar section's only real config today is the avatar attachment; per the task
ask, do NOT add new sliders):
  - avatarUrl: $this->config_source->get_runtime_config()['avatar_url'] (the
    global avatar), same source render_floating_preview_mount() uses.
  - bgType: 'color'; bgColor: a neutral light default (e.g. '#f6f7f9' or the
    page card background) — this is a passive "here's your model" preview.
  - bgTransparent: false; avatarScale: 1.0; avatarOffsetX/Y: 0.0;
    cameraRotationY: 0.0 (sensible built-in defaults).
  - Emit a mount div with a DISTINCT id (e.g. #khaveeai-avatar-preview),
    class "khaveeai-root", data-khaveeai-preview-config=esc_attr(wp_json_encode(...)),
    sized smaller than the floating preview (no chat panel to match — e.g.
    width:280px; height:340px; border + radius mirroring the floating mount).
Render it inside the Avatar card (below or beside the avatar picker row). It
auto-mounts via the bundle's observeDocument(document) top-document scan — no new
inline JS is required for the initial mount. It is a passive preview (no live
field wiring needed since there are no editable visual fields in this section).

Do NOT reuse the #khaveeai-floating-preview id, and do NOT alter
render_floating_preview_mount(), enqueue_settings_assets(), preview.ts, or the
build. Confirm the new id does not collide with any id in the JS-read list.
  </action>
  <verify>
    <automated>php -l wordpress-plugin/includes/Admin/SettingsPage.php && grep -c 'khaveeai-avatar-preview' wordpress-plugin/includes/Admin/SettingsPage.php</automated>
  </verify>
  <done>php -l clean. A new mount div (distinct id, e.g. #khaveeai-avatar-preview) with a valid data-khaveeai-preview-config JSON payload (global avatar_url + neutral default framing) renders inside the Avatar card. No change to the floating preview mount, enqueue_settings_assets(), preview.ts, or build artifacts. Visual confirmation deferred to the Task 6 checkpoint.</done>
</task>

<task type="auto">
  <name>Task 4: Root-cause + fix the transparent-toggle bug (item 4)</name>
  <files>packages/wp-bundle/src/preview/PreviewScene.tsx, packages/wp-bundle/src/mount.tsx, wordpress-plugin/build/khaveeai-preview.js, wordpress-plugin/build/khaveeai-preview.css, wordpress-plugin/build/khaveeai-bundle.js, wordpress-plugin/build/khaveeai-bundle.css</files>
  <action>
STEP 1 — INVESTIGATE THE ACTUAL ROOT CAUSE FIRST. Do NOT jump straight to the
key-prop hypothesis. Read PreviewScene.tsx's Canvas/gl/canvasStyle setup
(lines ~229-244, 269-273) and mount.tsx's AvatarScene (lines ~98-101, 211-219)
and reason about what actually happens when bgTransparent toggles true->false:
  - R3F's `gl` prop is initialization-only (documented in-file, Pitfall 7): the
    WebGL context is created once with `alpha: true` and is NOT recreated when
    `gl` changes to undefined on a later render. So the drawing buffer's alpha
    channel persists after the toggle.
  - The container div's CSS background DOES switch reactively (containerStyle
    background: transparent <-> bgColor) on re-render.
  - The MutationObserver path (mountPreview.tsx) only pushes new config into React
    state (re-render), it does not recreate the WebGL context.
  - The "fixes itself on drag/zoom" clue: a drag fires OrbitControls -> a repaint/
    frame invalidation. Determine WHY that repaint changes the visible result
    (e.g. it forces the canvas to re-clear, or triggers some layout/paint that the
    plain re-render did not). Confirm the mechanism against the real code — write a
    one-paragraph root-cause statement before touching anything.

STEP 2 — APPLY THE MINIMAL CORRECT FIX based on the confirmed cause. If (as the
in-file R3F docs strongly suggest) the cause is the initialization-only `gl`
alpha context not being recreated on toggle, the correct minimal fix is a `key`
on the <Canvas> that changes ONLY when bgTransparent flips (e.g.
key={config.bgTransparent ? "gl-alpha" : "gl-opaque"}), forcing React to
unmount/remount just the Canvas + its WebGL context on that specific transition.
CRITICAL: the key must depend ONLY on bgTransparent — it must NOT change on every
config edit, or it defeats mountPreview.tsx's whole no-teardown MutationObserver
design (which keeps one WebGL context alive across slider/color edits). If your
investigation finds a different true cause, apply that fix instead and document why.

STEP 3 — Check BOTH sites. Apply the same fix to mount.tsx's AvatarScene
(line ~98-101) if it shares the defect (it uses the identical
gl={... alpha ...} initialization-only pattern). The Settings-page preview
(PreviewScene) is the primary ask; fix the front-end AvatarScene too only if the
investigation confirms it is affected.

CONSTRAINTS: Do not touch editor.js or any block-attribute-driven config path.
The fix lives inside the shared PreviewScene/AvatarScene components (the Gutenberg
block consumes PreviewScene too) — verify the block editor preview still renders
and orbits after the change (Task 6 checkpoint covers this). Do not change the
MutationObserver, the CustomEvent bridge, or resolveSceneDefaults.

STEP 4 — REBUILD. Run `pnpm --filter @khaveeai/wp-bundle build`. Stage the
rebuilt wordpress-plugin/build/khaveeai-preview.{js,css} (and khaveeai-bundle.{js,css}
if mount.tsx changed) so they land in the commit.
  </action>
  <verify>
    <automated>pnpm --filter @khaveeai/wp-bundle build</automated>
  </verify>
  <done>A written root-cause statement (in the SUMMARY) confirms the actual mechanism against the real code — not merely the assumed hypothesis. The minimal fix is applied in PreviewScene.tsx (and mount.tsx if affected), scoped so any Canvas remount happens ONLY on the bgTransparent transition, never on ordinary config edits. `pnpm --filter @khaveeai/wp-bundle build` completes with no errors; rebuilt build/ artifacts are staged for commit. Live confirmation (uncheck transparent -> color returns immediately, without dragging) deferred to Task 6.</done>
</task>

<task type="auto">
  <name>Task 5: Static mock chat transcript in the floating preview (item 6)</name>
  <files>wordpress-plugin/includes/Admin/SettingsPage.php</files>
  <action>
Add a static (non-interactive) mock chat transcript below the avatar canvas
inside the SAME #khaveeai-floating-preview panel area, so the Floating Widget
live preview shows both the avatar and a representative chat — matching the real
floating widget's visual chat layout.

In render_floating_preview_mount() (~2095), AFTER the mount div, emit a static
chat block reusing the compiled floating/chat classes already loaded on this page
via the khaveeai-preview-style stylesheet (build/khaveeai-preview.css, compiled
from packages/wp-bundle/styles.css). Use the existing chat classes so it visually
matches: a container using .khaveeai-chat (and/or .khaveeai-chat--below), a
.khaveeai-chat__transcript, and 2-3 static bubbles alternating
.khaveeai-chat__bubble--assistant and .khaveeai-chat__bubble--user (each also
carrying the base .khaveeai-chat__bubble class). Example content (escape all text
with esc_html__()):
  - assistant: "Hi! How can I help you today?"
  - user: "What are your opening hours?"
  - assistant: "We're open 9am to 6pm, Monday to Friday."
This is a purely static visual mock — NO input row, NO send button, NO JS, not
wired to any chat logic. Constrain its width to match the 360px preview panel and
give it a small top margin so it reads as the panel's chat area beneath the avatar.

If, on visual check (Task 6), the reused classes render with unexpected default
sizing on the admin page, add a minimal scoped fallback in
render_settings_page_styles() under `.khaveeai-settings` to constrain the mock
transcript's max-height/width — but prefer relying on the already-loaded classes.
Do not change the mount div id/class/data-attribute or any existing behavior.
  </action>
  <verify>
    <automated>php -l wordpress-plugin/includes/Admin/SettingsPage.php && grep -c 'khaveeai-chat__bubble' wordpress-plugin/includes/Admin/SettingsPage.php</automated>
  </verify>
  <done>php -l clean. render_floating_preview_mount() emits a static 2-3 message mock chat transcript below the avatar mount using the existing .khaveeai-chat__bubble--user/--assistant classes, all text escaped, no input row / send button / JS. The mount div id/class/data-attribute are unchanged. Visual fidelity confirmed at the Task 6 checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
All six settings-page fixes:
  1. Clarifying caption near the Floating Widget live preview (camera-orbit vs avatar sliders).
  2. Remove-Key/Remove-Platform-Key checkboxes gone; blank-field-and-save now removes the stored key (harness updated + green).
  3. Small live avatar preview added to the Avatar section (reusing the khaveeai-preview bundle).
  4. Transparent-floating-background toggle fixed (unchecking restores the color without needing a drag); root cause investigated and documented; front-end AvatarScene checked/fixed; bundle rebuilt.
  5. All range sliders and checkboxes on the page now use purple #6929ff (accent-color).
  6. Static mock chat transcript shown below the avatar in the Floating Widget live preview.
Automated gates already green: php -l on SettingsPage.php; settings-page-harness.php,
platform-config-harness.php, render-logic-harness.php all pass; wp-bundle build clean.
  </what-built>
  <how-to-verify>
Open http://localhost:8888/wp-admin/admin.php?page=khaveeai-settings and check:

1. CAPTION: The Floating Widget live preview shows a caption explaining that
   dragging rotates the camera and the sliders reposition/resize the avatar.

2. REMOVE-KEY UX: There are NO "Remove Key" / "Remove Platform Key" checkboxes.
   Each key field description says to leave it blank and save to remove.
   - With a saved OpenAI key, save WITHOUT touching the field (masked placeholder
     shown) -> key is preserved (still shows masked on reload, avatar still works).
   - Clear the OpenAI key field to blank and Save -> the "not configured" banner
     returns and the field is empty on reload (key removed).
   - Enter a value not starting with sk- and Save -> inline error, previous key kept.
   - Repeat the blank-removes and masked-preserves checks for the Platform key.

3. AVATAR PREVIEW: The Avatar section shows a small live 3D preview of the
   currently-configured global avatar (or the "no avatar selected" empty state if
   none is set).

4. TRANSPARENT TOGGLE (the bug): Check "Transparent floating background" in the
   Floating Widget preview -> background goes transparent. UNCHECK it -> the
   background color returns IMMEDIATELY, WITHOUT needing to drag/zoom the preview.
   Also confirm the avatar still renders and orbit-drag still works after toggling.

5. PURPLE ACCENTS: All range sliders and checkboxes on the page render with a
   purple (#6929ff) thumb/fill instead of the browser-default blue.

6. MOCK CHAT: The Floating Widget live preview shows a static chat transcript
   (2-3 example messages, purple user bubble + lavender assistant bubbles) below
   the avatar canvas, visually matching the real floating widget chat.

7. NO REGRESSION (block editor): Open a post/page in the block editor, add/select
   the Khavee avatar block, and confirm its sidebar live preview still renders and
   orbits normally (the item-4 fix lives in the shared PreviewScene component).
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 checks pass, or describe which item(s) failed.</resume-signal>
</task>

</tasks>

<verification>
- `php -l wordpress-plugin/includes/Admin/SettingsPage.php` is clean.
- `php wordpress-plugin/tests/settings-page-harness.php` exits 0 (updated item-2 coverage).
- `php wordpress-plugin/tests/platform-config-harness.php` exits 0.
- `php wordpress-plugin/tests/render-logic-harness.php` exits 0.
- `pnpm --filter @khaveeai/wp-bundle build` completes with no errors (if PreviewScene.tsx/mount.tsx touched); rebuilt build/ artifacts staged.
- Human-verify checkpoint: all 6 items + block-editor no-regression confirmed live.
</verification>

<success_criteria>
All six user-reported items resolved on the live Settings page; the remove-key
behavior change preserves every prior masked-placeholder / format-rejection
guarantee with updated (not deleted) harness coverage; the transparent-toggle fix
is backed by a confirmed root-cause statement and scoped so it does not defeat the
preview's no-teardown re-render design; no regression to the Gutenberg block
sidebar preview or any block-attribute config path.
</success_criteria>

<output>
Update STATE.md's "Quick Tasks Completed" table with this task (260707-0u6) on
completion, and create
`.planning/quick/260707-0u6-settings-page-fixes-batch/260707-0u6-SUMMARY.md`
including the item-4 root-cause statement.
</output>
