---
phase: quick-260708-1ws
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/wp-bundle/src/preview/PreviewAvatarCanvas.tsx
  - packages/wp-bundle/src/preview/PreviewChatBox.tsx
  - packages/wp-bundle/src/preview/PreviewFloatingWidget.tsx
  - packages/wp-bundle/src/preview/PreviewScene.tsx
  - packages/wp-bundle/src/config.ts
  - wordpress-plugin/includes/Admin/SettingsPage.php
  - wordpress-plugin/build/khaveeai-preview.js
autonomous: false
requirements:
  - WS-01  # Floating preview reuses the real widget's exact CSS classes (khaveeai-floating-panel/header/avatar-area/chat)
  - WS-02  # Preview structurally matches real widget: header + fixed 200px avatar area + flexed chat
  - WS-03  # STUDIO-02 safety preserved (no useRealtime/getUserMedia/token endpoint/RealtimeProvider in preview bundle)
  - WS-04  # Avatar-section preview and Gutenberg block editor preview keep their current plain-avatar layout, unaffected

must_haves:
  truths:
    - "The Floating Widget preview on the Settings page renders a .khaveeai-floating-panel containing a purple .khaveeai-floating-header (title 'AI Assistant' + subtitle 'Usually replies instantly' + close button), a fixed 200px-tall .khaveeai-floating-avatar-area with the live avatar canvas, and a .khaveeai-floating-chat slot filling the remaining ~256px"
    - "The avatar area shows a static 'Click to talk' pill (khaveeai-cta-button) that is visually identical to the real widget's idle CTA but does nothing on click"
    - "The chat slot renders a PreviewChatBox with static example bubbles (matching the prior PHP mock chat exchange) using the same .khaveeai-chat* classes"
    - "The preview bundle still never references RealtimeProvider/getUserMedia/ephemeral — the build-time STUDIO-02 safety assertion passes"
    - "The Avatar-section preview and the Gutenberg block editor preview are visually unchanged (still plain-avatar layout) because previewMode is only 'floating' for the floating mount"
  artifacts:
    - path: "packages/wp-bundle/src/preview/PreviewFloatingWidget.tsx"
      provides: "Preview-safe floating widget layout mirroring FloatingWidget.tsx structure/classes"
      min_lines: 40
    - path: "packages/wp-bundle/src/preview/PreviewAvatarCanvas.tsx"
      provides: "Reusable preview avatar Canvas extracted from PreviewSceneInner (Canvas/camera/orbit/lighting/VRMAvatar/empty-state/talking-pill)"
      min_lines: 30
    - path: "packages/wp-bundle/src/preview/PreviewScene.tsx"
      provides: "Branches to PreviewFloatingWidget when config.previewMode === 'floating'"
      contains: "previewMode"
    - path: "wordpress-plugin/includes/Admin/SettingsPage.php"
      provides: "Floating mount config emits previewMode:'floating'; mock-chat PHP method removed"
      contains: "'floating'"
  key_links:
    - from: "SettingsPage.php render_floating_preview_mount() config array"
      to: "PreviewScene.tsx previewMode branch"
      via: "previewMode: 'floating' JSON key"
      pattern: "previewMode"
    - from: "SettingsPage.php inline rebuild() cfg object"
      to: "mount div data-khaveeai-preview-config"
      via: "previewMode: 'floating' preserved on every field change"
      pattern: "previewMode"
    - from: "PreviewFloatingWidget.tsx"
      to: "styles.css .khaveeai-floating-* rules"
      via: "reused CSS class names (no new classes invented)"
      pattern: "khaveeai-floating-panel"
---

<objective>
Rebuild the WordPress Settings page's Floating Widget live preview so it structurally mirrors the real front-end `FloatingWidget.tsx` — same CSS classes (`.khaveeai-floating-panel` / `-header` / `-avatar-area` / `-chat`), same header + fixed-200px-avatar + flexed-chat composition — instead of the current ad-hoc full-height avatar canvas plus a separately-appended static PHP mock chat.

Purpose: The preview currently gives the avatar canvas the full 520px panel height and appends a disconnected PHP mock chat below it, so it does not look like the real widget. Building the preview from the SAME CSS classes as the real widget means future style changes to the real widget automatically flow into the preview — the two can never drift apart.

Output: A new preview-safe `PreviewFloatingWidget` component (mirroring the real widget's JSX with a preview-safe avatar canvas and a static, non-functional "Click to talk" pill), wired into `PreviewScene` via a `previewMode: "floating"` config flag emitted only by the floating mount. The STUDIO-02 no-realtime safety guarantee is preserved. The Avatar-section preview and Gutenberg block editor preview are untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260708-1ws-floating-preview-real-widget-parity/260708-1ws-CONTEXT.md
@packages/wp-bundle/src/floating/FloatingWidget.tsx
@packages/wp-bundle/src/preview/PreviewScene.tsx
@packages/wp-bundle/src/preview/PreviewChatBox.tsx
@packages/wp-bundle/src/preview/mountPreview.tsx
@packages/wp-bundle/src/ui/ClickToTalkOverlay.tsx

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase — no exploration required. -->

Real widget structure (packages/wp-bundle/src/floating/FloatingWidget.tsx) — mirror this JSX, but preview-safe:
  <div className="khaveeai-floating-panel">
    <div className="khaveeai-floating-header">
      <div>
        <div className="khaveeai-floating-header-title">AI Assistant</div>
        <div className="khaveeai-floating-header-sub">Usually replies instantly</div>
      </div>
      <button className="khaveeai-floating-close" aria-label="Minimize"> <svg .../> </button>
    </div>
    <div className="khaveeai-floating-avatar-area" style={{ background: ... }}>
      {avatar} <ClickToTalkOverlay/> <ErrorOverlay/> <ControlBar .../>
    </div>
    <div className="khaveeai-floating-chat"> <ChatBox placement="below"/> </div>
  </div>
  (real widget ALSO has the .khaveeai-floating-launcher button + isOpen toggle — OMIT both in the preview; always expanded.)

Real CSS already present in packages/wp-bundle/styles.css (do NOT add new classes):
  .khaveeai-floating-panel { width:360px; height:520px; display:flex; flex-direction:column; overflow:hidden; }
  .khaveeai-floating-header { flex-shrink:0; background:#6929ff; ... }
  .khaveeai-floating-avatar-area { flex-shrink:0; height:200px; position:relative; overflow:hidden; }
  .khaveeai-floating-avatar-area canvas { display:block; width:100%; height:100%; }   ← gives the Canvas its 200px box
  .khaveeai-floating-chat { flex:1; min-height:0; display:flex; flex-direction:column; overflow:hidden; }
  .khaveeai-floating-chat .khaveeai-chat--below { width:100%; height:100%; max-height:none; margin:0; border-radius:0; }
  .khaveeai-overlay { position:absolute; inset:0; display:flex; align-items:center; justify-content:center; }
  .khaveeai-cta-button { border-radius:999px; background:#6929ff; color:#fff; ... }  ← static CTA reuses these two

Static CTA stand-in target markup (mirror ClickToTalkOverlay.tsx's idle branch WITHOUT useRealtime):
  <div className="khaveeai-overlay">
    <button type="button" className="khaveeai-cta-button">Click to talk</button>
  </div>

PreviewScene.tsx current avatar-area block to extract (PreviewSceneInner, ~lines 302-410): the <Canvas> with
  CameraController, CameraRefCapture, OrbitControls (onEnd={handleOrbitEnd}, makeDefault), ambient/directional
  lights, the VRMAvatar (src/position/scale/animations), the "No avatar selected" empty state, and the
  "Preview talking" pill. It depends on: sceneDefaults (resolveSceneDefaults(config)), liveCameraRef,
  handleOrbitEnd, isTalking, positionTuple, scaleTuple. NOTE: the fragment sets NO width/height itself — the
  PARENT provides the box (containerStyle div for plain mode, .khaveeai-floating-avatar-area for floating).

config.ts KhaveeAvatarConfig already has: containerWidth, containerHeight, chatShow, chatPlacement, floating,
  avatarUrl, bgColor, bgTransparent, avatarScale, avatarOffsetX/Y, cameraRotationY. PreviewAvatarConfig (in
  PreviewScene.tsx) extends it with previewTalking?: boolean. Add previewMode?: "floating" to the preview-only
  extension (PreviewAvatarConfig), NOT to the shared published-page config type.

SettingsPage.php floating mount (render_floating_preview_mount(), ~lines 2125-2174): emits the initial JSON
  config, then calls render_floating_preview_mock_chat() (~lines 2202-2222) which prints the static 3-bubble
  chat. The inline rebuild() JS (~lines 358-377) reconstructs the cfg object on every field change and writes
  it back to #khaveeai-floating-preview's data-khaveeai-preview-config.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract reusable PreviewAvatarCanvas and extend PreviewChatBox with example bubbles</name>
  <files>packages/wp-bundle/src/preview/PreviewAvatarCanvas.tsx, packages/wp-bundle/src/preview/PreviewScene.tsx, packages/wp-bundle/src/preview/PreviewChatBox.tsx</files>
  <action>
Create `PreviewAvatarCanvas.tsx` by lifting the avatar-area render block out of `PreviewScene.tsx`'s `PreviewSceneInner` (the `avatarArea` fragment: the `<Canvas>` with `CameraController`, `CameraRefCapture`, `OrbitControls` (`onEnd={handleOrbitEnd}`, `makeDefault`), ambient + directional lights, the `VRMAvatar`, the "No avatar selected" empty state, and the "Preview talking" pill). Move `CameraController`, `CameraRefCapture`, `usePreviewTalking`, and the VISEME constants into this file too (they are only used by the avatar canvas). Export `PreviewAvatarCanvas({ config, onCameraAngleChange })` which internally computes `sceneDefaults = resolveSceneDefaults(config)`, `isTalking`, `positionTuple`, `scaleTuple`, owns `liveCameraRef` + `handleOrbitEnd`, calls `usePreviewTalking(isTalking)`, and returns the same fragment. Keep the STUDIO-02 file-header safety comment (adapt it) — this file must NOT import any realtime provider, must NOT call useRealtime/getUserMedia. It renders inside a parent that supplies the box dimensions, so it sets NO width/height itself (the fragment stays dimension-agnostic exactly as today).

Then refactor `PreviewScene.tsx`: `PreviewSceneInner` now renders `<PreviewAvatarCanvas config={config} onCameraAngleChange={onCameraAngleChange} />` in place of the inlined `avatarArea` for BOTH the `chatShow` layout branch (inside the `khaveeai-layout` flex, wrapped in the same `position:relative; flex:1` div) and the no-chat branch. Preserve the existing `containerStyle` (dimensions + background) computation and the plain-layout structure exactly — this is a pure extraction, the plain/Avatar-section/block-editor preview output must be byte-for-byte equivalent. `usePreviewTalking` moved out means `PreviewSceneInner` no longer calls `useVRMExpressions()` directly — that hook now lives inside `PreviewAvatarCanvas`, which still renders inside `<KhaveeProvider>`, so the rules-of-hooks constraint is satisfied.

Extend `PreviewChatBox.tsx`: add an optional prop `exampleMessages?: Array<{ role: "assistant" | "user"; text: string }>`. When provided (and non-empty), render `.khaveeai-chat__header` ("AI Assistant") + a `.khaveeai-chat__transcript` containing `.khaveeai-chat__bubble .khaveeai-chat__bubble--assistant` / `--user` bubbles (matching the prior PHP mock chat markup and ChatBox.tsx nesting). When absent, render the existing static "Click the avatar to start, then type here." disconnected body unchanged (preserves current Avatar-section behavior — but note Avatar section does not set chatShow, so this branch is only exercised by future callers). Keep the `.khaveeai-chat khaveeai-chat--{placement} khaveeai-chat--ready` outer wrapper for both branches. Reuse existing CSS classes only — add no new classes.
  </action>
  <verify>
    <automated>cd packages/wp-bundle && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>PreviewAvatarCanvas.tsx exists and is rendered by PreviewSceneInner in both layout branches; PreviewChatBox accepts exampleMessages and renders bubbles when provided; TypeScript compiles clean; the plain-layout preview render path is structurally unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build PreviewFloatingWidget and wire previewMode:'floating' into PreviewScene</name>
  <files>packages/wp-bundle/src/preview/PreviewFloatingWidget.tsx, packages/wp-bundle/src/preview/PreviewScene.tsx, packages/wp-bundle/src/config.ts</files>
  <action>
Create `PreviewFloatingWidget.tsx` mirroring `FloatingWidget.tsx`'s JSX but preview-safe and always-expanded. Structure (reuse the real `.khaveeai-floating-*` CSS classes verbatim — invent NO new classes):
  - Outer `.khaveeai-floating-panel` ONLY (do NOT render `.khaveeai-floating-widget` wrapper, the `data-open` toggle, or the `.khaveeai-floating-launcher` button — per locked CONTEXT decision, always show the panel expanded, no launcher/collapse).
  - `.khaveeai-floating-header` with `<div>` containing `.khaveeai-floating-header-title` "AI Assistant" and `.khaveeai-floating-header-sub` "Usually replies instantly", plus the `.khaveeai-floating-close` button with the same X `<svg>` — render it for visual fidelity but make it non-functional (no onClick, or `onClick={() => {}}`; there is no collapsed state to return to).
  - `.khaveeai-floating-avatar-area` with the same `style={{ background: config.floatingBgTransparent ? "transparent" : (config.floatingBgColor ?? config.bgColor ?? "#6929ff") }}` intent. Note the floating mount currently maps its fields onto the GENERIC keys (bgColor/bgTransparent/avatarScale/...) — so read from those generic keys (bgColor/bgTransparent) here, matching what PHP emits. Inside: render `<PreviewAvatarCanvas config={config} onCameraAngleChange={onCameraAngleChange} />` (the avatar Canvas gets its 200px box from `.khaveeai-floating-avatar-area canvas { height:100% }`), followed by a STATIC CTA stand-in: `<div className="khaveeai-overlay"><button type="button" className="khaveeai-cta-button">Click to talk</button></div>` (NON-functional — no useRealtime, no onClick that connects; the button may have no handler at all). Do NOT render the real ClickToTalkOverlay/ErrorOverlay/ControlBar (all call useRealtime — STUDIO-02 violation).
  - `.khaveeai-floating-chat` containing `<PreviewChatBox placement="below" exampleMessages={FLOATING_EXAMPLE_MESSAGES} />` where FLOATING_EXAMPLE_MESSAGES is the prior 3-bubble exchange: assistant "Hi! How can I help you today?", user "What are your opening hours?", assistant "We're open 9am to 6pm, Monday to Friday." (preserves the deliberate content from quick task 260707-0u6).

Signature: `PreviewFloatingWidget({ config, onCameraAngleChange }: { config: PreviewAvatarConfig; onCameraAngleChange?: (deg: number) => void })`. Keep a STUDIO-02 safety file-header comment documenting it never calls useRealtime/getUserMedia and reuses the real widget's exact CSS classes.

In `config.ts`: no change needed if PreviewAvatarConfig lives in PreviewScene.tsx — instead add `previewMode?: "floating"` to the `PreviewAvatarConfig` interface in `PreviewScene.tsx` (the preview-only extension), NOT to the shared `KhaveeAvatarConfig` in config.ts. (If PreviewAvatarConfig needs importing by PreviewFloatingWidget/PreviewAvatarCanvas, export it from PreviewScene.tsx or move it into config.ts's preview section — implementer's call, but keep previewMode OUT of the published-page config transport.)

Wire `PreviewScene.tsx`: in `PreviewSceneInner` (or `PreviewScene`), when `config.previewMode === "floating"`, render `<PreviewFloatingWidget config={config} onCameraAngleChange={onCameraAngleChange} />` INSIDE the existing `<KhaveeProvider>` (PreviewAvatarCanvas + PreviewChatBox both need the provider context), and return early — do NOT apply the plain `containerStyle` dimensions or the `khaveeai-layout` wrapper in this mode (the `.khaveeai-floating-panel` CSS supplies fixed 360x520 sizing). Otherwise fall through to the existing plain layout unchanged.
  </action>
  <verify>
    <automated>cd packages/wp-bundle && npx tsc --noEmit -p tsconfig.json && grep -L "useRealtime\|getUserMedia\|RealtimeProvider\|ClickToTalkOverlay\|ErrorOverlay\|ControlBar" src/preview/PreviewFloatingWidget.tsx</automated>
  </verify>
  <done>PreviewFloatingWidget.tsx renders the panel/header/200px-avatar-area/chat structure with a static CTA pill and example-bubble chat, never importing useRealtime/getUserMedia/RealtimeProvider or the realtime overlay components; PreviewScene branches to it when previewMode==='floating'; TypeScript compiles clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire floating mode in SettingsPage.php and remove dead PHP mock-chat</name>
  <files>wordpress-plugin/includes/Admin/SettingsPage.php</files>
  <action>
In `render_floating_preview_mount()` (~lines 2125-2174): add `'previewMode' => 'floating'` to the `$config` array. Remove the `'containerWidth' => 360` / `'containerHeight' => 520` keys from this floating config — in floating mode PreviewScene no longer applies them (the `.khaveeai-floating-panel` + `.khaveeai-floating-avatar-area` CSS classes now fully determine sizing: fixed 360x520 panel with a 200px avatar area). Leave the mount `<div>` element's own inline `style="width:360px;height:520px;border:...;border-radius:20px;overflow:hidden;"` as-is so the React panel has a correctly-sized host (the panel fills it). Delete the `$this->render_floating_preview_mock_chat();` call at the end of the method (chat now renders inside React).

Delete the entire `render_floating_preview_mock_chat()` method (~lines 2176-2222) — it is now dead code (its 3-bubble content moved into PreviewFloatingWidget via PreviewChatBox's exampleMessages).

In the inline `rebuild()` JS `cfg` object (~lines 366-375): add `previewMode: 'floating'` so every field change (color/transparency/scale/offset/rotation) preserves the floating layout instead of reverting the mount to the plain-avatar layout on the first interaction. Do NOT re-add containerWidth/containerHeight here (they are unused in floating mode). Keep all existing cfg keys and the rest of the rebuild wiring untouched.
  </action>
  <verify>
    <automated>cd wordpress-plugin && php -l includes/Admin/SettingsPage.php && grep -c "render_floating_preview_mock_chat" includes/Admin/SettingsPage.php | grep -qx "0" && grep -c "previewMode" includes/Admin/SettingsPage.php | grep -qvx "0"</automated>
  </verify>
  <done>SettingsPage.php passes PHP lint; render_floating_preview_mock_chat is fully removed (0 references); both the PHP config array and the inline rebuild() cfg emit previewMode:'floating'; the floating mock-chat call is gone.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Rebuild preview bundle and confirm STUDIO-02 safety assertion</name>
  <files>wordpress-plugin/build/khaveeai-preview.js</files>
  <action>
Run the wp-bundle build (`pnpm --filter @khaveeai/wp-bundle build`, or `cd packages/wp-bundle && node build.mjs`) to recompile `wordpress-plugin/build/khaveeai-preview.js` (and khaveeai-preview.css) with the new PreviewFloatingWidget/PreviewAvatarCanvas source. The build's own STUDIO-02 grep assertion (build.mjs) fails the build if the preview output matches `/RealtimeProvider|getUserMedia|ephemeral/` — it must print "Safety assertion passed for khaveeai-preview.js." No CSS changes are expected (all `.khaveeai-floating-*`, `.khaveeai-overlay`, `.khaveeai-cta-button`, `.khaveeai-chat*` classes already exist in styles.css and are imported by preview.ts), so the compiled khaveeai-preview.css should already carry them.

Also confirm the source-level STUDIO-02 grep: none of the preview source files (`src/preview.ts`, `src/preview/*.tsx`) reference `useRealtime`, `getUserMedia`, `RealtimeProvider`, or the realtime overlay components.
  </action>
  <verify>
    <automated>cd packages/wp-bundle && node build.mjs 2>&1 | grep -q "Safety assertion passed" && ! grep -REl "useRealtime|getUserMedia|RealtimeProvider|ClickToTalkOverlay|ErrorOverlay|ControlBar" src/preview.ts src/preview/</automated>
  </verify>
  <done>node build.mjs completes with "Safety assertion passed for khaveeai-preview.js"; khaveeai-preview.js is regenerated; no preview source file references any realtime/useRealtime/getUserMedia symbol; the STUDIO-02 no-realtime guarantee is intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Rebuilt the Floating Widget live preview on the WordPress Settings page so it structurally matches the real front-end FloatingWidget: a `.khaveeai-floating-panel` with a purple header ("AI Assistant" + "Usually replies instantly" + close button), a FIXED 200px `.khaveeai-floating-avatar-area` showing the live avatar with a static (non-functional) "Click to talk" pill, and a `.khaveeai-floating-chat` slot filling the rest with example chat bubbles. The old PHP full-height canvas + separately-appended static mock chat were replaced. STUDIO-02 safety (no realtime/mic/token) is preserved and build-verified.
  </what-built>
  <how-to-verify>
NOTE FOR EXECUTOR: This step requires a live browser in wp-env — the executor does not have browser tooling. Stop here and hand off to the orchestrator, who has Chrome browser automation and will perform this verification directly.

Verification steps (orchestrator):
1. Ensure wp-env is running and open http://localhost:8888/wp-admin/admin.php?page=khaveeai-settings
2. Scroll to the Floating Widget section's Live preview. Confirm it now shows ONE cohesive 360x520 panel:
   - A purple header bar at top: "AI Assistant" title, "Usually replies instantly" subtitle, and an X close button on the right.
   - Directly below the header: a ~200px-tall avatar area with the VRM avatar rendered and a centered "Click to talk" pill button (clicking it does nothing — that is correct/intended).
   - Below the avatar area: the chat region filling the remaining ~256px, showing the "AI Assistant" chat header and the 3 example bubbles ("Hi! How can I help you today?" / "What are your opening hours?" / "We're open 9am to 6pm, Monday to Friday.").
3. Confirm there is NO separate loose mock-chat block appended below the panel (the old layout), and NO full-height avatar canvas filling the whole 520px.
4. Change a floating field (e.g. background color, avatar scale, or camera-angle drag) and confirm the preview updates AND keeps the header+200px-avatar+chat structure (does not revert to a plain full-height avatar).
5. Confirm the Avatar-section preview (higher on the page) is UNCHANGED — still a plain avatar-only box, no floating header/chat chrome.
  </how-to-verify>
  <resume-signal>Type "approved" once the floating preview matches the real widget's header + 200px avatar + flexed chat structure, or describe any visual/structural mismatch.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| preview bundle → realtime provider | The STUDIO-02 safety guarantee: the preview bundle must never reach useRealtime/OpenAIRealtimeProvider/getUserMedia/the token endpoint. This change adds new preview components in that bundle and is the primary security-relevant surface. |
| admin settings page → preview mount config (JSON) | PHP-emitted `data-khaveeai-preview-config` JSON is parsed by the browser; no untrusted user-to-user input crosses here (admin-only, escaped via wp_json_encode/esc_attr). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1ws-01 | Elevation | New preview components (PreviewFloatingWidget, PreviewAvatarCanvas, static CTA) accidentally importing a realtime symbol | mitigate | Static CTA is a plain non-functional button (no useRealtime); real ClickToTalkOverlay/ErrorOverlay/ControlBar are NOT imported; enforced by build.mjs grep assertion (/RealtimeProvider|getUserMedia|ephemeral/) + Task 2/4 source grep (Task 4 automated verify). |
| T-1ws-02 | Tampering | Admin-supplied floating config values (bgColor, scale) rendered into preview | accept | Admin-only screen; values are numeric/color strings escaped by wp_json_encode/esc_attr in PHP and consumed as React props (no dangerouslySetInnerHTML). Low-value, no cross-user surface. |
| T-1ws-03 | Denial of Service | WebGL context churn from preview re-render | accept | Reuses the existing MutationObserver single-context pattern (mountPreview.tsx) unchanged; no new Canvas remount introduced (PreviewAvatarCanvas mounts once per mount div). |
</threat_model>

<verification>
- `npx tsc --noEmit` clean in packages/wp-bundle (Tasks 1-2).
- `php -l` clean on SettingsPage.php; `render_floating_preview_mock_chat` fully removed; `previewMode` present in both PHP config and rebuild() JS (Task 3).
- `node build.mjs` prints "Safety assertion passed for khaveeai-preview.js"; no preview source references useRealtime/getUserMedia/RealtimeProvider (Task 4).
- Live human verification in wp-env: floating preview shows header + 200px avatar + flexed chat, static CTA, example bubbles; Avatar section + block editor previews unchanged (checkpoint).
</verification>

<success_criteria>
- The Floating Widget preview is built from the real widget's exact `.khaveeai-floating-*` CSS classes (no new classes) and renders header + fixed 200px avatar area + flexed chat.
- The avatar area shows the live avatar plus a static, non-functional "Click to talk" pill.
- The chat slot renders example bubbles via an extended PreviewChatBox.
- The old PHP `render_floating_preview_mock_chat()` and the full-height-canvas layout are gone.
- STUDIO-02 safety preserved: preview bundle never references realtime/getUserMedia; build assertion passes.
- Avatar-section preview and Gutenberg block editor preview remain on the plain-avatar layout (previewMode gates the new layout to the floating mount only).
</success_criteria>

<output>
Create `.planning/quick/260708-1ws-floating-preview-real-widget-parity/260708-1ws-SUMMARY.md` when done.
</output>
