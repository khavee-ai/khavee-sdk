---
phase: quick-260708-nh0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/wp-bundle/src/floating/FloatingWidget.tsx
  - packages/wp-bundle/src/preview/PreviewFloatingWidget.tsx
  - packages/wp-bundle/styles.css
  - wordpress-plugin/build/khaveeai-bundle.js
  - wordpress-plugin/build/khaveeai-bundle.css
  - wordpress-plugin/build/khaveeai-preview.js
  - wordpress-plugin/build/khaveeai-preview.css
autonomous: false
requirements:
  - NH0-01  # Remove "Usually replies instantly" subtitle from the floating widget header
  - NH0-02  # Make the header visually thinner (reduced padding)

must_haves:
  truths:
    - "The real front-end floating widget's header no longer shows 'Usually replies instantly'"
    - "The Settings-page floating preview's header (which mirrors the real widget) also no longer shows the subtitle, since both must stay in sync"
    - "The header is visually thinner than before (reduced vertical padding) in both the real widget and the preview"
  artifacts:
    - path: "packages/wp-bundle/src/floating/FloatingWidget.tsx"
      provides: "Header with subtitle div removed"
    - path: "packages/wp-bundle/src/preview/PreviewFloatingWidget.tsx"
      provides: "Header with subtitle div removed (mirrors FloatingWidget.tsx)"
    - path: "packages/wp-bundle/styles.css"
      provides: "Reduced .khaveeai-floating-header padding; dead .khaveeai-floating-header-sub rule removed"
  key_links:
    - from: "FloatingWidget.tsx header markup"
      to: "PreviewFloatingWidget.tsx header markup"
      via: "identical structure — both must change together since the preview mirrors the real widget's classes verbatim"
      pattern: "khaveeai-floating-header-title"
---

<objective>
Two small visual tweaks to the floating widget's header, requested directly by the user: (1) remove the "Usually replies instantly" subtitle line entirely, (2) make the header thinner (reduce vertical padding).

Since quick task 260708-1ws just made the Settings-page preview (`PreviewFloatingWidget.tsx`) mirror the real widget (`FloatingWidget.tsx`) using the exact same CSS classes and structure, BOTH files must be edited together — editing only one would immediately break the parity that task just established.

Output: `.khaveeai-floating-header-title` remains (just the "AI Assistant" title, no subtitle below it); `.khaveeai-floating-header-sub` div removed from both component files; the CSS rule for `.khaveeai-floating-header-sub` removed (dead once no consumer renders it); `.khaveeai-floating-header`'s padding reduced for a visually thinner bar. Rebuilt bundle artifacts for both the real widget (`khaveeai-bundle.js/css`) and the preview (`khaveeai-preview.js/css`), since `FloatingWidget.tsx` is part of the live front-end bundle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md

<interfaces>
<!-- Current header markup, IDENTICAL in both files (FloatingWidget.tsx and
     PreviewFloatingWidget.tsx) — must be edited in both, in the same way: -->
<!--
  <div className="khaveeai-floating-header">
    <div>
      <div className="khaveeai-floating-header-title">AI Assistant</div>
      <div className="khaveeai-floating-header-sub">
        Usually replies instantly
      </div>
    </div>
    <button type="button" className="khaveeai-floating-close" ...>
      <svg ...>...</svg>
    </button>
  </div>
-->

<!-- Current CSS, packages/wp-bundle/styles.css lines 479-498: -->
<!--
  .khaveeai-floating-header {
    flex-shrink: 0;
    padding: 16px 18px;
    background: #6929ff;
    color: #ffffff;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .khaveeai-floating-header-title {
    font-size: 15px;
    font-weight: 600;
  }

  .khaveeai-floating-header-sub {
    font-size: 12px;
    opacity: 0.85;
    margin-top: 2px;
  }
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove subtitle + thin the header in both widget files and CSS (NH0-01, NH0-02)</name>
  <files>packages/wp-bundle/src/floating/FloatingWidget.tsx, packages/wp-bundle/src/preview/PreviewFloatingWidget.tsx, packages/wp-bundle/styles.css</files>
  <action>
In BOTH `packages/wp-bundle/src/floating/FloatingWidget.tsx` and `packages/wp-bundle/src/preview/PreviewFloatingWidget.tsx`, remove the `<div className="khaveeai-floating-header-sub">Usually replies instantly</div>` line from the header markup. The outer wrapping `<div>` around the title (currently wrapping both title and subtitle) may be simplified/removed if it no longer serves a purpose once only the title remains — implementer's call, but keep the `khaveeai-floating-header-title` div itself and the sibling close `<button>` unchanged.

In `packages/wp-bundle/styles.css`:
- Reduce `.khaveeai-floating-header`'s padding from `16px 18px` to `12px 18px` (thinner bar, keeps horizontal padding unchanged).
- Remove the now-dead `.khaveeai-floating-header-sub` rule entirely (no longer rendered by either component).

Do NOT touch `.khaveeai-floating-header-title`, `.khaveeai-floating-close`, `.khaveeai-floating-panel`, `.khaveeai-floating-avatar-area`, `.khaveeai-floating-chat`, or any other floating-widget CSS rule — this is scoped strictly to the header subtitle removal and its padding.
  </action>
  <verify>
    <automated>cd packages/wp-bundle && npx tsc --noEmit -p tsconfig.json && ! grep -q "khaveeai-floating-header-sub" src/floating/FloatingWidget.tsx src/preview/PreviewFloatingWidget.tsx styles.css && echo "subtitle removed from both files + CSS"</automated>
  </verify>
  <done>`tsc --noEmit` clean; neither component file nor styles.css references `khaveeai-floating-header-sub` anywhere; `.khaveeai-floating-header`'s padding is `12px 18px`.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild bundle artifacts and confirm STUDIO-02 safety assertion</name>
  <files>wordpress-plugin/build/khaveeai-bundle.js, wordpress-plugin/build/khaveeai-bundle.css, wordpress-plugin/build/khaveeai-preview.js, wordpress-plugin/build/khaveeai-preview.css</files>
  <action>
Build the dependency chain then wp-bundle: `pnpm --filter @khaveeai/core build && pnpm --filter @khaveeai/react build && pnpm --filter @khaveeai/providers-openai-realtime build && pnpm --filter @khaveeai/wp-bundle build` (or `cd packages/wp-bundle && node build.mjs` if the workspace packages are already built). Confirm the build's own STUDIO-02 grep assertion prints "Safety assertion passed for khaveeai-preview.js." — this change only touches header markup/CSS, so it should not affect that assertion at all, but the build must still complete cleanly. Both `khaveeai-bundle.*` (real widget, used on the live front-end) and `khaveeai-preview.*` (Settings-page preview) must be regenerated since `FloatingWidget.tsx` feeds the former and `PreviewFloatingWidget.tsx` feeds the latter.
  </action>
  <verify>
    <automated>cd packages/wp-bundle && node build.mjs 2>&1 | grep -q "Safety assertion passed"</automated>
  </verify>
  <done>wp-bundle build completes; STUDIO-02 safety assertion passes; all four build artifacts (khaveeai-bundle.js/css, khaveeai-preview.js/css) are regenerated and contain no "Usually replies instantly" string.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Removed the "Usually replies instantly" subtitle from the floating widget's header (both the real front-end widget and the Settings-page preview, which must stay in sync) and reduced the header's vertical padding for a visually thinner bar.
  </what-built>
  <how-to-verify>
NOTE FOR EXECUTOR: This requires a live browser in wp-env — stop here and hand off to the orchestrator, who has Chrome browser automation.

Verification steps (orchestrator):
1. In wp-env at http://localhost:8888/wp-admin/admin.php?page=khaveeai-settings (hard-refresh), scroll to the Floating Widget preview. Confirm the header now shows ONLY "AI Assistant" (no "Usually replies instantly" subtitle below it), and the header bar is visibly thinner/more compact than before.
2. If a live front-end page with the floating widget enabled is reachable, confirm the same change there (the real widget, not just the preview) — same thin header, no subtitle.
3. Confirm the close (X) button and title remain properly aligned/centered in the thinner header (no layout breakage).
4. Confirm no other part of the panel (avatar area, chat) changed.
  </how-to-verify>
  <resume-signal>Type "approved" if the header is thinner with no subtitle in both the preview and (if checked) the real widget, or describe what still needs adjustment.</resume-signal>
</task>

</tasks>

<verification>
- `tsc --noEmit` clean in packages/wp-bundle
- Neither `FloatingWidget.tsx`, `PreviewFloatingWidget.tsx`, nor `styles.css` references `khaveeai-floating-header-sub`
- `node build.mjs` prints "Safety assertion passed for khaveeai-preview.js"; both bundle artifact pairs regenerated
- Live human verification confirms the thinner, subtitle-free header in the preview (and the real widget if reachable)
</verification>

<success_criteria>
- Floating widget header (real + preview) no longer shows "Usually replies instantly"
- Header is visually thinner (reduced padding) in both real widget and preview
- No layout breakage to the close button/title alignment
- No unrelated changes to avatar area or chat panel
</success_criteria>

<output>
Create `.planning/quick/260708-nh0-thin-header-remove-subtitle/260708-nh0-SUMMARY.md` when done.
</output>
