---
status: complete
---

# Quick Task 260709-p3k: Block Editor Preview Platform/Global-Default Fallback

See 260709-p3k-SUMMARY.md for full root cause and fix detail.

## Scope

- `wordpress-plugin/src/editor.js`: merge `window.khaveeaiGlobalConfig` (already localized, `Plugin.php:210-214`) into the preview config for `avatarUrl`/`bgType`/`bgColor`/`bgTransparent` when the block's own attribute is blank/global-default-sentinel.
- No new REST endpoint — `window.khaveeaiGlobalConfig` already carries the platform-overlaid `get_runtime_config()` output; a planned `PlatformDefaultsController` REST route was built, verified redundant against this, and reverted.
