---
quick_id: 260712-qo9
type: quick
files_modified:
  - src/app/openai-avatar-test/page.tsx
autonomous: true
---

<objective>
Fix `src/app/openai-avatar-test/page.tsx` (added in quick task 260712-mfz) so `VRMAvatar` actually animates. It was mounted with no `animations` prop, and `resolveBaseClip()` (`packages/react/src/animation/AnimationStateEngine.ts:35-45`) returns `currentAnimation ?? availableNames[0] ?? null` — with zero clips loaded, `availableNames` is empty and this always evaluates to `null`. `useAnimationController`'s effect (`AnimationStateEngine.ts:92-107`) then does nothing (`if (!targetName || !toAction) return;`), so the avatar sits frozen in its static VRM bind pose regardless of `chatStatus`. This was reported live by the user: "uh i see no animation here."

Purpose: The whole point of this demo page is to visually verify chatStatus-driven crossfades and lipsync against a real conversation — with no clips loaded there is nothing to crossfade between, defeating the page's purpose.
Output: `src/app/openai-avatar-test/page.tsx` updated to load the same bundled Mixamo FBX fixtures the Phase 10 `vrm-avatar-test` page already uses successfully.
</objective>

<context>
Confirmed working reference: `src/app/vrm-avatar-test/page.tsx` passes `animations={{ idle: "/models/animations/Idle.fbx", talking: "/models/animations/talking.fbx", talking1: "/models/animations/talking1.fbx" }}` to `VRMAvatar`. These files are confirmed present at `public/models/animations/{Idle,talking,talking1}.fbx`.

`resolveBaseClip` (`packages/react/src/animation/AnimationStateEngine.ts:35-45`):
```ts
if (chatStatus === "speaking") {
  const talkClip = availableNames.find((name) => /talk|gesture|speak/i.test(name));
  if (talkClip) return talkClip;
}
return currentAnimation ?? availableNames[0] ?? null;
```
With the `idle`/`talking`/`talking1` clip set loaded: when `chatStatus !== "speaking"`, it falls through to `availableNames[0]` (`"idle"`, the first object key) as long as no `currentAnimation` override is active; when `chatStatus === "speaking"`, the regex matches `"talking"` (contains `/talk/i`) and that clip plays instead. This gives automatic idle <-> talking crossfading driven purely by live `chatStatus` transitions from the real OpenAI Realtime conversation — exactly what this demo page is for.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Load the bundled idle/talking/talking1 clips on the VRMAvatar mount</name>
  <files>src/app/openai-avatar-test/page.tsx</files>
  <read_first>
    - src/app/openai-avatar-test/page.tsx (file being fixed — current VRMAvatar mount has no animations prop)
    - src/app/vrm-avatar-test/page.tsx (working reference for the exact AnimationConfig shape and import pattern)
  </read_first>
  <action>
    In `src/app/openai-avatar-test/page.tsx`:
    1. Add `type AnimationConfig` to the existing `@khaveeai/react` import.
    2. Define a module-scope constant (mirroring `vrm-avatar-test`'s `VRM_TEST_ANIMATIONS`):
       ```ts
       const AVATAR_ANIMATIONS: AnimationConfig = {
         idle: "/models/animations/Idle.fbx",
         talking: "/models/animations/talking.fbx",
         talking1: "/models/animations/talking1.fbx",
       };
       ```
    3. Pass `animations={AVATAR_ANIMATIONS}` to the `<VRMAvatar>` element inside `Scene()`, alongside the existing `src="/models/male.vrm"` and `enableBlinking` props.
    Do not change anything else on the page — chat UI, provider config, and layout stay as-is.
  </action>
  <verify>
    <automated>cd /Users/whitemalt/Documents/khavee-sdk && grep -q "AVATAR_ANIMATIONS" src/app/openai-avatar-test/page.tsx && grep -q "Idle.fbx" src/app/openai-avatar-test/page.tsx && grep -q "animations={AVATAR_ANIMATIONS}" src/app/openai-avatar-test/page.tsx && pnpm exec tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `VRMAvatar` in `src/app/openai-avatar-test/page.tsx` is mounted with an `animations` prop referencing the bundled `Idle.fbx`/`talking.fbx`/`talking1.fbx` fixtures
    - `pnpm exec tsc --noEmit` (root Next app typecheck) exits 0, modulo the pre-existing unrelated `vitest` type error in `src/app/generic-demo/__tests__/roundtrip-audio-contract.test.ts`
  </acceptance_criteria>
  <done>VRMAvatar on /openai-avatar-test has clips loaded, so resolveBaseClip resolves a real target clip and useAnimationController's crossfade effect actually fires — the avatar shows an idle animation immediately on load and crossfades to a talking clip when chatStatus becomes "speaking" during a live conversation.</done>
</task>

</tasks>

<verification>
- `pnpm exec tsc --noEmit -p tsconfig.json` passes (excluding the pre-existing unrelated `vitest` error)
- `grep` gates confirm the animations prop and clip paths are present
- Manual: opening `/openai-avatar-test` should show the avatar idling immediately (not frozen in bind pose), and crossfade to a talking pose when the AI is speaking — this manual visual check is the user's follow-up, not scriptable here
</verification>

<success_criteria>
- The reported "no animation" defect is fixed: the avatar animates on page load and reacts to chatStatus changes during a live conversation
</success_criteria>

<output>
Create `.planning/quick/260712-qo9-fix-src-app-openai-avatar-test-page-tsx-/260712-qo9-SUMMARY.md` when done
</output>
