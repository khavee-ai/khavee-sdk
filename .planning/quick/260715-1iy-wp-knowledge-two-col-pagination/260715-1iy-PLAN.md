---
phase: quick-260715-1iy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wordpress-plugin/includes/Admin/SettingsPage.php
autonomous: false
requirements: []
must_haves:
  truths:
    - "The Knowledge Base card shows the add-document form on the left and the document list on the right, matching the Avatar/Floating Widget two-column layout"
    - "The document list shows at most PAGE_SIZE (8) documents per page with prev/next controls when there are more"
    - "A footer under the list reads 'N documents' reflecting the total fetched count"
    - "Add and delete still work and refresh the list to a valid page"
    - "All list/row content is still rendered via textContent (no innerHTML of server data)"
  artifacts:
    - path: "wordpress-plugin/includes/Admin/SettingsPage.php"
      provides: "Two-column KB card markup + paginated vanilla-JS list"
      contains: "khaveeai-settings__two-col"
  key_links:
    - from: "render_knowledge_base_field() markup"
      to: "$kb_js inline script"
      via: "shared element IDs (khaveeai-kb-list, khaveeai-kb-content, khaveeai-kb-metadata, khaveeai-kb-add, khaveeai-kb-status)"
      pattern: "khaveeai-kb-list"
    - from: "$kb_js loadList()"
      to: "GET /khaveeai/v1/knowledge-admin"
      via: "fetch with X-WP-Nonce and ?limit=100"
      pattern: "khaveeaiKb.root"
---

<objective>
Redesign the WordPress plugin's Knowledge Base admin card into a two-column layout (add-form left, document list right) with client-side pagination on the list, visually mirroring the Khavee Platform's "Saved Knowledge" UI within the plugin's no-framework / plain-vanilla-JS constraint.

Purpose: The current single-column KB manager grows into one long unpaginated scroll and does not match the Avatar/Floating Widget two-column card style already on the page. This makes it consistent and navigable.

Output: An updated `SettingsPage.php` — restructured `render_knowledge_base_field()` markup plus a paginated `$kb_js` inline script. No new files, no REST/backend changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/whitemalt/Documents/khavee-sdk-wp-knowledge/.planning/STATE.md
@/Users/whitemalt/Documents/khavee-sdk-wp-knowledge/CLAUDE.md

<interfaces>
Current relevant structure in wordpress-plugin/includes/Admin/SettingsPage.php:

- render_page() Knowledge Base card (~lines 1525-1530): today wraps the field in a
  `<table class="form-table"><tbody>` + render_form_table_row() call. The Avatar
  (~1539-1555) and Floating Widget (~1563-1589) cards instead put a `<table>` in the
  left slot and a `<div class="khaveeai-settings__preview-col">` in the right slot,
  both directly inside `<div class="khaveeai-settings__two-col">` — reuse THAT pattern.

- render_knowledge_base_field() (~2193-2239): emits the `knowledge_base_enabled`
  checkbox (a real settings input, name="<OPTION_NAME>[knowledge_base_enabled]"),
  then (only when platform_api_key is set) a `#khaveeai-kb-manager` div containing
  `#khaveeai-kb-list`, an add block (`#khaveeai-kb-content` textarea,
  `#khaveeai-kb-metadata` input, `#khaveeai-kb-add` button), and `#khaveeai-kb-status`.

- $kb_js inline script (~505-693): renders rows into #khaveeai-kb-list via textContent
  only, wires add/delete against khaveeaiKb.root (GET/POST/DELETE) with X-WP-Nonce.
  loadList() currently fetches khaveeaiKb.root with NO limit param → server defaults to 50.

- Server list route (KnowledgeAdminController::list_documents): `limit` query param,
  min((int)limit, MAX_LIMIT=100), default 50. Fetching `?limit=100` is the max and
  needs NO backend change. There is no server-side offset/page param — pagination is
  100% client-side.

- Existing CSS (~1655-1673): `.khaveeai-settings__two-col` (grid: minmax(0,1fr) 380px)
  and `.khaveeai-settings__preview-col` (sticky) with a max-width:1100px single-column
  fallback. REUSE these; do not define new layout CSS.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure KB card into a two-column layout (add-form left, list right)</name>
  <files>wordpress-plugin/includes/Admin/SettingsPage.php</files>
  <action>
Rework the Knowledge Base card so it mirrors the Avatar/Floating Widget cards' two-column structure using the EXISTING `.khaveeai-settings__two-col` / `.khaveeai-settings__preview-col` classes (do NOT add new layout CSS).

In render_page() (~lines 1525-1530): remove the `<table class="form-table"><tbody>` + render_form_table_row( 'Knowledge Base', render_knowledge_base_field ) wrapper. Keep the `.khaveeai-settings__card` and the render_section_heading() call. Call render_knowledge_base_field() directly inside the card (the field renderer now owns its own layout, same as the preview-mount pattern). Leave the Avatar and Floating Widget cards below untouched.

In render_knowledge_base_field() (~2193-2239):
  - Keep the `knowledge_base_enabled` checkbox exactly as-is (same name/id/value — it is a real saved setting) and keep the early-return no-key branch/description unchanged.
  - When has_key: replace the single `#khaveeai-kb-manager` column with a two-column structure:
      * Outer wrapper stays `<div id="khaveeai-kb-manager" style="margin-top:12px;">` (drop the max-width:640px so the grid can use full card width). The `#khaveeai-kb-manager` id MUST remain — the JS self-guards on it.
      * Inside it, emit `<div class="khaveeai-settings__two-col">`.
      * LEFT column: the add block — keep the existing `.khaveeai-kb-add` div with `#khaveeai-kb-content` textarea, `#khaveeai-kb-metadata` input, and `#khaveeai-kb-add` button (IDs unchanged). Drop the `border-top` inline style since it is no longer a stacked divider. Put `#khaveeai-kb-status` at the bottom of this left column.
      * RIGHT column: `<div class="khaveeai-settings__preview-col">` containing the `#khaveeai-kb-list` div (id unchanged; keep its "Loading documents…" placeholder). The pager controls and doc-count footer are created by the JS at runtime (Task 2) inside/after this list, so no extra PHP elements are strictly required here — but you MAY emit an empty `<div id="khaveeai-kb-footer" class="description"></div>` right after `#khaveeai-kb-list` for the JS to populate; if you do, wire Task 2 to it, otherwise Task 2 creates it dynamically.
      * Close the two-col and manager divs.
  - Preserve every existing element ID the inline JS depends on: khaveeai-kb-manager, khaveeai-kb-list, khaveeai-kb-content, khaveeai-kb-metadata, khaveeai-kb-add, khaveeai-kb-status.
  - Preserve all security properties: manage_options gate and nonce are unchanged (server side); all printed strings stay esc_html/esc_attr as today; no server data is echoed unescaped.

Add a short leading `//` comment on the restructured block referencing quick task 260715-1iy, consistent with the file's convention of tagging changes with their task id.
  </action>
  <verify>
    <automated>php -l wordpress-plugin/includes/Admin/SettingsPage.php</automated>
    <human-check>Load the WP admin Settings page with a platform key configured: the Knowledge Base card shows the add-document form on the LEFT and the document list on the RIGHT, matching the Avatar/Floating Widget cards; on a narrow (&lt;1100px) viewport it collapses to a single column.</human-check>
  </verify>
  <done>render_knowledge_base_field() emits a `.khaveeai-settings__two-col` block with the add-form in the left column and `#khaveeai-kb-list` in a `.khaveeai-settings__preview-col` right column; all five KB element IDs preserved; `php -l` passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add client-side pagination + doc-count footer to the KB list JS</name>
  <files>wordpress-plugin/includes/Admin/SettingsPage.php</files>
  <action>
Update the `$kb_js` inline script (~505-693) to paginate the fetched documents client-side, matching ConfigureRAG.tsx's approach (fetch ALL docs once, paginate in JS, PAGE_SIZE = 8). No library, plain vanilla JS, preserve the existing IIFE/ready() structure and the textContent-only rendering discipline.

Changes:
  - Raise the fetch limit: in loadList(), request `khaveeaiKb.root + '?limit=100'` (100 = server MAX_LIMIT) instead of the bare root, so there are enough documents to paginate over. Do NOT add any offset/page query param — pagination is entirely client-side.
  - Introduce module-scoped state inside the IIFE: `var allDocs = []; var currentPage = 0; var PAGE_SIZE = 8;`.
  - Refactor renderList(docs): store the full array in `allDocs`, clamp `currentPage` to a valid range, then render only the current page's slice (`allDocs.slice(currentPage * PAGE_SIZE, currentPage * PAGE_SIZE + PAGE_SIZE)`) into `#khaveeai-kb-list` using the SAME per-row textContent rendering that exists today (preview, created date, Delete button). Keep the empty-state ("No documents yet.") when allDocs is empty.
  - After the rows, render pager controls (only when `allDocs.length > PAGE_SIZE`): a Prev button, a "Page X of Y" label, and a Next button, built with createElement + textContent + addEventListener (mirror how the Delete button is built). Prev/Next adjust `currentPage` and re-render the current `allDocs` (no re-fetch). Disable Prev on the first page and Next on the last page.
  - Render a doc-count footer reading `allDocs.length + ' document' + (allDocs.length === 1 ? '' : 's')` (e.g. "12 documents"). Populate `#khaveeai-kb-footer` if Task 1 emitted it; otherwise create the footer element dynamically and append it after the list. Use textContent.
  - Delete flow: after a successful delete, remove the doc from `allDocs` and re-render (rather than only detaching the row) so the page count / footer stay correct and the current page stays valid; keep the existing nonce'd DELETE fetch and status messaging. It is acceptable to call loadList() again after delete instead, as long as the footer/pager end up correct — pick one approach and keep it consistent.
  - Add flow: after a successful add, keep resetting the inputs and reloading the list (loadList()); reset `currentPage = 0` is not required but is acceptable.
  - Preserve every security property: keep the `X-WP-Nonce: khaveeaiKb.nonce` header on GET/POST/DELETE, keep Content-Type only on POST, and render EVERY piece of document/server content via textContent — never assign server strings to innerHTML.

Keep the self-guard (`if ( ! manager || typeof khaveeaiKb === 'undefined' ) return;`) and the `wp_add_inline_script( 'wp-color-picker', $kb_js, 'after' )` attachment unchanged.
  </action>
  <verify>
    <automated>php -l wordpress-plugin/includes/Admin/SettingsPage.php</automated>
    <human-check>With 9+ documents in the platform project: the list shows 8 rows, a "Page 1 of 2" pager, and an "N documents" footer; Next/Prev navigate without a network refetch; deleting a doc updates the count and keeps the page valid; adding a doc reloads the list. Confirm no console errors and that document text renders as plain text.</human-check>
  </verify>
  <done>$kb_js fetches `?limit=100`, paginates allDocs in JS at PAGE_SIZE=8 with working Prev/Next + "Page X of Y", shows an "N documents" footer, keeps add/delete working, preserves nonce headers and textContent-only rendering; `php -l` passes.</done>
</task>

</tasks>

<verification>
- `php -l wordpress-plugin/includes/Admin/SettingsPage.php` passes after both tasks.
- No new files created; no changes to KnowledgeAdminController.php, KnowledgeClient.php, Plugin.php, or any REST route.
- `grep -c "khaveeai-settings__two-col"` in the KB region confirms the reused layout class (no new layout CSS added).
- All five KB element IDs still present: khaveeai-kb-list, khaveeai-kb-content, khaveeai-kb-metadata, khaveeai-kb-add, khaveeai-kb-status.
</verification>

<success_criteria>
- Knowledge Base card renders add-form left / list right using the existing two-col/preview-col classes, consistent with Avatar & Floating Widget cards.
- Document list is paginated client-side at 8 per page with Prev/Next + "Page X of Y" and an "N documents" footer.
- Add and delete continue to work and leave the pager/footer in a correct state.
- Security unchanged: nonce header on all requests, manage_options gate intact, server content rendered via textContent only.
</success_criteria>

<output>
Create `.planning/quick/260715-1iy-wp-knowledge-two-col-pagination/260715-1iy-SUMMARY.md` when done.
</output>
