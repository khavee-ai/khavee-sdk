/**
 * packages/wp-bundle/src/preview.ts — STUDIO-02 preview entry stub.
 *
 * This file is a stub populated by plan 09-03 with the real mountAllPreviews()
 * implementation. It exists now so esbuild in build.mjs can resolve the entry
 * point without failing.
 *
 * IMPORTANT: This file MUST NOT import from "@khaveeai/providers-openai-realtime".
 * The build.mjs grep assertion enforces this constraint at build time (T-09-01-05).
 */

// Stub — plan 09-03 overwrites this with the real mountAllPreviews() implementation.
console.log("preview entry stub — populated in plan 09-03");
